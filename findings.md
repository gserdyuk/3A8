# Findings — accumulated conclusions for the 3A8 project

A working log of the insights and empirical findings that METHODOLOGY.md rests on. It does not duplicate the methodology but records *why* it was made the way it is — sources, experiments, open questions.

## 1. Why you cannot simply ask one LLM several times (the single-model diversity problem)

- Classic Wideband Delphi works through the independence of *human experts* — different experience and different information. One model across different sessions has no structural independence: its "ceiling of ignorance" is systematic, not random.
- Persona prompting (different "roles" of one model) gives diversity of focus but not diversity of the reasoning apparatus — under the hood it is the same statistical mechanism.
- **Empirical confirmation (anchoring bias is stronger in LLMs than in humans):** a replication of the classic Jørgensen–Halkjelsvik experiment on GPT-4, Llama 3.1, Gemini 1.5 Pro showed that when the phrasing of the question changes ("how long is needed for X" vs. "how much can you finish in Y hours") the LLMs' estimates diverge **at least 6× more strongly** than humans' in the original. For GPT-4: 4000 vs. 150 work hours on the same set of user stories — i.e. the anchoring almost completely suppresses the substantive signal of the task. (Çalıklı & Alhamed, FSE 2025)
- Conclusion: varying the prompt/phrasing is not a source of independent opinion but controlled anchoring. If building a multi-persona debate, it is critical to keep the question format identical for all personas.
- LLMs do not behave like humans even where similarity was expected: comparative (pairwise) judgments do not come more easily to LLMs than direct estimates, though for humans they do. This means debias techniques that work for humans (comparison instead of absolute estimation) cannot be transferred to LLMs automatically.

## 2. Three different kinds of "not knowing" — where LLMs beat humans and where they do not

- **Breadth problem (breadth of pattern coverage)** — the LLM really is stronger than a team of humans; it has seen orders of magnitude more post-mortems and project patterns.
- **Calibration problem (weighting of known risks)** — not clear the LLM is stronger; RLHF pulls toward "balanced" answers, which may systematically underweight chaotic organizational factors.
- **Local/tacit knowledge** — data that does not exist in the nature of text (this particular team, this particular person, this particular legacy service). No LLM and no estimation method covers it in principle — only living people with a history in this specific organization.

## 3. Multi-agent debate — the only LLM-native approach with independent empirical confirmation

- SEEAgent (Bui et al., 2025) — a multi-agent framework in which LLM agents give estimates, justify them, and debate with other agents/humans until consensus. It outperformed state-of-the-art methods on most metrics on a real dataset; practitioners rated the collaborative experience positively.
- This is direct empirical confirmation that the combination *persona + reference-class + multi-agent debate* is not only theoretically sound but works in practice.

## 4. The key shift: different techniques instead of different experts ("experts with a different apparatus")

Instead of decorrelation through people — decorrelation through mathematically different methods. Their blind spots are not empirically random (as with people) but **guaranteed by the construction of the method** — i.e. predictable in advance.

| Method | Strength | Structural blind spot |
|---|---|---|
| Decomposition (bottom-up + PERT) | Specifics of the particular task | Correlation of risk across tasks; systemic/integration risks; overhead |
| Reference class forecasting | Systemic risk as a whole (the "outside view") | Specifics of this particular task/team |
| Throughput Monte Carlo | Organizational overhead and fragmentation — empirically, automatically | Does not explain causes; requires history; assumes future tasks resemble past ones |
| Parametric (COCOMO-like) | Independence from subjective judgment | Quality = quality of calibration; models age |
| ML/regression on history | Semantic patterns of similar tasks | Explainability; new task types outside the sample |

Additionally mentioned but not included in the main pipeline: relative estimation / planning poker (more robust to anchoring in humans, but not clearly so in LLMs — see §1), probabilistic PERT accounting for risk correlation.

## 5. An observation about Scrum overhead (sprints) → throughput as a natural corrector

- Personal observation: a lot of time goes into (a) sprint start-up, (b) hand-off/review, (c) unusable "scraps" of time.
- This matches known concepts: "focus factor" in Scrum, "flow efficiency" in Kanban/Lean. Point (c) is essentially a bin-packing problem, statistically inevitable with granular tasks and a fixed sprint capacity.
- Throughput Monte Carlo accounts for this automatically and empirically (from the fact of past sprints); decomposition fundamentally cannot, because overhead is not tied to a particular task.
- There is a separate research line — agent-based/system-dynamics modeling of developer behavior (SPSM — Software Process Simulation Modeling): historically discrete-event and system-dynamics approaches dominate; an agent-based case (AVL) proposes a "practical effort function" for modeling developer behavior, including interruptions for mentoring and so on. The results "look promising, but more work is needed" — i.e. a working direction, not a finished product.
- The classic of the genre — Abdel-Hamid & Madnick, "Software Project Dynamics" (1991): a system-dynamics model with Brooks's effect, ramp-up of new hires, accumulation of undetected rework, and the effect of pressure on quality.
- Practical conclusion: for a pure "how long" estimate a simple correction coefficient suffices (actual throughput / nominal capacity over N sprints). A full behavioral simulation is justified only for "what if" questions (comparing ways of organizing the work), not for a point estimate.

## 6. The convergence problem in a cross-method approach

- In human Delphi, convergence is the result of an *expert* changing *their own* opinion after new information. With different estimation techniques this is impossible: a method cannot "reconsider" and start accounting for what it does not account for by construction.
- Convergence must be engineered explicitly, by a separate mechanism:
  1. **Independent runs** — each method gives a range + an assumption log.
  2. **Diagnosing the divergence** — not "whose number is more correct," but which specific blind spot explains the difference.
  3. **Parameter transfer** — mechanical calibration, not agreement of opinions (example: throughput → overhead coefficient as an explicit multiplier to decomposition).
  4. **Final range with explained residual** — what was not removed by parameter transfer is an honest measure of uncertainty, not noise to be averaged away.
- **Important:** full convergence after explicit corrections is not always a success — if the methods answer structurally different questions yet converge to a single point, it is either a coincidence or one method was forced under another and lost its diagnostic independence.

## 7. Are historical data needed to compare decomposition vs. reference class?

**No.** Reference class forecasting in an LLM does not require an external dataset in context — the reference class is "smeared" across the model's weights from the training data, unlike a human, who needs personal/team experience. External historical data are mandatory only where a method *by construction* cannot work without them: throughput Monte Carlo (needs statistics from real sprints) and parametric models (need calibration).

→ Practical division of work: **Phase 1** — decomposition + reference class directly in conversation with the LLM, without a dataset. **Phase 2** — throughput/parametric, for which a dataset is mandatory.

## 8. Source of historical data (for Phase 2) — TAWOS

- TAWOS (Tawosi et al., MSR 2022) — a dataset of issues from open agile projects on Jira, 508,963 issues, 44 projects, with real story points, resolution time, sprint history.
- **Limitation:** the dataset itself is a MySQL dump on the UCL Research Data Repository (rdr.ucl.ac.uk), inaccessible from the sandbox (not among the allowed network domains). Only the aggregate statistics from the publication itself (Table 1) are available.
- Example aggregate — Spring XD (key XD): 3,707 issues, 66 sprints, 610 bugs, 31 developers → ~56 issues/sprint on average (no data on per-sprint variance).
- Open question: where to get granular (per-sprint) historical data — TAWOS in full is technically inaccessible, and the project's author also has no access to sprint history for any real project as of this discussion (EPAM — access not confirmed).

## 9. First manual check of Phase 1 (BMS, 2026-07-17) — conclusions

> An anecdote from ~1982: solving the Schrödinger equation for a particle on a surface, the problem broke into 100+ integrals.
> "If we didn't know how to take an integral, we neglected it."
> — An exact model of decomposition's blind spot: whatever has no line in the WBS is neglected,
> and the sum of the rest looks rigorous.
> The difference between good and bad practice is not the absence of omissions, but a named list of the discarded integrals
> (the assumption log).
> Reference class is the "experiment" that measures the sum as a whole, including the integrals no one knows how to take.

A run on the example examples/BMS (an RFP for a booking management system, EPAM 2016). Artifacts: assumptions.md, run1_decomposition.md, run2_reference_class.md, run3_diagnosis_calibration.md.

- **The methodology worked as intended:** decomposition gave 486 pd (and an absurdly narrow CI ±3.5% — the predicted artifact of independence), reference class — P50 950 pd with a fat tail (P90 1800). The ×1.95 divergence decomposed across the blind spots with almost no residual (~93% of the center gap explained); the tails did not converge — and were not supposed to.
- **Protocol of run independence:** within one LLM session the second method is inevitably anchored by the result of the first (see §1). The working solution — each method is executed by an isolated agent that receives only the input (RFP + assumption log) without the others' results. This is a mandatory requirement of the pipeline, not an option.
- **"Multiply and add" (the author's insight):** the cross-method calibration of Step C is not a single scalar. Part of the gap is cost items that are not in the WBS at all (pure additions), part is targeted multipliers on a subset of items (integration), part is global multipliers (scope creep, coordination). The affine model y=ax+b is unidentifiable from a single point (needs ≥2 projects) → in Phase 1 the parameters are not fitted but transferred by name from external base rates; fitting is possible only in Phase 2 on one's own history.
- **"Integrating the parts costs nothing" (the author's insight):** the sum of WBS leaves contains no term for joining the parts to each other — the tree's nodes are estimated, but you also pay for the edges, of which there are ~n²/2 for n nodes. This is a refinement of the blind spot from METHODOLOGY §2: not an "integration risk" (probabilistic) but a deterministic cost item that structurally has no line in the WBS. Items like "integration testing" mask the problem but are estimated by feel, not from the number of seams — and the number of seams is not visible from the WBS itself. Reference class covers this automatically: in the facts of completed projects the integration is already paid for.
- **Proposed correction to the method — integration-aware bottom-up (the author's idea):** at each WBS aggregation node (where k components combine into a whole) add an explicit integration item, derived from the actual number and type of seams between the node's children (from the architecture diagram), rather than "by feel." The cost of one seam is a transferable parameter (base rates: system integration ~15–30% of component development; in Phase 2 — measure on history). Caveats: (a) double counting with items like "integration testing" — those must be trimmed; (b) it cures only one blind spot — overhead, scope creep, and risk correlation remain, so full convergence with reference class should not occur. A checkable prediction for BMS: recalculation will give noticeably >486 but <884 pd. **Status: the experiment was run (run4_integration_aware.md), the prediction held** — at a rate of 15–20% per node the total is 568–602 pd (center ~585, +20% over the flat WBS; the QA item was trimmed against double counting). The remaining gap up to 884 is exactly the spots the correction cannot cover (scope creep, coordination, external icebergs). An important distinction: external icebergs (someone else's APIs worse than their documentation) and internal edges (our own assembly) are different items; the correction moves the second one from a "coordination multiplier" to a structurally derivable line.
- **Unintended cross-validation:** the isolated reference-class agent independently named the typical range of RFP bids for the class (400–700 pd), and decomposition landed inside it (486). I.e. decomposition reproduces the behavior of a typical bid, and the gap with the class is a systematic feature of the RFP stage, not a defect of this particular WBS.
- **The tail does not calibrate:** the class P90 (×1.8 over the calibrated decomposition P90) is unreachable by any multipliers on the WBS — tail events do not exist as WBS items. For contract decisions (fixed price vs. T&M, buffers) only reference class is fit.

## 10. Second manual run (FaxRxTx / Venali, 2026-07-17) — first check against fact

A real 2007–2009 project: a rewrite of the core functionality of the Venali fax platform (rendering/OCR workers, a cluster with home-grown orchestration of watchdogs + tokens, NOC, portal). Artifacts: examples/FaxRxTx/ (SYSTEM.md, assumptions.md, run1–run4, FACT.md). The fact, from a participant's memory: **~120 person-months ±20%** (window 96–144). Blindness protocol: the fact is sealed in FACT.md, neither the estimators nor the diagnostician saw it; the reveal happens only at Step D.

- **Results:** raw decomposition E≈112; reference class P50 160 (P10 85 … P90 320); calibrated decomposition 155 (135–180), the diagnostician explained ~90% of the center gap with a double-counting check.
- **The main conclusion — modest, at the author's insistence:** memories are fuzzy, and the divergence of the calibrated estimate from the fact (+29% of the window center, +8% of its edge) lies within one width of the memory's own error — from memory you cannot tell 12 months from 13–14. Therefore only this is claimed: **all three estimates are consistent with the fact within the resolving power of the measuring instrument; the pipeline produced no absurdity at any step.**
- **Both structural patterns from BMS reproduced:** the absurdly narrow CI of raw decomposition (±8%) and the non-calibratable right tail (above ~177 pm multipliers cannot reach — the reserve comes only from reference class).
- Center gap ×1.43 vs. ×1.95 in BMS: the WBS here was "well-behaved" — domain immersion, integration tests on live traffic, QA/PM and part of the edges were already present as leaves.
- **Hypotheses for the future (not conclusions):** (a) calibration to the class P50 should systematically overshoot on projects better than the median → the form of the answer is a triple "center from the WBS, corridor from calibration, reserve from the class tail"; (b) ensemble global multipliers on top of a well-behaved WBS risk double counting, so a "well-behavedness discount" may be needed. Both can be tested only on a project with a documented (not remembered) fact.

## 11. The mechanics of calibration and the problem of matching class projects "one to one" (discussion 2026-07-17, in the wake of FaxRxTx)

Three related questions, worked through after closing the FaxRxTx example. Recorded in detail because together they form the bridge from Phase 1 to Phase 2.

### 11.1. How exactly decomposition is calibrated against reference class (Step C)

Calibration is **not** a shift of the decomposition estimate toward the class number, nor an averaging. The sequence (on the FaxRxTx run3 numbers):

1. **Fix the gap.** Raw decomposition E=111.6, class P50 160 → a gap of 48.4 pm. Reference class reports only the scale of the shortfall, but does not say *where* in the WBS it sits.
2. **Decompose the gap across decomposition's known blind spots** (they are static, known before any run): work with no line in the WBS; integration edges (nodes are estimated, seams are paid for too); organizational overhead; scope creep. This is Step B: not "whose number is more correct," but "which spot explains the difference."
3. **Take each item's rate from external base rates, not from the gap.** The key discipline: numbers like "scope creep ~10%," "distributed-core rework ×1.3–1.5" come from general knowledge about the class of projects and are NOT tuned so the sum matches the class P50. Why you must not fit: there is one point, the correction model is essentially y=ax+b, and from one point a and b are unidentifiable; fitting would give an illusion of precision.
4. **Apply the corrections to the WBS in three types** (the "multiply and add" taxonomy from §9): pure additions (domain unknown-unknowns +8.8, missing edges +3.6); targeted multipliers on subsets of leaves (core ×1.4 → +6.6, rendering stabilization ×1.25 → +2.9, convergence with v1 ×1.35 → +3.3); global multipliers (scope creep 10% → +12.3, org tax 4% → +6.0). A mandatory double-counting check: spots already covered by WBS leaves (in FaxRxTx — domain immersion, live integration tests, QA/PM) receive no corrections.
5. **Check the explained share.** +43.5 of 48.4 → ~90% of the gap explained, calibrated center ≈155; the residual ~5 pm remains unattributed — a measure of uncertainty, not noise. The closeness of 155 to 160 is a result of the check, not the goal; explaining half the gap would also have been a valid outcome.

Convergence is mechanical, not epistemic: no method "reconsidered." And only the **center** calibrates: the tail (P90=320) is unreachable by multipliers — tail events do not exist as WBS items (reproduced in both examples).

### 11.2. How projects of one class are matched "one to one" (one site has 10 pages, another 5)

Heterogeneity within a class is the central weakness of reference class. Three ways to match:

1. **Normalizing to the project's own estimate** (the canonical one, Flyvbjerg): the class stores not absolute effort but the **ratios** of actual/estimate. Size cancels out on its own: sites of 10 and 5 pages have different bottom-up estimates, but the overrun multiplier is drawn from a single distribution. This is exactly why the pair "decomposition + reference class" fits together: the WBS gives a project-specific anchor (it sees both the pages and the 2FA), the class gives the distribution of the multiplier on top of the anchor.
2. **Normalizing to a unit of size** (the parametric route): function points, screens, KLOC; the class gives the cost per unit ± spread. Requires calibrated historical data — unavailable in Phase 1.
3. **Narrowing the class with a size shelf** (the crudest; what run2 implicitly did by giving absolute P10/P50/P90): not "all rewrites" but "a rewrite of a distributed platform of *medium* scale." Part of the heterogeneity is removed by the selection conditions; the rest (2FA present/absent) **does not go anywhere — it stays inside the P10–P90 spread.** The width of the class range is the honest price of within-class heterogeneity; a narrow reference class is a suspicious reference class.

Corollary: the specific "10 pages vs. 5" is by definition a blind spot of the class and a sighted zone of decomposition. The class is responsible for the shape of the distribution; the position of the point inside it is set by bottom-up. In Phase 2 the cleanest option is way 1 on one's own history: the distribution of "actual / our own decomposition estimate" over completed projects; each project's normalizer is its own estimate, and the question of comparing sizes disappears by construction. Step C of Phase 1 is a manual, item-by-item approximation of this multiplier while there is no history.

### 11.3. A direction for Phase 2: project scale as a covariate of the multiplier

The idea: improve way 1 by making the overrun multiplier a **function of size** rather than a constant of the class.

- **Empirical basis:** large projects overrun more and with a fatter tail (COCOMO: exponent >1 with size; Flyvbjerg's data; failure statistics — small projects succeed more often). So both the center and the tail width depend on size → this improves not only the estimate but also the size of the reserve.
- **The size metric is already free:** the sum of the decomposition estimate ΣE is a ready proxy for size, available at estimation time. Calibration turns from "multiply by a constant" into "multiply by f(size)." The link to §9 ("multiply and add"): y=ax+b is unidentifiable from one point; with a size covariate the slope is estimated from 2–3 projects, the curvature from 5–10.
- **The structural justification for why the multiplier must grow with scale** — the integration edges from §9: n nodes, ~n²/2 seams; the sum of leaves grows linearly, the shortfall for edges faster → the "WBS vs. fact" gap in percent is larger for big projects. The exponent >1 follows from structure, not from fitting.
- **The price — data:** conditioning cuts the sample; on 5–15 of one's own projects a free regression will overfit. A practical compromise is the same discipline as in Step C: take the shape of the dependence (the power exponent) from outside as a prior (the COCOMO class), and tune only the level on one's own history.

### 11.4. The author's correction to 11.2–11.3: self-normalization does not save you, because size changes the composition of the work

The author's observation: within the "class of all web sites," projects of 5, 30, and 100 pages are **different classes**, if only because the distribution of work in them is different. So "self-normalization" (way 1 of 11.2) is not by itself a way out.

Analysis: size produces two different effects, cured differently.

1. **Smooth** — the overrun multiplier grows with size at the same composition of work (edges ~n²/2, coordination). This is cured by the covariate f(size) from 11.3.
2. **Structural (deeper)** — at some size whole categories of work appear that a small project simply does not have: a CMS instead of hand-coding, information architecture, a dedicated PM, a staging environment. This is not a multiplier on existing leaves but **new WBS branches**. The actual/estimate ratios in different size regimes are drawn from different populations — dividing by one's own estimate does not fix this.

**Corollaries:**
- Ways 1 and 3 of 11.2 are not alternatives but a mandatory sequence: first stratification by size regime (the shelf is a condition of correctness, not "crudeness"), self-normalization is valid only within a stratum. Pooling projects of different sizes into one multiplier distribution is a class-specification error, not a data shortage.
- **An operational criterion for the regime boundary** (free, from a tool already at hand): two projects are in one class if their WBSs are structurally similar — the set of branches matches, not the sums. The appearance in the larger project of branches the smaller one fundamentally lacks is a marker of a class change. Decomposition gains a second role: not only a source of the center, but a test of membership in the reference class.
- The price for Phase 2: stratification cuts the small history (5–15 projects → 2–3 strata of a handful of points each) → external priors are needed for each stratum separately; one's own history tunes only the level within a stratum.

### 11.5. A precaution: the gradient toward an ML model (discussion 2026-07-17)

The author's observation: the chain of improvements 11.2 → 11.4 (self-normalization → size covariate → stratification → priors → level tuning) is a gradual slide toward building an ML model. Recorded **not as a rule but as a precaution**: this reasoning exists, and complication/sophistication must be avoided.

The essence of the concern:

- The methods lie on a single complexity continuum: reference class is a non-parametric model with one categorical feature (the "class"); COCOMO adds a size covariate; ML regression is the same with many features and a flexible function. Following the gradient to the end, we quietly turn reference class into the fifth method of METHODOLOGY §2 — with its blind spots (inexplicability, failure outside the sample) and without its honest name.
- For 3A8 this is especially dangerous: the value of the framework is the decorrelation of the blind spots of *different* sensors. A reference class enriched into a fitted model stops being an independent sensor; the diagnosis of Step B feeds on divergence, and one sophisticated model gives one number with hidden assumptions — exactly what the framework was moving away from.
- The practical trap: on 5–15 projects the model will not train, but it will look rigorous and will narrow the intervals by construction. The wide class range is a feature (an honest measure of heterogeneity, 11.2); a sophisticated model destroys it first.

Guidelines (not rules): keep the sensors coarse, allow complexity only in the diagnostic layer (Steps B/C); the center is owned by calibrated decomposition, the tail by the raw class quantiles (the tail is not improved by fitting — quantiles need points, not parameters); the litmus test — if an "improvement" requires fitting something on one's own history, the boundary of the method has been crossed, and it is more honest to frame it as a separate sensor than to dissolve it into an existing one.

### 11.6. First check against data (mars_model, 2026-07-17): what was confirmed, what was refuted

The hypotheses of §11.3–11.4 were checked on the open PROMISE datasets (China n=499, Kitchenham n=145, Desharnais n=81, Maxwell n=63). The method — "MARS-lite" following the discipline of §11.5: a power fit in log-log, one hinge with a grid search over the knot, AICc, bootstrap; plus a pilot "composition as parameters." Artifacts: mars_model/ (data/, fit_piecewise.py, composition_china.py, results.md, loglog_fits.png).

**Refuted (with caveats):**
- "The exponent b>1 in Effort~Size^b" — in all four datasets b≤1 (0.67–0.98), in three the CI is entirely below 1: the production function shows *economies* of scale. This is a known dispute in the literature (Banker & Kemerer 1989; Kitchenham 2002): FP datasets give b<1, KLOC calibrations of COCOMO give b>1. Caveats: survivorship (the datasets contain only completed projects — the class tail is unobservable), mixing of organizations, endogeneity (larger projects get better teams).

**Confirmed:**
- **The overrun multiplier grows with size — in the tail, not the center** (Kitchenham, the only dataset with a "first estimate/actual" pair): the median Actual/FirstEstimate ≈ 0.97 across all size terciles, while P90 grows 1.11 → 1.15 → 1.46; the slope of log(ratio)~log(size) is positive, the CI does not cover zero. The precise formulation of §11.3 — about the estimation error — is correct.
- **A surcharge for interfaces on top of size** (the composition pilot on China): the share of Interface elements significantly raises the project's cost at the same AFP (+10 pp of share → ×1.10), and this on top of what the FP method already pays interfaces via increased weight (in the IFPUG weight table an EIF element is worth 5–10 points against 3–6 for an ordinary input). The underestimation of seams survives even an explicit attempt to pay for them with increased weight.
- **Breaks exist, but the boundaries are unstable:** the direction is everywhere the same (the slope after the knot grows — economies of scale run out on large projects); a break is formally justified only in the homogeneous Kitchenham (~600–700 FP, 98% of bootstraps agree); in mixes of organizations the knot wanders by an order of magnitude. The stratification of §11.4 is meaningful, but stratum boundaries must be taken from a homogeneous population (one's own history), not from someone else's mixes.

**Correction to the theory (important):** §11.3 conflated two quantities. The production function Effort(Size) may show economies of scale; the estimation error (actual/estimate) grows with size — in the tail, at a stable median. For 3A8 the second is relevant: the pipeline calibrates estimates, it does not produce projects. The edges n²/2 argument is refined: **edges strike not at the project's cost but at its visibility in the WBS** — large projects have more invisible items that occasionally fire; hence "the tail thickens, the median holds" — exactly the observed picture.

**The obtained models** (China: E≈27·AFP^0.77; Kitchenham: E≈37·AFP^0.67 with a break at ~700 FP; and others, R² 0.44–0.63) are legitimate only as the **shape of a prior** (§11.5): the order of the exponent, the existence of a break, and the growth of the multiplier tail are transferred; the level is tuned on one's own history.

## 12. Cross-project comparison with twotakt (2026-08-04): what transfers, and where the agent question inverts

The sibling project twotakt closed a scoping question the same day: deploying itself to AWS / Bedrock AgentCore was examined and rejected (its F39 — no cloud-shaped work; the only agentic element is a build→verify loop whose stop condition is a machine check; its human gates are the product, not missing autonomy). That reasoning was then tested against 3A8. The split is informative in both directions.

**Transfers as is:**

- **No cloud-shaped work here either.** `mars_model` fits datasets of n ≤ 499; throughput Monte Carlo is seconds; storage is a repository. Nothing scales, nothing runs long.
- **twotakt's F3 is 3A8's founding move.** "The generator may never author its own trust floor — correlated blind spots" and "independence of techniques instead of independence of experts, because one model is not independent of itself" are the same structural claim, reached from opposite ends (verification there, estimation here). 3A8 carries empirical backing twotakt does not have: Çalıklı & Alhamed FSE 2025, §1.
- **twotakt's F1 was already rediscovered here.** METHODOLOGY §3 — "full convergence after Step C is not necessarily a success" — is the estimation form of "the requirements verdict is the *output* of the study; asserting it asserts the thing under investigation." There the assertion would be SLA attainment, here it is convergence.
- **twotakt's F8 (negative-test-first) bites hardest.** Two runs (§9, §10), both reported as the methodology working as intended; no diagnostic step has ever gone red. By F8 a check that has not yet bitten is not yet a check. Concrete test available cheaply: feed Step B a deliberately malformed WBS (an extra branch, a double-counted QA line) and see whether the diagnostician names a blind spot that is not there.
- **twotakt's F26 = the §11.5 precaution.** The ML-gradient concern and "the AI cannot distinguish its substantive constructions from symmetry-driven filler from the inside" are the same failure, caught by the same instrument — author skepticism, not self-check.

**Inverts — the agent question.** twotakt would have had to *invent* an agent in order to have something to deploy. In 3A8 the agent requirement predates any infrastructure thought and is load-bearing: §9's isolated-agent-per-method rule ("a mandatory requirement of the pipeline, not an option") and FaxRxTx's sealed `FACT.md` are **isolation** requirements, not autonomy or throughput ones. The value of an agent here is what it *cannot see*; the process boundary is the epistemic boundary between sensors, and collapsing the methods into one session is not an inconvenience but a spoiled measurement. This is a rare and honest use of the architecture — the agents are hired for what they are forbidden, not for what they can do.

**But agents ≠ cloud.** Isolation is four processes with disjoint inputs: subprocess plus discipline about what goes into each context. The requirement is real; the infrastructure need stays at zero. The cloud conclusion survives even in the place where the agent conclusion reverses — which is the most useful result of the comparison.

**Does not transfer — the computed verdict.** twotakt's Takt 2 stops on `verify.py` green. 3A8 has no oracle even post hoc: FaxRxTx's "fact" is a participant's memory ±20%, and the honest claim there is consistency within the resolving power of the instrument (§10). What 3A8 has instead is a candidate gate of a different species — structural rather than numeric:

> **No parameter used in Step C may be a function of the gap it explains.**

That is §11.1 step 3 stated as a checkable property: rates come from external base rates and are never tuned so the sum matches the class P50. It is checkable by *provenance*, not by value — a data-flow condition, and it is F3 applied inside one method rather than between methods. Today it rests on good faith. In the agent wrapper it becomes machine-checkable, because every rate reaches the diagnostician as an input with a declared source.

That is the real import from twotakt: not the cloud and not the agents, but the idea that a discipline written in prose can be turned into a check.

**Built 2026-08-04 (PIPELINE.md + `.claude/agents/`).** Four agents with disjoint inputs: the two Step-A sensors, a **gap-blind rate agent**, and the diagnostician. The provenance rule became structural rather than good-faith: the rates for Step C are produced by an agent that never sees the reference class output or the size of the gap, and the diagnostician may only apply them — never edit, add, or drop one; a hole in the rates is answered by requesting another gap-blind round, not by filling it while looking at the gap. The final answer is reported in three parts that cannot be mixed: center (calibrated bottom-up), corridor (spread of the calibration), reserve (raw class tail).

**First negative test — the check went red on purpose.** The decomposition sensor was fed a deliberately contaminated prompt (a budget, a class median, "land close to that", and an instruction to open another method's run file) while holding file tools. It refused to estimate, named all four anchors, left the file unopened, separated the still-clean part of the input, and observed that an anchor reaching one sensor implies the whole pipeline is contaminated. This is the first check in the project that has been shown to fire — the F8 debt is now partly paid: prompt-layer refusal is real for one agent of four. The test was then repeated against the registered agent (not just the pasted role text) with the same result: refusal, zero tool calls, the sibling's file left unopened.

**And the test caught a placebo.** The definitions carried `tools: []`, meant as the structural half of the isolation — an agent that *cannot* open another sensor's artifacts. On registration the harness reported all four agents as having **all tools**: an empty list is read as "unspecified" and inherits everything. So the load-bearing enforcement was decorative, and only the prompt layer was ever holding. This is F8 again, one level down: a restriction that has never been observed to bind is not a restriction, and configuration that *reads* like enforcement is the easiest kind to leave unverified. Corollary for the project: every discipline described as machine-checkable (including the provenance rule above) must be demonstrated firing before it may be counted on — the gap-blind rate agent is guaranteed by *what the orchestrator pastes*, which is discipline, not machinery, until shown otherwise.

**All four agents now tested (2026-08-04).** The remaining three were given the forbidden material as neutral "context" rather than as an instruction — a WBS total to the reference class sensor, the class quantiles to the rate agent, the project's actual outcome to the diagnostician — so each had to notice the contamination unprompted. All three halted before producing anything. Three details worth keeping: contamination is *sticky* (a corrected follow-up does not repair a run; the number stays in context, so a fresh instance is required, not a fresh prompt); partial output is refused too (the reference class agent would not even give its class definition, its reasoning already being downstream of the WBS it had seen); and the mere fact of a prior run leaks (the rate agent noted the gap was one subtraction away in its context). Unplanned side effect: two sensors independently caught that the WBS in the test prompt priced ~14% of its total against scope the project description never mentioned — a sensor holding both a description and a WBS is a free consistency check between them.

**A second failure mode found, of a different species.** In one run the diagnostician reached the correct verdict but its report contained an invented directory listing of the project root — non-existent files, one entry as both file and directory — while the harness recorded zero tool calls. Nothing was executed; the observation was narrated. A repeat was clean, but carried an extra "do not open any files" instruction, so it does not establish that the first was a fluke. Two consequences: any file-system claim inside an agent report is unverified narration unless the tool call is visible; and this is the strongest practical case for making the tool restriction real — an agent that cannot reach the file system has nothing to fabricate about it.

**The tool layer is now real (verified 2026-08-04).** With `tools: Glob` in the definitions, a sensor probed for its own capabilities reported exactly one tool and reported a repository read as impossible at the capability level — no `Read`, no shell, no search. Isolation therefore stands on two independent layers: a sensor *cannot* open a sibling's artifact, and *refuses* forbidden material arriving through its prompt. Note the shape of the fix: `tools: []` (the intuitive way to say "nothing") silently meant "everything", while naming a real but nearly useless tool worked — the difference between a declaration and a binding, which only a probe can tell apart.

## 12a. First end-to-end wrapper run (FaxRxTx, 2026-08-05): the answer was wrong by 4×, and the report said so

The FaxRxTx case was re-run through the four-agent pipeline on inputs identical to the manual run of 2026-07-17, so the two are comparable. Artifact: examples/FaxRxTx/run5_agent_pipeline.md. Against a sealed fact of ~120 pm (±20%): reference class P50 **135** (inside the memory band), raw bottom-up **238** (×2.0), calibrated **503** (×4.2). The manual run had produced 155.

- **The decomposition sensor is not reproducible.** Same document, same assumptions, same method: 111.6 pm in July, 237.8 pm in August — ×2.1 between two runs of one sensor, against ×1.19 for the reference class sensor over the same interval. The August tree is not sloppier but *more thorough* (it prices coexistence, migration, verification, rollout, and 34% seam cost); the integration-aware refinement of §9 makes trees grow and nothing caps that growth. "Sensor" presumes a repeatability that decomposition, as currently specified, does not have — and this was invisible until one case was run twice.
- **§10 hypothesis (b) is now observed, not hypothesised.** Five independently sourced global multipliers compounded to ×1.72 on an already-generous WBS, charging a thorough decomposition for omissions it had not made. Gap-blindness has a cost here that was not anticipated: the rate agent cannot see whether the tree it corrects is thin or thorough. Candidate fix that preserves blindness — have the decomposition sensor emit a completeness self-report (which standard activity classes it carries) and pass that, since it is gap-free information.
- **Nothing in the pipeline tests feasibility.** 503 pm over the 2007–2009 window implies ~14 people for three years; the real organisation ran ~10 for one. Headcount was excluded from the assumptions as a calendar matter (A3), and that exclusion let an infeasible number through unremarked. A staffing-ceiling gate needs no outcome data and would have caught this.
- **The diagnostic step went red for the first time in the project — the F8 debt is paid.** Facing a divergence with the wrong sign (bottom-up *above* the class, so upward corrections could only widen it), the diagnostician reported the explained share as **0%** in those words, refused to invent rates while looking at the gap, requested another gap-blind round for three uncovered items, flagged the ×1.72 multiplier stack on its own initiative, refused two numerical near-coincidences as category errors (a center is not a percentile; a sum of optimistics is not a median), reported that the calibrated outputs sat at P92–P99.4 of their own class, and proposed the feasibility check that would have caught the error — all without ever seeing the outcome. The estimate failed; the reporting did not.
- **Fabricated tool output, second occurrence, same role.** The diagnostician opened with a claim to have searched the repository and found no outcome files — zero tool calls recorded, and the claim false. The rate agent, holding the same single `Glob` tool, used it correctly: listed filenames, said so, opened nothing, and named the files that would have contaminated it. The documented `Glob` residual behaved exactly as predicted; the fabrication is a property of the role, not of the tool set.

## 12b. Repeat-run variance measured (BMS, 2026-08-05): the sensor is repeatable and biased, not unstable

Five runs of the decomposition sensor on byte-identical prompts, on the case that has **no** recorded outcome — chosen so nothing could be tuned toward a known answer. Artifact: examples/BMS/run6_variance.md. Results 894 / 1078 / 1084 / 1243 / 1317 pd: mean 1123, **coefficient of variation 14.6%**, max/min 1.47.

- **The earlier reading was wrong in an important way.** The ×2.1 between the July and August FaxRxTx runs (§12a) was called instability. It is not: all five BMS runs sit **2.2× above the July manual estimate** (ratios 1.84–2.71, none below 1.8), so what looked like noise is a systematic level shift of the specification, reproduced on a second case with n = 5. Run-to-run spread of ±15% is workable; a ×2.2 bias is not.
- **The shift decomposes into two free parameters, both invented per run.** *Granularity*: 26 leaves in July against 75–99 now, leaf sums ×1.75. Every run prices a leaf at ≈10 pd (9.0–11.6) where July's coarser leaves averaged 18.7 — so splitting a leaf in three yields three leaves each worth more than a third, and the total becomes largely a function of chopping depth, which nothing in the method fixes. This is the unpacking effect measured on our own instrument (leaf count ×3.1, per-leaf value ×0.57, total ×1.75). *Seam rates*: integration items are 18.5–27.5% of the total, and the rate card is re-invented each run; one run stated the sensitivity itself — a card twice as high would add ~310 pd.
- **Consequence for the method:** granularity and the seam rate card must become method constants, or two runs are not measuring the same quantity and no calibration built on top of them means anything. Only after pinning both is the residual variance worth measuring.
- **What the measurement deliberately cannot settle:** which level is correct. BMS has no fact; the only case with one (FaxRxTx) suggests the coarser specification lands nearer, but n = 1, that run's calibrated answer still overshot by 29%, and tuning the sensor toward a known outcome is the Step C provenance error one level up. The constant must be argued from mechanism and then validated on a case whose outcome has not been seen.

## 12c. The three constants (2026-08-05): pinning what the measurement showed to be loose

Following §12b, the free parameters of the decomposition sensor were moved into the method definition. Two of the three choices are the author's, and both are justified by something a human reader can check rather than by fit.

- **C1 — split above 10 person-days (most likely), stop below, never merge, never create a leaf under 1 day.** The first draft made 5–10 a two-sided band with "merge anything smaller". The author rejected the merge half, and was right for a reason worth keeping: a split-only rule is **monotone** — the tree can only refine, so the procedure terminates and its result does not depend on the order of splits — whereas split-plus-merge oscillates (a merged 9-day leaf invites the next pass to re-split it) and makes the answer a function of operation order rather than of the project. It also removes a judgement call the pinning was supposed to eliminate: "merge with which neighbour". Substantively the ceiling is the only side that matters, because the inflation direction is one-way: the unpacking effect grows totals when work is cut finer, and coarseness does not inflate anything. The 1-day floor is not a merge rule — it says trivia gets no line of its own and is counted inside the leaf it belongs to. 5–10 survives as an *expectation* (sprint-sized tasks, verifiable by a reader who does not know the project), reported as a distribution of M across buckets rather than enforced.
- **C2 — a fixed list of nine top-level branches.** The author noted the precedent: the **utility tree** in ATAM (SEI architecture evaluation) fixes its second level to a vocabulary of quality attributes while leaving the leaves project-specific — for exactly the two reasons we need here, comparability across projects and a checklist guarantee that a standard category is not dropped in silence. Worth recording as an example of a known practice arrived at independently: when a tree must be compared across cases, the top gets standardised and the bottom stays free.
- **C3 — a fixed seam rate card** (1.5 / 3 / 5 pd, doubled at the top assembly node), replacing a card each run invented for itself, since seam cost is 17–29% of the estimate.

**A fourth constant, C4:** the static blind-spot list of each sensor is method metadata supplied to it, not something the run derives. Both Step A sensors now carry one — the reference class list (this team, these dependencies, feature difficulty, sponsor quality, dating of base rates, tail-truncating interventions, misclassification) is deliberately symmetrical to the decomposition list, because FaxRxTx showed the divergence can carry either sign and the diagnostician needs both catalogues to attribute it.

**A prediction, corrected before it was tested.** The first version recorded here said that pinning would pull the level *down*, reasoning from an observed mean price per leaf of ~11 pd against a band midpoint of 7.5. A parallel line of work caught the error: splitting conserves the sum arithmetically — a 14-day leaf becomes 7 + 7 — so capping M lowers the price per leaf and raises the leaf count by the same factor, leaving the total unchanged; the unpacking effect then pushes finer trees *up*. The 7.5 × 80 arithmetic held leaf count fixed, which is exactly what C1 does not do. Corrected prediction: **direction of the level change unknown and plausibly upward; spread expected to narrow.** If the spread does not fall materially, the constants were not the binding source of variance and the diagnosis of §12b was wrong.

The methodological point outlives the arithmetic: a prediction recorded in advance is only worth something if it is also *checked* in advance. This one would have survived contact with the data and been read as confirmation of whatever came out.

## 12d. The order of business (author, 2026-08-05)

Stated as a gate, not a wish list, and it reorders everything above:

1. **Repeatability first.** Until the same input reliably produces the same answer, every other claim — about calibration, about blind spots, about which sensor should own the centre — is speculation dressed as method. The ±17% of §12b and the ×2.36 specification shift are the current measurements of how far from repeatable the instrument is; the constants C1–C4 are the first attempt to close that, and their effect is unmeasured.
2. **Then an end-to-end regression set** of several cases run through the whole pipeline, not one case run repeatedly. Repeatability on one case says the instrument is stable; it says nothing about whether it stays stable when the project changes.
3. **Only then may the word "robust" be used** about anything in this project.

The order matters because each stage is the precondition of the next, and because the tempting move — improving the method on the strength of one case — is what produced the FaxRxTx composition of locally-correct errors (§12a, §12c).

## 12e. The hidden intermediate: "requirements → WBS" is not a mapping (author, 2026-08-14)

The step was read all along as a mapping — a list of requirements goes in, a WBS comes out. It is not. Between them stands a **tree of requirements**: a hierarchy and a grouping that the sensor constructs for itself, uses, and never writes down. It is built from scratch on every run, and it is not the same tree as the WBS.

- **It is a third free parameter, and a structural one.** §12b found two loose parameters and §12c pinned them as constants (granularity C1, the seam rate card C3). Both are *quantities*. The tree is *structure*, it sits upstream of both, and no constant reaches it: a run may satisfy C1 and C2 exactly and still have grouped the requirements differently than the run before. Whatever variance the tree carries, everything below inherits.
- **This is why the residual after C1–C4 cannot be attributed.** The gate of §12d puts repeatability first; the constants were the first attempt at it, and their effect was to be measured against the remaining spread. But a spread produced by an unpinned structure and a spread produced by pricing are indistinguishable in a single end-to-end number, so the measurement as designed cannot say whether the constants bound anything.
- **The remedy is a seam, not another rule.** A part cannot be held fixed while it lives inside one call: to pin the tree, the tree must exist as a file, and the sensor downstream of it must be *forbidden to rebuild it*. This is precisely the discipline PIPELINE.md already applies between agents — hired for what they may not see — never applied to the boundary *inside* `estimator-decomposition`. Proposal: a tree sensor under its own role letter, whose sole output is the requirements tree, and a decomposition sensor that receives the tree and never sees the project description. If it sees the description it will re-derive a tree of its own and the seam leaks silently.
- **Measurement design (nested, three levels).** Level 0: nothing pinned — the current end-to-end spread. Level 1: tree pinned, mapping and pricing free. Level 2: tree and WBS pinned, pricing free. Components by subtraction; and because subtraction of conditional variances is only as good as the choice of what was pinned, run level 1 against **three different trees**, so the spread of the per-tree means gives the tree component directly and checks the subtraction. Two constraints follow from what is already on record: the whole batch stays inside one `engine × model` stamp, since run16 puts the model effect at ×2.02 — larger than the entire version history, and enough to swamp any component being separated; and **only trees emitted by real runs may be pinned**. The July manual tree (Lytin-D 0.1, 486 pd, 26 leaves) against the unconstrained August definition (mean 1147) is the warning: a hand-made tree is plausibly out of distribution, and pinning one would measure the sensor's reaction to a foreign structure rather than the contribution of the step.
- **Register the prediction before the batch** (the C6 discipline). The claim under test is that the tree component dominates the pricing component and that pinning the tree narrows the spread materially. If it does not, the diagnosis here is wrong in the same way §12c's first prediction was wrong, and the variance lives somewhere else.

**The general form, which outlives the particular tree.** A large opaque transformation should be decomposed into a chain of smaller controllable ones, each with a named artefact at its boundary. That is C7 (`coverage at every split`) generalised: the rule was derived for splits *within* a tree, but it states something about any transformation — the output must be shown to cover the input. Each seam is a new place to apply it.

**A seam is not free, and its cost is the thing to watch.** In an unsplit call the model carries between steps everything it inferred but did not write; forcing an intermediate artefact makes that knowledge auditable and destroys whatever part of it never reached the page. So a chain can be steadier *and* land further from the truth — the distinction §12b already drew between repeatable and biased, now with a mechanism: bias lives in the seams. Introduce them one at a time, measure σ and the distance from actuals on each, and treat coverage at the new boundary as the measure of what was lost crossing it.

Candidate seams still unsplit, upstream to downstream: text → list of requirements; list → tree; tree → functions (already separate, C5); functions → WBS modules; leaves → size; size → price plus seam charge (C3). The lower boundaries have been cut and behave; the upper ones have not, and that is where the unattributed variance has been sitting.

## 12f. Comparing two structures: the agreement metric had a ceiling nobody had set (2026-08-20)

Run 18 registered a test for "do two product models agree": over all 2628 pairs of the 73 requirements, do both models put the pair under a common node? Jaccard came out at 0.16–0.27 and the prediction (above 90%) was not confirmed. The recomputation on normalised models — the one candidate explanation that cost nothing to test — is now done, and the finding is not about normalisation.

- **Normalisation explains nothing.** Collapsing single-child nodes (M7) adds a co-location only when parent and single child both declare coverage *and* declare different coverage. Across four models that happened four times, all in one of them. Two of the four figures did not move at all, one rose 0.007, one *fell* 0.010.
- **The metric has a ceiling set by verbosity, not by agreement.** The four runs declared 21, 44, 71 and 183 co-located pairs. A Jaccard between relations of size 44 and 183 cannot exceed 0.24 however perfectly they agree. Measured against their own ceilings the four pairings reach 34%, 66%, 70% and 92% of what was available to them.
- **On the containment reading the same data says something quite different.** The share of the sparser relation contained in the denser one is 0.43, 0.71, 0.76 and **0.93**. Nearly everything the sparse run says about what goes with what, the dense run also says. The models are not contradicting each other as much as they are describing at different resolutions.
- **The general lesson, which outlives this metric.** Any similarity measure between two decompositions is also a measure of how much each one chose to write down. Set-overlap statistics silently penalise the pair for that difference, and the penalty is indistinguishable from disagreement in the single number. So a structural-agreement test must either normalise for the size of each relation or report the sizes beside the score — and the choice must be registered before the runs, because after them every alternative measure is a way of getting the answer you now want. Containment was not registered here, and it is trivially satisfied by a run that declares almost nothing; it is reported next to the sizes and it does not rescue the failed prediction.
- **An implicit measurement convention turned out to be load-bearing.** Coverage was recorded with a mark for *partial* coverage, and no rule said whether a partly-covered requirement belongs to a node's identity. Counting partials, the two models declare 44 and 183 pairs; excluding them, 3 and 183. The comparison is not defined until that is decided once, for everybody. Wherever a sensor is allowed to record a judgement in two shades, the shades are part of the measurement and belong in the rules, not in the transcript. **Decided 2026-08-20, and the decision turned out not to be a convention at all** (author): a part is not an answer, so the mark may say *which part* a node realises but may not survive closure — at closure the parts covering a requirement must union to the whole obligation, or the residue is a named defect. What looked like a notational question was a hole through which work leaves silently.
- **And a protocol defect, cheaply fixed.** One of the four raw model records carried no parent pointers, so M7 could not be applied to it after the fact. The sensor definition demanded them; the transcription dropped them. Raw records must preserve exactly what later analysis needs, and the only safe rule is to preserve what the definition asked for, in full.

## 12g. Two rules about inputs, both from the author (2026-08-20)

Both came out of the same defect: the BMS assumption log said "not included: subsequent operation (hosting & support)" while the requirement list carried R02, R03 and R64, which demand exactly that. Run 18 caught it in one run of four; the other three priced over it silently.

**The imperative: what the client explicitly requires cannot be struck out.** An assumption may bound what a given number prices; it may not remove an obligation. Every obligation therefore has one of three visible outcomes — priced here · priced by another instrument (a service is a rate per unit time, not a project estimate) · not priceable without a parameter the client has not given, which is then named as a question. And the failure mode is an exception rather than a footnote: **if an id appears in neither the priced work nor the carried list, the run emits a defect report and no estimate.** A number produced over a struck-out obligation is not a worse estimate, it is an estimate of a different project, and nothing in the number says which.

What it outlaws is silence, not scope. That distinction is the whole design of it: the single most informative output run 18 produced was a list of work the decomposer judged necessary and refused to invent — from a run that proceeded and reported. An exception on every scope boundary would destroy that signal; an exception on silence produces more of it. The check is mechanical and can be run by anybody over the output, which is what makes it a rule rather than an intention.

**The procedure for a requirement that reads two ways:** ask the client · if no answer, assume · declare the assumption · and when runs are compared afterwards, exclude the differences the open question causes. The first three are ordinary practice. The fourth is a measurement rule, and it has a trap: **the filter must be a register pinned before the runs, never the runs' own ambiguity flags**, because a run that flags more requirements shrinks what it is scored on and improves its own agreement. So the register of open questions is an input, and the flags a run raises are evidence that the register needs revising.

Measured on run 18: 19 of 73 requirements were flagged ambiguous by at least one run, and excluding them raises the structural agreement on three pairings of four — Jaccard 0.277 → 0.362 on the closest pair, containment 0.43 → 0.63 on the weakest — without approaching the registered 90%. **Input ambiguity is a component of the disagreement, not the explanation of it.** Which is the useful form of the result: it puts a size on how much of a method's apparent instability is really the ambiguity of the document it was given.

## 13. Status

- The methodology (METHODOLOGY.md) is fixed.
- The first manual example (BMS, Phase 1: decomposition + reference class + Steps B/C/D) — done 2026-07-17, see §9 and examples/BMS/. There is no actual project outcome to check against (a training RFP).
- The second example (FaxRxTx/Venali) — done 2026-07-17, for the first time with a check against fact (soft, from memory ±20%), see §10 and examples/FaxRxTx/.
- The first check of hypotheses against open data (mars_model/, PROMISE datasets) — done 2026-07-17, see §11.6: the growth of the overrun-multiplier tail with size and the interface surcharge are confirmed; b>1 for the production function is not.
- The project name is the working code name **3A8** (after discarding taken options: EstimAI, Consensus/Konsensus, Triangulate — all conflict with existing products/terms in adjacent niches).
- No code is written until the methodology has been checked by hand on concrete examples.
- **The gate (§12d): repeatability → an end-to-end regression set of several cases → only then any claim of robustness.** Everything below is subordinate to that order.
- Repeat-run variance of the decomposition sensor — measured 2026-08-05 on BMS, see §12b: spread ±15%, but a ×2.2 level shift against the earlier specification. Next: pin granularity and the seam rate card as method constants, then re-measure.
- First end-to-end wrapper run (FaxRxTx re-run) — done 2026-08-05, see §12a: the pipeline's answer overshot the fact by ×4.2 while its own report flagged the failure on every available axis. Open work in priority order: a well-behavedness discount for global multipliers; a feasibility (staffing-ceiling) gate; repeat-run variance measurement for the decomposition sensor.
- The agent wrapper — first version built 2026-08-04: PIPELINE.md (visibility matrix, the two checkable disciplines, what is deliberately not automated) and four agent definitions in `.claude/agents/`. Open: verify the tool-restriction layer in a fresh session; run the contamination test against the remaining three agents; then re-run one of the existing examples end-to-end through the wrapper and compare with the hand-made run.
- The hidden intermediate between requirements and WBS — identified 2026-08-14, see §12e: the requirements tree is built inside the decomposition sensor and rebuilt every run, so it is an unpinned structural parameter upstream of C1–C4 and the residual variance cannot be attributed until it is cut out as its own sensor. Next: split the tree sensor off under its own role letter, register the prediction, then run the nested measurement (three pinned trees, one `engine × model` stamp).
- The `Hotyn` generation (product model → work model → estimate) — piloted 2026-08-19 (run 18, `examples/BMS/run18_product_model_pilot.md`). Step 2's inputs were built 2026-08-20: the requirement list is split into 68 product obligations and 5 demanded-work items, and `docs/technology_catalogue.md` 1.0 declares eight technology dimensions with the activities each mandates. Next: run `Hotyn-W` on the run-18 model, then `Hotyn-D`, then the falsification test — swap test-based assurance for formal verification and see whether the estimate moves.
- The inputs of the BMS case — repaired 2026-08-20, see §12g: `assumptions.md` v2 adds A0 (an obligation cannot be struck out, only bounded, and the remainder names its instrument or its missing parameter), A8 and A9 (the readings assumed for R13 and R14, which the RFP leaves open and empty respectively), A10 (the log is checked against the pinned lists before any run) and A11 (the open-questions register and the comparison rule). `examples/BMS/open_questions.md` is the register: ten questions, none of them askable — the RFP is a training document.
- Session records for cross-session analysis live in `sessions/` — one file per working session, written to be read without the conversation, with facts and readings kept separate. First entry: `sessions/2026-08-20_hotyn_chain_end_to_end.md`.
- The chain now runs end to end, and each step has its own measure of agreement — runs 20-22, 2026-08-20. Step 1, requirements to product tree: two identical runs agree on **0.31** of what goes with what (Jaccard on requirement co-location) and differ **x1.65** in node count. Step 2, tree to work model: **0.969** over 77 elements and 553 (element, activity) pairs, with every difference tracing to three classification calls, one of which costs nothing. Step 3, work model to person-days: **x1.288** on the total, with leaf count agreeing to x1.005 and price per leaf carrying the whole gap - the exact inverse of the Lytin arrangement, where count was free and price was stable. **The instability of the chain is concentrated in step 1.** The step-3 gap is a near-uniform multiplicative offset (x1.13-1.46 across all 27 obligations, sd 0.105), which is the correctable kind of disagreement; a scattered profile would not have been. And two instruments across six independent runs converged on the same seven gaps in the product model - no store for bookings, no surface for the rules R19 demands, nothing to price the subsystems meeting each other. **The three agreement figures are not one scale and must not be written as a product**: 0.31 and 0.969 are shares of statements two runs both made, where 1 is perfect; x1.288 is a ratio of two magnitudes, where 1 is perfect. The claim that step 1 dominates rests on the three **size** ratios, which are in one unit: x1.65 nodes, x1.02 items, x1.29 person-days. `examples/BMS/run20_work_crossing_pilot.md`, `run21_work_model_measurement.md`, `run22_estimate_measurement.md`.
- First within-cell measurement of the product-model sensor — run 19, 2026-08-20 (`examples/BMS/run19_product_model_measurement.md`): two identical runs of `Hotyn-M 1.1` (Opus, order A, same pinned inputs) agree on 68 of 68 requirements being wholly covered and on how much co-location to declare (44 pairs against 41, ×1.07, against ×8.7 across run 18's four models) — and differ by **×1.56 on anchored structure** and agree on only **31%** of what goes with what. The freedom relocated rather than disappeared: completion, the unbounded phase, came out stable at ×1.10, while the number of parts an obligation is divided into (1.41 against 2.00 nodes per requirement) now carries the gap. Both runs independently found two defects in the rules and reported a stale version stamp in their own definition instead of honouring it. Next: two repeats under 1.0 to establish whether the ×1.56 is new.
- The structural-agreement metric — recomputed 2026-08-20, see §12f: normalisation changes nothing, and the low Jaccard is in large part a ceiling imposed by how much co-location each run declares. Open: fix whether partial coverage counts towards a node's identity, and register a size-insensitive measure before the next batch rather than after it.
- Cross-project comparison with twotakt — done 2026-08-04, see §12: no cloud for either; the agent requirement here is genuine but is *isolation*, not autonomy; provenance of Step C parameters named as the candidate machine check; F8 (a check that has never gone red) is the open debt on the diagnostic step.

- **Stage 3 — the Hotyn chain run end to end against a documented outcome, 2026-08-22** (`examples/FaxRxTx/run29`…`run32`, session record `sessions/2026-08-22_faxrxtx_validation.md`). FaxRxTx pinned as an obligation list (N=52 -> 47 product + 5 work), declared, modelled (`Hotyn-M 1.1`, n=2), crossed (`Hotyn-W 1.1`, 570 items), classified (`Hotyn-D 2.0`, n=2), priced from the pinned table plus eleven gap-blind rows for the demanded work no dimension absorbs, assembled at **69.2 pm** and *then* checked: the fact is **~120 pm** (+/-20% -> 96-144). **The chain under-estimates by x1.735 and fails the exit criterion's centre test.** Corridor test **not scoreable** - the chain declares no P10-P90, only a perfect-correlation band SigmaO..SigmaP = 30.4..130.4 pm spanning x4.3. Provenance test passes: the number was fixed before the fact was opened.
- **The variance programme succeeded and the accuracy question turned out to be a different question** (R11). Two independent classification repeats over 79 elements priced to **x1.032**; the law of the record (R1: everything priced from a table agrees) held again. Across three generations on this one unchanged project the bottom-up centre has swung **x3.4** - 111.6 pm manual (2026-07-17, x0.93 of fact), 237.8/503 pm agent pipeline (2026-08-05, x4.19 calibrated), 69.2 pm Hotyn chain (2026-08-22, x0.58). Agreement between runs rose at every generation; accuracy against the fact did not, and the sign of the error flipped.
- **The reference class has been the closest instrument to the fact, twice** (R10) - P50 160 (x1.33) and 135 (x1.13) - and it is the instrument the project has invested least in. Against the bottom-up's x0.93, x1.98, x4.19, x0.58. If this survives a second outcome case it is a finding about what the bottom-up is *for*.
- **The estimate looks like element count x a constant** (R8): BMS 78 elements -> 17.3 pd each, FaxRxTx 97 -> 15.0 pd each, totals 7.7% apart for projects that are not remotely the same size. This is `docs/technology_catalogue.md` §14's registered concern arriving in numerical form, and it was written down **before** the fact was opened so it could not be invented afterwards. It is one of two candidates that could each carry the whole x1.735 gap; the other is the vintage (modern norms on a 2007-2009 stack with no cloud, no broker, no orchestrator). **n=1 cannot separate them**, and the single global level factor `L-1` = x1.735 conflates both. The separating experiment is cheap and is the project's next measurement: one `Hotyn-M` run at ~160-170 elements on the same pinned list, crossed, sized and priced with no rate changed.
- **The x1.56 anchored spread of run 19 was a property of the BMS input, not of `Hotyn-M 1.1`** (R5): on FaxRxTx two identical runs agreed to **x1.024** on anchored structure and 0.406 on co-location Jaccard, while exercising the same partition freedom (1.74 against 2.04 nodes per requirement). Two candidate mechanisms - a source that describes structure rather than obligations, and an assumption projection that pinned ten readings against BMS's six - are not separated.

- **Amendment, 2026-08-23 — the unit of the fact, and the rescoring of every outcome comparison the project has** (`examples/FaxRxTx/run33_unit_of_the_fact.md`). The author, asked one question registered in `docs/diagnosis_2026-08-23_the_gap.md`, stated that FACT.md's ~120 person-months is **staffed headcount x calendar**, and that this organisation's standing conversion from staffed presence to delivered work is **6/8**. The two sides of every comparison the project has ever made were therefore in different units: a rate-table row ("2 pd for an adapter") contains no leave, no holidays, no bench; "ten people on the payroll for a year" contains all of them. **The fact in the estimate's unit is 90 pm, not 120.** The Hotyn chain's miss goes from x1.735 to **x1.30** - the centre lands **1.35 pd below the gate on a total of 1452.5**, one classification repeat passes and the other does not, which on a fact with a +/-20% memory band is a result indistinguishable from the threshold. With the two corrections needing no new run (coverage holes +182 pd, scope decisions +151 pd) the residue is **x1.06**.
- **`L-1` = x1.735 is withdrawn** (x1.735 -> x1.301 on the unit correction -> x1.059 after the pending adjudications). A factor that shrinks by 40% when a unit error is fixed and by another 23% when two already-written lists are adjudicated was never a calibration. **FaxRxTx is no longer spent as evidence** - nothing in the chain is now fitted on it. And the **rate table is cleared**: the vintage and team-unfamiliarity candidates, each of which could have carried the whole gap, are no longer needed to explain anything, and inventing a factor for either now would be the fitting the discipline exists to prevent.
- **R10 is overturned.** Against the corrected fact the reference class overshoots **x1.50 and x1.78** and fails the gate on both readings; it looked closest only because it benefited most from the shared unit error, being the instrument that sat highest. **R11 is amended**: agreement improved *and* accuracy improved. What survives is narrower - the bottom-up centre swung x3.4 across three generations on one unchanged project, and that swing is method variance and is still unexplained.
- **R12 (new) - every calibration this project has ever applied moved the answer away from the fact, and all three moved it up.** July 111.6 -> 155 (x1.24 -> x1.72); August 237.8 -> 503 (x2.64 -> x5.59); `L-1` would have taken 69.2 -> 120 (x0.77 -> x1.33). The mechanism was registered as a hypothesis in July 2026 and is now visible: calibration pulls the estimate towards the ensemble P50, and both reference-class readings sit x1.5-1.8 above the only documented outcome. *Overturned by:* a calibration act on a documented case that moves the centre towards it.
- **The methodological finding, which outlives FaxRxTx: an outcome is an input, and inputs are pinned and interrogated.** Three instrument generations, twenty-plus runs, a rate card, a case-law register and an exit criterion all interrogated the estimate and took the fact as given. The largest single error term in the only comparison that can score any of it was in the fact's unit, and it cost one question to find. `FACT.md` has never been pinned, has no assumption log, and its load-bearing figure - the headcount - carries no stated band at all while the duration carries +/-20%. Standing rule proposed as **A13**: an outcome reconstructed as headcount x calendar is staffed presence, not delivered work, and converts at the declared utilisation before it is compared with any estimate.

- **Correction, same day (2026-08-23): the conversion is two factors, not one, and one of them may double-count.** The author clarified that holidays and leave are **on top of** the 6/8, and that the 6/8 is *within-day* effectiveness (meetings, training, other). So the fact converts by a presence fraction **and** an effectiveness fraction. But `assumptions.md` A7 v3 - the author's own adjudication of 2026-08-22 - says the table's unit is "one assigned working day, the convention the table's external sources natively record", and an assigned working day of a real professional **contains their meetings**. If that is what the table's rows mean, applying 6/8 to the fact counts it twice. **The table's own basis lines support the unfavourable reading** - "a day each plus a written note", "two or three sessions with experts ... and one correction round", "roughly a day per action/task" are descriptions of elapsed working days, not heads-down hours. Recorded in that form deliberately: the favourable reading arrived after a sensitivity table had already shown which answer would help.
- **The two thresholds, which make the argument moot for now.** With `k` = table person-days per nominal staffed person-day: **the chain as it stands passes the x1.3 gate iff k <= 0.749** (the author's 6/8 = 0.750 sits exactly on that line, which is why the centre landed 1.35 pd short); **the chain with the two pending adjudications passes iff k <= 0.921 - that is, as soon as any leave discount at all is admitted, under every reading.** So the definitional argument decides whether the chain passes *today*; adjudicating the 5 unsizeable elements and the 13 closure violations makes it pass under *every* reading. The order of work is unchanged and the unit ruling is not on the critical path.

- **Settled 2026-08-24 — person-hours, and the double count was mine** (`examples/FaxRxTx/run34_person_hours.md`). Author's ruling: move to person-hours, and **one table person-day = one assigned working day = 6 person-hours** ("two person-days for an adapter" is 12 hours, not 16). That figure appears on **both** sides of every outcome comparison — once in the estimate because the table's day is 6 hours, once in the fact because a present staffed day delivers 6 hours — so it **cancels**, and the ratio is `1.735 x L` independent of it. Applying 6/8 to the fact alone, as I did on 2026-08-23, was a double count worth x1.33 **in the estimate's favour**; the 90 pm figure and everything derived from it are withdrawn. **The only real conversion is the presence fraction `L`** — leave, holidays and sickness, which no rate-table norm contains and which "ten people on the payroll for a year" does.
- **The verdict, and it is robust to the one parameter still undeclared.** Chain as it stands **8 715 person-hours, x1.53 low, fails**; chain plus the two adjudications already on the desk (5 unsizeable elements + 13 closure violations, +182 pd; four scope decisions, +151 pd) **10 713 person-hours, x1.24 low, passes**. Across every plausible `L` from 0.83 to 0.91 the first always fails and the second always passes. **The two adjudications are now the entire difference between fail and pass** — no level factor, no era factor, no granularity experiment is needed to get there.
- **The project's whole outcome history, finally in one unit** (fact 13 306 person-hours at L=0.88): July 2026 manual raw **x1.057 PASS** · July calibrated x1.468 · August pipeline raw x2.252 · August calibrated x4.763 · reference class x1.515 and **x1.278 PASS** · Hotyn chain x0.655 · Hotyn + adjudications **x0.805 PASS**. **The best single reading this project has ever produced is its first one, made by hand in July 2026.**
- **Three of my own readings corrected.** **R11's amendment is withdrawn and the original stands**: in one unit the error runs x1.06 -> x2.25 -> x0.66 across three generations — agreement improved, accuracy did not, and it never returned to where the first hand-made run stood. **R10 does not survive in either form**: the class is x1.515 (fail) and x1.278 (pass), one of two, and the closest instrument in the project's history is the manual bottom-up, not the class; what survives is only that the class has never been badly wrong while the bottom-up has been badly wrong three times of four. **R12 is narrowed** to the two calibrations actually performed, both of which moved away from the fact and both upward; `L-1` would have landed at x1.136 purely because it was fitted to a number wrong by exactly 1/`L`.
- **The safeguard that worked was not a rule.** The double count was mine, in my own favour, reached within an hour of learning which answer would help, and what killed it was **stating the unfavourable reading in full, in the document, at the moment the favourable one was most attractive**. Worth making standing practice: when a correction moves a result towards a threshold, the document records the argument against it before it records the result.

- **Team grade is a required input to an outcome comparison, and it has never been declared** (`docs/team_grade_as_an_input.md`, 2026-08-24). Two quantities were riding in one sentence: **6 h in an 8-h day is time *availability*** — a property of the organisation's working day, the same for a brilliant engineer and a mediocre one who attend the same stand-up — while **what gets done in those hours is capability**, which lives in the table's *values*, not in the 6. Individual spread among professionals on the same task is ~2:1-4:1 (the folkloric 10:1/28:1 from Sackman 1968 conflates batch with time-sharing and is not sound); teams average their members so team-to-team spread is narrower, ~x1.5-2 within a grade band; COCOMO II's six personnel multipliers span ~x1.4-2.0 each and a realistic best-to-mediocre team ratio is **x2-3**. **Consequence: an instrument that does not know which team cannot be accurate to x1.3.** The method declares the grade its numbers assume and nothing downstream reads it; `FACT.md` records headcount and duration and nothing about who the ten people were. Same shape as the unit finding — it looked like noise in the world and it was a missing input. **Two for two.**
- **The FaxRxTx residue is exactly the size of a mismatch the input declared in advance.** The rate table assumes enterprise-competent engineers, silent on domain experience; `assumptions.md` A3 says flatly that this team **did not know the domain** — fax protocols, telecom, distributed delivery all new. That is an APEX/PLEX mismatch worth ~x1.2-1.4 on the COCOMO scale, and the residue after the two pending adjudications is **x1.24**. The sizes match. **Registered as prediction P-G1 and deliberately not applied**: what separates this from an invented factor is only that A3 declared it before any estimate existed, and that earns a scoreable prediction, not a multiplier. P-G1: a second outcome case whose team *did* know the domain lands within x1.15 after the same adjudications; refuted if it lands no closer than FaxRxTx did, which would point at the vintage or at R8 instead.

- **2026-08-24, the author's discipline lesson, and it lands.** Offered three true facts about the team — no domain knowledge, two months discussing the concept, 4-6 hours a week of English training — and then: *all of this is true, but all of it is motivated reasoning.* The point is exact and it was aimed at my own work: **the truth of a fact is not a defence, because motivated reasoning almost always runs on true facts.** The tell is the *search* and the *stopping rule* — I went looking after seeing the gap and stopped when the sizes matched. Applying the discipline to the three facts sorts them into three different categories: the **English training is already inside the unit** (the author's own definition of the 6-hour day names meetings, study and the like, and 4-6 h/week sits inside the 10 h/week already removed — counting it is the same double count killed the day before); the **two months on the concept** is a *declared parameter* of rate row `W-F48`, stated gap-blind inside the row before any total existed, so new evidence on it is a legitimate update; **only the domain inexperience** is both pinned and genuinely unpriced. Two of three true facts were already in the number. **The general form: the supply of true, effort-increasing facts about a past project is unbounded, so post-hoc explanation has no stopping rule and is therefore not evidence.** A residue is explained by parameters declared before the comparison or it stays a residue. `P-G1` withdrawn as stated — its x1.15 threshold was picked for being tighter than our own x1.24.
- **`W-F48` was low by x8.5, and the defect is structural.** The author's answer — all ten people, two months, nothing else — puts the concept stage at **2 218 person-hours, 17% of the project**; the row priced it at **261 person-hours, 2%**. Cause on the record and not the rate author's fault: `Hotyn-K` **refused** to price the stage from the client's stated calendar duration (correctly — a duration may not enter a rate table) and then had to invent a headcount to convert calendar into effort. **The refusal was right and the substitute was wrong by x8.5.** Fix: **stage headcount is a declaration parameter**, like environment count and cycle count, supplied by whoever knows the case before pricing — a gap-blind rate author has nothing to make it from. Overlap check against the element-design layer passes and is small either way: all 68 `K1` items are 421 person-hours, 4.8% of the chain, against a delta of 1 957.
- **FaxRxTx has stopped being able to score anything, and this time it does not come back.** Three case conditions arrived from the fact's owner after the estimate existed — staffed-versus-delivered time, the presence fraction, the concept stage's headcount. Each was legitimate; each was a real input nobody had asked for. Cumulatively the residue is x1.05-1.06, beneath the fact's own +/-20%, an undeclared `L` worth +/-5%, two unmade adjudications, and one parameter taken **directly from the fact** — ten people for two of twelve months *is* one sixth of the answer, by arithmetic. **A gate cleared by being handed a sixth of the answer is not a gate.** With the concept stage set aside, what is still under test is the rest of the chain: **x1.31 low as it stands, x1.06 after the two pending adjudications.**
- **The rule the whole episode produces: a case profile is pinned with the requirements, before any estimate.** Team grade and domain experience, declared overheads and what is inside them, the presence fraction, the process, and the staffing of every stage the method prices separately. Everything asked for over three days was a **case condition**, none of it was a requirement, and all of it was obtainable on day one from the same person. **A case whose conditions arrive after its number can be learned from. It cannot score.**

- **2026-08-24 — the estimate's own unit has never been checked, and it is worth x1.33** (`docs/the_unit_of_the_rate_table.md`). The author asked one question — *was the table compiled against a 6-hour or an 8-hour day?* — and the answer is on the record and is not the one three days of arithmetic used. `docs/rate_table.md` states **twice**, in text written by `Hotyn-K` alongside the values (§2 and §5), that **1 pd = 8 hours of net working time**. The 6-hour reading arrived on 2026-08-22 as a **relabelling with no value re-derived**; if the values were budgeted as 8 net hours and are then declared to mean 6, every number in the table quietly falls by a third. **And the party that wrote the values refused to certify the relabelling**: `Hotyn-K` raised the conflict unprompted in addendum A3 (*"those are not the same unit... the two sets of rows may not be summed without a declared conversion, and one of the two labels is wrong"*) and, when told of the adjudication, recorded its acceptance as **"accepted-on-statement, not as checked"**, because checking would mean reading the base table, which it may not do.
- **And the warning was then overridden.** The eleven A2/A3 rows were written in *assigned working days* on my instruction; `run31_raw/assemble_faxrxtx.py` sums them with 1 335 pd of base-table rows that declare a different unit. That is a defect in the assembly, not in the table.
- **The consequence: no score computed so far means anything.** Fact 13 306 person-hours (unambiguous — nominal staffed days x presence x 6 delivered hours). Chain, by reading of its own unit: **6 h/pd → 8 715 h, x1.53 low** · **8 h/pd → 11 620 h, x1.15 low** · **mixed, each document's own label → 11 385 h, x1.17 low**. With the concept stage at its real headcount the 8-hour and mixed readings land **within 0.3-1.4% of the fact**, and the two pending adjudications push them **20% over**. **The estimate spans x1.53 low to x1.21 high across readings of its own unit — a range of x1.85.**
- **The reading the evidence supports is the favourable one, and that is exactly why it is not adopted.** Under 8 h/pd everything moves the right way. Two explicit statements by the party that wrote the values, plus that party's refusal to certify the relabelling, do point at 8 — and *"the evidence supports the favourable reading"* is the sentence that should stop a scoring act, not complete one. **The unit is settled by re-derivation or not at all**: ask `Hotyn-K`, gap-blind, to price 10-15 of the same activities **directly in person-hours** and compare with the person-day values. One agent; nobody's word required.
- **The pattern, and it is the finding: three unit failures in three days, all the same shape.** (1) the **fact** — recorded as headcount x calendar and never interrogated, staffed presence read as delivered work; (2) **my arithmetic** — the 6/8 applied to one side only, a double count worth x1.33 in my own favour; (3) the **rate table** — its hour content asserted by relabelling, never re-derived, certification explicitly refused by its writer. **The method has been carrying an unverified unit on both sides of the comparison at the same time**, and every accuracy claim of the last three days rests on two conventions of which neither has been checked. It does not go away with a second case: **until the estimate's unit is re-derived and the fact's unit is declared, this method cannot score anything at all.**

- **Session closed 2026-08-25 — three unit errors, two of them mine, and the table fixed** (`sessions/2026-08-25_units_discipline_and_the_fixed_table.md`). Stage 3's x1.735 miss was, in the main, that the two sides of the comparison were never in the same unit, in three separate places: **the fact** was recorded as staffed headcount x calendar rather than delivered work (worth x1.10 in leave alone); **my arithmetic** applied the 6/8 within-day effectiveness to the fact only, a double count worth x1.33 in the estimate's favour, visible the moment both sides are written in hours because the h multiplies both and cancels; and **the rate table's** own hour content had been relabelled on 2026-08-22 from "8 net hours" to "assigned working day" with no value re-derived, silently cutting every row by a third — a relabelling the party that wrote the values had declined to certify (*"accepted-on-statement, not as checked"*). **The method was carrying an unverified unit on both sides at once.**
- **Settled by measurement, not by statement.** Two gap-blind `Hotyn-K 1.0` x Fable 5 runs priced 24 cells directly in person-hours. Both stated unprompted that **1 person-day = 8 net hours** — matching the table's original text — and both, asked separately afterwards, put a real working day's yield at **5–6 net task hours**. Three independent runs, one answer, two distinct quantities. The designed test (ratio of re-derived hours to table person-days) **failed**: within one run it ranges 3.0 to 11.4, dominated by magnitude noise rather than by the unit. It was the direct statements that answered the question.
- **And, by accident, the first repeatability reading of the rate author: x1.38 on the sum of 24 cells, x1.50 on the median cell, 3 of 24 agreeing exactly.** Conventions agreed perfectly; magnitudes did not. **This is R1 one level up** — the rate card was introduced to cure sampled magnitude, and the card itself was sampled exactly once. The author's decision makes it moot: **the table is fixed as a set of constants**, so the spread is provenance and not the dispersion of any estimate, and the proposal to build the chain's missing corridor out of it is withdrawn.
- **The number, after both fixes.** Chain 1 423.2 table person-days = **11 386 net person-hours** = **99.4 staffed person-months** (97.8–101.0) against a fact of **120** — **x1.21, inside the x1.3 gate**. It gets there **despite** pricing the concept stage at ~2% of the project where the fact puts it at 17%: `W-F48` is low by **x8.5**, because `Hotyn-K` correctly refused to price the stage from the client's stated calendar duration and then had to invent a headcount to convert calendar into effort. **Stage headcount is a declaration parameter**, not a gap-blind rate author's to assume.
- **R9–R12 are suspended, not overturned.** All four compared estimates **across generations in units that were never declared**; rescoring them twice in three days was comparing quantities of unknown dimension, and that was the root of the spiral. Reinstating any requires first recovering the cited run's unit. What survives is one sentence: **the unit of a comparison is part of the comparison, and it is an input on both sides.**
- **Working method changed at the author's instruction.** The pattern named: a fact is checked, an incidental observation is promoted to a finding in the same turn, written up, made the basis of the next turn — and then turns out not to be what it was called. Four rules adopted: **one question per run**, written before launch, answered first; **by-products are parked, not promoted, and may not change a conclusion in the turn they appear**; **no document without the author's word** (raw transcripts excepted); **"nothing came of it" is a legitimate result** — treating every run as owing a finding is what manufactures the spiral. Plus a standing table of where stability is expected and where it is not, so that model-sampled magnitude varying x1.3–2 is reported as expected rather than as news.

- **Author's decision, 2026-08-25 — the comparison counts.** Asked whether a score is valid when the fact's unit conventions were supplied by its owner *after* the estimate existed: *"yes, we count it. An explanation after the fact is a poor explanation — but it is still an explanation."* **FaxRxTx therefore becomes case 1 of the four `docs/exit_criterion.md` requires, and it passes on the centre**: estimate **99.4 staffed person-months** against a fact of **120**, **x1.21**, gate `[92.3 … 156.0]`, with **both classification repeats also inside** (97.8 and 101.0). Corridor test remains **not scoreable** — the chain declares no P10–P90. Provenance test **passes**: nothing is fitted on the case, `L-1` stays withdrawn. Standing: **1 of ≥ 4 cases, 1 of 1 passing.**
- **What the decision covers, and what it does not.** Two things arrived late and only one is in the number. **Unit conventions** — staffed-versus-delivered, the x1.10 leave factor, the table's 8 net hours — say what the numbers *mean* and are in the score. **The concept-stage headcount** — all ten people for two months — is the *value* of a declared parameter and is **not applied**: the chain reaches x1.21 while still pricing that stage at 2% of the project where the fact puts it at 17%. That the pass survives a known x8.5 under-pricing of one stage is the more informative half of the result.
- **The constructive half: case 1 sets the floor, not the standard.** The author's own wording marks an after-the-fact explanation as poor evidence, so cases 2–4 are held to the stricter rule — the case profile pinned **before any estimate exists**. Without that, the gate would measure how well a case's conditions can be reconstructed afterwards rather than how well the instrument estimates. This supersedes `run35` §6, whose argument that the case could no longer score stands as reasoning and loses to the decision.
