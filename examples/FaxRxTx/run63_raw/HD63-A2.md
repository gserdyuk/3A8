<!-- orchestrator header — written by the orchestrator, not by the sensor
reading: HD63-A2
engine (sensor's own stamp): Hotyn-D 2.0
run: 63
subagent: 3a8:work-estimator · agent id ad51d96bf6159b282 · launched with model alias `opus`
model per turn (from the transcript): claude-opus-5
harness version(s): 2.1.270
prompt: prompt_size_A.md · md5 bba73201a3db407c3d6d7c47247ea1ef · md5 of the message the sensor received bba73201a3db407c3d6d7c47247ea1ef · identical: yes
turns: 1 · stop reasons: end_turn · output tokens per turn: 9461
user-side messages after the prompt (continuations): none
tool uses: 0 · harness attachments: environment, model, instructions, session_context, date, prompt_snapshot, prompt_snapshot
the reply below is verbatim: the assistant text blocks of the transcript, in order, joined by one blank line
-->

# Hotyn-D 2.0: size classification of subtree N10 of HM61-1

I read no files. The input was the pasted batch and nothing else.

## 1. Contamination check

**Clean. Classification proceeds.**

- **Input 1 and Input 2:** neither contains an effort figure, person-day value, rate, price, budget, duration, team size, prior estimate or rate-table row.
- **Numbers that are not effort:**
  - Activity codes (A2, A10, D4, G1, K1 and so on) are labels.
  - "10–20 points of presence" (F38) counts sites.
  - Catalogue versions (1.3, 1.4) and the S / M / L / XL thresholds are part of the rules.
- **Ambient repository information:** the harness put some in my context: a git status, recent commit subjects, a user memory index and environment details. Following the quarantine instruction, I did not use any of it as input and did not treat it as contamination.

## 2. Element sizing table

**Reading used throughout:** an element's declared content is its name plus the full text of every obligation it covers. The rules give no way to limit an obligation's text to one element. So an obligation shared by several leaves (F05, F11) is counted in full on each of them. The shorter, name-limited reading is given in the doubts wherever it would change a class.

**The F05 list, used on every element that covers F05:**
1. select the attachment form per user configuration
2. attach the fax page by page as TIFF
3. convert the fax to PDF
4. attach the PDF

### Subtree N12: Inbound conversion

| id | name | class | enumeration (verbatim) | count | size class |
|---|---|---|---|---|---|
| L05 | Delivery-mode selector per user configuration | behaviour | 4 actions: select attachment form per user configuration (F05; also the name token "selector per user configuration", not counted again under P-2), attach fax page by page as TIFF (F05), convert fax to PDF (F05), attach PDF (F05) | 4 | **L** |
| L06 | Page-by-page TIFF attachment packager | behaviour | 4 actions: select attachment form per user configuration (F05), attach fax page by page as TIFF (F05; also the name token "page-by-page TIFF attachment packager", P-2), convert fax to PDF (F05), attach PDF (F05) | 4 | **L** |
| L07 | TIFF-to-PDF converter | behaviour | 4 actions: select attachment form per user configuration (F05), attach fax page by page as TIFF (F05), convert fax to PDF (F05, F06; also the name token "TIFF-to-PDF converter", P-2), attach PDF (F05) | 4 | **L** |
| L08 | OCR step within PDF conversion | behaviour | 1 action: digitise (OCR) the fax (F07). "As part of the conversion to PDF" is context; the conversion is not named as this element's action | 1 | **S** |
| L09 | Third-party OCR library adapter | interface | 1 operation: perform OCR through the third-party library (F08). No protocol or auth concern is named | 1 | **S** |
| L10 | OCR worker process | behaviour | 1 action: carry out digitisation (F09; also the name token "OCR worker", P-2) | 1 | **S** |
| L11 | Conversion worker process | behaviour | 2 actions: convert (F11; also the name token "conversion worker", P-2), send (F11) | 2 | **M** |

### Subtree N13: User email delivery

| id | name | class | enumeration (verbatim) | count | size class |
|---|---|---|---|---|---|
| L02 | Incoming-fax email delivery to user | behaviour | 5 actions: send the fax email to the user (F02 "receives incoming faxes by email" and F10 "email carrying the fax is sent to the user", one action; also the name token "email delivery to user", P-2), select attachment form per user configuration (F05), attach fax page by page as TIFF (F05), convert fax to PDF (F05), attach PDF (F05) | 5 | **L** |
| L12 | Email sending worker process | behaviour | 2 actions: convert (F11), send (F11; also the name token "email sending worker", P-2) | 2 | **M** |
| L55 | Email delivery outcome tracking | behaviour | 1 action: control delivery of every fax (F46; also the name token "delivery outcome tracking", P-2). No outcomes are named, so P-3 does not apply | 1 | **S** |

### Directly under N10: Inbound fax path

| id | name | class | enumeration (verbatim) | count | size class |
|---|---|---|---|---|---|
| L04 | PoP inbound TIFF receiver | interface | 1 operation: receive fax TIFF files from points of presence at the Miami data centre (F04). F38 "10–20 points of presence" is a number of sites, not an operation (P-6 reasoning), and counts nothing | 1 | **S** |
| D01 | Fax number to user directory | store | 2 entity kinds: fax number, user (from the name, read with triggers L02 and L04) | 2 | **M** |

## 3. Special counts

**D01, activities G1 / G2 / G3 (entity kinds that must be pre-loaded before the system is usable):**

1. **Fax number.** Numbers are assigned at the points of presence, and no element in this batch creates them. Until a number exists in the directory, a fax arriving through L04 has no recipient to route to, so the inbound path cannot work.
2. **User (the email recipient).** No element in this batch creates users. L02 cannot deliver until the user a number maps to already exists, with an email address.

**Count: 2. Class: M.**

- No other special counts apply: G2m / G3m / G4m are not crossed onto any element, and A9 does not apply because the batch has no statement elements.
- D4 is computed outside me and is not reported.

## 4. Unsizeable elements

None. Every element's name and coverage was enough to enumerate from.

## 5. Doubts

1. **F05 counted in full vs limited by name (L05, L06, L07).** If each element counts only the F05 action its name claims, L05 counts 2 (the selector's two outcomes, TIFF mode and PDF mode, under P-3), L06 counts 1 and L07 counts 1. All three would drop from L to M or S. L02 would be unaffected because its name covers all of delivery. I kept the full-text reading.
2. **"Select attachment form per user configuration" as a named action.** F05 says "depending on the user's configuration", which implies a choice but has no explicit verb. Without it, L05, L06 and L07 count 3 and drop from L to M. L02 counts 4 and stays L.
3. **L02: F02 and F10 as one action or two.** "Receives by email" and "is sent" read as the same delivery. Counted separately, L02 counts 6 and stays L.
4. **L04: TIFF as a protocol concern.** If "arrive as TIFF files" counts as a named format or transport concern, L04 counts 2 and moves from S to M. I read TIFF as the form of the payload, not a protocol.
5. **L04: F38 names a range of sites (10–20), not operations.** It counts nothing here, but it may matter to per-site work computed outside me.
6. **L08: "as part of the conversion to PDF" as an action of this element.** Counted, L08 counts 2 and moves from S to M.
7. **F11 counted in full vs limited by name (L11, L12).** If each worker counts only its own verb (convert for L11, send for L12), each counts 1 and drops from M to S. I kept the full-text reading.
8. **L55 / F46:**
   - "Delivery control" names no outcomes (such as delivered or failed), so it counts 1.
   - F46 says "every fax", which reaches beyond this inbound email subtree, but the element sits only here.
   - The obligation is worded like a property ("a mandatory property, not an option") while the element is classed behaviour. I named this and did not change the class.
9. **D01 entity kinds.**
   - The number-to-user assignment itself could be read as a third entity kind: count 3, still M.
   - The user's delivery configuration (F05, reached through trigger L02) could be read as a fourth: count 4, class L. The name does not mention it, so I did not count it.
10. **D01 pre-load count.** If users are created at run time by an element outside this batch, the pre-load count drops from 2 to 1 (S), with only fax numbers remaining.
11. **Duplicated coverage.** F05 is covered by four leaves (L02, L05, L06, L07) and F11 by two (L11, L12). Under the full-text reading, this raises the counts of each of those leaves.

**Contradictions between obligations:** none found.

## 6. Closure violations

Each item below is work I judge necessary that the model does not contain. None is sized.

1. **Resolving a fax number to its user.** No behaviour element looks up the called number in D01 to find the user who should receive an incoming fax. D01 is triggered by L02 and L04, but no leaf's content names the lookup.
2. **Where the user's delivery configuration is kept.** F05 depends on "the user's configuration", but no store or directory in the batch names it as content.
3. **Handing work to the workers.** F09 and F11 name workers (L10, L11, L12), but no element hands jobs to them or passes a fax between the OCR, conversion and sending steps.
4. **Interface to the outbound email channel.** L12 sends email, but the batch has no interface for the email channel. Under P-1, a named delivery channel is a protocol concern that belongs on an interface.
5. **Keeping D01 up to date at run time.** No element assigns, changes or releases the link between a fax number and a user. D01 carries only seed-data activities (G1 to G3).

## 7. Instrument readings

**Engine: Hotyn-D 2.0**

- Elements received: 15 (3 aggregates: N10, N12, N13; 12 elements to size)
- Elements sized: 12
- Size-class distribution: S 5 (L04, L08, L09, L10, L55) · M 3 (L11, L12, D01) · L 4 (L02, L05, L06, L07) · XL 0
- Statement kinds: compliance 0 · behavioural 0
- Unsizeable count: 0
- Doubts count: 11
- Closure violations count: 5
