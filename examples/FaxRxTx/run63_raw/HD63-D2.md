<!-- orchestrator header — written by the orchestrator, not by the sensor
reading: HD63-D2
engine (sensor's own stamp): Hotyn-D 2.0
run: 63
subagent: 3a8:work-estimator · agent id a74fc99d54ae47c58 · launched with model alias `opus`
model per turn (from the transcript): claude-opus-5
harness version(s): 2.1.270
prompt: prompt_size_D.md · md5 3ba460367bd0c7cf9bf24051ce54841e · md5 of the message the sensor received 3ba460367bd0c7cf9bf24051ce54841e · identical: yes
turns: 1 · stop reasons: end_turn · output tokens per turn: 2384
user-side messages after the prompt (continuations): none
tool uses: 0 · harness attachments: environment, model, instructions, session_context, date, prompt_snapshot, prompt_snapshot
the reply below is verbatim: the assistant text blocks of the transcript, in order, joined by one blank line
-->

# Hotyn-D 2.0 — size classification for model `HM61-1`, subtrees L39 and L56

## 1. Contamination check

**Clean. Classification went ahead.**

- **Ambient context:** the harness added repository and user material to my context: a git status, recent commit subjects, directory names and a user memory index. I quarantined all of it and did not use it. I read no files.
- **Pasted input (INPUT 1 and INPUT 2):** it contains no effort figure, person-day value, rate, price, budget, duration, team size, prior estimate or rate-table row. The identifiers it does contain (F29, F47, A2–A4, D4, K1, K2, catalogue 1.3 and 1.4) are labels, not quantities.

## 2. Element sizing table

**Subtree L39**

| element id | name | class | enumeration (verbatim) | count | size class |
|---|---|---|---|---|---|
| L39 | Inter-component API | behaviour | 2 actions: components communicate through the database; components communicate through an API. (Under P-2, the name token "API" is already covered by F29 and is not counted again.) | 2 | **M** |

**Subtree L56**

| element id | name | class | enumeration (verbatim) | count | size class |
|---|---|---|---|---|---|
| L56 | First-version core function set, as replaced | behaviour | none possible: F47 names "the core functionality of the existing first version" but never lists its functions (see section 4) | — | **unsizeable, no class** |

## 3. Special counts

None apply. Neither element carries A9, G1, G2, G3, G2m, G3m or G4m, and neither is a statement or a store. The D4 driver is computed outside me.

## 4. Unsizeable elements

- **L56 — First-version core function set, as replaced: unsizeable, model defect (M10).**
  - **What was missing:** the actions to count are the functions of the first version's core set, and neither the element name nor F47 ("The new system replaces the core functionality of the existing first version") names any of them.
  - **Why it cannot be counted:** "core functionality" is an unnamed set, so under P-5 it counts nothing. It is also the case P-6 guards against: the element declares a set it never lists. The model needs to name the core functions being replaced before this element can be sized.

## 5. Doubts

1. **L39, count could be one shorter (2 → 1, which would make it S).** "communicate through the database and an API" could be read as one action with two channels rather than two actions. I kept 2 because the two routes are named separately, and P-1 and P-3 both count named alternatives separately. This doubt decides the class, since the class boundary sits between 1 and 2.
2. **L39, class does not fit the content.** The element is named like an interface ("Inter-component API"), but it is classed behaviour. Had it been classed interface, the thing to count would be operations exposed or consumed, and F29 names none, so it would be unsizeable (M10). I sized it under its declared class and did not reclassify it.
3. **L56, a literal reading was rejected.** L56 could be read as one action, "replace core functionality", which would make it S. I rejected that reading: the verb there describes the whole element rather than naming a function inside it, and sizing on it would mean guessing a scope the model does not declare (P-6).

I found no contradictions between F29 and F47.

## 6. Closure violations

**None.** This batch shows only two leaves and not the rest of the model, so I cannot claim that any work is missing from the model as a whole.

One adjacent point, noted but not raised as a violation: F47 says the first version's functionality is replaced, but says nothing about its data. If no store element elsewhere covers first-version data, that gap would be a closure violation. I cannot check that from this input.

## 7. Instrument readings

- Engine: **Hotyn-D 2.0**
- Elements received: 2 (both behaviour leaves, no aggregates)
- Elements sized: 1
- Size-class distribution: S 0 / M 1 / L 0 / XL 0
- Statement kinds: compliance 0 / behavioural 0
- Unsizeable count: 1 (L56)
- Doubts count: 3
- Closure violations count: 0
