<!-- orchestrator header — written by the orchestrator, not by the sensor
reading: HD63-D1
engine (sensor's own stamp): Hotyn-D 2.0
run: 63
subagent: 3a8:work-estimator · agent id a8d86b8dc25426206 · launched with model alias `opus`
model per turn (from the transcript): claude-opus-5
harness version(s): 2.1.270
prompt: prompt_size_D.md · md5 3ba460367bd0c7cf9bf24051ce54841e · md5 of the message the sensor received 3ba460367bd0c7cf9bf24051ce54841e · identical: yes
turns: 1 · stop reasons: end_turn · output tokens per turn: 2349
user-side messages after the prompt (continuations): none
tool uses: 0 · harness attachments: environment, model, instructions, session_context, date, prompt_snapshot, prompt_snapshot
the reply below is verbatim: the assistant text blocks of the transcript, in order, joined by one blank line
-->

# Hotyn-D 2.0 — size classification, HM61-1, subtrees L39 and L56

## 1. Contamination check

**Clean. Classification went ahead.**

- **Pasted input.** It has no effort figures, person-day values, rates, prices, budgets, durations, team sizes, prior estimates or rate-table rows. The only numbers in it are:
  - ids (L39, L56, F29, F47, N00);
  - activity codes (A2, A3, A4, D4, K1, K2);
  - catalogue version labels;
  - the pinned count thresholds.
- **Activity codes K1 and K2.** These are ids of work activities, not rate-table rows. They carry no values.
- **Ambient context.** The harness added repository information before the task: a git status, recent commit subjects and a memory index. I set all of it aside and did not use it. As instructed, it does not stop the run. I read no files.

## 2. Element sizing table

### Subtree L39

| element id | name | class | enumeration (verbatim) | count | size class |
|---|---|---|---|---|---|
| L39 | Inter-component API | behaviour | 2 actions: communicate between components through the database, communicate between components through the API. The name token "API" is already covered by F29, so it is not counted again (P-2). | 2 | **M** |

### Subtree L56

| element id | name | class | enumeration (verbatim) | count | size class |
|---|---|---|---|---|---|
| L56 | First-version core function set, as replaced | behaviour | — unsizeable, see section 4 | — | **none** |

## 3. Special counts

**None required.** Neither element carries A9, G1/G2/G3 or G2m/G3m/G4m, and neither is a statement or a store. The D4 count is computed outside this engine, so it is not reported here.

## 4. Unsizeable elements

- **L56 — First-version core function set, as replaced: unsizeable, model defect (M10).**
  - **What it declares:** the name says it is a function *set*, and F47 says "The new system replaces the core functionality of the existing first version."
  - **What is missing:** neither names a single member function. "core functionality" is a collective reference with no named members (P-5, P-6).
  - **Why it cannot be counted:** the replaced functions are the element's whole content, and they are never listed. Counting them would mean guessing.
  - **Needed to size it:** the model must name the first-version core functions this element replaces.

## 5. Doubts

1. **L39, one item shorter (count 1, class S).** "communicate … through the database and an API" could be read as one action, "communicate between components", with two ways of doing it. I kept the stated reading: the obligation names two different objects the communication goes through, so it counts as two actions.
2. **L39, scope of the database item.** The element is named "API", but F29 also names communication through the database. That item might belong to a store-side element instead. I did not reassign it, because coverage decides and F29 sits on L39.
3. **L39, possible interface content.** F29 names an API, which is interface-type content, but no operations are named. Any interface element holding those operations would be outside this batch, so I cannot confirm whether it exists. I am not raising this as a closure violation.
4. **L56, alternative reading rejected.** The element could be read as one action, "replace core functionality" (class S). I rejected that because the element declares a set and names none of its members (P-6).

I found no contradictions between obligations.

## 6. Closure violations

1. **Missing inventory of the first version's core functions.**
   - **What is missing:** F47 depends on a defined list of the existing first version's core functions, and no element or work item in this batch produces that list.
   - **What it would cover:** identifying and naming each core function that the new system must replace. That list is also what L56 needs before it can be sized.
   - **Price:** not priced.

## 7. Instrument readings

- **Engine:** Hotyn-D 2.0
- **Elements received:** 2 (L39, L56)
- **Elements sized:** 1 (L39)
- **Size-class distribution:** S 0 / M 1 / L 0 / XL 0
- **Statement kinds:** compliance 0 / behavioural 0
- **Unsizeable count:** 1 (L56)
- **Doubts count:** 4
- **Closure violations count:** 1
