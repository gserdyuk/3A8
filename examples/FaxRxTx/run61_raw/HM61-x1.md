<!-- orchestrator header — written by the orchestrator, not by the sensor
reading: HM61-x1
engine (sensor's own stamp): Hotyn-M 2.1
run: 61
subagent: 3a8:model-builder · agent id a9ec21cd5d171b8da · launched with model alias `opus`
model per turn (from the transcript): claude-opus-5
harness version(s): 2.1.270
prompt: prompt_M.md · md5 b7eb5e73e2b2d09df2839e15b32bb459 · md5 of the message the sensor received b7eb5e73e2b2d09df2839e15b32bb459 · identical: yes
turns: 1 · stop reasons: end_turn · output tokens per turn: 2283
user-side messages after the prompt (continuations): none
tool uses: 0 · harness attachments: environment, model, instructions, session_context, date, prompt_snapshot, prompt_snapshot
the reply below is verbatim: the assistant text blocks of the transcript, in order, joined by one blank line
note: first launch, stopped at section 1 for contamination (a duration figure quoted in assumptions_product.md); not a reading
-->

# Hotyn-M 2.1: contamination report, no model built

The model was not built. The input contains a duration figure, and the engine rules say to stop and report contamination when that happens. No section after §1 was produced: no skeleton, accretion, completion or closure, and no instrument readings.

## 1. Contamination check

**Contaminated: this run stopped at §1.** Separately, ambient context from the harness was present (the git status, recent commit subjects, an untracked file name, and a user memory index). I quarantined all of it and did not use it. On its own it would not have stopped the run.

**Where the contamination is.** It is in INPUT 2 (`assumptions_product.md`), in the paragraph headed "Removed here, and where it lives". Its first item names the effort scope of the immersion stage and gives the stage's length in months. That is a duration figure. I am not repeating it here because this output must carry no such figure.

**Why it stops the run even though it is described as removed.**
- The paragraph says what was removed, but it quotes the removed figure while doing so. The builder has therefore already seen it.
- The rule is to stop on any duration in the input, whatever its framing. The pinned log gives its own reason: "a builder shown a duration starts estimating."
- Once the figure has been seen, the run cannot be un-seen, so a model built now would be a contaminated reading of the list.

**Other numbers I checked and judged not to be contamination:**

| where | why it is not contamination |
|---|---|
| F36 and F37 (the faxes-per-day figure and the burst rate) | Throughput the product must meet. The day length in F36 is part of a rate, not how long the work takes. |
| F38 and F39 (the PoP count and the node count) | Scale properties of the product. |
| F19 (the rough count of input formats) | A count of things in the product. The note that each format is separate work is about how the work is split, not its size. |
| P3 (the era the stack comes from) | Describes the technology generation; it is not a deadline. |
| The pin date and the version number | Identify the input; neither is a deadline. |
| INPUT 1 header (the full list has 52 entries; 5 work obligations sit in another file) | Requirement counts and ids only; no effort or time. |

## Proposed fix

One edit is needed before a clean run is possible:
1. **Edit:** in `assumptions_product.md`, rewrite that "Removed here" item so it names what was removed without quoting the figure. For example: "the effort scope attached to the immersion stage (A1 — the stage is an obligation on the **work**, F48, and is not in your list)".
2. **Re-pin:** take the new md5 and bump the pin version.
3. **Re-run:** use a fresh instance. This instance has seen the figure, so it cannot be the one that builds the model.

INPUT 1 (`requirements_product.md`) needs no change. I found nothing contaminating in it.

## Engine stamp

Engine `Hotyn-M 2.1`. No counts are reported, because no model was built.

## Files

I read no files. The files this report concerns are named only in the input (I only saw their names):
- `assumptions_product.md` needs the edit above; its full path was not given.
- `requirements_product.md` is unchanged (pinned md5 `306046dc6cab35147040224e7a4e9662`).
