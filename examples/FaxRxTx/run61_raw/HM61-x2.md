<!-- orchestrator header — written by the orchestrator, not by the sensor
reading: HM61-x2
engine (sensor's own stamp): Hotyn-M 2.1
run: 61
subagent: 3a8:model-builder · agent id aea353735b9272cd2 · launched with model alias `opus`
model per turn (from the transcript): claude-opus-5
harness version(s): 2.1.270
prompt: prompt_M.md · md5 b7eb5e73e2b2d09df2839e15b32bb459 · md5 of the message the sensor received b7eb5e73e2b2d09df2839e15b32bb459 · identical: yes
turns: 1 · stop reasons: end_turn · output tokens per turn: 1763
user-side messages after the prompt (continuations): none
tool uses: 0 · harness attachments: environment, model, instructions, session_context, date, prompt_snapshot, prompt_snapshot
the reply below is verbatim: the assistant text blocks of the transcript, in order, joined by one blank line
note: first launch, stopped at section 1 for contamination (a duration figure quoted in assumptions_product.md); not a reading
-->

**Hotyn-M 2.1**: I stopped this run for contamination and built no model.

## 1. Contamination check

**Contaminated, so the run is stopped.** INPUT 2, the assumption log, contains a work-duration figure. Its "Removed here, and where it lives" paragraph names "the effort scope and the ~1–2 month figure attached to the immersion stage (A1…)". The paragraph says the figure was removed, but it states the figure while doing so. That is a duration tied to an effort scope. My engine definition says I must stop when the input contains a duration or an effort figure, and it gives no exception for figures that are about work outside the product.

**Other checks, all clean:**
- There is no budget, deadline, team size, "projects like this cost X" anchor or previously built tree.
- F36 and F37 (~1,000,000 faxes per 10-hour day, ~30/s, ~300/s) are throughput properties of the product, not how long the work takes, so they are not contamination.
- F38 and F39 (10–20 points of presence, a cluster of ~16–20 nodes) are the size of the deployment, not the size of a team.
- F19 says each format is "a separate piece of integration and stabilisation work". That describes how the renderer set is structured and gives no figure.

**Ambient repository information:** the harness prepended a git status, recent commit subjects, a branch name and a memory index. I quarantined all of it: I did not read or use it, and it played no part in this verdict. I read no files.

## What was not produced

Sections 2–9 (skeleton, accretion, completion, convergence, final model, closure, residues, instrument readings, assumption log) are not produced. Building any part of the model after seeing the figure would make this run's readings invalid.

## Fix for a re-run

- **Change needed:** in the assumption log's "Removed here" paragraph, delete the "~1–2 month" figure and keep only the reference, e.g. "the effort scope and the duration attached to the immersion stage (A1 — …)".
- **Check afterwards:** since the log's text changes, its checksum (md5) will change too. Re-pin it and re-launch with the same pasted input and Order A.
- **Requirement list:** the 47-item list itself was clean on every check and needs no change.
