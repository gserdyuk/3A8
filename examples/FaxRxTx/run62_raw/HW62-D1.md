<!-- orchestrator header — written by the orchestrator, not by the sensor
reading: HW62-D1
engine (sensor's own stamp): Hotyn-W 1.2
run: 62
subagent: 3a8:work-crosser · agent id ac2ad406f2311dc10 · launched with model alias `opus`
model per turn (from the transcript): claude-opus-5
harness version(s): 2.1.270
prompt: prompt_D.md · md5 1768017472077fb49cb0986be35219ea · md5 of the message the sensor received 1768017472077fb49cb0986be35219ea · identical: yes
turns: 1 · stop reasons: end_turn · output tokens per turn: 4285
user-side messages after the prompt (continuations): none
tool uses: 0 · harness attachments: environment, model, instructions, session_context, date, prompt_snapshot, prompt_snapshot
the reply below is verbatim: the assistant text blocks of the transcript, in order, joined by one blank line
-->

# Hotyn-W 1.2 — work model for subtrees L39 and L56 of HM61-1 (partial run)

## 1. Contamination check

**Clean. Proceeding.**

- **Figures in the input:** none. There is no effort, budget, duration, team size, cost anchor, prior work model or estimate. F48 carried a duration, but the orchestrator struck it before handing over the input, so nothing numeric remains. I did not guess at what it said.
- **Ambient context:** the harness added a git status, a branch name, recent commit subjects, a user-memory index and environment details. All of it is quarantined. I did not read, use or act on any of it, and I treated it neither as input nor as contamination.
- **Files:** none were read.

## 2. Scope of this run

- **Partial run.** Scope is the subtrees rooted at **L39** and **L56** of product model `HM61-1` (`Hotyn-M 2.1`), **2 elements** in all.
- Both roots are leaves, so the batch has **no parents**.
- The root `N00` and every other subtree are outside this run.
- Once-scoped and per-environment activities are **deferred** (listed in section 9).

## 3. Classification log

| id | name | position | class | notes |
|---|---|---|---|---|
| L39 | Inter-component API | leaf, covers F29 | **behaviour** | An API between components *of this system* is something the system does at run time. `interface` is reserved for exchanges with a system *outside* this one, and "inter-component" names an internal exchange. |
| L56 | First-version core function set, as replaced | leaf, covers F47 | **behaviour** | A "core function set" is what the system does at run time. "As replaced" places it as the new implementation of the first version's functions. |

There are no `statement` elements, so no justifying phrases are needed.

**Choices that could change the result (logged for comparison between runs):**
- **L39:** if the "components" are separately deployed systems, the reading becomes `interface`. That would add one A10 item and change nothing else, because K1, K2, A2, A3 and A4 apply to both classes.
- **L56:** see the product-model finding in section 7.

## 4. The crossing (compacted)

| activity | scope | elements receiving an item | count |
|---|---|---|---|
| K1 element design | per element | K1@L39, K1@L56 | 2 |
| K2 element implementation | per element | K2@L39, K2@L56 | 2 |
| K3 statement realisation and evidence | per element (statement) | — | 0 |
| A2 test design | per element | A2@L39, A2@L56 | 2 |
| A3 unit and component test implementation | per element | A3@L39, A3@L56 | 2 |
| A4 code review | per element | A4@L39, A4@L56 | 2 |
| A5 test execution | per parent × 2 cycles | — (no parent in batch) | 0 |
| A6 defect resolution | per parent × 2 cycles | — (no parent in batch) | 0 |
| A7 automated regression suite | per parent | — (no parent in batch) | 0 |
| A8 test data preparation | per parent | — (no parent in batch) | 0 |
| A9 performance and availability testing | per element (statement with measurable target) | — | 0 |
| A10 interface contract testing | per element (interface) | — | 0 |
| D2 planning and tracking | per parent | — (no parent in batch) | 0 |
| D4 requirement elaboration | per element (covers at least one requirement) | D4@L39, D4@L56 | 2 |
| G1 seed data set specification | per element (store) | — | 0 |
| G2 seed data preparation and load | per element (store) | — | 0 |
| G3 load reconciliation | per element (store) | — | 0 |
| O1 user documentation | per parent (surface in subtree) | — (no parent in batch) | 0 |

**Deferred, not generated:** A1, U1d, D1, D3, D6, E1, E2, E3, E4, E6, E7, O2, O3, O4.

**Total: 12 items.**

## 5. The `no` log

**None.** Each time an element's class matched an activity, the answer was `yes`.
- **Filter refusals:** 0.
- **Judgement refusals:** 0.
- Activities whose classes matched no element here (K3, A9, A10, G1–G3), or that need a parent (A5–A8, D2, O1), had no candidates. So there were no refusals to log for them.

## 6. Demanded-work branch

| id | entry |
|---|---|
| F48 | **Its own branch:** W-F48, domain immersion and architecture/technology selection. No declared activity absorbs it. |
| F49 | **Its own branch:** W-F49, integration tests on the real message stream. I agree with the declaration: A5 and A8 do not cover testing against live traffic, and no parent is in scope in any case. |
| F50 | **Its own branch:** W-F50, comparing integration-test results against the old system. No declared activity absorbs it. |
| F51 | **Accounted for at E6** (production cutover). E6 is once-scoped, so it is **deferred** from this partial run and must be generated in the whole-model pass. |
| F52 | **Its own branch:** W-F52, decommissioning the old version. Catalogue 1.4 §13 names this absence. |

**Warning to the orchestrator:** the demanded-work list covers the whole model, not this batch. If other batches also emit W-F48, W-F49, W-F50 and W-F52, the assembler must keep **exactly one** of each.

## 7. W5 findings

**Activities that applied to nothing in this batch.** These are batch-level only, not findings about the model, because the whole-model pass may use them.
- K3, A9 and A10: no statement or interface elements here.
- G1–G3: no store elements here.
- A5–A8, D2 and O1: no parents in the batch.

**Elements no activity touched:** none.

**Findings about the product model** (reported only; the model was crossed as it stands):
- **L56** ("First-version core function set, as replaced") is a leaf that **looks like a grouping**. A "function set" probably holds several distinct behaviours, yet it is crossed as one element with one of each item. The wording "as replaced" is also unclear: it could mean re-implementing the old functions or replacing them. As a leaf it keeps its work (it is classed `behaviour`, not `aggregate`), but splitting it is a question for the model builder.
- **L39**: whether "inter-component" means internal or cross-system is not settled by the name and F29's id alone (see section 3).

## 8. Projection onto the requirement anchor

| requirement | covering elements | work items tracing to them |
|---|---|---|
| F29 | L39 | 6 |
| F47 | L56 | 6 |

The demanded ids F48–F52 appear in no coverage set in scope, so they get no rows here. They are handled in section 6.

## 9. Instrument readings

**Engine: `Hotyn-W 1.2`.** Partial run over the subtrees L39 and L56 of `HM61-1`, 2 elements.

**Items**
- **Total work items:** 12 derived from the technology, plus 4 standing demanded branches.
- **Items by activity:** K1 2, K2 2, A2 2, A3 2, A4 2, D4 2; every other activity 0.
- **Items by scope:** per element 12; per parent 0; once and per environment deferred.
- **Items per element:** mean 6, min 6, max 6.

**Elements**
- **Elements by class:** behaviour 2; surface 0, interface 0, store 0, statement 0, aggregate 0.
- **Elements untouched:** 0.

**Refusals:** 0 `no` answers (filter 0, judgement 0).

**Activities**
- **Unused in this batch:** K3, A5, A6, A7, A8, A9, A10, D2, G1, G2, G3, O1.
- **Deferred:** A1, U1d, D1, D3, D6, E1, E2, E3, E4, E6, E7, O2, O3, O4.

**Demanded items:** 1 absorbed (F51, at deferred E6) and 4 standing alone (F48, F49, F50, F52).

No files were read or written.
