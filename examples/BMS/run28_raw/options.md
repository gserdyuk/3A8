# Run 28 — `Lytin-K 1.0`, conditional step-event option prices — transcribed verbatim

`tool_uses: 0`. Model: Opus 5 (orchestrator-recorded). Gap-blind: no class numbers, no diagnosis,
no calibrated total in the task; the run confirmed it checked every numeral.

**Provenance completion (orchestrator, closing the run's own condition):** the run demanded the
engine stamp of the base whose structure these options attach to, which the task omitted — a prompt
defect caught by the sensor. Supplied here: **`Hotyn-M 1.1` → `Hotyn-W 1.1` → `Hotyn-D 2.0` × rate
table v0.1 + A1, assembly of 2026-08-22 (base branch {1342.13, 1352.92})**. The output below is
therefore fully stamped: `Lytin-K 1.0` options over that chain.

**Second independent catch of the environment leak:** the run named the same `gitStatus` commit-line
gap ratio the first `Lytin-K` round caught, did not use it, noted this round is structurally immune
(option prices attach to triggers; nothing can be tuned to sum), and repeated the standing
recommendation: strip git log/status and repository memory files from Step C sensor launch contexts.

---

[Sensor output, verbatim:]

## 1. Input inventory

Received: project description (condensed) · base composition facts (73 sized elements, 18 parents,
pinned 20% at every parent incl. root, once-scoped layer) · the named existing pieces relevant to
the options · computed per-item E facts from the pinned rate table · four declared narrowings with
refused readings.

**Explicitly not given, confirmed absent:** no reference-class forecast, no quantiles, no target, no
budget, no deadline, no gap statement, no calibrated total, no diagnosis, no bottom-up total, no
request that anything sum. Every numeral in the prompt checked and classified (unit prices, pinned
20%, 73/18 counts, ×2 cycles, ~6.5 FTE, illustrative N = 10/20/30 as a client-supplied axis, H2 =
5/15/40 as a netting input). **Clean; proceeding.** No probabilities and no expected values appear
anywhere below.

## 2. Coverage / double-counting check — how the high-coverage base moved the rates

| coverage fact | movement |
|---|---|
| pinned 20% at every parent incl. root | no interface-overhead correction of my own; added leaf work gets the base's own ×1.44 (two parent levels) and nothing more |
| per-parent test/UAT/doc chains | added subtrees get the pinned chain values (9.5 / 18.9 / +8.0), never a generic QA uplift |
| fault-handling behaviour exists | E-1's per-supplier fault handling priced as a residue only (zero at low band) |
| disruption carried on the technical reading | E-3 prices only the business reading; resilience/DR untouched |
| H2 (5/15/40) already in the calibration | E-4 nets H2 out on firing — the residue and the engine cannot both be charged |
| SMS gateway adapter carried at size M | E-2 is a replacement; the adapter is deducted with its assembly share |

**Partition result: holds.** None of the four rows is a "they forgot things" correction — all four
are reversals of a declared narrowing: work the base correctly excluded. Everything the base carries
and an option would consume (notification channels, prioritisation rules, booking store, stage
model, resilience/DR, environments) is named and **not** priced; residual seams are priced as
residues and labelled.

## 3. Form

A multiplier cannot express "this either does not happen or triples a subsystem". Each event is a
**conditional pure addition or replacement with a stated netting rule** and its trigger verbatim.
**No global multipliers are emitted; the two-global budget is untouched.**

Common conventions: added leaf pd × **1.44** (two parent levels; the applying step may substitute
×1.2 only where the tree shape says one level, and must say so) · parent chains and once-layer items
attract no assembly · contract tests are inside the interface own-items figure · bands are
low/central/high **magnitude** bands within the fired reading, not confidence intervals.

## 4. The options table

### E-1 · R15 wide reading — direct supplier integrations

**Trigger:** "the client means many **direct** hotel/transport system integrations."
**Netting — a declared switch:** default, the priced aggregator adapter **stays** (a direct-connect
programme practically always retains an aggregated source for long-tail coverage); deduct 9.9 pd
only on explicit client confirmation of full substitution.

**Per-integration unit** (interface own items S/M/L 6.9/13.3/22.5 · + quirk/fallback behaviour at
high · × assembly 1.44 · + amortised per-parent test share 1.9/2.4/3.2 · + certification/credential/
sandbox pass 1/2/4): **12.8 / 23.6 / 51.6 pd per counterparty**.

**Programme once-costs** (canonical supply model L/XL · supplier & mapping catalogue store+seed ·
connection-health surface · onboarding process behaviour · fault-handling residue · × 1.44 · new
subtree parent chain + UAT/doc · once-layer sandbox/credential items): **45 / 124 / 149 pd**.

**Illustrative totals (N is client information, never a central case):**

| N | low | central | high |
|---|---:|---:|---:|
| 10 | 173 | 360 | 665 |
| 20 | 301 | 596 | 1181 |
| 30 | 429 | 832 | 1697 |

At N ≥ 24 a third parent tier appears; honouring it moves ×1.44 → ×1.728 on leaves (+2.0/+3.8/+8.9
per unit) — stated, not baked in. **Basis:** OTA/HTNG specification practice (partial, inconsistent
adoption is why per-counterparty cost varies ×3) · HEDNA/HTNG direct-connect certification practice
· PMI WBS span-of-control for the fan-out. Confidence: medium shape, medium-low size.

### E-2 · R27 wide reading — own SMS infrastructure

**Trigger:** "own SMS sending infrastructure — carrier agreements, sending service, delivery
handling at the infrastructure level." **Netting:** the gateway adapter is replaced: −18.0 / −19.2 /
−20.3 pd; the shared fault-handling behaviour is not deducted.

Elements (carrier SMPP interface(s) 1→3 · sending service queue/throttle/route L→XL+L · DLR
handling M→L · DLR reconciliation at high · mandatory opt-out/STOP handling at all bands · message
& DLR store · ops surface · sender-ID management): leaf 45.5 / 110.2 / 147.3 → ×1.44 → + parent and
UAT/doc chains + once-layer (SMPP allowlisting, HA failover, carrier monitoring, pentest-scope
extension) − netting →

**Conditional net delta: 60 / 178 / 239 pd.** Plus a **separate switch line 5 / 10 / 20 pd** —
carrier commercial & regulatory onboarding (10DLC/sender-ID/A2P registration), zeroed entirely if
the client treats it as commercial work; kept separate precisely so it can be zeroed without
touching the build number (confidence low). **Basis:** SMPP 3.4 (windowing and DLR correlation are
the documented send-vs-know-delivered asymmetry) · A2P registration regimes · GSMA opt-out codes of
practice (why STOP handling is priced at every band). Confidence: medium-high inventory, medium
sizes.

### E-3 · R13 business reading — disruption-response capability

**Trigger:** "detect affected bookings at scale, bulk re-search/re-book/re-route, communicate to
affected travellers, prioritised queues for the travel team." **Netting: none — purely additive**;
the technical reading stays untouched.

Elements (detection & affected-set matching L→XL(+feed interface at high) · bulk
re-search/re-book/re-route L→XL(+re-route rules) · bulk traveller communication consuming the
existing channels M→L · prioritised queues consuming the existing rules M→L — the largest single
avoided double-charge: no new rules engine · disruption console 1→3 × M surfaces · incident store +
booking-store extension residue · bulk-action audit behaviour): leaf 63.9 / 123.0 / 164.0 → ×1.44 →
+ chains + load-rehearsal once-items →

**Conditional addition: 112 / 212 / 288 pd.** **Basis:** IROPS/disruption management is a documented
separate subsystem in travel operations (distinct modules in Amadeus/SITA-class platforms) — the
warrant for a subtree with its own parent chain. Confidence: medium-high shape, medium sizes.

### E-4 · R10 broad reading — configurable process engine

**Trigger:** "process definitions as data, a definition surface for the administrator, versioned
process instances, migration of in-flight processes on definition change." **Netting:** the narrow
reading is superseded — deduct H2 at the value actually applied (5 / 15 / 40), band-matched. The
stage model stays priced; this is the delta above it.

Elements (definitions-as-data model L→XL(+expression language at high) · versioned definition
store+seed · admin definition surface 1→3 × M, the graphical-editor case expressed as k×M — an
explicit modelling choice, "I do not invent rows in someone else's rate table" · versioned instance
behaviour M→L · in-flight migration L→XL(+dry-run report) · engine runtime above the fixed stage
model L→XL+L): leaf 71.0 / 121.2 / 168.9 → ×1.44 → + chains + definition-promotion once-items −
H2 →

**Conditional net delta: 117 / 192 / 252 pd.** **Basis:** every production workflow engine ships
dedicated process-instance migration APIs (Camunda, Flowable, Activiti) — external evidence that
migration is a first-class element; the dynamic-change problem (van der Aalst) underneath.
Confidence: medium-high inventory, medium sizes; at the low band the definitions model and the
runtime collapse into one element, stated.

### X-13 · Interaction row — conditional on BOTH E-1 and E-3

Multi-supplier bulk re-search orchestration (fan-out, partial failure, per-supplier quota during a
mass event): M→XL × 1.44 → **12 / 22 / 37 pd. Applies only when both trigger; never on its own.**
Omitting it when both fire understates E-1+E-3 by a known kind.

## 5. Deliberately unpriced

Probability of any trigger and any expected value · N in E-1 (client information; 10/20/30 is
arithmetic on a client-supplied axis) · money terms (carrier rate cards, commissions, licence costs)
· schedule and team-shape consequences (E-1 at N=20 high changes duration and hiring, not just pd) ·
tail/failure scenarios (suppliers declining onboarding, carrier deregistration, a migration
corrupting in-flight bookings, a bulk re-book mis-messaging travellers) · **the E-3 × E-4
interaction — sign unsourceable** (engine-hosted disruption response could cut or raise E-3), so it
is named and not priced — X-13 was priced because its sign is unambiguous · the two client
declarations kept as switches (aggregator survival; carrier onboarding as project vs commercial) ·
whether any reading is the right one ("I price the refused reading; I do not adjudicate readings").

## 6. Order and usage — binding constraints

**Client-facing option prices; not reserves, not contingency, not part of any base or calibrated
total.** (1) No probability or EV may be attached by any later step without a separately-sourced
act. (2) A row enters a total only when its trigger fires — at full magnitude with netting, never a
fraction, never a multiplier. (3) Within a fired event: leaf items → ×1.44 → chains (no assembly) →
once-items (no assembly) → netting last → declared switches as separate visible lines. (4) X-13
only after both E-1 and E-3. (5) Bands do not mix across events: low-low-low is a construction, not
a scenario. (6) Every quoted figure travels with both stamps — `Lytin-K 1.0` over the base chain
named in the header.
