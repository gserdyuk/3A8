# HD23-B2 — size classification, batch B, repeat 2

Transcribed verbatim from the sensor's reply. `tool_uses: 0`. Same protocol note as HD23-B1: the
harness loaded the stale `Hotyn-D 1.0` definition; the run executed the task-pinned 2.0 sizing rules
over it, stamped `Hotyn-D 1.0`, and reported the mismatch itself. Model: Opus 5
(orchestrator-recorded). Identical prompt to HD23-B1, launched together, neither told of the other.

---

# Size classification — batch B (27-element slice of a 78-element product model)

## 1. Contamination check

**Clean — proceeding.**

Checked and found absent: effort figures, person-days, hours, durations of work, budgets, costs,
rates, prior estimates, "projects like this took X", calibration factors, reference-class figures.

Three items examined and cleared:

- **R66 "Screen loads under 2 seconds"** — a product-side latency ceiling stated in the pinned
  requirement text, not a work duration. It is also the exact thing the special count for the
  statement element asks me to enumerate. I quote it verbatim below. It is the only numeral
  resembling a time anywhere in this output, it came in with the input, and it is not produced by
  me.
- **Structural counts** ("78-element product model", "27 elements", "Elements to size: 21") —
  cardinalities of the model, not effort. Clean.
- **Files** — none read. No repository file was opened for this run; everything below comes from the
  task text alone.

**Identity note (not contamination, but it belongs here).** My engine identity section pins me as
**`Hotyn-D 1.0`** and instructs that the stamp be reproduced verbatim from that section every run,
never a version taken from elsewhere. The launching task addressed me as `Hotyn-D 2.0`. I have
stamped **`Hotyn-D 1.0`**. If a 2.0 definition exists, it was not loaded into this run; these
readings must be filed against 1.0, and a 1.0 reading and a 2.0 reading are not two readings of one
scale.

---

## 2. Element sizing

Counts are of named things in the element's own substance: its name, its declared role, and the
texts of the obligations it covers. Children, siblings and parents are not part of an element's
substance and were not counted.

### Subtree N16 — UX Foundation

| id | name | class / kind | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| N16 | UX Foundation | statement / **behavioural** | systems or components constrained: 1) "Modern web-based technologies" (R61) — the web delivery stack; 2) "fresh / clean / intuitive UX" (R61) — the user-facing presentation; 3) "Screen loads" (R66) — the screen render/load path | 3 | **M** |
| N17 | Design System & Common Look and Feel | surface | — see §4, not sizeable under the pinned surface count | — | — |
| N18 | Responsive Presentation | surface | — see §4, not sizeable under the pinned surface count | — | — |

**Kind justification for N16.** Behavioural. The phrase that decides it: **"Screen loads under 2
seconds"** — it constrains an observable run-time scenario and cannot be discharged by declaring a
configuration. Its other obligation reads compliance on its own; the element's kind is set by the
obligation that entails run-time behaviour. Mixed substance is recorded in §5.

### Subtree N19 — Integration Layer (N19 is an aggregate, ∅ coverage — not sized)

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| N20 | CTC Integration | interface | 1) "via API" (R06) — CTC API, named protocol/access concern; 2) "Booking requirements are initially defined in the Cost Tracking Center" (R21) — initial read of booking requirements; 3) "Frequent integration to the Cost Tracking Center to update booking requirements" (R31) — recurring refresh; 4) "Pick-up and drop-off locations initially loaded from the Cost Tracking Center" (R41) — initial load of journey locations | 4 | **M** |
| N21 | UPSA Integration | interface | 1) "via API" (R07) — UPSA API, named protocol/access concern. No operation is named in the obligation | 1 | **S** |
| N22 | Travel Content Aggregator Integration | interface | 1) "search across multiple third-party systems" (R15) — one query operation consumed. No protocol and no auth concern named | 1 | **S** |
| N23 | Uber Integration | interface | 1) "One transport supplier (Uber) is integrated automatically" (R39) — one booking-placement operation consumed. No protocol and no auth concern named | 1 | **S** |
| N24 | Email Channel Integration | interface | 1) "notifications" (R26) — one dispatch operation exposed; 2) "via email" (R26) — email as a named channel/protocol | 2 | **M** |
| N25 | SMS Gateway Integration | interface | 1) "notifications" (R27) — one dispatch operation exposed; 2) "via SMS" (R27) — SMS as a named channel/protocol | 2 | **M** |
| N84 | Integration Fault Handling & Retry | behaviour | 1) "adapters fail" → detect adapter failure at run time; 2) "retry" → retry the failed call; 3) "signalling to the orchestration that consumes the failure signal" → emit the failure signal | 3 | **M** |

### Subtree N30 — Search & Prioritisation (N30 aggregate — not sized)

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| N85 | Search (federated, normalised, cached) | behaviour | 1) "search across multiple third-party systems" (R15) → query third-party systems; 2) "Search across manually uploaded bookings" (R16) → query uploaded bookings; 3) "Search based on requirements specified in the system" (R17) → build and apply the query from stored requirements; 4) "normalised" (element name) → normalise heterogeneous results; 5) "cached" (element name) → cache results | 5 | **L** |
| N72 | Prioritisation (ranking & recalculation) | behaviour | 1) "Prioritisation based on matching to booking requirements" (R18) → match options against requirements; 2) "Prioritisation based on custom rules defined by the Travel Manager" (R19) → evaluate custom rules; 3) "Prioritisation may change over time as requirements or rules change" (R24) → recalculate on change | 3 | **M** |

### Subtree N33 — Approval & Booking Workflow (N33 aggregate — not sized)

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| N34 | Stage Model & Transitions | behaviour | transitions named by the stage list (R25): 1) "Proposed" → propose; 2) "Accepted" → accept; 3) "Rejected" → reject; 4) "Approved" → approve; 5) "Declined" → decline; 6) "Booked" → book; 7) "Cancelled" → cancel; 8) "Paid" → record paid | 8 | **XL** |
| N83 | Stage Notifications & Delivery | behaviour | 1) "Stage changes supported by notifications via email" (R26) → dispatch on stage change to the email channel; 2) "…via SMS" (R27) → dispatch to the SMS channel; 3) "delivery tracking" (trigger) → track the delivery outcome; 4) "retry" (trigger) → retry what was not delivered | 4 | **L** |
| N73 | Automatic Booking Orchestration & Fallback | behaviour | 1) "Automatic booking" (R29) → place a booking with no human in the loop; 2) "Orchestration" (element name) → sequence the booking steps across suppliers; 3) "Fallback" (element name) → fall back when the failure signal arrives | 3 | **M** |

### Subtree N36 — Hotel Booking (N36 aggregate — not sized)

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| N37 | Automatic Hotel Booking | behaviour | 1) "Automatic booking" (R29) → book a hotel with no human in the loop | 1 | **S** |
| N38 | Manual Hotel Booking | surface | user tasks: 1) "Manual hotel booking managed by the Travel Manager via web site forms" (R30) → the Travel Manager enters and submits a hotel booking. R38 "Hotel supplier booking is handled manually" restates the same task from the supplier side and is not counted twice | 1 | **S** |
| N39 | Hotel Selection Change | behaviour | 1) "Manual change of a selected hotel as required" (R35) → change the selected hotel | 1 | **S** |

### Subtree N40 — Transport Booking (N40 aggregate — not sized)

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| N41 | Automatic Transport Booking | behaviour | 1) "Automatic booking" (R29) → place a transport booking with no human in the loop; 2) "One transport supplier (Uber) is integrated automatically" (R39) → route the booking to the automatically integrated supplier | 2 | **M** |
| N42 | Manual Transport Booking | surface | user tasks: 1) "All other transport suppliers are manual" (R40) → the Travel Manager places a booking with a non-integrated supplier. R48 restates the same task and is not counted twice | 1 | **S** |
| N43 | Journey Locations | store | entity kinds held: 1) "Pick-up … locations" (R41, R44); 2) "drop-off locations" (R41, R44) | 2 | **M** |
| N44 | Transport Booking Combination | behaviour | 1) "Combining of multiple transport bookings" (R45) → combine bookings into one | 1 | **S** |

---

## 3. Special counts

**N16 — measurable targets (statement, behavioural).**

| # | target, verbatim | source |
|---|---|---|
| 1 | "Screen loads under 2 seconds" | R66 |

R61 states no threshold and no service level; it contributes no target. **Count 1 → S.** The gap
between the element's constrained-components class (M) and its target class (S) is itself a reading:
most of this element's substance is unmeasured intent.

**N43 — entity kinds needing pre-load (store).**

| # | kind | why it must exist before the system is usable |
|---|---|---|
| 1 | pick-up locations | "initially loaded from the Cost Tracking Center" (R41) — a journey cannot be raised without an origin, and CTC is named as the origin's source of truth |
| 2 | drop-off locations | same clause (R41) — a destination is required before a booking can be placed |

Both held kinds require pre-load; the count equals the held count. R44 is a run-time edit of an
already-loaded kind and adds no pre-load kind. **Count 2 → M.**

---

## 4. Unsizeable elements

**N17 · Design System & Common Look and Feel · surface.** The pinned count for `surface` is
*distinct user tasks served*. N17's substance names **no user task at all**. Two readings are
available and they land at opposite ends of the scale: taken literally the count is zero, below the
S band; taken as "all users", the element serves every task in the product, above the XL band. A
count that yields both extremes on the same text is not a count I may resolve by picking the more
comfortable end, and the thresholds are not mine to bend. **No class assigned.**

**N18 · Responsive Presentation · surface.** Same failure. R62 names resolutions and device classes
— "mobile", "portable devices" — and no user task. Counting device classes as if they were tasks
would be substituting a different count for the pinned one. **No class assigned.**

What these two have in common is a class question, not a sizing question: both are cross-cutting
presentation constraints that govern how every task looks, rather than surfaces defined by the tasks
they serve. Their substance would be enumerable under the count used for constraints on systems or
components. **I am not reclassifying them and I am not assigning a shadow size** — class is the
model owner's, not mine. I am reporting that the pinned rule does not close over them as declared.

**Consequence for coverage.** R09, R61 and R62 are, in this batch, carried only by N17 and N18 (R61
also by N16). If the two remain unsizeable, three obligations have no sized element in batch B.

**Not sized by rule** (aggregates): N19, N30, N33, N36, N40.

---

## 5. Doubts

**Band-sensitive classifications — half the batch.** Ten of the twenty sized elements sit on a
reading that, taken the other way, moves the class by one band:

| id | assigned | the contested reading | alternative |
|---|---|---|---|
| N20 | M (4) | "Pick-up and drop-off locations initially loaded" counted as one load operation over two location roles | two operations → 5 → **L** |
| N24, N25 | M (2) | a named delivery channel ("via email", "via SMS") counts as a protocol concern alongside the dispatch operation | channel and operation are the same thing → 1 → **S** |
| N34 | XL (8) | each slash-separated outcome is a distinct transition action | five slash-groups → 5 → **L**. The largest single swing in the batch |
| N72 | M (3) | "ranking" in the element name is the umbrella verb over matching and rule evaluation, not a fourth action | ranking counted separately → 4 → **L** |
| N83 | L (4) | dispatch to email and dispatch to SMS are two actions, since they are two separately mandated obligations | one dispatch action across two channels → 3 → **M** |
| N38, N42 | S (1) | the second obligation (R38, R48) restates the first rather than naming a second task | supplier-side handling read as a distinct task → 2 → **M** |
| N41 | M (2) | routing to the automatically integrated supplier is a distinct action from placing the booking | one action → 1 → **S** |
| N43 | M (2) | pick-up and drop-off are two entity kinds | one kind with a role attribute → 1 → **S**, and the pre-load count falls with it |

**Elements sized at the floor because their obligation names almost nothing.** N21, N22, N23, N37,
N38, N39, N42, N44 — all eight S classes — reached 1 because the requirement text names a single
thing, not because the element is known to be small. N21 is the sharpest case: "Integration with
UPSA via API" names a system and a mechanism and not one operation, so its S is a floor set by the
requirement's silence. These are readings on the requirement texts, not on the elements.

**N16's kind is mixed.** One obligation is compliance-shaped, the other behavioural. I assigned
behavioural.

**N16's constrained-component count is robust.** Own substance gives 3; counting the two governed
children gives 4. Both are M.

**N73's count is robust.** Reading the orchestration as one compound action gives 2, as two gives 3.
Both M.

**N37 contradicts R38.** N37 covers R29 "Automatic booking" while N38 in the same subtree covers R38
"Hotel supplier booking is handled manually". I may not remove it; sized S with the doubt recorded.

**"Intelligent" in R15 was not counted.** An adjective, not a verb on an object. If read as a
distinct action of N85, that element would be 6 and still L.

**Input arithmetic.** The task header says "Elements to size: 21". I count **22** non-aggregate
bulleted elements (3 + 7 + 2 + 3 + 3 + 4). I sized on the 22 I was given rather than dropping one to
match the header.

---

## 6. Closure violations

Work I judge necessary that this work model does not contain. **None of it is sized.** Standing
caveat: 27 of 78 elements — any entry may exist in a batch I cannot see.

1. **Credential and token handling for the named integrations.** R06 and R07 say "via API"; six
   adapters are declared. Nothing in batch B holds credential storage, token refresh or the failure
   mode where an integration is reachable but not authorised. N84's trigger names transport-level
   failure, not authorisation.
2. **A surface for authoring the Travel Manager's prioritisation rules.** R19 names "custom rules
   defined by the Travel Manager". N72 evaluates them; nothing lets the Travel Manager define them.
3. **The override of pick-up and drop-off locations.** R44 names "Ability to override". N43 holds
   the locations; no element performs or exposes the override.
4. **Intake of manually uploaded bookings.** R16 names them and N85 searches them. Nothing receives,
   validates or stores the upload.
5. **Payment.** R25's stage list ends at "Paid" and N34 models the transition. Nothing records,
   requests or reconciles a payment.
6. **Cancellation against a supplier.** R25 names "Cancelled". Nothing withdraws a booking already
   placed with a supplier.
7. **A store for bookings and booking requirements.** N85 searches bookings, N72 matches against
   requirements, N20 loads them from CTC. The only store in batch B is N43. Named for closure
   because three sized elements presuppose it.

---

## 7. Instrument readings

**Engine: `Hotyn-D 1.0`** — stamped verbatim from the engine identity section, as required. The
launching task addressed me as `Hotyn-D 2.0`; that version was not adopted. File these readings
against 1.0.

**Population** — elements in batch B: 27 · aggregates excluded: 5 · non-aggregates presented: **22**
(header said 21; discrepancy of 1, unresolved) · sized: **20** · unsizeable: **2** (N17, N18) —
9.1% of presented.

**Size distribution over the 20 sized**

| class | count | share |
|---|---|---|
| S | 8 | 40% |
| M | 9 | 45% |
| L | 2 | 10% |
| XL | 1 | 5% |

Modal class: **M**. Single XL: **N34**.

**Element-class mix over the 20 sized** — behaviour 10 · interface 6 · surface 2 · store 1 ·
statement 1. Surfaces presented 4, sized 2 — **50% of the surface class failed to size**, the
sharpest single reading here.

**Statement kinds** — behavioural 1, compliance 0. Justifying phrase: "Screen loads under 2
seconds".

**Special counts** — N16 measurable targets: 1 → **S**. N43 pre-load kinds: 2 → **M**; pre-load
count equals held count — this store is fully pre-load-dependent.

**Reading stability** — band-sensitive classifications: **10 of 20 = 50%**; largest swing N34 (XL
vs L). Band-robust under a tested alternative: N16, N73. Sized at the count floor of 1 because the
substance names only one thing: **8 of 20 = 40%** — a reading on the requirement texts, not the
elements.

**Closure** — violations recorded: **7**, all unsized. Obligations without a sized element in this
batch if N17/N18 stay unsizeable: **3** (R09, R61, R62).

**Prohibition compliance** — no person-day, hour, cost, rate, duration, calendar, allocation or
contingency figure anywhere above. The only produced numerals are enumeration and population counts;
the one time-valued quantity, "Screen loads under 2 seconds", is a verbatim quotation of R66. No
element added, removed, merged or reclassed. No repository file read.
