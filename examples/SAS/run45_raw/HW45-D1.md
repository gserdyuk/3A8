<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-08. Not part of the sensor's reply.
run: 45 · reading: HW45-D1 · engine (sensor's own stamp): Hotyn-W 1.1 · model (launch record): the harness's
`opus` alias, i.e. Claude Opus 5 · partial crossing of HM44-OA1, prompt generated by make_prompts.py (batch D)
· tool_uses: 0 · duration 160 s · received complete, beginning at section 1; transcribed verbatim by the
orchestrator from the reply as delivered (the harness transcript file for this agent was empty). -->

## 1. Contamination check

**Clean — the run proceeds.**

- The pasted input contains no effort figure, no budget, no duration, no team size, no cost anchor, no prior work model and no estimate. INPUT 1 is a closed product model subtree listing; INPUT 2 is a technology declaration with its parameters.
- **Ambient repository material was present** in my context (a git status block, recent commit subjects, a directory name, a memory index). Per the quarantine instruction it was **not read as input, not acted on, and not treated as stopping contamination**. Nothing from it enters the crossing below.
- **No files were read.** The crossing rests on the pasted text alone.
- **One input is absent, and I record it rather than infer it:** no demanded-work list accompanied this batch. §6 is therefore empty by supply, not by omission — see the note there.

## 2. Scope of this run

**Partial run.** Subtrees rooted at **S-07** and **S-08** of `HM44-OA1`: **28 elements**, of which **6 are parents** (S-07, S-07.1, S-07.2, S-07.3, S-08, S-08.1) and 22 are leaves. The root `S-00` and all other subtrees are outside this batch and were neither crossed nor speculated about.

**Deferred, not generated:** all `once`-scoped activities (A1, U4, D1, D3, D6, E2, E3, E4, E6, E7, G1m, G5m, O2, O3, O4, S1, S2, S3) and the `per environment` activity E1. They belong to the whole model; generating them here would double-count when the remaining subtrees are crossed. `per parent` activities were generated only for the six parents inside the batch.

## 3. Classification log

Every element, exactly once, from declared content.

| id | name | class | note / statement justification |
|---|---|---|---|
| S-07 | Publish and subscribe | aggregate | empty own coverage, has children |
| A-060 | Publish-and-subscribe mechanism (publications, subscriptions, grants) | behaviour | run-time mechanism; leaf, so not an aggregate despite naming three things (see §7) |
| S-07.1 | Publication (owner side) | aggregate | empty own coverage, has children |
| A-061 | Publication at any hierarchy level | behaviour | the act of publishing, at run time |
| A-062 | Basic and full attribute-set profiles | behaviour | the system serves a different attribute set per profile at run time; "profile" here selects behaviour, it is not a policy |
| A-063 | Bulk publication for a set of records | behaviour | |
| A-069 | Owner decision on access requests | behaviour | an approve/reject action taken at run time |
| C-019 | Publication revocation | behaviour | derived leaf, empty own coverage; a leaf is never an aggregate, so classified from content |
| S-07.2 | Subscription and access requests | aggregate | empty own coverage, has children |
| A-011 | Subscription entitlement model (view scope, export options) | store | entitlements (view scope, export options) are data the system holds and is responsible for; the enforcement of them is a separate element (A-067) |
| A-067 | Consumer view of published data, gated by entitlement | surface | a view a user meets |
| A-068 | Access request from consumer to data owner | behaviour | |
| S-07.3 | Share audiences and groups | aggregate | empty own coverage, has children |
| A-064 | Share audiences: individual, user-defined, controlled, public | behaviour | audience resolution at run time, not a policy statement |
| A-065 | User-defined group management | behaviour | |
| A-066 | X-Customer-controlled group management | behaviour | |
| A-126 | Controlled-group membership request, approved by X-Customer | behaviour | |
| C-020 | Group membership lifecycle | behaviour | derived leaf, empty own coverage; classified from content |
| S-08 | Access Data module | aggregate | empty own coverage, has children |
| A-010 | Read-only module boundary (no editing functionality) | **statement** | justifying phrase: **"no editing functionality"** — a boundary constraint on the module, not something the system does |
| S-08.1 | Access Data search | aggregate | empty own coverage, has children |
| A-123 | Search over prefix, GIN and LN via the read store | behaviour | |
| A-124 | Field-level search inputs | surface | inputs a user meets |
| A-125 | Advanced search and filter builder | surface | a builder a user meets |
| A-127 | Published record detail view including hierarchy | surface | |
| A-128 | Record print rendering (one or more selected) | surface | a printable document |
| A-012 | Anonymous public access profile (limited, no export) | behaviour | **contested, decided against `statement`:** the content names an unauthenticated access path the system provides at run time; "(limited, no export)" bounds that path rather than converting it into a policy. Classifying it `statement` would have deleted its design, implementation and test items |
| A-130 | Informational page on paying for ad hoc access | surface | a page |

Class totals: behaviour 14 · surface 6 · store 1 · statement 1 · interface 0 · aggregate 6 = 28.

## 4. The crossing (compact — one row per activity)

Item ids are `ACTIVITY@ELEMENT`; cycle-multiplied activities append `#1`, `#2`.

| activity | scope | elements receiving an item | count |
|---|---|---|---|
| **K1** element design | per element (beh/surf/int/store) | A-060, A-061, A-062, A-063, A-069, C-019, A-011, A-067, A-068, A-064, A-065, A-066, A-126, C-020, A-123, A-124, A-125, A-127, A-128, A-012, A-130 | 21 |
| **K2** element implementation | per element (beh/surf/int/store) | same 21 as K1 | 21 |
| **K3** statement realisation and evidence | per element (statement) | A-010 | 1 |
| **A2** test design | per element (beh/surf/int/store) | same 21 as K1 | 21 |
| **A3** unit/component test implementation | per element (beh/surf/int/store) | same 21 as K1 | 21 |
| **A4** code review | per element (beh/surf/int/store) | same 21 as K1 | 21 |
| **A5** test execution | per parent × 2 cycles | S-07, S-07.1, S-07.2, S-07.3, S-08, S-08.1 (each #1, #2) | 12 |
| **A6** defect resolution | per parent × 2 cycles | S-07, S-07.1, S-07.2, S-07.3, S-08, S-08.1 (each #1, #2) | 12 |
| **A7** automated regression suite | per parent | S-07, S-07.1, S-07.2, S-07.3, S-08, S-08.1 | 6 |
| **A8** test data preparation | per parent | S-07, S-07.1, S-07.2, S-07.3, S-08, S-08.1 | 6 |
| **A9** performance/availability testing | per element (qualified statement) | — | 0 |
| **A10** interface contract testing | per element (interface) | — | 0 |
| **U1** UAT scenario preparation | per parent with surface in subtree | S-07, S-07.2, S-08, S-08.1 | 4 |
| **U2** UAT support | per parent × 2 UAT cycles | S-07, S-07.2, S-08, S-08.1 (each #1, #2) | 8 |
| **U3** UAT defect triage and fix | per parent × 2 UAT cycles | S-07, S-07.2, S-08, S-08.1 (each #1, #2) | 8 |
| **D2** planning and tracking | per parent | S-07, S-07.1, S-07.2, S-07.3, S-08, S-08.1 | 6 |
| **D4** requirement elaboration | per element covering ≥1 requirement | A-060, A-061, A-062, A-063, A-069, A-011, A-067, A-068, A-064, A-065, A-066, A-126, A-010, A-123, A-124, A-125, A-127, A-128, A-012, A-130 | 20 |
| **G2m** field mapping specification | per element (store) | A-011 | 1 |
| **G3m** extraction and transformation | per element (store) | A-011 | 1 |
| **G4m** load and reconciliation | per element (store) | A-011 | 1 |
| **O1** user documentation | per parent with surface in subtree | S-07, S-07.2, S-08, S-08.1 | 4 |
| | | **total** | **195** |

**Deferred activities (generated nowhere in this batch, count 0 each):** A1, U4, D1, D3, D6, E1, E2, E3, E4, E6, E7, G1m, G5m, O2, O3, O4, S1, S2, S3.

## 5. The `no` log

Every element that matched an activity's applicable classes (or, for unrestricted activities, every element) and received no item.

| element | activity | kind | reason |
|---|---|---|---|
| S-07 | D4 | filter | aggregate, empty own coverage — covers no requirement |
| S-07.1 | D4 | filter | aggregate, empty own coverage — covers no requirement |
| S-07.2 | D4 | filter | aggregate, empty own coverage — covers no requirement |
| S-07.3 | D4 | filter | aggregate, empty own coverage — covers no requirement |
| S-08 | D4 | filter | aggregate, empty own coverage — covers no requirement |
| S-08.1 | D4 | filter | aggregate, empty own coverage — covers no requirement |
| C-019 | D4 | filter | derived element, empty own coverage — covers no requirement |
| C-020 | D4 | filter | derived element, empty own coverage — covers no requirement |
| S-07.1 | U1 | filter | subtree contains no `surface` element (A-061, A-062, A-063, A-069, C-019 all behaviour) |
| S-07.1 | U2 | filter | same condition; logged once, cycles do not multiply refusals |
| S-07.1 | U3 | filter | same condition; logged once |
| S-07.1 | O1 | filter | subtree contains no `surface` element |
| S-07.3 | U1 | filter | subtree contains no `surface` element (A-064, A-065, A-066, A-126, C-020 all behaviour) |
| S-07.3 | U2 | filter | same condition; logged once |
| S-07.3 | U3 | filter | same condition; logged once |
| S-07.3 | O1 | filter | subtree contains no `surface` element |
| A-010 | A9 | filter | class `statement` matched, but the content is a read-only boundary, not a performance, capacity or availability property, and states no measurable target |

**17 refusals: 17 filter, 0 judgement.** No element passed both class and declared condition and was then declined. The freedom exercised in this batch lives entirely in §3 (classification), not in applicability — in particular in A-011 (`store` rather than `behaviour`), A-012 (`behaviour` rather than `statement`) and A-062 (`behaviour` rather than `store`/`statement`). A run disagreeing with this one should be compared there.

## 6. Demanded-work branch

**No demanded-work list was supplied with this batch.** The section is empty by supply, not by exclusion: no requirement id was struck out, absorbed silently, or dropped. If `HM44-OA1` carries a demanded-work list at model level, it is unassigned by this run and must be crossed in the batch that carries it — it cannot be inferred from the two subtrees pasted here. Flagged so the orchestrator can confirm the omission is intentional.

## 7. W5 findings (reported, not repaired)

**Activities that applied to nothing in this batch**

1. **A10, interface contract testing** — no element in S-07 or S-08 classified `interface`. Either these two subtrees genuinely hold no exchange with an external system, or the product model does not model such exchanges as elements. A-123's content names "the read store" and A-066/A-126 name "X-Customer" as an approving party; neither is present as an element here. Whether they exist elsewhere in `HM44-OA1` is outside this batch.
2. **A9, performance and availability testing** — the batch holds exactly one `statement` (A-010), and it is not a performance, capacity or availability property. No performance obligation with a measurable target is present in these subtrees. If the client expects one, it is not in the product model.

**Elements no activity touched:** none. All 28 elements appear as the element of at least one item.

**Further findings from the crossing**

3. **A-060 reads like an aggregate but is a leaf with own coverage.** Its content, "publications, subscriptions, grants", names the same three concerns as its sibling subtrees S-07.1, S-07.2 and S-07.3, while carrying coverage (G-13, P-17, L-12) of its own. It may be a granularity error — one leaf standing for a whole branch — and its items may overlap those of its siblings' children. Crossed as it stands, as one behaviour element. Not split, not moved.
4. **C-019 and C-020 are derived leaves with empty own coverage.** They receive build and test items but no D4, and they project onto no requirement id in §8 — work that exists in the model but is anchored to nothing the client stated.
5. **The whole migration branch of this batch rests on one classification.** G2m, G3m and G4m generated three items, all against A-011, because it is the only `store`. Reclassify A-011 as `behaviour` and the migration dimension applies to nothing in these subtrees.

## 8. Projection onto the requirement anchor

One row per requirement id appearing in the coverage sets of the batch; "items" counts every work item tracing to the covering elements.

| requirement | covered by | items |
|---|---|---|
| G-13 | A-060 | 6 |
| G-13.1 | A-061 | 6 |
| G-13.2 | A-062 | 6 |
| G-13.3 | A-063 | 6 |
| G-13.4 | A-064, A-065, A-066 | 18 |
| G-13.5 | A-011, A-067 | 15 |
| G-13.6 | A-068 | 6 |
| G-13.7 | A-069 | 6 |
| P-17 | A-060, A-067 | 12 |
| L-12 | A-060, A-067 | 12 |
| I-8 | A-010, A-123, A-127 | 14 |
| I-9 | A-011, A-067 | 15 |
| I-10 | A-012 | 6 |
| D-1 | A-123 | 6 |
| D-1.1 | A-124 | 6 |
| D-1.2 | A-125 | 6 |
| D-2 | A-062, A-068 | 12 |
| D-2.1 | A-069, A-011, A-068 | 21 |
| D-3 | A-066, A-126 | 12 |
| D-4 | A-062, A-127 | 12 |
| D-5 | A-011 | 9 |
| D-6 | A-128 | 6 |
| D-8 | A-130 | 6 |

23 requirement ids. **129 of 195 items** trace to requirement-covering elements; the remaining **66** trace to the six aggregates (S-07 13, S-07.1 7, S-07.2 13, S-07.3 7, S-08 13, S-08.1 13) plus C-019 and C-020, which carry no coverage — the projection therefore does not sum to the total, by construction. I-8 shows the lowest ratio of items to covering elements (14 across three), because one of its three carriers is the `statement` A-010, which receives only K3 and D4.

## 9. Instrument readings

**Engine `Hotyn-W 1.1`.** Partial run over the S-07 and S-08 subtrees of `HM44-OA1`; readings below are batch readings, not model readings.

| reading | value |
|---|---|
| total work items | **195** |
| items by activity | K1 21 · K2 21 · K3 1 · A2 21 · A3 21 · A4 21 · A5 12 · A6 12 · A7 6 · A8 6 · A9 0 · A10 0 · U1 4 · U2 8 · U3 8 · D2 6 · D4 20 · G2m 1 · G3m 1 · G4m 1 · O1 4 |
| items by scope | per element 129 · per parent 26 · per parent × cycles 40 · once 0 (deferred) · per environment 0 (deferred) |
| elements by class | behaviour 14 · surface 6 · store 1 · statement 1 · interface 0 · aggregate 6 — total 28 |
| items per element | mean 6.96 · min 2 (A-010) · max 13 (S-07, S-07.2, S-08, S-08.1) |
| `no` answers | **17 total — 17 filter, 0 judgement** |
| elements untouched | 0 |
| activities unused (applied to nothing, in scope) | 2 — A9, A10 |
| activities deferred (whole-model scope, partial run) | 19 — A1, U4, D1, D3, D6, E1, E2, E3, E4, E6, E7, G1m, G5m, O2, O3, O4, S1, S2, S3 |
| demanded items absorbed vs standing alone | 0 vs 0 — no demanded-work list supplied with this batch (§6) |
| parameters consumed | test execution cycles 2 (A5, A6) · UAT cycles 2 (U2, U3) · environments 4 and migration rehearsal cycles 2 both untouched, their activities being deferred |

No effort, duration, size, cost or complexity value appears anywhere above; every figure is a count of elements, activities or items.
