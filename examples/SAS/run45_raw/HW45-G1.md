<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-08. Not part of the sensor's reply.
run: 45 · reading: HW45-G1 · engine (sensor's own stamp): Hotyn-W 1.1 · model (launch record): the harness's
`opus` alias, i.e. Claude Opus 5 · partial crossing of HM44-OA1, prompt generated by make_prompts.py (batch G)
· tool_uses: 0 · received complete, beginning at section 1; text taken from the harness transcript of the agent,
identical to the reply as received. -->

# Work model `HM44-W` — batch `S-09 + S-14`
**Engine: `Hotyn-W 1.1`**

---

## 1. Contamination check

**Clean — crossing proceeds.**

The pasted input (INPUT 1, INPUT 2, INPUT 3) contains no effort figure, no budget, no duration, no team size, no cost anchor, no complexity score, no previously built work model and no estimate. It contains a product model, a technology declaration, its parameters and a demanded-work list — exactly the four admissible inputs.

**Ambient repository information was present and has been quarantined.** The harness prepended a git status, a branch name, recent commit subjects, a memory index and a directory listing. None of it was treated as input: it was not read for content, not used to shape any classification or applicability answer, and not counted as contamination that stops the run, per the quarantine instruction. Some of those ambient strings appear to reference prior runs and prior measured quantities; they were discarded unread and no number from them appears anywhere below.

**No files were read.** Everything below derives from the pasted input alone.

---

## 2. Scope of this run

**Partial run.** Subtrees rooted at **S-09** (Import and export) and **S-14** (Configuration and reference data) of the closed product model `HM44-OA1`.

- **Elements in scope: 30** (20 in the S-09 subtree, 10 in the S-14 subtree), each with its parent present in the batch except the two batch roots, whose parent `S-00` is out of scope.
- The model root `S-00` and every other subtree are **outside this batch**; they were not crossed and are not speculated about.
- **Deferred, not generated:** all `once`-scoped activities and all `per environment` activities. They belong to the whole model and would be double-counted when the remaining subtrees are crossed.
- **`per parent` activities** were applied only to elements that have children **inside this batch** — 7 such positions.
- The **demanded-work list is whole-model** and is recorded in full at §6, with each id marked absorbed-in-scope, accounted-for-at-a-deferred-activity, or standing alone.

Every reading in §9 is a reading **of this batch**, not of the model.

---

## 3. Classification log (W7)

Every element, exactly one class, read from its declared content — name together with its own coverage — never from its position or its id prefix. No `statement` was found in this batch, so no justifying quotes are required; the three judgements that were close are flagged.

| id | name | class | note |
|---|---|---|---|
| S-09 | Import and export | **aggregate** | no own coverage, has children |
| A-059 | Record selection for import and export | **behaviour** | selecting records is done at run time |
| S-09.1 | Format adapters | **aggregate** | no own coverage, has children |
| A-051 | Excel adapter | **behaviour** | a file format, not a system outside this one |
| A-052 | CSV adapter | **behaviour** | as above |
| A-053 | Numbers adapter | **behaviour** | as above |
| A-054 | XML adapter | **behaviour** | as above |
| A-055 | Tab-delimited adapter | **behaviour** | as above |
| A-056 | IDoc adapter | **interface** | ⚠ judgement — content carries P-19 and L-15, which A-051–A-055 do not; an IDoc exchange is with a system outside this one |
| A-057 | Import and export through the member API | **interface** | exchange with the member system, stated in the content |
| A-058 | PC/Mac encoding and line-ending variants | **behaviour** | run-time handling of encoding and line-ending variants |
| S-09.2 | Import pipeline | **aggregate** | no own coverage, has children |
| A-049 | Record import service | **behaviour** | a service the system runs |
| A-102 | Hierarchy import modes | **behaviour** | modes selected and applied at import time |
| C-007 | Import validation result report | **surface** | a report the user meets |
| C-008 | Import staging and preview | **surface** | ⚠ judgement — "preview" is met by a user; the staging is its mechanism. Classified once, as the declaration requires; see §7 |
| S-09.3 | Export pipeline | **aggregate** | no own coverage, has children |
| A-050 | Record export service | **behaviour** | a service the system runs |
| C-009 | Asynchronous export job handling | **behaviour** | job handling at run time |
| C-025 | Export artefact retention and cleanup | **behaviour** | ⚠ judgement — "cleanup" is a run-time action; classifying it `statement` on the strength of "retention" would lose the build work (W7 warning) |
| S-14 | Configuration and reference data | **aggregate** | no own coverage, has children |
| S-14.1 | Industry configuration | **aggregate** | no own coverage, has children |
| A-074 | Industry attribute-set configuration | **store** | attribute sets held per industry and maintained by the system |
| A-098 | Reuse-rule configuration by industry and product type | **store** | the configured rule set is data the system holds; applying it is elsewhere |
| A-117 | Industry LN-pool configuration | **store** | pools held per industry |
| C-018 | Configuration administration console | **surface** | a console the administrator meets |
| S-14.2 | Prefix registry and X-Customer reference data | **aggregate** | no own coverage, has children |
| A-013 | X-Customer reference data in the read store under the same rules | **store** | reference data held in the read store |
| A-027 | Prefix capacity computation (used vs available) | **behaviour** | a computation performed at run time |
| C-015 | Identifier allocation ledger | **store** | a ledger the system holds and is responsible for |

**Class totals (30):** aggregate 7 · behaviour 13 · store 5 · surface 3 · interface 2 · statement 0.

**Where this batch's freedom sat.** Four decisions carry it: A-056/A-057 as `interface` rather than `behaviour` (this alone creates the A10 items); A-074/A-098/A-117 as `store` rather than `behaviour` (this alone creates nine migration items); C-008 as `surface` rather than `store`; C-025 as `behaviour` rather than `statement`. A run that classifies differently will differ from this one at exactly those points and nowhere else — see §9, where the judgement-refusal count is zero.

---

## 4. The crossing (compacted at orchestrator's instruction)

Item id form `ACTIVITY@ELEMENT`; for `× cycles` activities the two cycles are `…#1` and `…#2`.

| activity | scope | elements receiving an item | count |
|---|---|---|---|
| **K1** element design | per element (behaviour, surface, interface, store) | A-059, A-051, A-052, A-053, A-054, A-055, A-056, A-057, A-058, A-049, A-102, C-007, C-008, A-050, C-009, C-025, A-074, A-098, A-117, C-018, A-013, A-027, C-015 | **23** |
| **K2** element implementation | per element (behaviour, surface, interface, store) | same 23 elements as K1 | **23** |
| **K3** statement realisation and evidence | per element (statement) | — none — | **0** |
| **A2** test design | per element (behaviour, surface, interface, store) | same 23 elements as K1 | **23** |
| **A3** unit and component test implementation | per element (behaviour, surface, interface, store) | same 23 elements as K1 | **23** |
| **A4** code review | per element (behaviour, surface, interface, store) | same 23 elements as K1 | **23** |
| **A5** test execution | per parent × 2 cycles | S-09, S-09.1, S-09.2, S-09.3, S-14, S-14.1, S-14.2 (each ×2) | **14** |
| **A6** defect resolution | per parent × 2 cycles | S-09, S-09.1, S-09.2, S-09.3, S-14, S-14.1, S-14.2 (each ×2) | **14** |
| **A7** automated regression suite | per parent | S-09, S-09.1, S-09.2, S-09.3, S-14, S-14.1, S-14.2 | **7** |
| **A8** test data preparation | per parent | S-09, S-09.1, S-09.2, S-09.3, S-14, S-14.1, S-14.2 | **7** |
| **A9** performance and availability testing | per element (statement with a measurable target) | — none — | **0** |
| **A10** interface contract testing | per element (interface) | A-056, A-057 | **2** |
| **U1** UAT scenario preparation | per parent with a `surface` in subtree | S-09, S-09.2, S-14, S-14.1 | **4** |
| **U2** UAT support | per parent with a `surface` in subtree × 2 UAT cycles | S-09, S-09.2, S-14, S-14.1 (each ×2) | **8** |
| **U3** UAT defect triage and fix | per parent with a `surface` in subtree × 2 UAT cycles | S-09, S-09.2, S-14, S-14.1 (each ×2) | **8** |
| **D2** planning and tracking | per parent | S-09, S-09.1, S-09.2, S-09.3, S-14, S-14.1, S-14.2 | **7** |
| **D4** requirement elaboration | per element covering ≥1 requirement | A-059, A-051, A-052, A-053, A-054, A-055, A-056, A-057, A-058, A-049, A-102, A-050, A-074, A-098, A-117, A-013, A-027 | **17** |
| **G2m** field mapping specification | per element (store) | A-074, A-098, A-117, A-013, C-015 | **5** |
| **G3m** extraction and transformation | per element (store) | A-074, A-098, A-117, A-013, C-015 | **5** |
| **G4m** load and reconciliation | per element (store) | A-074, A-098, A-117, A-013, C-015 | **5** |
| **O1** user documentation | per parent with a `surface` in subtree | S-09, S-09.2, S-14, S-14.1 | **4** |

**Technology-derived items in this batch: 222.** Every one traces to a (product element, mandated activity) pair; no item traces to anything else (W2).

**Deferred (not generated, belong to the whole model): 19 activities** — `once`: A1, U4, D1, D3, D6, E2, E3, E4, E6, E7, G1m, G5m, O2, O3, O4, S1, S2, S3; `per environment`: E1.

---

## 5. The `no` log

Every element that matched an activity's applicable set and did not receive an item. Per-cycle activities are logged once per element; cycles do not multiply refusals.

| element | activity | kind | reason |
|---|---|---|---|
| S-09 | D4 | **filter** | own coverage empty — the declaration's condition "covers at least one requirement" excludes it |
| S-09.1 | D4 | **filter** | own coverage empty — declaration's condition excludes it |
| S-09.2 | D4 | **filter** | own coverage empty — declaration's condition excludes it |
| S-09.3 | D4 | **filter** | own coverage empty — declaration's condition excludes it |
| S-14 | D4 | **filter** | own coverage empty — declaration's condition excludes it |
| S-14.1 | D4 | **filter** | own coverage empty — declaration's condition excludes it |
| S-14.2 | D4 | **filter** | own coverage empty — declaration's condition excludes it |
| C-007 | D4 | **filter** | own coverage empty — declaration's condition excludes it |
| C-008 | D4 | **filter** | own coverage empty — declaration's condition excludes it |
| C-009 | D4 | **filter** | own coverage empty — declaration's condition excludes it |
| C-025 | D4 | **filter** | own coverage empty — declaration's condition excludes it |
| C-018 | D4 | **filter** | own coverage empty — declaration's condition excludes it |
| C-015 | D4 | **filter** | own coverage empty — declaration's condition excludes it |
| S-09.1 | U1 | **filter** | no `surface` element in its subtree (A-051…A-058 are behaviour and interface) |
| S-09.3 | U1 | **filter** | no `surface` element in its subtree (A-050, C-009, C-025 are all behaviour) |
| S-14.2 | U1 | **filter** | no `surface` element in its subtree (A-013, A-027, C-015) |
| S-09.1 | U2 | **filter** | no `surface` element in its subtree |
| S-09.3 | U2 | **filter** | no `surface` element in its subtree |
| S-14.2 | U2 | **filter** | no `surface` element in its subtree |
| S-09.1 | U3 | **filter** | no `surface` element in its subtree |
| S-09.3 | U3 | **filter** | no `surface` element in its subtree |
| S-14.2 | U3 | **filter** | no `surface` element in its subtree |
| S-09.1 | O1 | **filter** | no `surface` element in its subtree |
| S-09.3 | O1 | **filter** | no `surface` element in its subtree |
| S-14.2 | O1 | **filter** | no `surface` element in its subtree |

**25 refusals: 25 filter, 0 judgement.**

Stated plainly, because it is the measurement: **in this batch no element that matched both an activity's classes and the declaration's own further condition was declined.** Once the classification of §3 was fixed, the crossing was mechanical. All of this batch's freedom is therefore located in §3, not here, and a second run's divergence from this one will be traceable to a class assignment.

---

## 6. Demanded-work branch (W6)

All 7 ids of the whole-model demanded list, each recorded **once**.

| id | disposition | where |
|---|---|---|
| **W-1** | **accounted for at technology-derived items in this batch** | Absorbed at D4 (17 items) and K1 (23 items), anchored at **K1@A-059**. The remaining claimed absorbers A1 (test strategy) and S1 (security review of the design) are `once`-scoped and deferred from this partial run. Not entered as its own branch. |
| **W-2** | **accounted for at technology-derived items in this batch** | Absorbed at K2, A2, A3, A4, A5, A6, A7, A8 and A10, anchored at **K2@A-059**. K3 and A9 are in scope but generated nothing here (§7). Not entered as its own branch. |
| **W-3** | **accounted for at technology-derived items in this batch** | Absorbed at G2m, G3m, G4m, U1, U2 and U3, anchored at **U1@S-09**. The remaining claimed absorbers E1–E7, G1m, G5m and U4 are `once`- or `per environment`-scoped and deferred. Not entered as its own branch. |
| **W-4** | **accounted for at deferred once-scoped activities, with an unabsorbed remainder recorded** | The absorbed portion sits at O3 (support handover pack) and E6 (production cutover), both `once`-scoped and deferred from this partial run. The **post-production support period itself is absorbed by no declared activity**; the declaration assigns it to the assumption log as not priceable without a term. It is recorded here, visibly, and **no activity was invented for it** — see §7. |
| **NFR-11** | **accounted for at deferred activities** | E1 (`per environment`, ×4) and E2, E3 (`once`) — all deferred from this partial run. Nothing generated here. Not entered as its own branch. |
| **NFR-12** | **accounted for at technology-derived items in this batch** | Absorbed at G2m, G3m, G4m across the five `store` elements (15 items), anchored at **G2m@A-013**. G1m and G5m are `once`-scoped and deferred. Not entered as its own branch. |
| **NFR-15** | **stands alone in this batch** — item **`DW-NFR-15`** | The declaration claims absorption at A9. **A9 is in scope in this batch and generated no item**, because the batch contains no `statement` element and therefore none with a measurable performance, capacity or availability target. The absorption claim is unrealised here, so per W6 the obligation enters as its own branch carrying requirement id NFR-15. **Reconciliation flag for whole-model consolidation:** if A9 generates items on a `statement` element in another batch, `DW-NFR-15` is retired there and must not be counted twice. |

**Every demanded id appears. None was struck out, and none is silent.**

---

## 7. W5 findings (reported, not repaired)

**Mandated activities that applied to no element in scope — 2**

1. **K3 — statement realisation and evidence.** No element in either subtree classified as `statement`. Either the technology's statement-realisation dimension has no purchase on import/export and configuration data, or these two subtrees of the product model express their policies inside behaviours rather than as elements of their own. Not repaired; no element was reclassified to give K3 work.
2. **A9 — performance and availability testing.** Same cause: no `statement` element, hence none with a measurable target. This finding has a consequence outside the crossing — it is why NFR-15 stands alone at §6 — and it should be resolved at whole-model consolidation, not here.

**Elements no activity touched — 0.** Every one of the 30 elements in scope receives at least five work items.

**Granularity strain observed while classifying (reported as findings about the product model; nothing was added, split, merged or moved)**

- **C-008 "Import staging and preview"** declares two things of different kinds — data held before commit, and a screen a user meets. It was forced into one class (`surface`). If the intent was a staging store, three migration items (G2m/G3m/G4m@C-008) are absent from this work model.
- **C-025 "Export artefact retention and cleanup"** joins a retention policy to a cleanup action. Classified `behaviour` so as not to lose the build work; if a retention policy is separately intended, K3 work is absent.
- **A-058 "PC/Mac encoding and line-ending variants"** reads as a variant set rather than a distinct thing built, and sits as a sibling of eight named adapters.
- **A-059** is a leaf sibling of three aggregates directly under S-09, mixing levels at that position.
- **S-09.1** holds eight near-identical adapter siblings; five of them (A-051–A-055) carry identical coverage `G-12.1, D-5`, and the crossing gives each an identical set of six items. This is a shape the product model chose, not one the crossing can flatten.

---

## 8. Projection onto the requirement anchor

One row per requirement id appearing in the own-coverage sets of the in-scope elements; "items" counts **all** work items in this batch tracing to the covering elements.

| requirement | covering elements (in scope) | elements | work items tracing to them |
|---|---|---|---|
| G-6.1 | A-027 | 1 | 6 |
| G-12 | A-049, A-050 | 2 | 12 |
| G-12.1 | A-051, A-052, A-053, A-054, A-055, A-056, A-057 | 7 | 44 |
| G-12.2 | A-058 | 1 | 6 |
| G-12.3 | A-059 | 1 | 6 |
| P-1 | A-049, A-074 | 2 | 15 |
| P-5 | A-049 | 1 | 6 |
| P-7.1 | A-059 | 1 | 6 |
| P-7.2 | A-059 | 1 | 6 |
| P-10.3 | A-098 | 1 | 9 |
| P-11 | A-049 | 1 | 6 |
| P-11.2 | A-049, A-102 | 2 | 12 |
| P-19 | A-049, A-056, A-057 | 3 | 20 |
| L-1 | A-049, A-074 | 2 | 15 |
| L-2.1 | A-117 | 1 | 9 |
| L-5 | A-049 | 1 | 6 |
| L-6.1 | A-059 | 1 | 6 |
| L-6.2 | A-059 | 1 | 6 |
| L-9.3 | A-098 | 1 | 9 |
| L-10 | A-049 | 1 | 6 |
| L-10.2 | A-049, A-102 | 2 | 12 |
| L-15 | A-049, A-056, A-057 | 3 | 20 |
| I-8 | A-050 | 1 | 6 |
| I-11 | A-013 | 1 | 9 |
| D-5 | A-059, A-051, A-052, A-053, A-054, A-055, A-056, A-057, A-050 | 9 | 56 |
| NFR-4 | A-057 | 1 | 7 |

**26 requirement ids** are anchored by this batch. Rows overlap: an element covering several requirements contributes its full item count to each row, so the column does not sum to 222 and must not be added up.

---

## 9. Instrument readings

**Engine `Hotyn-W 1.1` · partial run · batch `S-09 + S-14` of product model `HM44-OA1` (`Hotyn-M 1.1`) · technology declaration SAS, approved 2026-09-08.**

**Total work items: 223** — 222 technology-derived + 1 demanded-work branch item (`DW-NFR-15`).

**Items by activity**

| K1 | K2 | K3 | A2 | A3 | A4 | A5 | A6 | A7 | A8 | A9 | A10 | U1 | U2 | U3 | D2 | D4 | G2m | G3m | G4m | O1 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 23 | 23 | 0 | 23 | 23 | 23 | 14 | 14 | 7 | 7 | 0 | 2 | 4 | 8 | 8 | 7 | 17 | 5 | 5 | 5 | 4 |

**Items by scope** — per element **149** · per parent **29** · per parent × cycles **44** · once **0 (deferred)** · per environment **0 (deferred)**. Total 222.

**Elements by class** — aggregate 7 · behaviour 13 · store 5 · surface 3 · interface 2 · statement 0. Total 30.

**Items per element** — mean **7.4** · min **5** (C-007, C-008, C-018, C-009, C-025) · max **13** (S-09, S-09.2, S-14, S-14.1).

**`no` answers: 25** — **filter 25 · judgement 0.**

**Elements untouched: 0.**

**Activities unused (in scope, generated nothing): 2** — K3, A9.
**Activities deferred (whole-model scope, correctly not generated): 19** — A1, U4, D1, D3, D6, E2, E3, E4, E6, E7, G1m, G5m, O2, O3, O4, S1, S2, S3 (`once`) and E1 (`per environment`).
**Activities used: 19.** 19 + 2 + 19 = 40 declared activities, all accounted for.

**Demanded items: 7** — absorbed at technology-derived items in this batch **4** (W-1, W-2, W-3, NFR-12) · accounted for at deferred once/per-environment activities **2** (W-4 in part, NFR-11) · standing alone **1** (NFR-15) · unabsorbed remainder recorded without an item **1** (the post-production support period within W-4, per the declaration's own instruction).

**Parameters as read:** environments 4 (deferred) · test execution cycles 2 (applied at A5, A6) · UAT cycles 2 (applied at U2, U3) · migration rehearsal cycles 2 (deferred with G5m).

**No effort, duration, size, cost or complexity figure appears anywhere above.** Every number in this document is a count of elements, activities, items or refusals.