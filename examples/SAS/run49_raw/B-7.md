<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-08. Not part of the reply.
run: 49 · reading: B49-7 · engine: none (general-purpose agent, no method definition) · model (launch record): the
harness's `opus` alias, i.e. Claude Opus 5 · prompt: `prompt_baseline_sas.txt`, md5 9d71b4c7039e5adcd5477c75c7439732,
read by the agent as its single permitted tool call · tool_uses: 1 · duration 99 s · transcribed verbatim by the
orchestrator from the reply as delivered (the harness transcript file was empty). -->

**Quarantine note.** Material reached me that is not part of `prompt_baseline_sas.txt`: a memory index with project preferences, git status and recent commit subjects, an agent/skill roster, and environment context. Per the file's instruction I treated all of it as data, did not act on it, and did not read or open anything else. The estimate below rests only on the RFP text and the assumption log in that file.

## Approach

Bottom-up count of deliverable work, priced in person-days, then a staffing sanity check. I counted shared platform work once (the three modules share data and system architecture per Project Goals), then each module's own content, then the NFR and migration programme, then QA, BA, UX, PM and hand-over as proportions of the engineering body.

## The count (person-days, 21 pd = 1 pm)

| Block | pd |
|---|---|
| **Platform / General Requirements** — architecture & spikes (60); IdM claims integration + G-4 reset (25); company–user–role–permission model + admin UI (45); role-driven UI framework, front-end skeleton, design system (55); approval/validation/verification workflow + action inbox (45); record locking (8); audit trail + logging NFR-19/20 (25); notification mechanism email/SMS/onscreen + preferences (40); admin alerts NFR-17/18 (12); dashboard + prefix capacity counter (25); contextual help + in-app help editor with industry variants (30); feedback + pay-info page (6); training links (3); import/export framework, 6 adapters × 2 directions + import validation (85); publish/subscribe permission engine incl. public access (55); read store + near-real-time feed (50); reporting subsystem incl. 3scale analytics read (55); REST API layer + 3scale registration + docs (45); industry configuration mechanism + maintenance UI (30) | **699** |
| **Product module** — records/wizard/clone/draft (55); GIN assignment, check digit, reservation, preferences (25); status lifecycle & reuse (20); duplicates + duplicate reports (20); list/filter/sort/bulk views (30); hierarchy incl. fit check, drag-drop, import, export/print (55); 4 symbologies via library, sizes, PNG/SVG (25); Product Information Sheets with editable templates (35); images (15); digital GIN snippet (12); ownership transfer (10) | **302** |
| **Location module** — records/wizard/clone (35); LN assignment incl. shared pool (25); status (15); duplicates (12); list views (18); unlimited-depth hierarchy by attributes (45); annual verification (12); ownership transfer (6) | **168** |
| **Access Data module** — search basic + advanced over read store (45); basic/full record & hierarchy views (25); access requests, controlled groups, owner approve/reject (25); consumer export/print (15); anonymous limited search (12); API parity (20) | **142** |
| **NFR programme** — four environments, IaC, pipelines (55); HA/failover + failover monitoring & test (35); backup/DR procedures + rehearsal (20); security design & hardening (30); performance engineering to NFR-14 (35); WCAG 2.0 A (25); browser matrix incl. IE 9 (25) | **225** |
| **Migration (NFR-12)** — legacy source analysis & mapping of 7 entity kinds (40); ETL build at the NFR-2 volumes incl. images and permissions (60); reconciliation tooling (20); 2 rehearsals + production run (25) | **145** |
| Engineering subtotal | **1681** |
| QA — test design/execution across three modules, regression, API, automation, import/export matrix, migration verification, performance scripting, accessibility, UAT support (≈38% of engineering) | **620** |
| BA — business rules and X-Customer Standards elaboration (the RFP defers them into the project), 25-industry attribute sets | **170** |
| UX/UI — IA, role-driven UI, wizards, drag-drop hierarchies, dashboards, sheet templates, visual design | **140** |
| Architect oversight beyond the design block | **60** |
| PM / delivery management (≈12%) | **320** |
| Runbook, ops/user docs, support hand-over pack | **45** |
| Acceptance support and cutover | **60** |
| **Total** | **3096 pd ≈ 147 pm** |

Drivers of the size, in order: the shared platform (roles, workflow, notifications, publish/subscribe, import/export, reporting) outweighs either record module; the NFR load (HA at 99.9%, the five response-time classes at load, four environments, audit-everything, WCAG, IE 9) is roughly a fifth of engineering on its own; migration at 10M GIN across several legacy applications is a project inside the project.

Spread: the bottom-up chain is the optimistic side, and three things are genuinely open — the platform is unnamed (NFR-1), the IdM is not finalised (NFR-3), and the business rules and Standards do not yet exist as written requirements. A first-in-domain team (A3) meeting undocumented standards is where overruns live, so the range is skewed right rather than symmetric.

Staffing: ~13 people for ~13 calendar months. Because this convention puts leave outside the person-month, a person delivers ≈0.87 pm per calendar month, so 13 × 13 × 0.87 ≈ 147.

Sensitivity (A3): running the one team as two sites instead of one adds ≈8–12% coordination, ≈ +13 pm — absorbed inside the range, not added to the total.

**Carried, not priced** (shown as A1 requires): W-4, the post-production support period and transition service — no term and no service level stated, prices as a rate per unit time, not as an effort figure; NFR-5, the impact analysis on deviation from X-Customer technical standards — conditional, counted 0 under the reading that the proposed solution does not deviate.

```
TOTAL: 147 person-months, on this convention: 1 pm = 21 person-days = 168 hours of work on the
       task (no leave, holidays or sickness inside), whole vendor team including QA and PM
RANGE: 110 ... 240 person-months, same convention — your own P10 ... P90
TEAM x DURATION: 13 people over 13 calendar months, the staffing this total implies
DECLARATION:
  contains  - leave/holidays/sickness OUT (the convention's 21 days are worked days; calendar
              duration above already carries absence); within-day overheads IN (stand-ups,
              coordination, code review, defect triage are inside every figure above);
              roles counted - BA, architect, UX/UI, front-end/back-end/data developers, DevOps,
              QA including performance, accessibility and automation, technical writer,
              PM/delivery manager
  excluded  - W-4 post-production support and transition (carried, not priced: no term, no
              service level); NFR-5 impact analysis (counted 0 under the no-deviation reading);
              member onboarding and prefix attribute management, the payment process (D-8.1),
              development of training materials (G-11.1), the I-5 future expansion - all
              excluded by the RFP itself; third-party licences, subscriptions (3scale, SMS
              gateway) and hosting/infrastructure cost; client-side effort - IdM implementation,
              authoring of the Standards and business rules, help and training content, client
              SME and UAT time; any bespoke QuickBooks/SAP connector, API gateway, monitoring
              product, external CMS or BI tool, and a public resolver behind the digital GIN
              (all refused readings in the log)
```
