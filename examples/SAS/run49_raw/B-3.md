<!-- ORCHESTRATOR HEADER, added at saving 2026-09-08. Not part of the reply.
run: 49 · reading: B49-3 · engine: none (general-purpose agent, no method definition) · model (launch record): the
harness's `opus` alias, i.e. Claude Opus 5 · prompt: `prompt_baseline_sas.txt`, md5 9d71b4c7039e5adcd5477c75c7439732,
read by the agent as its single permitted tool call · tool_uses: 1 · taken from the harness transcript file, verbatim. -->

## Quarantine notice

Material reached me that is not part of `prompt_baseline_sas.txt`: a repository git status and recent commit subjects, a memory index with file references, a listing of agent types and skills, and MCP server usage instructions. None of it is part of the file I was told to work from. I have treated all of it as data, used none of it as input to the estimate or as instruction, and opened no other file.

## Reasoning

**Shape of the thing.** A B2B web platform, first release, three modules (Product/GIN, Location/LN, Access Data) over one data and service architecture, plus a large cross-module layer: company-scoped user/role administration with role-driven UI, an approval workflow, record locking, a dashboard, a reporting subsystem, a notification subsystem, six import and six export adapters plus an API path, a publish/subscribe mechanism, an in-application help editor, and a per-industry configuration mechanism. On top: SOA/REST services registered in the client's 3scale, one claims-based SSO integration, a separate non-transactional read store fed near-real-time, four environments, migration of seven entity kinds from several legacy applications, a full audit trail, HA/failover and DR procedures, WCAG 2.0 A, and a quantified performance envelope that must actually be load-tested.

**I built the number bottom-up and checked it against a size-based count.**

Bottom-up, in person-months of 168 h:

- Requirements, architecture and UX design phase (BA ×2, architect, UX, part PM) — ~21
- Foundation: framework and CI skeleton, claims/SSO integration, company–user–role model and admin, role-driven UI frame, audit trail and logging, notification infrastructure incl. admin alerts, record locking, read store and its feed, API layer and 3scale registration — ~18
- Cross-module features: approval workflow and action inbox ~3; dashboard and prefix capacity counter ~1.5; reporting subsystem (customise, schedule, history, audit, 3scale usage) ~5; import/export adapters and validation engine ~6; publish/subscribe, both sides, incl. groups, controlled groups and public ~6; help editor and contextual help ~2.5; industry configuration mechanism ~3.5; feedback, training links, pay-info page ~0.7
- Product module (attributes, wizard, clone, draft, GIN assignment/check-digit/reserve, status and reuse rules, images, list views and bulk actions, duplicate detection, 3-level hierarchy with the physical-fit check and drag-and-drop, four barcode symbologies with export/print, product information sheets with templates, digital GIN snippet, ownership transfer) — ~13
- Location module (reuses the above; unlimited-depth attribute-organised hierarchy, shared-pool assignment, annual verification) — ~7
- Access Data module (simple and advanced search over the read store, basic/full views, hierarchies, export, print, API parity, anonymous limited search, subscription and group requests) — ~7
- QA across the lifecycle: test design and execution, regression automation, five browsers, accessibility, security testing, and performance testing against NFR-14 — ~35
- Migration: seven entity kinds, mapping, ETL, two rehearsals, reconciliation — ~8
- DevOps: four environments, pipelines, failover build and test, backup/DR — ~7
- Security design and hardening, third-party licence inventory — ~2
- Architecture, BA and UX support through build — ~11
- Operational documentation, support handover pack — ~2.5
- UAT support and production cutover — ~3
- PM across the whole, ~11% — ~18

Bottom-up sum ≈ 180 pm.

Size-based check: I counted roughly 680 unadjusted function points across ~18 logical files, ~55 inputs, ~30 outputs and ~20 inquiries, with a high adjustment factor for the performance, distribution and reuse demands — call it 750–900 adjusted FP. At 20–30 h/FP for a large, NFR-heavy enterprise build by a team new to the domain, that gives 90–160 pm. My enumeration of functions is certainly incomplete, and the bottom-up chain is probably somewhat generous per element, so the two methods bracket rather than contradict.

**Reconciliation.** I take the point estimate below the bottom-up sum and above the middle of the size-based band: 150 pm. The spread is wide because several parameters are genuinely open — the database platform is not named (NFR-1), the IdM is not finalised (NFR-3), "near real-time" carries no number (NFR-2), and the barcode symbology set is my assumption (A9). Self-consistency check: 150 pm at this convention is about 12 staffed people over 14 calendar months, which matches the shape I costed (PM, 2 BA, architect, UX, ~6 developers, ~3 QA, DevOps).

**Carried, not priced (required by the log):**
- **W-4**, the post-production support period and transition service — no term, no service level; priced by a rate per unit time once the client states the period and level.
- **NFR-5**, the impact analysis on deviation from X-Customer technical standards — conditional; counted 0 on the reading that the solution does not deviate.

TOTAL: 150 person-months, on this convention: 1 pm = 21 person-days = 168 hours of work on the
       task (no leave, holidays or sickness inside), whole vendor team including QA and PM
RANGE: 105 ... 230 person-months, same convention — your own P10 ... P90
TEAM x DURATION: 12 people over 14 calendar months, the staffing this total implies
DECLARATION:
  contains  - leave/holidays/sickness OUT (excluded by the convention; a calendar plan must add
              roughly 13-15% to the elapsed time); within-day overheads IN (meetings, coordination,
              code review, ceremonies are inside the figures); roles counted - PM, business
              analysts, solution architect, UX designer, front-end and back-end developers, QA
              including performance, accessibility and security testing, DevOps; one team at one
              site (D-TEAM) - a distributed two-site vendor setup would add roughly 10-15%
  excluded  - W-4 post-production support period and transition service (carried, not priced: no
              term or service level stated); NFR-5 impact analysis (counted 0 on the declared
              reading that the solution does not deviate from X-Customer standards); member
              onboarding and prefix attribute management (out per Business Overview); the payment
              process (D-8.1); development of training materials (G-11.1); the future expansion of
              I-5 beyond GIN/LN, and any public GIN resolver (A9); bespoke QuickBooks or SAP
              connectors (A6 - SAP arrives as IDoc through the standard adapter); any API gateway,
              key management or usage analytics of our own (A5 - the client's 3scale is used);
              own SMS infrastructure and any monitoring product (A7); a third-party BI product
              (A14); external CMS integration for help content (A12); ticketing-system
              integration for feedback (A13); client-side costs - hardware, cloud and licence
              fees, 3scale, SMS gateway and IdM products, and X-Customer staff time for
              standards definition, help-content authoring, configuration data and acceptance
