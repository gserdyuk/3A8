<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-08. Not part of the sensor's reply.
run: 47 · reading: HD47-B2 · engine (sensor's own stamp): Hotyn-D 2.0 · model (launch record): the harness's `opus`
alias, i.e. Claude Opus 5 · batch B of the work model HW45 (prompt generated by make_sizing_prompts.py), repeat 2
· tool_uses: 0 · received complete; text taken from the harness transcript of the agent, identical to the reply as received. -->

# Size classification — batch `S-03` of product model `HM44-OA1`

## 1. Contamination check

**Clean — proceed.**

The pasted input (INPUT 1 and INPUT 2) contains no effort figure, no person-day value, no rate, no price, no budget, no duration, no team size, no prior estimate and no rate-table row. The only numerals in the work model are identifier format facts inside obligation texts (GIN 12 or 14 digits, LN 13 digits, licensed prefix 6–9 digits) — these are counts of digits in a data format, not units of work, and they do not steer any total.

Ambient repository material **was** present in my context (a git status block, recent commit subjects, a memory index, working-directory paths). Per the quarantine instruction I did not read it as input, did not act on it, and did not treat it as run-stopping contamination. Classification below rests on the pasted input alone. No files were read.

**One adjudication declared up front, because it applies batch-wide.** Almost every element here covers a P-\* (GIN) obligation and its L-\* (LN) twin. Because the subtree is declared *"Shared record core"*, I count the paired obligations as **one action over a parameterised record kind**, not as two — the differences between the twins are in actor and qualifier, not in the verb-on-object. This is a single reading applied uniformly; it is logged as doubt D-0 and it is the one decision that could move many counts at once.

---

## 2. Element sizing table

### Subtree S-03.1 — Record authoring (aggregate, not sized)

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A-070 | Record creation (individual entry) | behaviour | 3 actions: create a record by individual entry; enforce the required attributes at entry; vary the required/non-required attribute set by industry | 3 | **M** |
| A-071 | Step-by-step record creation wizard | surface | 1 user task: complete the creation of a record by walking the wizard step by step | 1 | **S** |
| A-072 | Record cloning | behaviour | 1 action: create a new record by cloning an existing record | 1 | **S** |
| A-081 | Attribute editing before finalisation | behaviour | 1 action: edit record attributes while the record is not yet finalised | 1 | **S** |
| A-082 | Attribute editing after finalisation (manual and import) | behaviour | 2 actions: edit record attributes manually; edit record attributes via import (P-3: "manually or via import" names two channels, not one) | 2 | **M** |

### Subtree S-03.2 — Identifier services (aggregate, not sized)

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A-004 | Identifier format and check-digit rules | behaviour | 5 actions: validate the company's licensed prefix (6–9 digits); form the serial part; compute the check digit; validate total identifier length by type (GIN 12 or 14, LN 13); validate the identifier against X-Customer Standards (NFR-16 record validation) | 5 | **L** |
| A-076 | Identifier assignment service (auto/manual, any point) | behaviour | 3 actions: assign an identifier automatically; assign an identifier manually; assign the check digit automatically | 3 | **M** |
| A-078 | Choice of auto or manual at assignment | behaviour | 2 actions: opt an identifier series into auto-assign; opt it into manual assignment (P-3: the two outcomes are distinct) | 2 | **M** |
| A-079 | Identifier reservation (held out of auto-assign) | behaviour | 2 actions: reserve an identifier; hold a reserved identifier out of the auto-assign pool | 2 | **M** |
| A-080 | Stored assignment preference | store | 1 entity kind: the assignment preference (auto-assign or manual) held per prefix licence | 1 | **S** |
| A-084 | Back-reference to a superseded identifier | store | 1 entity kind: the back-reference link from a current identifier to the identifier it superseded | 1 | **S** |
| A-097 | Out-of-use and reuse-availability dates on an identifier | store | 2 entity kinds: the identifier lifecycle dates (date taken out of use, date available for reuse); the reuse rule keyed by industry and product type | 2 | **M** |

### Subtree S-03.3 — Record status and lifecycle (aggregate, not sized)

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A-075 | Draft state for incomplete records | behaviour | 1 action: save an incomplete record as Draft | 1 | **S** |
| A-083 | Status-driven attribute editability rules | behaviour | 1 action: block editing of an attribute on the basis of the record's status | 1 | **S** |
| A-093 | Status view and management for records and identifiers | surface | 4 user tasks: view the status of a record; view the status of an identifier; manage (change) the status of a record; manage the status of an identifier | 4 | **L** |
| A-095 | Propagation of record status to identifier status | behaviour | 5 actions: transition an identifier to Reserved; to In Use; to For Reuse; to Available (P-3: a stage list counts by outcome); propagate a record status change onto the identifier's status | 5 | **L** |
| A-096 | Record status model (system and user assignment) | behaviour | 2 actions: the system assigns a status to a record; the user assigns a status to a record | 2 | **M** |

### Subtree S-03.4 — Duplicate handling (aggregate, not sized)

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A-090 | Duplicate candidate view with remove/edit | surface | 3 user tasks: view potential duplicate records; remove a duplicate; edit a duplicate | 3 | **M** |
| A-091 | Duplicate alert during creation | behaviour | 2 actions: detect a possible duplicate during record creation; alert the user to it | 2 | **M** |
| A-092 | Duplicate report over completed records | surface | 1 user task: run a duplicate report over completed records | 1 | **S** |

### Subtree S-03.5 — Hierarchy services (aggregate, not sized)

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A-099 | Hierarchy model with manual create, edit and view | store | 2 entity kinds: the hierarchy itself (a named tree over identifiers); the hierarchy link (parent-child relation between identifiers, e.g. each → case → pallet) | 2 | **M** |
| A-100 | Drag-and-drop hierarchy editor | surface | 3 user tasks: create a hierarchy in the visual editor; edit a hierarchy by drag-and-drop; view a hierarchy in visual format | 3 | **M** |
| A-105 | Hierarchy status-impact analysis and notification | behaviour | 3 actions: detect a status change on a record inside a hierarchy; determine whether it affects the status of other records in that hierarchy; notify the user | 3 | **M** |
| A-106 | Hierarchy export and print rendering | surface | 2 user tasks: export a hierarchy in a user-friendly format; print a hierarchy in a user-friendly format | 2 | **M** |
| C-016 | Hierarchy integrity constraints | statement | — see section 4, unsizeable (M10); no kind assigned | — | — |

### Direct child of S-03

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A-113 | Record ownership transfer between members | behaviour | 1 action: transfer ownership of a record to another member | 1 | **S** |

### Subtree S-03.7 — Record list views (aggregate, not sized)

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A-086 | Record list view with filter and sort | surface | 3 user tasks: view all records in a single view; filter the records; sort the records | 3 | **M** |
| A-087 | Single-record selection and action menu | surface | 3 user tasks: select an individual record; edit the selected record; export the selected record ("etc." is an unnamed catch-all and counts nothing, P-5) | 3 | **M** |
| A-088 | Multi-record selection and bulk action | surface | 2 user tasks: select multiple records; apply the same action to all selected records | 2 | **M** |

### Subtree S-03.8 — Record locking (aggregate, not sized)

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A-025 | Record edit lock (single editor) | behaviour | 2 actions: lock a record when a user opens it for editing; refuse a second concurrent editor on a locked record | 2 | **M** |
| C-017 | Lock expiry and administrative release | behaviour | 2 actions: expire a lock; release a lock administratively (both counted from name tokens under P-2 — the element has empty own coverage, so no obligation covers either token) | 2 | **M** |

**XL findings: none.** No element in this batch reached the XL threshold for its class, so no coarseness finding (M10) arises from size.

---

## 3. Special counts

The only special-count activities present in this batch are **G2m / G3m / G4m** (field mapping, extraction and transformation, load and reconciliation, on a store). They are crossed onto four elements — A-080, A-084, A-097, A-099 — all stores. **No A9 activity is crossed onto any element in this batch**, so no measurable-target enumeration is due.

**A-080 — Stored assignment preference** — pre-loaded entity kinds: **1**
- *assignment preference per prefix licence* — must arrive from the predecessor application because a migrated licensee's identifier assignment mode is already chosen there, and the first automatic assignment after cut-over can occur before any user visits a preference screen; created at run time it would default rather than continue.

Count 1 → **S**.

**A-084 — Back-reference to a superseded identifier** — pre-loaded entity kinds: **1**
- *back-reference link (current identifier → superseded identifier)* — must arrive from the predecessor because the supersession event that created the link happened before migration; no run-time action in the new system can regenerate a link to a number that was already replaced.

Count 1 → **S**.

**A-097 — Out-of-use and reuse-availability dates on an identifier** — pre-loaded entity kinds: **2**
- *identifier lifecycle dates* — must arrive from the predecessor because identifiers retired before cut-over carry a past date of withdrawal and a derived reuse-availability date; the withdrawal is a historical fact, not a run-time event of the new system.
- *reuse rule per industry and product type* — must exist before the system is usable because the reuse-availability date of every migrated retired identifier is computed against these rules from day one; with no rule loaded, migrated identifiers have no reuse date to display.

Count 2 → **M**. (The second kind is arguable — see doubt D-9.)

**A-099 — Hierarchy model with manual create, edit and view** — pre-loaded entity kinds: **2**
- *hierarchies* — must arrive from the predecessor because existing product and location hierarchies are in use at cut-over; recreating them by run-time user action would mean re-entering every structure by hand before the system is usable.
- *hierarchy links* — must arrive from the predecessor because the parent-child assignment of each migrated identifier is data held in the predecessor and is not derivable from the migrated records themselves.

Count 2 → **M**.

---

## 4. Unsizeable elements

**C-016 — Hierarchy integrity constraints** · statement · parent S-03.5 — **unsizeable — model defect (M10). No class assigned, no kind assigned.**

What is missing: the element is derived and has **empty own coverage**, and its declared content is the name alone. A `statement` is sized by enumerating *the systems or components the property constrains*; the declaration names no constraint (what integrity property is asserted) and no constrained component (which store, surface or behaviour it binds). It also offers no phrase from which the compliance/behavioural kind could be justified, so P-4's tie-break has nothing to act on. Assigning either a class or a kind here would require me to invent the constraint from the parent subtree — a guess, not a count.

---

## 5. Doubts

**D-0 (batch-wide, the one that matters most).** Every element except A-083, A-025 and the two derived C-\* elements covers a P-\* / L-\* obligation pair (GIN and LN). I read the pair as one action over a parameterised record kind, because the subtree is declared *"Shared record core"*. Read the other way — GIN record and LN record as distinct objects of the verb — most counts in this batch would double, and many M rows would become L. The arguable item is *the record kind as object*: I did not treat it as one.

**D-1 · A-070.** "records include required and non-required attributes" — I counted enforcing required attributes as one action and industry variation as another, but did not count *accepting non-required attributes* as a third. If it is counted, the enumeration is 4 and the class moves M → L.

**D-2 · A-071.** I counted one user task (walk the wizard to create a record). If entering the industry-varying required/non-required attribute set inside the wizard is a second distinct task, the count is 2 and the class moves S → M.

**D-3 · A-072.** "cloning an existing record" — I counted one action. Selecting the source record, or adjusting the clone's attributes, could each be argued as a separate named action; neither is named in the obligation text, so I did not count them.

**D-4 · A-081.** "Edit record attributes before finalising" — the arguable item is *finalise the record*: the obligation names finalisation only as a boundary condition, so I did not count it as an action of this element. Counted, the class moves S → M.

**D-5 · A-004.** I collapsed the GIN length check (12 or 14) and the LN length check (13) into one "validate total identifier length by type". Split, the count is 6 — still **L**, so the class does not move.

**D-6 · A-076.** NFR-16's clause "identifiers, auto-generated or manual, comply with X-Customer Standards" is covered by both A-004 and A-076. I counted the standards check on A-004 only, to avoid counting the same named thing twice across two elements. Counted here too, A-076 is 4 and moves M → L.

**D-7 · A-078.** "opt for auto-assign or manual" could honestly be read as a single action ("choose the assignment mode"), giving 1 and moving M → S. I applied P-3.

**D-8 · A-079.** Likewise: "reserve identifiers to hold them out from auto-assign" could be read as one action rather than reserve-plus-exclude, moving M → S.

**D-9 · A-097 (element and its special count).** The arguable item is *the reuse rule keyed by industry and product type*: it may be configuration owned by another element rather than an entity kind of this store. Dropped, the element is 1 → **S** and the pre-load count is 1 → **S**.

**D-10 · A-080.** I counted one entity kind (a preference parameterised by identifier type). If the GIN assignment preference and the LN assignment preference are separate stored kinds, the count is 2 and the class moves S → M. This is D-0 in store form.

**D-11 · A-084.** I counted the link as the single entity kind. If the superseded identifier itself is retained as a distinct stored kind, the count is 2 and the class moves S → M.

**D-12 · A-093.** "View and manage the status of a record or GIN" — I crossed two verbs against two objects for 4 tasks. Read as two tasks over one parameterised object, the count is 2 and the class moves L → M. This is the single largest swing in the batch.

**D-13 · A-095.** I applied P-3's stage-list rule to the status vocabulary (Reserved, In Use, For Reuse, Available), counting four transitions plus propagation. Two things weaken this: the obligation qualifies the list with "**currently**", suggesting a vocabulary that may change, and L-9.1 names **no** LN statuses at all. Read as a single "set identifier status" action plus propagation, the count is 2 and the class moves L → M.

**D-14 · A-096.** "The system or the user can assign a status" — I counted system-assignment and user-assignment as two actions, for consistency with the auto/manual split I made on A-078. Read as one action with two actors, the count is 1 and the class moves M → S.

**D-15 · A-091 / A-092.** Both elements cover the same obligation (P-9.1 / L-8.1), which names an alert during creation *and* a report over completed records. I scoped each element by its declared name rather than counting both halves twice. Had I counted the full obligation on both, each would be larger — that would be double-counting one named thing.

**D-16 · A-091.** The arguable item is *detect a possible duplicate*: it can be read as internal to "alert the user", giving 1 and moving M → S.

**D-17 · A-099.** I counted hierarchy and hierarchy link as two entity kinds. Read as a single kind (the hierarchy, with links as its internal structure) the count is 1 → **S**; adding the level type (each / case / pallet) as a third kind gives 3, still **M**.

**D-18 · A-025.** "Lock records so that only one user at a time can edit" could be read as one action rather than lock-plus-refuse, moving M → S.

**D-19 · C-017.** The whole enumeration rests on name tokens under P-2, since own coverage is empty. This is a weaker footing than any other row in the batch: if the model intends the name as a label rather than a declaration of two behaviours, the element is closer to C-016's condition than to a sized behaviour. I sized it because both tokens name a verb on an object and no obligation covers either; I record the discomfort rather than hide it.

### Contradictions between obligations — named, not resolved

**X-1 · A-078.** P-2.1 says "**Users** can opt for auto-assign or manual"; L-2.2 says "**The prefix licensee** can opt". The same shared element is given two different actor scopes. I sized on the action, which is identical in both readings, and leave the actor question open.

**X-2 · A-079.** The same split: P-2.2 grants reservation to "a user", L-2.3 to "the prefix licensee".

**X-3 · A-083.** The element sits in a subtree declared *shared*, but its only covered obligation is P-5.1 (product side); there is no LN counterpart. Either the rule is product-only and the element is mis-parented, or an L-\* obligation is missing from coverage. I sized the stated reading.

**X-4 · A-097.** P-10.3 says reuse rules "may vary by industry **and product type**"; L-9.3 says "may vary by industry" only. One shared store is asked to key its rules two ways. I enumerated the wider (product-side) reading and flagged it in D-9.

**X-5 · A-095.** P-10.1 names a four-value GIN status vocabulary; L-9.1 names none for LNs while asserting the same propagation. The shared element therefore has one vocabulary declared and one undeclared.

---

## 6. Closure violations

Work I judge necessary that this batch does not contain. Each is named and left unsized and unpriced. Where an item may legitimately live in a subtree outside this batch, I say so — I can only see S-03.

- **Record creation via import.** P-1 and L-1 name four creation channels: individually, via import, through a wizard, by cloning. The batch realises three (A-070, A-071, A-072) and contains no element for the import channel. It would cover ingesting a record file, mapping its columns to record attributes and reporting per-row rejections. It may exist in an import subtree outside this batch; if it does not, the obligation is uncovered.
- **Hierarchy creation and editing via import.** P-11 and L-10 name manual, import and visual channels. A-099 covers the manual/model side and A-100 the visual side; no element covers the import channel for hierarchies.
- **Record approval workflow.** NFR-16 is covered by A-004 and A-076, but its first clause — "records entered via UI or import are submitted, reviewed, rejected or approved" — is realised by no element in this batch. It would cover the submit, review, reject and approve transitions and the reviewer's queue.
- **Record verification workflow.** The third clause of the same obligation — "attributes entered via UI or import are verified for accuracy" — likewise has no element here. It would cover the verification pass over attribute values and its outcome recording.
- **Duplicate match criteria.** A-090, A-091 and A-092 present, alert on and report duplicates, but no element in the batch declares what makes two records candidate duplicates, or stores that criterion. Without it the three duplicate elements have no defined input.
- **Lock visibility to the blocked user.** A-025 refuses a second editor and C-017 expires or releases the lock, but nothing in the batch covers telling the refused user that a lock exists or who holds it. This is the weakest of the six — it is arguably a detail of A-025 rather than absent work — and I name it as such rather than assert it.

---

## 7. Instrument readings

**Engine: `Hotyn-D 2.0`.**

- **Elements received:** 31 sized-candidate elements (plus 8 aggregates listed for context and never sized: S-03, S-03.1, S-03.2, S-03.3, S-03.4, S-03.5, S-03.7, S-03.8).
- **Elements sized:** 30
- **Size-class distribution:** S 9 · M 18 · L 3 · XL 0
- **Statement kinds:** compliance 0 · behavioural 0 (the batch's only `statement` element, C-016, is unsizeable, so no kind was assigned)
- **Unsizeable:** 1 (C-016)
- **Doubts:** 24 (19 enumeration doubts D-0…D-19, 5 unresolved obligation contradictions X-1…X-5)
- **Closure violations:** 6

Special counts issued: 4 (G2m/G3m/G4m pre-load enumerations on A-080, A-084, A-097, A-099 — distribution S 2 · M 2). No A9 special count was due in this batch. No position-derived size (per-parent, once-bracket, per-environment) is reported; that is tree arithmetic computed outside this engine. No D4 count is reported. Every number above is a count of named things.