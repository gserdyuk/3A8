<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-08. Not part of the sensor's reply.
run: 47 · reading: HD47-D1 · engine (sensor's own stamp): Hotyn-D 2.0 · model (launch record): the harness's `opus`
alias, i.e. Claude Opus 5 · batch D of the work model HW45 (prompt generated by make_sizing_prompts.py), repeat 1
· tool_uses: 0 · duration 188 s · received complete; transcribed verbatim by the orchestrator from the reply as
delivered (the harness transcript file was empty). -->

# Size classification — batch `S-07`, `S-08` of `HM44-OA1`

## 1. Contamination check

**Clean — proceed.**

The pasted input (INPUT 1, INPUT 2) contains no effort figure, no person-day value, no rate, no price, no budget, no duration, no team size, no prior estimate and no rate-table row. Every quantity in it is a count of named things or an id.

Ambient repository material *was* present in my context, prepended by the harness: a git status line, a branch name, a list of recent commit subjects (one of which carries a measurement-looking token), a memory index and directory names. **It was quarantined**: not read as input, not used in any enumeration, not treated as a stop condition. Classification below rests on the pasted input alone. No files were read.

---

## 2. Element sizing table

### Subtree S-07 — Publish and subscribe (aggregates S-07, S-07.1, S-07.2, S-07.3 not sized)

| id | name | class (kind) | enumeration | count | size |
|---|---|---|---|---|---|
| A-060 | Publish-and-subscribe mechanism (publications, subscriptions, grants) | behaviour | 4 actions: publish (select a record for sharing by the data owner); subscribe (request viewing by a data consumer); record a publishing permission (grant); view a published record within the Data Access module. "Multiple share/view functions" names no members and counts nothing (P-5/P-6). Name tokens *publications*, *subscriptions*, *grants* are already covered by the counted obligations and are not counted again (P-2). | 4 | **L** |
| A-061 | Publication at any hierarchy level | behaviour | 1 action: publish a record at a level in the hierarchy. "At any level" is a scope qualifier on the same action, not a second action. | 1 | **S** |
| A-062 | Basic and full attribute-set profiles | behaviour | 7 actions: publish the basic attribute set; publish the full attribute set; request access to a basic record; request access to a full record; view a basic record; view a full record; view a record hierarchy. "Basic or full" are two named outcomes, counted separately per P-3. | 7 | **XL** |
| A-063 | Bulk publication for a set of records | behaviour | 1 action: publish data for a group of records. | 1 | **S** |
| A-069 | Owner decision on access requests | behaviour | 3 actions: approve a view request; reject a view request (P-3 — approve/reject are two transitions); make the data available once approved. | 3 | **M** |
| C-019 | Publication revocation | behaviour | 1 action: revoke a publication. Derived element with empty own coverage; the name token *revocation* is covered by no obligation and is therefore counted (P-2). | 1 | **S** |
| A-011 | Subscription entitlement model (view scope, export options) | store | 4 entity kinds: client subscription; view-scope entitlement (which data the subscription permits viewing); export-option entitlement (which export options the subscription permits); access request pending owner approval. "Any format detailed in the General Requirements" names no format and counts nothing (P-5). | 4 | **L** |
| A-067 | Consumer view of published data, gated by entitlement | surface | 2 user tasks: view published data permitted by the consumer's subscription; take up the export options the subscription makes available. | 2 | **M** |
| A-068 | Access request from consumer to data owner | behaviour | 3 actions: request access to a basic record; request access to a full record (P-3); send the request to the data owner. "Subscribe to the record" is named by D-2 as the same act as requesting access and is not counted again (P-2). | 3 | **M** |
| A-064 | Share audiences: individual, user-defined, controlled, public | behaviour | 4 actions: share a record with an individual; share with a user-defined group; share with an X-Customer-controlled group; share with the public. | 4 | **L** |
| A-065 | User-defined group management | behaviour | — see §4, unsizeable (M10) | — | — |
| A-066 | X-Customer-controlled group management | behaviour | 2 actions: share records with a controlled group managed by X-Customer; handle a request to be added to a controlled group. | 2 | **M** |
| A-126 | Controlled-group membership request, approved by X-Customer | behaviour | 2 actions: request to be added to a controlled group; approve the membership request (name token *approved by X-Customer*, covered by no obligation — counted per P-2). | 2 | **M** |
| C-020 | Group membership lifecycle | behaviour | — see §4, unsizeable (M10) | — | — |

### Subtree S-08 — Access Data module (aggregates S-08, S-08.1 not sized)

| id | name | class (kind) | enumeration | count | size |
|---|---|---|---|---|---|
| A-010 | Read-only module boundary (no editing functionality) | statement (**behavioural**) | 1 constrained component: the Access Data module ("contains no editing functionality"). No second system or component is named by the covered obligation. | 1 | **S** |
| A-123 | Search over prefix, GIN and LN via the read store | behaviour | 3 actions: search prefix records; search GIN records; search LN records. "Via the read store" names a component, not an action. | 3 | **M** |
| A-124 | Field-level search inputs | surface | 1 user task: run a field-level search over the named search fields (name, city). "Several fields" states a cardinality without naming its members (P-6) and "e.g." is an open catch-all (P-5); neither adds a counted task. | 1 | **S** |
| A-125 | Advanced search and filter builder | surface | 2 user tasks: run an advanced search; build and apply filters. "Options" names no individual option and counts nothing (P-5). | 2 | **M** |
| A-127 | Published record detail view including hierarchy | surface | 3 user tasks: view a basic record in detail; view a full record in detail; view the record's hierarchy. | 3 | **M** |
| A-128 | Record print rendering (one or more selected) | surface | 2 user tasks: print a single selected record; print a set of selected records. | 2 | **M** |
| A-012 | Anonymous public access profile (limited, no export) | behaviour | 2 actions: let a non-member search the limited public data set; deny export to non-members. | 2 | **M** |
| A-130 | Informational page on paying for ad hoc access | surface | 1 user task: read the information on how to pay for ad hoc access to data. No payment act is named by the obligation. | 1 | **S** |

**Coarseness finding (M10) — A-062.** Classed XL *and* reported as a probable coarseness defect of the product model: this one element bundles the publish side (two attribute profiles), the request side and the view side, across both profiles plus the hierarchy view. XL classification and this finding do not substitute for one another.

---

## 3. Special counts

**A-011 — Subscription entitlement model · G2m / G3m / G4m (store loaded from predecessor applications).**

Entity kinds that must arrive from the predecessor applications:

1. **Client subscription records** — a consumer's viewing and export scope is determined by their subscription (I-9, G-13.5); an existing client cannot view any published data on day one unless their subscription is already present, so it cannot be created at run time.
2. **View-scope and export-option entitlement definitions per subscription** — the mapping from a subscription to what it permits is the rule the gate applies (I-9); it must exist before the first consumer view, and nothing in the covered obligations creates it at run time.

**Count: 2 → M.**

Not loaded: **access requests pending owner approval** — these arise at run time from D-2/D-2.1 and have no predecessor origin named.

**A9 (performance and availability targets).** No element in this batch carries A9. The only `statement` element, A-010, is crossed with D4 and K3 only, so no measurable-target enumeration is demanded here.

---

## 4. Unsizeable elements

| id | name | class | what is missing |
|---|---|---|---|
| A-065 | User-defined group management | behaviour | Its single covered obligation, G-13.4, names "a user-defined group" only as a *share audience* — it names no group-management operation. The word "management" in the declared content is an unnamed catch-all (P-5): create, rename, add member, remove member and delete are none of them named by the content or by any covered obligation. **Unsizeable — model defect (M10).** No class assigned. |
| C-020 | Group membership lifecycle | behaviour | Derived element with empty own coverage; the declared content is the name alone, and "lifecycle" names no stage and no transition (P-5, P-6). Unlike C-019, whose name carries one specific verb (*revoke*), this name carries no verb on an object at all. **Unsizeable — model defect (M10).** No class assigned. |

---

## 5. Doubts

1. **A-060** — "record a publishing permission (grant)" could honestly be read as an aspect of *publish* rather than a distinct action; on that reading the count is 3 and the class M. The arguable item is **the grant/permission record**.
2. **A-060 / A-011** — G-13's "multiple share/view functions" states that there are several without naming them; I counted nothing for it (P-5/P-6), but it signals the element's true extent may be undeclared.
3. **A-062** — the arguable item is **"view a record hierarchy"**: if D-4's "and their hierarchies" is read as a qualifier on the view action rather than a distinct action, the count is 6 and the class L, not XL. A second, larger doubt: if "basic or full" is read as one parameter rather than two named outcomes (against P-3), the count falls to 4 and the class to L.
4. **A-069** — the arguable item is **"make the data available once approved"**: readable as a consequence of approval rather than a separate action, giving 2. The class stays M either way.
5. **C-019** — the entire enumeration rests on one uncovered name token; the element has empty own coverage, so S is the thinnest defensible sizing rather than a well-supported one. It is sized (P-2 permits the token) where C-020 is not (its token names no verb).
6. **A-011** — the arguable items are **view-scope entitlement** and **export-option entitlement**: readable as two attributes of the single subscription record rather than two entity kinds, giving 2 and class M. The same doubt propagates to the G2m/G3m/G4m special count, which would fall to 1 (S).
7. **A-067** — the arguable item is **"take up the export options"**: the export act itself is named in A-011's coverage (D-5), not here, so this surface's export task may be 0, giving 1 and class S.
8. **A-123 and A-127** — both cover I-8, whose text names *search, view and export* together. I enumerated each element within its declared content (A-123 = search, A-127 = detail view), because the declared content bounds the element. Read strictly off I-8's text instead, A-123 gains "view data" and "export data" (5 → L) and A-127 gains "export from the detail view" (4 → L). The arguable items are **view and export on A-123** and **export on A-127**.
9. **A-124** — the arguable items are the two example fields, **name and city**: read as two distinct tasks the count is 2 and the class M. Separately, "several fields" is a cardinality without named members (P-6) and the surface's actual field set is undeclared; I did not treat this as fatal because one task is clearly named.
10. **A-128** — the arguable item is **"print a set of selected records"**: "one or more records" may be one task with a cardinality qualifier, giving 1 and class S.
11. **A-012** — the arguable item is **"restrict the data set exposed to non-members"** as a third action separate from the anonymous search; the count would be 3, class unchanged at M.
12. **A-066** — sized on its obligations, but the declared "management" concern (creating the controlled group, defining its membership) is named by no obligation; only the share action and the join request are countable. The element is countable but its declaration is thinner than its name.
13. **A-126** — only **approval** is named; rejection of a controlled-group membership request is named nowhere, so it is not counted (P-5). Contrast A-069, where G-13.7 names both outcomes.
14. **Contradiction, named and not resolved** — I-8 declares the Access Data module "contains no editing functionality", while A-069 (approve/reject view requests) and A-126 (membership requests approved by X-Customer) place state-changing decisions on records viewed within that module (P-17/L-12: "published records are viewed within the Data Access module"). Whether an approval decision counts as "editing functionality" is unresolved by the obligations. I proceeded on the stated reading: the boundary constrains record editing only, so A-010 counts one constrained component.
15. **A-010 kind** — assigned `behavioural` under P-4 (run-time wins), because I-8 entails run-time behaviour: *"allows users to search for, view and export prefix, GIN and LN data"*. Read on its property alone ("contains no editing functionality") the statement would look like a policy and hence `compliance`; P-4 forbids that reading when any covered obligation entails run-time behaviour.

---

## 6. Closure violations

Work I judge necessary that this batch's model does not contain. Named, not sized, not priced. Where the missing work might legitimately sit in a subtree outside this batch, I say so.

1. **Notifying the requester of the owner's decision.** A-068 sends the access request, A-069 records approve/reject, and D-2.1 makes data availability conditional on approval — but no element in either subtree covers the decision reaching the consumer, nor the consumer discovering that a request was rejected. Would cover: outcome delivery to the requesting consumer for both G-13.7 outcomes and for A-126's controlled-group membership decision.
2. **Performing the export.** D-5 ("Export one or more records to any format detailed in the General Requirements") and I-9's "export options" are covered only by the entitlement *store* A-011, and I-8 names export as an Access Data capability — yet no behaviour or surface element in S-07 or S-08 performs an export or renders format selection. A-128 covers print, not export. Would cover: the export operation and format selection for an entitled consumer. Caveat: an export element may exist in a subtree outside this batch; I can see only S-07 and S-08.
3. **The ad hoc access payment itself.** A-130 covers only the information page required by D-8 ("information on how to pay"). No element in this batch covers the payment transaction, its entitlement grant, or its expiry — yet I-10 and I-9 make entitlement the gate on viewing and export. Would cover: taking payment for ad hoc access and turning it into a viewing entitlement. Caveat: may sit in a billing subtree outside this batch.
4. **Withdrawing an individual granted access.** C-019 covers revocation of a *publication* (owner-side, all consumers). Nothing covers withdrawing one previously approved consumer's access while the publication stands, which the approve/reject model of G-13.7 and D-2.1 implies. Would cover: revocation of a single granted view entitlement.

---

## 7. Instrument readings

**Engine: `Hotyn-D 2.0`.**

- **Elements received:** 28 — of which 6 aggregates (S-07, S-07.1, S-07.2, S-07.3, S-08, S-08.1), never sized; 22 sizing candidates.
- **Elements sized:** 20.
- **Size-class distribution:** S 6 · M 10 · L 3 · XL 1.
- **Statement kinds:** compliance 0 · behavioural 1.
- **Unsizeable (M10):** 2.
- **Doubts:** 15.
- **Closure violations:** 4.
- **Special counts produced:** 1 (A-011, G2m/G3m/G4m, count 2 → M). A9 demanded on 0 elements.
- **Coarseness findings (M10, XL):** 1 (A-062).

No effort, duration, rate, cost or team figure appears anywhere above; every number in this deliverable is a count of named things or an element identifier. Position-derived sizes (per-parent, once-bracket, per-environment) and the D4 driver are tree arithmetic and are deliberately absent.
