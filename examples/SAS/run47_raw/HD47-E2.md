<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-08. Not part of the sensor's reply.
run: 47 · reading: HD47-E2 · engine (sensor's own stamp): Hotyn-D 2.0 · model (launch record): the harness's `opus`
alias, i.e. Claude Opus 5 · batch E of the work model HW45 (prompt generated by make_sizing_prompts.py), repeat 2
· tool_uses: 0 · duration 162 s · received complete; transcribed verbatim by the orchestrator from the reply as
delivered (the harness transcript file was empty). -->

# Size classification — batch `HM44-OA1` subtrees S-10, S-11

## 1. Contamination check

**Clean — proceed.**

- The pasted input contains **no effort figure, person-day value, rate, price, budget, duration, team size, prior estimate, or rate-table row**. Checked specifically: `NFR-17` / `NFR-18` say "near-real-time", which is a qualitative service property inside an obligation text, not a numeric duration; `G-7.2`'s "hour, day, week, month and year" are reporting granularities (scope tokens), not work durations; "3scale" is a vendor product name. None of these were read as effort.
- **Ambient repository information was present** in my context (harness-prepended git status, branch name, recent commit subjects, directory names). It was **quarantined**: not read as input, not acted on, not treated as run-stopping contamination. All classification below rests on the pasted input alone.
- **No files were read.**

---

## 2. Element sizing table

No `statement` elements occur in this batch, so no kind column applies.

### Subtree S-10 — Notifications and communications

#### S-10.1 — Notification engine

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A-036 | Notification composition, routing and dispatch | behaviour | 7 actions: compose a notification; route a notification to its recipients; dispatch/provide a notification to users in the available formats (G-8 — the name token "dispatch" is not counted again, P-2); notify Editors and Approvers of a change in approval status (G-3.2); notify the user when a record's status change affects the status of other records in the hierarchy (P-11.5 / L-10.3, identical texts, counted once); notify on system errors occurring (NFR-17); notify on failure of critical business functions (NFR-18) | 7 | **XL** |
| A-040 | Global broadcast notifications | behaviour | 1 action: broadcast a global notification to all users (G-8.1; "scheduled server downtime" is an "e.g." illustration, not a further named action) | 1 | **S** |
| A-041 | Opt-in record-event notifications | behaviour | 3 actions: record a member's opt-in choice for record events (G-8.2 "a user opts"); notify the opted-in member when a record is updated; notify the opted-in member when a record is added (P-3 — "updated or added" names two outcomes) | 3 | **M** |
| A-042 | Member-to-member and member-to-X-Customer messages | behaviour | 3 actions: send a member-to-member message; send a member-to-X-Customer message; raise a challenge against record data (G-8.3) | 3 | **M** |
| C-001 | Notification template catalogue | store | 1 entity kind: notification template (the element declares a catalogue of one named kind; no channel-specific or event-specific template kinds are named, P-5) | 1 | **S** |

#### S-10.2 — Delivery channels

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A-037 | Email delivery via the client SMTP relay | interface | 2: submit a message to the client SMTP relay (the consumed operation); the email delivery channel / SMTP protocol concern, counted once as a single named protocol concern (P-1) | 2 | **M** |
| A-038 | SMS delivery via external gateway | interface | 2: submit a message to the external SMS gateway (the consumed operation); the SMS delivery channel (P-1). The gateway's authentication is not named, so it counts nothing (P-5) | 2 | **M** |
| A-039 | Onscreen notification centre | surface | 1 user task: view one's onscreen notifications (G-8 names "onscreen" as a format; no further task — marking read, dismissing, filtering — is named) | 1 | **S** |
| C-002 | Delivery retry and failure handling | behaviour | 2 actions: retry a failed delivery; handle a delivery failure that is not retried | 2 | **M** |

#### S-10.3 — Communication preferences

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A-043 | Per-user channel and frequency preferences | store | 2 entity kinds: per-user notification method (channel) preference; per-user notification frequency preference (G-8.5 names "the method and frequency" as two settable things) | 2 | **M** |
| C-003 | Notification digest batching | behaviour | 1 action: batch pending notifications into a digest | 1 | **S** |

### Subtree S-11 — Reporting and audit

#### S-11.1 — Report definitions

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A-029 | Report generation and viewing | surface | 4 user tasks: customise a report; schedule a report; run/generate a report (G-7 "generate" and G-7.1 "run" treated as one task, and the name token "generation" is therefore not counted again, P-2); view a report | 4 | **L** |
| A-030 | Add / change / delete report definitions | behaviour | 3 actions: add a report definition; change a report definition; delete a report definition (P-3 — slash-separated outcomes are distinct actions) | 3 | **M** |
| A-031 | Report customisation | behaviour | 1 action: customise a report definition (G-7.1 "Customise") | 1 | **S** |

#### S-11.2 — Scheduling and history

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A-032 | Report scheduling | behaviour | 1 action: schedule a report run (G-7.1 "schedule") | 1 | **S** |
| A-034 | Report run history | store | 1 entity kind: report run history entry (G-7.3 "Run report data history") | 1 | **S** |

#### S-11.3 — Audit trail and audit reports

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A-035 | Audit report definitions over the trail | surface | 2 user tasks: define an audit report over the trail (the name token "definitions", covered by no obligation, P-2); run an audit report (G-7.4) | 2 | **M** |
| A-159 | System-wide audit trail of all activity | store | 1 entity kind: audit-trail activity entry (NFR-20's "all activity taking place at any point in the system" is an unnamed catch-all and names no further kind, P-5) | 1 | **S** |
| C-023 | Audit trail retention and archiving | behaviour | 2 actions: apply a retention rule to audit-trail entries; archive audit-trail entries | 2 | **M** |

#### Directly under S-11

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A-033 | Usage reporting from the 3scale analytics API | interface | 2: retrieve usage metrics from the 3scale analytics API (the consumed operation); the 3scale analytics API as a named protocol concern (counts as one). The five named periods are read as parameters of the one retrieval, not as separate operations — see Doubts | 2 | **M** |

---

## 3. Special counts

**A9** (measurable targets of a behavioural statement) — **not applicable**: this batch contains no `statement` element and no element carries A9.

**G2m / G3m / G4m** (entity kinds pre-loaded from the predecessor applications) — demanded on the three stores that carry those activities: C-001, A-043, A-159.

| element | enumeration of kinds loaded from the predecessor, with the reason per kind | count | special-count class |
|---|---|---|---|
| **C-001** — Notification template catalogue | 1 kind: **notification template** — no notification of any class (approval-status, hierarchy propagation, global broadcast, opt-in record event, member message, system-error) can be composed until a template for it exists; the dispatch path consumes templates and never creates them, so the catalogue must be populated before the system is usable, and the wording of the predecessor's existing member-facing notices is the only source for it | 1 | **S** |
| **A-043** — Per-user channel and frequency preferences | 1 kind: **per-user notification method (channel) preference** — an existing member's chosen contact method is held only in the predecessor application; the new system cannot derive it at run time and, if it starts empty, would notify every migrated member on a system default channel the member never chose. The **frequency preference** is *not* counted: G-8.5 has the user set it, and a system default is a legitimate starting state — this is arguable, see Doubts | 1 | **S** |
| **A-159** — System-wide audit trail of all activity | 1 kind: **audit-trail activity entry** — activity performed in the predecessor applications has already happened and cannot be regenerated by the new system at run time; NFR-20 asserts a trail "of all activity", and audit reports (A-035, G-7.4) over any pre-cutover period return nothing unless the predecessor's entries arrive | 1 | **S** |

---

## 4. Unsizeable elements

**None.** All 20 sized elements had a declaration (name plus own coverage, or name alone for the derived C-elements) sufficient to name the counted things. No element in this batch states a cardinality without naming its members (P-6).

**Related finding, not an unsizeable:** A-036 is classed **XL** and is *therefore also* reported as a probable **coarseness finding about the product model (M10)** — one element carries composition, routing, dispatch and four distinct notification triggers spanning member-facing approval/hierarchy events and operational error/failure events. The class stands; the finding stands alongside it. Neither substitutes for the other, and I have not reshaped the element.

---

## 5. Doubts

Enumerations that could honestly be read one item longer or shorter, with the arguable item named. Class-changing doubts are marked **(class)**.

1. **A-036** — P-11.5 and L-10.3 carry *identical* obligation text under two different obligation prefixes; I counted the action once. Read as two distinct hierarchies, the count is 8 (still XL).
2. **A-036** — NFR-17 (system errors) and NFR-18 (critical business function failures) could be read as one action, "near-real-time notification of operational events", giving 6. **(class: XL → L)**
3. **A-041** — "records are updated or added" counted as two actions under P-3; read as one record-event notification, the count is 2 (still M).
4. **A-042** — "a user can challenge record data" is introduced by "e.g."; read as an illustration of the two message directions rather than a distinct action, the count is 2 (still M).
5. **C-001** — channel-specific template variants (email / SMS / onscreen) are not named in the declaration, so they count nothing (P-5); if the catalogue is intended to hold one template kind per channel, the count is 3 (still M... in fact S → M). **(class: S → M)**
6. **A-037** — I counted the email channel and the SMTP protocol concern as one named thing. Read as two (channel plus protocol), the count is 3 (still M).
7. **A-038** — the "external gateway" names no authentication or protocol; if an auth concern is intended, the count is 3 (still M).
8. **A-039** — a "notification centre" arguably serves both a list task and an individual-notification task; neither is separately named, so I declared 1.
9. **C-002** — "retry" and "failure handling" may name one action, retry *being* the handling, giving 1. **(class: M → S)**
10. **A-043** — G-8.5's "method and frequency" may describe two fields of a single "communication preference" entity kind rather than two kinds, giving 1. **(class: M → S)**
11. **A-043, special count** — the arguable item is the **frequency preference**: excluded because it is defaultable, giving 1. Read as equally member-specific it gives 2; read strictly ("a user *can set*" — everything is run-time settable) the honest answer is that nothing must be pre-loaded, which the pinned S..XL band cannot express.
12. **C-003** — dispatching the assembled digest on the member's chosen frequency could count as a second action alongside batching, giving 2. **(class: S → M)**
13. **A-029** — "generate" (G-7) and "run" (G-7.1) counted as one task; read as distinct, the count is 5 (still L).
14. **A-030** — G-7.1's "reports for adds, changes and deletes" most naturally names the *reported subject matter* (record adds/changes/deletes), not CRUD on report definitions; the element's own name nevertheless names the three actions on definitions and I counted from it under P-2. The two readings are not reconciled here.
15. **A-034** — G-7.3 "Run report data history" is ambiguous; the arguable item is a second kind, the **retained report output data** distinct from the run record, giving 2. **(class: S → M)**
16. **A-159** — "all activity taking place at any point in the system" could be read as demanding a distinct entry kind per activity source; no source is named, so under P-5 it counts nothing beyond the single entry kind.
17. **A-159, special count** — whether historical audit entries are intended to be migrated at all is not stated anywhere in the declaration; the G2m/G3m/G4m activities crossed onto the element are my only evidence that something is loaded.
18. **A-033** — the arguable items are the **five named periods** (hour, day, week, month, year). Declared reading: parameters of one retrieval, count 2. Read as five distinct usage-report retrievals plus the protocol concern, the count is 6. **(class: M → L)**
19. **C-023** — purging entries once retention expires is not separately named; read as distinct from archiving, the count is 3 (still M).
20. **Redundant coverage of G-7.1, not resolved.** The single obligation G-7.1 ("Customise, schedule, run and view reports for adds, changes and deletes") is covered by four elements — A-029, A-030, A-031, A-032 — and each enumeration draws on it. A-029's four tasks therefore restate, at surface level, what A-030/A-031/A-032 name at behaviour level. I may not merge or reshape; the overlap is named and left standing.
21. **Redundant coverage of G-8, not resolved.** The single obligation G-8 ("email, SMS and/or onscreen") is covered by A-036, A-037, A-038 and A-039 simultaneously; the same three formats are the counted channels in three of them and the dispatch action in the fourth. Named, not resolved.

**Doubts count: 21.**

---

## 6. Closure violations

Work I judge necessary that the model does not contain. Named only — no size class, no price.

1. **No element captures activity into the audit trail.** A-159 is the *store* of the trail and C-023 is its *retention*, but no behaviour element in this batch names the act of recording an activity event into the trail as it happens. NFR-20 asserts the trail exists "at any point in the system"; the write path across every other subtree has no home here. Would cover: emitting and persisting an audit entry for each activity, system-wide.
2. **No element detects the events that NFR-17 and NFR-18 notify about.** A-036 dispatches notifications on system errors and on critical business function failures, but nothing in the batch names the detection of a system error or the determination that a critical business function has failed. Dispatch has no source. Would cover: identifying error and business-function-failure events and raising them to the notification engine.
3. **No element delivers the output of a scheduled report.** A-032 schedules a report run and A-034 records the run history, but the only way to obtain a report's content is A-029's interactive "view". A run that fires on a schedule with no one watching has nowhere to put its output. Would cover: routing a scheduled run's output to its intended recipients (which would in turn lean on the S-10.2 delivery channels).

**Closure violations count: 3.**

---

## 7. Instrument readings

**Engine: `Hotyn-D 2.0`.**

- **Elements received:** 28 (20 sized elements + 8 aggregates listed for context: S-10, S-10.1, S-10.2, S-10.3, S-11, S-11.1, S-11.2, S-11.3 — aggregates are never sized).
- **Elements sized:** 20.
- **Size-class distribution:** S = 8 · M = 10 · L = 1 · XL = 1.
  - S: A-040, C-001, A-039, C-003, A-031, A-032, A-034, A-159
  - M: A-041, A-042, A-037, A-038, C-002, A-043, A-030, A-035, C-023, A-033
  - L: A-029
  - XL: A-036 (also an M10 coarseness finding)
- **Statement kinds:** compliance = 0 · behavioural = 0 (no `statement` element in this batch).
- **Special counts produced:** 3 (G2m/G3m/G4m on C-001, A-043, A-159; A9 not applicable).
- **Unsizeable count:** 0.
- **Doubts count:** 21.
- **Closure violations count:** 3.

Every number above is a count of named things. No position-derived class (per-parent, once-bracket, per-environment) and no D4 driver value is reported — those are tree arithmetic computed outside this instrument.

**File paths:** none. No repository file was read; the entire deliverable derives from the pasted input.
