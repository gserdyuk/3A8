<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-08. Not part of the sensor's reply.
run: 47 · reading: HD47-G2 · engine (sensor's own stamp): Hotyn-D 2.0 · model (launch record): the harness's `opus`
alias, i.e. Claude Opus 5 · batch G of the work model HW45 (prompt generated by make_sizing_prompts.py), repeat 2
· tool_uses: 0 · duration 211 s · received complete; transcribed verbatim by the orchestrator from the reply as
delivered (the harness transcript file was empty). -->

# Element sizing — batch `HM44-OA1` subtrees S-09, S-14 · engine `Hotyn-D 2.0`

## 1. Contamination check

**Clean — proceed.**

The pasted input (INPUT 1 work model, INPUT 2 sizing rules) contains no effort figure, no person-day value, no rate, no price, no budget, no duration, no team size, no prior estimate and no rate-table row. The only numerals in INPUT 2 are threshold boundaries for counts (1 / 2–3 / 4–6 / ≥7 etc.) and precedent identifiers; these are counting boundaries, not prices.

**Quarantine notice.** Ambient repository information was present in my context, prepended by the harness — a git status block, a branch name, recent commit subjects, and a working-directory path. None of it is input to this run. I did not read it as model content, did not act on it, and did not treat it as contamination that stops the run. It is quarantined; the classification below rests on the pasted input alone. No files were read.

---

## 2. Element sizing table

### Subtree S-09 — Import and export (aggregate S-09, S-09.1, S-09.2, S-09.3 not sized)

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-059 | Record selection for import and export | behaviour | "3 actions: select an individual record to act on; select multiple records; apply one action to all selected records." | 3 | **M** |
| A-051 | Excel adapter | behaviour | "2 actions: import records from an Excel file; export records to an Excel file." | 2 | **M** |
| A-052 | CSV adapter | behaviour | "2 actions: import records from a CSV file; export records to a CSV file." | 2 | **M** |
| A-053 | Numbers adapter | behaviour | "2 actions: import records from a Numbers file; export records to a Numbers file." | 2 | **M** |
| A-054 | XML adapter | behaviour | "2 actions: import records from an XML file; export records to an XML file." | 2 | **M** |
| A-055 | Tab-delimited adapter | behaviour | "2 actions: import records from a tab-delimited file; export records to a tab-delimited file." | 2 | **M** |
| A-056 | IDoc adapter | interface | "3 operations/concerns: consume an inbound iDoc; emit an outbound iDoc; the iDoc format itself as the named exchange concern with an external system (e.g. SAP)." | 3 | **M** |
| A-057 | Import and export through the member API | interface | "6 operations/concerns: expose an import operation to members; expose an export operation to members; REST binding (named, preferred); SOAP binding (named as the alternative); compatibility with the named API management solution (3scale) as the management/auth concern; ingest of data created and managed in an external system (QuickBooks, SAP)." | 6 | **L** |
| A-058 | PC/Mac encoding and line-ending variants | behaviour | "4 actions: import a PC-format file (PC encoding and line endings); import a Mac-format file (Mac encoding and line endings); export in PC format; export in Mac format." | 4 | **L** |
| A-049 | Record import service | behaviour | "7 actions: create GIN records via import; create LN records via import; edit GIN record attributes via import; edit LN record attributes via import; import GIN hierarchy information (each → case → pallet); import LN hierarchy information; ingest records created and managed in an external system." | 7 | **XL** |
| A-102 | Hierarchy import modes | behaviour | "2 actions: import full records carrying hierarchy information; import hierarchy information for records that already exist." | 2 | **M** |
| C-007 | Import validation result report | surface | "1 task: review the validation outcome of an import run." | 1 | **S** |
| C-008 | Import staging and preview | surface | "2 tasks: inspect staged import data before it is committed; preview the outcome the import would produce." | 2 | **M** |
| A-050 | Record export service | behaviour | "3 actions: export prefix data; export GIN data; export LN data." | 3 | **M** |
| C-009 | Asynchronous export job handling | behaviour | "2 actions: run an export as a background job off the request path; hand the finished export back to the requester on job completion." | 2 | **M** |
| C-025 | Export artefact retention and cleanup | behaviour | "2 actions: retain a produced export artefact; remove an export artefact once retention lapses." | 2 | **M** |

**XL signal (W10).** A-049 is classed **XL** *and* reported as a probable coarseness finding about the product model — see section 4/M10 note.

### Subtree S-14 — Configuration and reference data (aggregate S-14, S-14.1, S-14.2 not sized)

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-074 | Industry attribute-set configuration | store | "4 entity kinds: industry; attribute definition (with its required / non-required marking); GIN attribute set per industry; LN attribute set per industry." | 4 | **L** |
| A-098 | Reuse-rule configuration by industry and product type | store | "3 entity kinds: GIN reuse rule (varying by industry and product type); LN reuse rule (varying by industry); product type." | 3 | **M** |
| A-117 | Industry LN-pool configuration | store | "2 entity kinds: shared LN pool; the industry-to-pool rule marking which industries assign LNs automatically from that pool." | 2 | **M** |
| C-018 | Configuration administration console | surface | "1 task: administer the configuration and reference data of the industry configuration area." | 1 | **S** |
| A-013 | X-Customer reference data in the read store under the same rules | store | "1 entity kind: X-Customer-owned reference data held in the read store." | 1 | **S** |
| A-027 | Prefix capacity computation (used vs available) | behaviour | "3 actions: count the numeric indicators used against a prefix; count the numeric indicators still available on that prefix; display the prefix capacity counter." | 3 | **M** |
| C-015 | Identifier allocation ledger | store | "1 entity kind: identifier allocation entry (an issued identifier bound to its allocation)." | 1 | **S** |

No `statement` element occurs in this batch, so no kind assignment was required and no A9 special count applies.

---

## 3. Special counts

**A9** — not triggered: A9 is crossed onto no element in this batch.

**G2m / G3m / G4m** — crossed onto five stores: A-074, A-098, A-117 (S-14.1) and A-013, C-015 (S-14.2). Per kind, why it must arrive from the predecessor rather than be created at run time:

**A-074 — Industry attribute-set configuration** · "4 kinds: **industry** — records are created against an industry from the first day, so the industry list cannot be authored while records are already being created; **attribute definition** — the required / non-required marking governs validation of the very first record entered, so it must pre-exist any run-time entry; **GIN attribute set** — GIN records already issued in the predecessor carry industry-varying attributes that must resolve against a set already present; **LN attribute set** — same, for already-issued LNs." → **4 → L**

**A-098 — Reuse-rule configuration by industry and product type** · "3 kinds: **GIN reuse rule** — identifiers already taken out of use in the predecessor have a reuse date that only a pre-loaded rule can reproduce; **LN reuse rule** — same, for withdrawn LNs; **product type** — the reuse rule is keyed by product type, so the taxonomy must exist before any withdrawn identifier can be evaluated." → **3 → M**

**A-117 — Industry LN-pool configuration** · "2 kinds: **shared LN pool** — LNs already drawn from the pool in the predecessor must be known before the first automatic assignment, or an in-use number would be issued again; **industry-to-pool rule** — which industries assign automatically is established practice carried in, not a choice made at run time by the first user." → **2 → M**

**A-013 — X-Customer reference data in the read store** · "1 kind: **X-Customer-owned reference data** — the Access Data module returns X-Customer-owned data alongside published data, so on day one it can return nothing unless that reference content is already loaded; it is not authored by members at run time." → **1 → S**

**C-015 — Identifier allocation ledger** · "1 kind: **identifier allocation entry** — every identifier issued by the predecessor applications must be in the ledger before the new system allocates anything, otherwise it would allocate a number already in use." → **1 → S**

---

## 4. Unsizeable elements

None. All 23 sized elements admitted an enumeration from declared content plus own coverage.

**M10 coarseness finding (not an unsizeable finding).** A-049 *Record import service* is classed XL and is simultaneously reported as a probable coarseness defect of the product model: one leaf carries create, edit, hierarchy and external-system ingest across two record types (12 covered obligations). I do not split it — splitting would be reshaping the model, which is not mine.

Six derived elements (C-007, C-008, C-009, C-025, C-018, C-015) have empty own coverage; their enumerations rest on the declared name alone under P-2. Each was sizeable, but each is thin — see doubts 9–13, 18.

---

## 5. Doubts

1. **A-059** — D-5 ("Export one or more records to any format") sits in this element's own coverage and names an export action. I did not count it, reading A-059 as selection only; counted, the list is 4 and the class L.
2. **A-058** — the arguable item is the direction split: I read PC-format and Mac-format handling as distinct on import and on export (4). Read as two direction-level actions each covering both platforms, the list is 2 and the class M.
3. **A-049** — the arguable items are the LN twins of the GIN actions. I counted GIN and LN separately because P-1/L-1 and P-5/L-5 name the two record types explicitly (7, XL); collapsed to a single "record" object the list is 4 and the class L.
4. **A-049** — the two hierarchy-import actions are also the entire content of A-102. Excluded from A-049 as belonging to the sibling, the list is 5 and the class L. I counted them because P-11.2 and L-10.2 are in A-049's own coverage.
5. **A-102** — the arguable item is the record type: unlike P-1/L-1, the texts of P-11.2 and L-10.2 name no GIN or LN token, so I counted 2 modes; split by record type the list is 4 and the class L. This is the mirror image of doubt 3 and the two readings should be settled together.
6. **A-050** — I-8 names "search for, view and export"; I counted only the export actions, since search and view are Access Data surface tasks. Counted here, the list is 5 and the class L.
7. **A-056** — the arguable item is the external-system connection (P-19 / L-15) as a concern distinct from the iDoc format itself; counted, the list is 4, class unchanged (M).
8. **A-057** — the arguable item is the external-system ingest (P-19 / L-15) as distinct from the exposed import operation; dropped, the list is 5, class unchanged (L).
9. **C-007** — declaration is the name only. A second task (correcting and re-submitting rejected rows) is entailed but not named; unnamed, so not counted (P-5). Counted, the list is 2 and the class M.
10. **C-008** — "staging" and "preview" read as two tasks; read as one composite pre-commit task the list is 1 and the class S.
11. **C-009** — "handling" is the arguable token. Read as job-status tracking in addition, the list is 3 (class unchanged); read as a single composite action, the list is 1 and the class S.
12. **C-025** — retention is arguable as a stored policy rather than an action; dropped, the list is 1 and the class S.
13. **C-018** — declaration is the name only. Read as serving the three sibling configuration stores of S-14.1 as three tasks, the list is 3 and the class M; I did not import task names from siblings.
14. **A-074** — the arguable item is whether GIN attribute set and LN attribute set are one kind (an attribute set keyed by record type); merged, the list is 3 and the class M. The same doubt propagates to the G2m/G3m/G4m special count (4 → 3).
15. **A-098** — industry is referenced by both reuse rules but is enumerated as a kind of A-074; counted again here the list is 4 and the class L.
16. **A-117** — industry as a third kind (owned by A-074) would give 3; class unchanged (M).
17. **A-013** — I-11 names two data categories: X-Customer-owned data and data published by data owners. I counted only the first as this store's responsibility; counting the published category the list is 2 and the class M.
18. **A-027** — "(GINs or LNs)" is a parenthetical subtype of "numeric indicators", so I did not split used/available by record type; split, the list is 4 or 5 and the class L.
19. **C-015** — "identifier" is generic; split into GIN allocation and LN allocation entries the list is 2, class M, and the special count rises to 2.
20. **Duplicate coverage, named and not resolved** — P-11.2 and L-10.2 appear in the own coverage of both A-049 and A-102. Two elements claim the same obligation text; I have sized each on its own stated coverage and have not resolved which owns it.
21. **Open-ended adapter set, named and not resolved** — G-12.1 says formats include "**at least** Excel, CSV, Numbers, XML, tab-delimited, iDocs, and through the API". "At least" is an unnamed catch-all and counts nothing (P-5), but it leaves the adapter set of S-09.1 formally open; I proceeded on the six named adapters plus the API as modelled.

**Doubts: 21.**

---

## 6. Closure violations

1. **Correction and re-submission of records rejected by import validation.** C-007 reports the validation outcome and C-008 stages and previews, but no element in S-09.2 covers repairing rejected rows and re-running only those rows. It would cover: carry the rejected subset of an import run back to the submitter, accept corrections, and re-enter the corrected subset without re-importing the accepted remainder. Named, not sized.
2. **The prefix registry store itself.** S-14.2 is named "Prefix registry and X-Customer reference data", and A-027 computes used-vs-available capacity against a prefix, but no element in this batch is responsible for prefix entities (prefix, its owner, its allocated range). It would cover: holding prefixes, their owners and their capacity ranges, which A-027 and C-015 both read. Caveat: this may be modelled in a subtree outside this batch; I can only observe its absence here. Named, not sized.
3. **Retrieval point for a completed asynchronous export.** C-009 runs the export off the request path and C-025 retains and later removes the artefact, but no surface element in S-09.3 serves the user task of finding and collecting a finished export before retention lapses. It would cover: listing a member's completed export jobs and handing over the artefact. Named, not sized.

---

## 7. Instrument readings

**Engine: `Hotyn-D 2.0`.**

- **Elements received:** 30 (23 sized elements; 7 aggregates listed for context — S-09, S-09.1, S-09.2, S-09.3, S-14, S-14.1, S-14.2 — never sized)
- **Elements sized:** 23
- **Size-class distribution:** S 4 · M 15 · L 3 · XL 1
- **Statement kinds:** compliance 0 · behavioural 0 (no `statement` element in this batch)
- **Special counts produced:** 5 (G2m/G3m/G4m pre-load counts on A-074, A-098, A-117, A-013, C-015); A9 not triggered
- **Unsizeable count:** 0 (one M10 coarseness finding raised alongside the XL class on A-049)
- **Doubts count:** 21
- **Closure violations count:** 3

Every number above is a count of named things. No effort, duration, rate, cost or position-derived size appears in this reading; per-parent, once-bracket and per-environment sizing is tree arithmetic computed outside this instrument.
