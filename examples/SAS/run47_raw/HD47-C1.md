<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-08. Not part of the sensor's reply.
run: 47 · reading: HD47-C1 · engine (sensor's own stamp): Hotyn-D 2.0 · model (launch record): the harness's `opus`
alias, i.e. Claude Opus 5 · batch C of the work model HW45 (prompt generated by make_sizing_prompts.py), repeat 1
· tool_uses: 0 · duration 193 s · received complete; transcribed verbatim by the orchestrator from the reply as
delivered (the harness transcript file was empty). -->

# Element sizing — batch `HM44-OA1` subtrees S-04, S-05, S-06

## 1. Contamination check

**Clean — proceed.**

No effort figure, person-day value, rate, price, budget, duration, team size, prior estimate or rate-table row appears anywhere in INPUT 1 or INPUT 2. The activity tokens crossed onto elements (A2, A3, A4, A9, A10, D4, G2m, G3m, G4m, K1, K2) are activity identifiers, not quantities.

**Quarantine notice.** Ambient repository information was present in my context from the harness — a git status line, a branch name, recent commit subjects, and directory names. It was not part of my input. I did not read it, did not act on it, and did not treat it as contamination that stops the run. It is quarantined; everything below is derived from the pasted input alone. No file was read.

## 2. Element sizing table

No `statement` elements occur in this batch, so no kind column is populated. `D4` and all position-derived sizes are computed outside me and are not reported.

### Subtree S-04 — Workflow and validation

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-020 | Approval workflow engine for new and edited records | behaviour | 4 actions: submit a record for approval, review a submitted record, reject a record, approve a record. (P-3: the outcome list "submitted, reviewed, rejected or approved" counts by outcome. "New and edited" names two record origins, not two actions.) | 4 | **L** |
| A-021 | Approval rule configuration | behaviour | 4 actions: assign a role to a user, assign a task to a user, designate the Editor and Approver roles, define the rules for record approval. (Arguable: "designate Editor and Approver roles" may be the same action as "assign a role to a user".) | 4 | **L** |
| A-022 | Approval-status change notification trigger | behaviour | 2 actions: detect a change in approval status, notify the Editors and Approvers of that change. ("Trigger" in the name is covered by no obligation verb, so it is counted separately under P-2; "Editors and Approvers" are two audiences of one notify action, not two actions.) | 2 | **M** |
| A-023 | Action queue of records awaiting the user | surface | 3 user tasks: find the records requiring my action, see the tasks assigned to me, assign roles and tasks to users as company admin. (Arguable: the assignment tasks may belong to A-021 rather than to this surface.) | 3 | **M** |
| A-008 | Configurable standards and business-rule set | store | 2 entity kinds: X-Customer Standard definitions, business rules. ("All functionality is validated against X-Customer Standards and business rules" names exactly these two kinds.) | 2 | **M** |
| A-009 | Validation enforcement points (UI, import, assignment) | behaviour | 3 actions: validate a record entered via the UI, validate a record upon import, validate an identifier at assignment against the standards. (Arguable: "identifiers, auto-generated or manual" could split the assignment point into two actions.) | 3 | **M** |
| A-155 | Record verification workflow | behaviour | 2 actions: verify for accuracy the attributes entered via the UI, verify for accuracy the attributes entered via import. ("Workflow" names no stages in the content — P-5/P-6 — so no stage is counted.) | 2 | **M** |

### Subtree S-05 — Product (GIN) module

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-073 | GIN record type and attribute model | store | 3 entity kinds: the GIN record, the attribute definition (required and non-required), the industry-specific attribute variation set. ("Required and non-required" is one kind with a flag, not two kinds; "may vary by industry" names the variation set as its own kind.) | 3 | **M** |
| A-077 | GIN allocation from the licensed prefix range | behaviour | 3 actions: assign a GIN automatically during record creation, assign a GIN manually during record creation, assign the check digit automatically. ("Automatically or manually" are two named assignment actions; "at any point during record creation" is a timing qualifier, not an action.) | 3 | **M** |
| A-089 | GIN inventory view with filter and sort | surface | 3 user tasks: view all GINs in a single view, filter the GINs, sort the GINs. | 3 | **M** |
| A-094 | GIN status model (Reserved / In Use / For Reuse / Available) | behaviour | 5 actions: set a GIN to Reserved, set a GIN to In Use, set a GIN to For Reuse, set a GIN to Available, update the GIN status to reflect the record status. (P-3: the slash-separated status list counts by outcome. "The system or the user assigns" names two agents of one action, not two actions.) | 5 | **L** |
| A-101 | Pre-defined levels: each, case, pallet | store | 3 entity kinds: the "each" level definition, the "case" level definition, the "pallet" level definition. | 3 | **M** |
| A-103 | Physical fit check (height, weight) between adjacent levels | behaviour | 2 actions: check the height of an item type against the next item type in the hierarchy, check the weight of an item type against the next item type in the hierarchy. (Arguable: this may be one fit check evaluated over two dimensions.) | 2 | **M** |
| A-104 | Item-level change during hierarchy construction | behaviour | 1 action: change the item level on a record while creating a hierarchy. | 1 | **S** |
| A-107 | Barcode generation (UPC-A, EAN-13, ITF-14, GS1-128; sizes) | behaviour | 4 actions: generate a UPC-A barcode, generate an EAN-13 barcode, generate an ITF-14 barcode, generate a GS1-128 barcode. (The four type tokens are named in the element content and covered by no enumerated obligation term — P-2 counts them; "various types and sizes" and "; sizes" name no members — P-5 — and count nothing.) | 4 | **L** |
| A-108 | Barcode preview in the application | surface | 1 user task: view a generated barcode inside the application before export. | 1 | **S** |
| A-109 | Barcode export and print (PNG, SVG) | behaviour | 3 actions: export a barcode as PNG, export a barcode as SVG, print a barcode. (Arguable: "export" may be one action parameterised by format, giving two.) | 3 | **M** |
| A-085 | Product image upload and attachment | behaviour | 2 actions: upload a product image, add an uploaded image to a record. | 2 | **M** |
| A-110 | Product Information Sheet: create, save, export, print | surface | 4 user tasks: create a Product Information Sheet, save a Product Information Sheet, export a Product Information Sheet, print a Product Information Sheet. ("With all record attributes and images" names the sheet's content, not a further task.) | 4 | **L** |
| A-111 | Product-sheet layout template library with editing | surface | 2 user tasks: select a page-layout template from the several offered, edit a page-layout template. ("Library" is covered by "select from … multiple templates" — P-2 does not count it again.) | 2 | **M** |
| A-112 | Digital GIN embeddable snippet (QR plus structured markup) | interface | 4 operations/concerns: create the digital GIN for a record, render the QR component, emit the structured markup component, expose the snippet over the web-site embedding channel (P-1: "embedded in a web site" is a named delivery channel and counts as one). | 4 | **M** |

### Subtree S-06 — Location (LN) module

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-114 | LN record type and attribute model | store | 3 entity kinds: the LN record, the attribute definition (required and non-required), the industry-specific attribute variation set. | 3 | **M** |
| A-115 | LN allocation from the licensee's prefix range | behaviour | 3 actions: assign an LN automatically during record creation, assign an LN manually during record creation, assign the check digit automatically. | 3 | **M** |
| A-116 | Shared LN pool allocation | behaviour | 1 action: assign an LN automatically from the shared pool. (Arguable: determining that the industry uses a shared pool could be counted as a second action; return of an LN to the pool is named nowhere and is not counted.) | 1 | **S** |
| A-118 | LN inventory view with filter and sort | surface | 3 user tasks: view all LNs in a single view, filter the LNs, sort the LNs. | 3 | **M** |
| A-119 | LN status model | behaviour | 2 actions: assign a status to an LN (by the system or the user), update the LN status to reflect the record status. (No status values are named in the content or the obligation — P-5/P-6 — so no per-status transition is counted here.) | 2 | **M** |
| A-120 | Unlimited-depth LN hierarchy organised by attributes | store | 3 entity kinds: the LN hierarchy, the hierarchy level, the organising attribute. ("An unlimited number of levels" names no members — P-6 — so the level is one kind, not many; "various attributes" is an unnamed catch-all — P-5 — but "attributes" itself is the named organising kind.) | 3 | **M** |
| A-121 | Annual verification record (who, when, outcome) | store | 1 entity kind: the annual verification record. ("Who, when, outcome" are fields of that single kind, not separate kinds; the verifying user is owned by a store outside this batch.) | 1 | **S** |
| A-122 | Verification-due view | surface | 1 user task: see which records are due for annual verification. (Arguable: L-13's verb "record annual verification" may make capturing the verification a second task of this surface — see doubts.) | 1 | **S** |

## 3. Special counts

**A9** (performance and availability targets) is crossed onto no element in this batch, and there are no behavioural statements here — no A9 special count is owed.

**G2m / G3m / G4m** are crossed onto six elements, all stores. For each I enumerate the entity kinds that must be present before the system is usable, with the reason per kind.

| id | enumeration of pre-loaded entity kinds | count | driver class |
|---|---|---|---|
| A-008 | 2 kinds: **X-Customer Standard definitions** — no record can be validated on entry, import or identifier assignment until the standard that validation is measured against exists, so it cannot be created at run time by the users it constrains; **business rules** — I-7 makes all functionality conditional on the rule set, so the rules must exist before the first record is entered. | 2 | **M** |
| A-073 | 3 kinds: **GIN records** — the customer's existing product identifiers are already allocated and in commercial use; they must be present before allocation can avoid re-issuing them, and cannot be re-created at run time without changing what a GIN denotes; **attribute definitions** — a record cannot be created before the attributes it must carry are defined; **industry attribute variation sets** — which attributes are required varies by industry, so the industry profile must exist before a record of that industry can be validated. | 3 | **M** |
| A-101 | 3 kinds: **the "each" level definition**, **the "case" level definition**, **the "pallet" level definition** — all three are pre-defined reference levels; a hierarchy cannot be constructed, and A-103's fit check has no adjacent pair to test, until the level definitions exist. They are declared pre-defined, which is the opposite of created at run time. | 3 | **M** |
| A-114 | 3 kinds: **LN records** — existing location numbers are already in use in the customer's operations and must be present before allocation and before hierarchies can reference them; **attribute definitions** — a location record cannot be created before its attributes are defined; **industry attribute variation sets** — required attributes vary by industry, so the profile must exist before validation of that industry's records. | 3 | **M** |
| A-120 | 3 kinds: **the LN hierarchy** — the customer's existing location tree describes physical sites that exist independently of the system and must arrive whole rather than be re-keyed; **the hierarchy level** — nodes cannot attach before the levels that shape the tree exist; **the organising attribute** — the tree is organised by attributes, which must exist before the tree can be organised at all. | 3 | **M** |
| A-121 | 1 kind: **the annual verification record** — the verification cycle is annual, so what is due depends on when each record was last verified; without the prior verification history arriving, A-122's due view is empty on day one and every record appears un-verified. (Arguable — see doubts.) | 1 | **S** |

## 4. Unsizeable elements

**None.** Every one of the 29 sized elements has declared content or covered obligations sufficient to enumerate its counted things. No element was sized on a guessed reading, and no element hit P-6 (a stated cardinality without named members). Empty is a legitimate answer here.

No element reached **XL**, so no XL-derived M10 coarseness finding is raised.

## 5. Doubts

**Enumerations that could honestly be read one item longer or shorter**

1. **A-021** — "designate the Editor and Approver roles" is arguably the same action as "assign a role to a user" from G-1.2; on that reading the count is 3 and the class M rather than L. I proceeded on the stated reading that G-3.1 names a distinct designation of two specific approval roles.
2. **A-023** — whether the G-1/G-1.2 assignment tasks are served by *this* surface or only by A-021. If they belong to A-021, this surface serves 1 task and is S, not M.
3. **A-009** — "identifiers, auto-generated or manual" could split the assignment enforcement point into two actions, giving 4 and class L rather than M.
4. **A-073 / A-114** — whether the industry attribute variation set is an entity kind of its own or a property of the attribute definition. Two versus three; class M either way.
5. **A-077 / A-115** — whether managing the licensed/licensee prefix range (reserving and tracking the drawn range) is an action distinct from assigning the identifier. Four versus three; L versus M.
6. **A-103** — height and weight may be two checks or one fit check over two dimensions. Two versus one; M versus S.
7. **A-107** — rendering at a requested size could be counted as a fifth action; the class stays L.
8. **A-109** — "export" may be one format-parameterised action rather than one per format. Three versus two; class M either way.
9. **A-112** — QR and structured markup may be two renderings of one representation rather than two operations. Four versus three; class M either way.
10. **A-116** — determining that the industry assigns from a shared pool could be a second action. One versus two; S versus M.
11. **A-119** — no status values are named, so this counts two actions and sizes M, while its GIN twin A-094 names four status values and sizes L. If the LN status set is intended to be the same named set, A-119 is L. The asymmetry is in the model's declarations, not in my counting; I did not import A-094's status names into A-119.
12. **A-120** — "various attributes" is an unnamed catch-all under P-5, and the organising attribute may be the same kind already owned by A-114. Three versus two; class M either way.
13. **A-122** — L-13 says "record annual verification", a verb this view's name does not carry. If recording the verification is a task of this surface, the count is 2 and the class M rather than S. See also closure violation C-1.
14. **A-121 special count** — a customer with no prior verification history would pre-load nothing, which the pinned table cannot express (its floor is one). I enumerated one kind on the reading that an annual cycle presupposes a last-verified date.

**Contradictions between obligations, named and not resolved**

15. **NFR-16 is one obligation text covering three different workflows** — record approval, record validation, record verification — and it is carried in full by four separate elements (A-020, A-008, A-009, A-155). The same sentences are therefore available to four enumerations at once. I counted, for each element, only the clause matching that element's declared content, and did not merge, split or reassign any element. On the alternative reading in which each element inherits the whole of NFR-16, A-020, A-009 and A-155 would each count higher. This is a contradiction in the coverage model; I state it and proceed on the stated reading.
16. **I-7 versus the migration activities on A-008** — I-7 declares that the standards and business rules "will be defined as part of the project", while G2m/G3m/G4m (extraction and transformation from predecessor applications) are crossed onto A-008. Content defined during the project is not content extracted from a predecessor. I enumerated on the "must exist before the system is usable" reading, which is what the driver's pre-loading test asks; the source contradiction is not mine to resolve.
17. **No predecessor application is named anywhere in this batch**, yet G2m/G3m/G4m are crossed onto all six stores. Every special count in section 3 therefore rests on the crossed activity asserting a migration that no obligation text names a source for.

**Doubts count: 17.**

## 6. Closure violations

Work I judge necessary that the model does not contain. Named, never priced, never added to the model.

- **C-1 — no element captures an annual location verification.** A-121 is the store that holds who, when and outcome; A-122 is the view of what is due. No behaviour element performs the act of recording a verification against a location record, and none computes the due date from the last verification and the annual cycle. A-155 does not cover this: it verifies the accuracy of attributes entered via UI or import, which is a different verification from L-13's periodic re-attestation of a location. The missing work would cover the capture action, the outcome recording, and the derivation that populates A-122.
- **C-2 — no element delivers the approval-status notification.** A-022 is declared as the trigger and detects the status change; no element in the batch names a delivery channel, a message template or a recipient preference. G-3.2 requires that Editors and Approvers *are notified*, which a trigger alone does not achieve. The missing work would cover the dispatch channel and the notification content.
- **C-3 — no element configures the standards and business rules that A-008 stores.** A-008's own name is "Configurable standards and business-rule set", and I-7 says the rules will be defined as part of the project, but no surface or behaviour in this batch covers defining, editing, versioning or activating them. The missing work would cover the administration of the rule set that every validation in A-009 depends on.
- **C-4 — S-06 has no hierarchy-construction behaviour to match S-05.2.** For GIN hierarchies the model carries A-103 (fit check between adjacent levels) and A-104 (item-level change during construction) alongside the level store A-101. For LN hierarchies it carries only the store A-120; no element covers building, re-parenting or reorganising an unlimited-depth location hierarchy by attributes, which L-10.1 explicitly permits. The missing work would cover the construction and reorganisation actions on the LN tree.

**Closure violations count: 4.**

## 7. Instrument readings

**Engine: `Hotyn-D 2.0`.**

- **Elements received:** 39 — 29 sized elements plus 10 aggregates carried for context (S-04, S-04.1, S-04.2, S-05, S-05.1, S-05.2, S-05.3, S-05.4, S-06, S-06.1, S-06.3 — of which S-04, S-05, S-06 are subtree roots; aggregates are never sized).
- **Elements sized:** 29.
- **Size-class distribution:** S 5 · M 19 · L 5 · XL 0.
  - S: A-104, A-108, A-116, A-121, A-122.
  - L: A-020, A-021, A-094, A-107, A-110.
- **Statement kinds:** compliance 0 · behavioural 0 (no `statement` element occurs in this batch).
- **Special counts produced:** 6 (G2m/G3m/G4m pre-load, on A-008, A-073, A-101, A-114, A-120, A-121) · A9 special counts 0.
- **Unsizeable count:** 0.
- **Doubts count:** 17.
- **Closure violations count:** 4.
