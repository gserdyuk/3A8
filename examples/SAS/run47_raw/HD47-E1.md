<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-08. Not part of the sensor's reply.
run: 47 · reading: HD47-E1 · engine (sensor's own stamp): Hotyn-D 2.0 · model (launch record): the harness's `opus`
alias, i.e. Claude Opus 5 · batch E of the work model HW45 (prompt generated by make_sizing_prompts.py), repeat 1
· tool_uses: 0 · received complete; text taken from the harness transcript of the agent, identical to the reply as received. -->

# 1. Contamination check

**Clean — classification proceeds.**

No effort figure, person-day value, rate, price, budget, duration, team size, prior estimate or rate-table row appears anywhere in INPUT 1 or INPUT 2. The only numbers in the input are threshold boundaries in the sizing table and requirement identifiers.

Ambient repository material **was** present in my context (a git status line, branch and commit subjects, a directory path). It was **quarantined**: not read as input, not acted on, not treated as run-stopping contamination. All classification below rests on the pasted input alone. No files were read.

One near-miss reported rather than suppressed: NFR-17 and NFR-18 use the phrase *"near-real-time"*. This is an unquantified service qualifier, not a duration figure, and no threshold or numeric target accompanies it — it does not contaminate. It is logged as a doubt (§5).

---

# 2. Element sizing table

## Subtree S-10 — Notifications and communications

### S-10.1 — Notification engine (aggregate, not sized)

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-036 | Notification composition, routing and dispatch | behaviour | 7 actions: compose notification content; route notification to its recipients; dispatch notification; notify Editors and Approvers on a change of approval status (G-3.2); notify the user when a hierarchy record's status change affects other records in the hierarchy (P-11.5 / L-10.3, identical texts, counted once); emit an event notification when a system error occurs (NFR-17); emit an event notification when a critical business function fails (NFR-18). | 7 | **XL** |
| A-040 | Global broadcast notifications | behaviour | 1 action: issue a global notification to all users (G-8.1). "e.g. scheduled server downtime" is an example of the notification, not a second action. | 1 | **S** |
| A-041 | Opt-in record-event notifications | behaviour | 3 actions: a user opts in to be notified about records; notify the opted-in member when a record is updated; notify the opted-in member when a record is added (G-8.2; "updated or added" counts by outcome, P-3). | 3 | **M** |
| A-042 | Member-to-member and member-to-X-Customer messages | behaviour | 3 actions: send a member-to-member message; send a member-to-X-Customer message; challenge record data (G-8.3, the named user act). | 3 | **M** |
| C-001 | Notification template catalogue | store | 1 entity kind: notification template. Derived element, empty own coverage; the declaration names no second kind and no catch-all is counted (P-5). | 1 | **S** |

### S-10.2 — Delivery channels (aggregate, not sized)

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-037 | Email delivery via the client SMTP relay | interface | 2: the send-message operation consumed on the client SMTP relay; the SMTP/email channel as a named protocol concern (P-1). No authentication concern is named in content or coverage. | 2 | **M** |
| A-038 | SMS delivery via external gateway | interface | 2: the send-SMS operation consumed on the external gateway; the SMS channel as a named protocol concern (P-1). No authentication concern is named in content or coverage. | 2 | **M** |
| A-039 | Onscreen notification centre | surface | 1 user task: view one's onscreen notifications (G-8, "onscreen"). No read/dismiss/filter task is named. | 1 | **S** |
| C-002 | Delivery retry and failure handling | behaviour | 2 actions: retry a failed delivery; handle (record and surface) a delivery failure. Derived element, empty own coverage; both actions come from the declared name. | 2 | **M** |

### S-10.3 — Communication preferences (aggregate, not sized)

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-043 | Per-user channel and frequency preferences | store | 2 entity kinds: a per-user notification method (channel) preference; a per-user notification frequency preference (G-8.5). | 2 | **M** |
| C-003 | Notification digest batching | behaviour | 1 action: batch pending notifications into a digest. Derived element, empty own coverage; no emission or scheduling act is separately named. | 1 | **S** |

## Subtree S-11 — Reporting and audit

### S-11.1 — Report definitions (aggregate, not sized)

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-029 | Report generation and viewing | surface | 4 user tasks: generate (run) a report (G-7, G-7.1); view a report (G-7.1); customise a report (G-7.1); schedule a report (G-7.1). | 4 | **L** |
| A-030 | Add / change / delete report definitions | behaviour | 3 actions: add a report definition; change a report definition; delete a report definition (slash-separated outcomes are distinct actions, P-3). | 3 | **M** |
| A-031 | Report customisation | behaviour | 1 action: customise a report definition (G-7.1, "customise"). No customisation dimension (fields, filters, layout) is named. | 1 | **S** |

### S-11.2 — Scheduling and history (aggregate, not sized)

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-032 | Report scheduling | behaviour | 2 actions: schedule a report run; run the report on its schedule (G-7.1 names "schedule" and "run" as separate verbs). | 2 | **M** |
| A-034 | Report run history | store | 2 entity kinds: a report run record (the execution instance); the retained report data of that run (G-7.3, "run report data history"). | 2 | **M** |

### S-11.3 — Audit trail and audit reports (aggregate, not sized)

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-035 | Audit report definitions over the trail | surface | 2 user tasks: define an audit report over the trail (name token, covered by no obligation, P-2); run an audit report (G-7.4). | 2 | **M** |
| A-159 | System-wide audit trail of all activity | store | 1 entity kind: an audit trail entry for a system activity. "all activity taking place at any point in the system" (NFR-20) is an unnamed catch-all and names no further kind (P-5). | 1 | **S** |
| C-023 | Audit trail retention and archiving | behaviour | 2 actions: retain (and expire) audit trail entries per retention; archive audit trail entries. Derived element, empty own coverage; both actions come from the declared name. | 2 | **M** |

### Directly on S-11 (no third-level aggregate)

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-033 | Usage reporting from the 3scale analytics API | interface | 2: the usage-metrics retrieval operation consumed from the 3scale analytics API; the 3scale analytics API as a named integration/protocol concern (P-1, by analogy with a named channel). The period granularities named in G-7.2 (hour, day, week, month, year) are read as parameters of that one retrieval, not as separate operations — see doubt D-12. | 2 | **M** |

---

# 3. Special counts

No `statement` element appears in this batch, and no element carries **A9**; there is therefore no measurable-target count to report.

Three stores carry **G2m / G3m / G4m**: C-001, A-043, A-159. The pinned driver is *"entity kinds in the store that are loaded from the predecessor applications — say per kind why it must arrive from the predecessor rather than be created at run time."*

| element | enumeration under the pinned driver | count | class |
|---|---|---|---|
| C-001 — Notification template catalogue | No predecessor application is named anywhere in this element's declared content or coverage (coverage is empty), and no kind is declared as arriving from one. Enumeration is empty. | 0 | **none assigned — driver-level finding (M10)** |
| A-043 — Per-user channel and frequency preferences | No predecessor application is named. G-8.5 declares the opposite origin for both kinds: *"A user can set communication preferences for the method and frequency"* — both the method preference and the frequency preference are created at run time by the user. Enumeration is empty. | 0 | **none assigned — driver-level finding (M10)** |
| A-159 — System-wide audit trail of all activity | No predecessor application is named. NFR-20 declares the trail as a record of activity *"taking place at any point in the system"* — the single named kind is written at run time. Enumeration is empty. | 0 | **none assigned — driver-level finding (M10)** |

The scale for this driver has no zero rung (S begins at 1). I do not round an empty enumeration up to S and I do not guess a predecessor that the model never names. The three counts are reported as findings, not as classes. See doubt D-15 for the competing pre-load reading, under which C-001 would enumerate one kind and the other two would still enumerate none.

---

# 4. Unsizeable elements

**None.** All 20 sized elements carried enough declared content plus coverage to support an enumeration. This section being empty is a legitimate answer.

One separate finding, which is not an unsizeability:

- **A-036 — coarseness finding (M10).** A-036 is classed **XL** on a count of seven actions and is *simultaneously* reported as a probable coarseness defect in the product model: one behaviour element carries six distinct obligations spanning three unrelated trigger families — approval-status change (G-3.2), hierarchy-status propagation (P-11.5 / L-10.3), and system-health events (NFR-17, NFR-18) — in addition to the composition/routing/dispatch pipeline named in its own title. The XL class and this finding do not substitute for one another. I do not split the element (that would be reshaping).

The three empty special counts in §3 are driver-level M10 findings on otherwise sizeable elements; the elements themselves remain sized.

---

# 5. Doubts

**Enumerations that could honestly read one item longer or shorter**

- **D-1 (A-036, class-changing: XL vs L).** The arguable items are *compose*, *route* and *dispatch*. G-8's *"Provide notifications to users in various formats"* may be read as already covering the whole provision pipeline, in which case the three name tokens collapse into it under P-2 and the count falls to 5 (L). I counted them because P-2's test is coverage of the action, and no obligation names composing or routing as such — the obligations name *notifying*, and name recipients, not routing.
- **D-2 (A-042, class stable at M).** The arguable item is *challenge record data*. G-8.3 offers it as an example ("e.g.") of a member-to-X-Customer notification rather than as a separate act. Counting 2 instead of 3 leaves the class M either way.
- **D-3 (C-002, class-changing: M vs S).** The arguable item is *handle a delivery failure*. "Retry and failure handling" may be one compound act, retry being the handling; then the count is 1 (S).
- **D-4 (A-043, class-changing: M vs S).** The arguable item is *frequency preference* as a kind of its own. Method and frequency may be two attributes of a single per-user preference record, one entity kind (S).
- **D-5 (C-003, class-changing: S vs M).** The arguable item is *emit the digest*. "Digest batching" may name accumulation plus emission as two actions (M); I read the name as one act because emission is nowhere named.
- **D-6 (A-029, class-changing: L vs M).** The arguable items are *customise* and *schedule*. They enter only through the element's own coverage of G-7.1; its declared name is narrower — "Report generation and viewing" — and A-031 and A-032 exist as separate elements for exactly those two verbs. On the name-token reading the count is 2 (M). I counted 4 because the rule says the enumeration is drawn from declared content **plus its own coverage**, and G-7.1 is in A-029's own coverage. That the same obligation is counted again on neighbouring elements is a property of the model's coverage assignment, which I may not reshape.
- **D-7 (A-030, class stable at M).** Two readings of the same three: the element name reads as create/update/delete on report *definitions*; G-7.1 reads as reports *about* record adds, changes and deletes. The readings name different things but both enumerate 3, so the class does not turn on it. Not resolved.
- **D-8 (A-031, class-changing: S vs M).** The arguable expansion is customisation per report subject (adds, changes, deletes) giving 3. I did not count it: G-7.1 names those as report subject matter, not as customisation acts.
- **D-9 (A-032, class-changing: M vs S).** The arguable item is *run the report on its schedule*. G-7.1 names "run" as its own verb, but A-029 also covers G-7.1 and I counted "run" there too; a reading that assigns "run" solely to A-029 leaves A-032 at 1 (S).
- **D-10 (A-034, class-changing: M vs S).** The arguable item is *retained report data* as a kind separate from the run record. G-7.3's phrase "Run report data history" is terse enough to name a single kind (S).
- **D-11 (A-159, noted, class not in dispute).** NFR-20's "all activity" is a universal catch-all naming no activity kinds, so an enumeration by kind of audited activity is impossible; under P-5 the store is sized on its single named kind. This is a thinness in the obligation, not a defect I can repair by guessing kinds.
- **D-12 (A-033, class-changing: M vs L).** The arguable items are the five period granularities named in G-7.2 — hour, day, week, month, year. Read as distinct consumed retrievals they add five, giving 6 or 7 (L). I read them as parameters of one retrieval because the interface driver counts *operations*, and because counting them would break consistency with P-1 as applied to A-037 and A-038.

**Contradictions between obligations, named and not resolved**

- **D-13.** G-8.5 scopes preferences to *"notifications received outside the system"*, while G-8 and A-039 establish an **onscreen** notification centre inside the system. Whether a user may set a preference governing onscreen delivery is left contradictory by the obligations. I proceeded on the stated reading of G-8.5 and did not extend A-043's enumeration to an onscreen preference kind.
- **D-14.** P-11.5 and L-10.3 carry **verbatim identical texts** under two identifiers on A-036. I counted the action once. Whether the duplication is intentional (two source documents) or a coverage defect is not mine to resolve.
- **D-15 (driver conflict, affects §3).** My instrument's framing of the pre-loading driver ("entity kinds that need pre-loading — why each must exist before the system is usable") and the pinned catalogue's framing ("entity kinds loaded from the predecessor applications") are not the same test. Under the pinned catalogue text all three stores enumerate none, as reported. Under the must-exist-before-usable reading, C-001 would enumerate **1 kind** — the notification template, which no run-time act creates and without which no notification can be composed — while A-043 and A-159 would still enumerate none, both being written at run time. I reported on the pinned text and did not silently pick the reading that yields a rung.
- **D-16.** NFR-17 and NFR-18 both assert *"near-real-time"* delivery, an unquantified service qualifier. The batch contains no `statement` element carrying it and no A9 activity on any element, so this qualifier is enumerated nowhere as a measurable target. Named, not resolved.

---

# 6. Closure violations

Work I judge necessary that the model as given does not contain. Named only; no class assigned, nothing priced.

1. **No producer of system-health events.** A-036 covers NFR-17 and NFR-18 — notification when a system error occurs and when a critical business function fails — but no element in either subtree names the detection or emission of those events. What it would cover: recognising a system error and a critical-business-function failure as events, and handing them to the notification engine. The consumer exists; the source does not.
2. **No delivery of scheduled report output.** A-032 schedules and runs reports and A-034 retains run history, but no element names what happens to the output of an unattended scheduled run — no delivery to a recipient, no notification of completion. What it would cover: routing a completed scheduled run to the user who scheduled it. Neither A-029 (interactive viewing) nor the S-10 subtree names it.
3. **No authoring or management of notification templates.** C-001 exists as a store of templates, but no behaviour element in S-10.1 names creating, editing, versioning or retiring a template. What it would cover: bringing a template into the catalogue and changing it thereafter. Without it the store has no writer.
4. **No store for member-to-member and member-to-X-Customer message content.** A-042 is a behaviour that sends messages and supports challenging record data; the only store in S-10.1 is the template catalogue. What it would cover: the message or challenge itself — its text, its parties, its state — which must persist for a challenge to be answerable.

---

# 7. Instrument readings

**Engine: `Hotyn-D 2.0`.**

- **Elements received:** 28 (20 sized elements + 8 aggregates listed for context and never sized: S-10, S-10.1, S-10.2, S-10.3, S-11, S-11.1, S-11.2, S-11.3)
- **Elements sized:** 20
- **Size-class distribution:** S 6 · M 12 · L 1 · XL 1
- **Statement kinds:** compliance 0 · behavioural 0 (no `statement` element in this batch)
- **Unsizeable count:** 0 elements (plus 3 driver-level M10 findings on the G2m/G3m/G4m special count, and 1 XL coarseness finding on A-036)
- **Doubts count:** 16
- **Closure violations count:** 4

Every number above is a count of named things. No position-derived size (per-parent, once-bracket, per-environment) is reported; no D4 count is reported; no effort, duration, rate or price appears in this reading, in any form.