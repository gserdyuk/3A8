<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-08. Not part of the sensor's reply.
run: 47 · reading: HD47-G1 · engine (sensor's own stamp): Hotyn-D 2.0 · model (launch record): the harness's `opus`
alias, i.e. Claude Opus 5 · batch G of the work model HW45 (prompt generated by make_sizing_prompts.py), repeat 1
· tool_uses: 0 · duration 237 s · received complete; transcribed verbatim by the orchestrator from the reply as
delivered (the harness transcript file was empty). -->

# Size classification — batch `HM44-OA1` / subtrees S-09, S-14 · engine `Hotyn-D 2.0`

## 1. Contamination check

**Clean — proceed.**

No effort figure, person-day value, rate, price, budget, duration, team size, prior estimate or rate-table row appears anywhere in INPUT 1 or INPUT 2. The inputs carry element ids, classes, parents, obligation texts and activity codes only.

Ambient repository information *was* present in my context (a git status block, a branch name, recent commit subjects, a memory index, a working-directory path). It is **quarantined**: not read as input, not acted on, not treated as stopping contamination. One commit subject visible in that ambient block contains a numeric run reference and a ratio; it is not from the pasted input and plays no part in any count below. All classification below rests on the pasted work model and sizing rules alone.

**No files were read.**

**Counting convention stated once (applies uniformly below):** for a `store`, I count as an entity kind anything the store must hold rows of for its declared purpose, *including* the keying dimensions whose value sets the configuration must enumerate. Where a keying dimension is arguably referenced rather than owned, that is logged as a doubt, not silently resolved.

## 2. Element sizing table

### Subtree S-09 — Import and export (own child)

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-059 | Record selection for import and export | behaviour | "select an individual record; select multiple records; apply the same action to all selected records" | 3 | **M** |

### Subtree S-09.1 — Format adapters

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-051 | Excel adapter | behaviour | "import records from Excel format; export records to Excel format" | 2 | **M** |
| A-052 | CSV adapter | behaviour | "import records from CSV format; export records to CSV format" | 2 | **M** |
| A-053 | Numbers adapter | behaviour | "import records from Numbers format; export records to Numbers format" | 2 | **M** |
| A-054 | XML adapter | behaviour | "import records from XML format; export records to XML format" | 2 | **M** |
| A-055 | Tab-delimited adapter | behaviour | "import records from tab-delimited format; export records to tab-delimited format" | 2 | **M** |
| A-056 | IDoc adapter | interface | "consume an inbound iDoc; emit an outbound iDoc; the iDoc format itself as a named protocol concern (P-1); ingest data created and managed in a named external system (QuickBooks, SAP)" | 4 | **M** |
| A-057 | Import and export through the member API | interface | "expose an import operation through the API; expose an export operation through the API; ingest data created and managed in a named external system (QuickBooks, SAP); REST as a named protocol; SOAP as the named alternative protocol; compatibility with the named API management solution (3scale) as a gateway concern" | 6 | **L** |
| A-058 | PC/Mac encoding and line-ending variants | behaviour | "import a PC-format file; import a Mac-format file; export a PC-format file; export a Mac-format file" (P-3: "PC and Mac" names two variants, not one) | 4 | **L** |

### Subtree S-09.2 — Import pipeline

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-049 | Record import service | behaviour | "ingest a record file (G-12); create GIN records from an import (P-1); create LN records from an import (L-1); update record attributes from an import (P-5, L-5); create a GIN hierarchy from an import (P-11); create an LN hierarchy from an import (L-10); apply hierarchy information to already existing records (P-11.2, L-10.2); ingest data created and managed in an external system (P-19, L-15)" | 8 | **XL** |
| A-102 | Hierarchy import modes | behaviour | "import full records carrying hierarchy information; import hierarchy information for records that already exist" (P-3: the "or" names two outcomes) | 2 | **M** |
| C-007 | Import validation result report | surface | "review the validation outcome of an import run" (whole enumeration rests on the name token; own coverage is empty) | 1 | **S** |
| C-008 | Import staging and preview | surface | "stage an import so it is held before it is applied; preview the staged content before applying it" (both name tokens uncovered by any obligation — P-2) | 2 | **M** |

### Subtree S-09.3 — Export pipeline

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-050 | Record export service | behaviour | "export prefix data; export GIN data; export LN data" (I-8 names the three data kinds; D-5's "one or more records" is a cardinality, not a further named action — P-6) | 3 | **M** |
| C-009 | Asynchronous export job handling | behaviour | "run an export as an asynchronous job" (own coverage empty; "asynchronous" is a manner, not a second action) | 1 | **S** |
| C-025 | Export artefact retention and cleanup | behaviour | "retain an export artefact; delete an export artefact that has passed retention" (both name tokens uncovered — P-2) | 2 | **M** |

### Subtree S-14.1 — Industry configuration

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-074 | Industry attribute-set configuration | store | "industry; attribute definition (required and non-required); the GIN attribute set per industry (P-1); the LN attribute set per industry (L-1)" | 4 | **L** |
| A-098 | Reuse-rule configuration by industry and product type | store | "industry; product type; the GIN reuse rule keyed by industry and product type (P-10.3); the LN reuse rule keyed by industry (L-9.3)" | 4 | **L** |
| A-117 | Industry LN-pool configuration | store | "industry; the shared LN pool; the pool-assignment configuration entry saying which industries assign automatically from which pool" | 3 | **M** |
| C-018 | Configuration administration console | surface | "maintain the configuration reference data" (own coverage empty; the name names one task and no members) | 1 | **S** |

### Subtree S-14.2 — Prefix registry and X-Customer reference data

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A-013 | X-Customer reference data in the read store under the same rules | store | — no enumeration possible — see §4 | — | **unsizeable (M10)** |
| A-027 | Prefix capacity computation (used vs available) | behaviour | "compute the used count for a prefix; compute the available count for a prefix; display the capacity counter" (P-3: "used vs available" names two) | 3 | **M** |
| C-015 | Identifier allocation ledger | store | "identifier; allocation entry (which identifier was allocated, to whom, when)" | 2 | **M** |

Aggregates listed for context and correctly not sized: S-09, S-09.1, S-09.2, S-09.3, S-14, S-14.1, S-14.2.

No `statement` element appears in this batch, so no kind assignment arises.

## 3. Special counts

**A9** is crossed onto no element in this batch — no measurable-target counts arise.

**G2m / G3m / G4m** (field mapping, extraction and transformation, load and reconciliation — on a store) are crossed onto A-074, A-098, A-117, A-013 and C-015. Per-kind reason why the kind must arrive from the predecessor rather than be created at run time:

**A-074 — Industry attribute-set configuration** — 4 kinds → **L**
1. *industry* — the set of industries served is settled business fact; a record cannot be created until the industry it belongs to resolves against an existing list.
2. *attribute definition (required / non-required)* — the attribute vocabulary in force per industry exists in the predecessor; without it no imported or manually created record can be validated on the first day.
3. *GIN attribute set* — the per-industry binding of attributes to GIN records must be in place before the first GIN record is accepted, or the record would be stored against an undefined shape.
4. *LN attribute set* — same, for LN records.

**A-098 — Reuse-rule configuration by industry and product type** — 4 kinds → **L**
1. *industry* — the rules are keyed by it; the key's value set cannot be invented at run time.
2. *product type* — likewise the GIN-side keying vocabulary.
3. *GIN reuse rule* — the waiting periods already in force govern GINs taken out of use before cutover; the availability date shown to a user on day one is only correct if the rule came across.
4. *LN reuse rule* — same, for LNs taken out of use before cutover.

**A-117 — Industry LN-pool configuration** — 3 kinds → **M**
1. *industry* — which industries assign automatically must be known before the first LN request is served.
2. *shared LN pool* — the pool as it stands in the predecessor (what is spent, what remains) must carry over, or numbers already in the field would be handed out again.
3. *pool-assignment configuration entry* — the industry-to-pool mapping must exist before automatic assignment can run at all.

**C-015 — Identifier allocation ledger** — 2 kinds → **M**
1. *identifier* — identifier values already issued are printed on goods in the field and cannot be regenerated; they must arrive as data.
2. *allocation entry* — the ledger is the record of what has been spent; without the predecessor's entries the system would re-issue live numbers.

**A-013 — X-Customer reference data in the read store** — **no special count possible.** The element is unsizeable (§4); its entity kinds are never named, so the pre-loading enumeration cannot be made either. Reported as a finding, not as zero.

## 4. Unsizeable elements, and the XL coarseness signal

**A-013 — X-Customer reference data in the read store under the same rules — *unsizeable, model defect (M10)*.**
The store driver is *entity kinds the store is responsible for*. The declared content names a category ("X-Customer reference data"), not kinds; its single covered obligation I-11 says only that "X-Customer-owned data as well as data published by data owners can be accessed in the Access Data module" — again a category of ownership, not an enumeration of kinds. By P-5 an unnamed catch-all is not a named thing, and by the no-guess rule I will not read "prefix, GIN and LN" across from I-8, which is another element's coverage. **Missing from the declaration:** which entity kinds X-Customer owns and holds here, and what "under the same rules" refers to. No class assigned; no special count assigned.

**A-049 — Record import service — sized XL *and* reported as a probable coarseness finding (M10).**
Eight distinct actions land on one element, spanning creation, attribute update, hierarchy construction, hierarchy attachment to existing records and external-system ingest, across two record types. The XL class stands; separately, this is a signal that the product model is coarse at this point. I do not split it — splitting would be reshaping.

## 5. Doubts

1. **A-059** — D-5 ("export one or more records to any format") sits in this element's own coverage and names an export action. The arguable item is *"export the selected records"*: counted on A-050 instead, since this element declares selection. Counting it here gives 4 → L.
2. **A-051 to A-055 (one doubt, five elements)** — each counts an import action and an export action. The arguable item is *the export action*, which D-5 could be read as discharging on A-050 rather than on the adapter; on that reading each adapter is 1 → S.
3. **A-056** — the arguable item is *"ingest data created and managed in a named external system"*, which may restate the inbound iDoc operation rather than add to it. Dropping it gives 3 — still M, no class change.
4. **A-057** — two arguable items: *SOAP as a second named protocol* ("REST (preferred) or SOAP" may be one protocol concern with an alternative), and *external-system ingestion* as a restatement of the exposed import operation. Dropping either gives 5 — still L; dropping both gives 4 → M.
5. **A-058** — I read "PC and Mac formats" as a variant axis crossed with import and export, giving 4. The arguable reading treats it as a single variant dimension over two actions, giving 2 → M. Separately, the name tokens *encoding* and *line-ending* name two concerns but no verb on an object, so I counted neither.
6. **A-049** — the largest swing in the batch. I counted GIN and LN as distinct objects, following the separate obligation pairs (P-1/L-1, P-5/L-5, P-11/L-10). Merging each GIN/LN pair into one record-type-agnostic action gives 6 → L instead of XL. The arguable items are *"create LN records from an import"* and *"create an LN hierarchy from an import"*.
7. **A-049 / A-102 overlap** — P-11.2 and L-10.2 appear in the own coverage of *both* elements. I counted them on both, as each element's declared coverage requires. I do not resolve which element owns them; that is a model question.
8. **A-050** — I-8's text also names *"search for"* and *"view"*. If those actions count on the export service rather than on a search or view element elsewhere, the count is 5 → L. Arguable items named.
9. **C-007, C-008, C-009, C-018 (derived elements, own coverage empty)** — each enumeration rests entirely on name tokens under P-2. Specifically: C-008's *"stage an import"* may be an internal mechanism rather than a user task (→ 1, S); C-009's *"handling"* verges on an unnamed catch-all under P-5, and if it names nothing at all the element would be unsizeable rather than S; C-018's *"configuration"* could be read as one task per configuration store standing under its parent, giving 3 → M.
10. **A-074** — the arguable items are *"the GIN attribute set"* and *"the LN attribute set"* as two kinds rather than one record-type-agnostic attribute set; merged, 3 → M. The same merge would carry into the pre-loading count (§3).
11. **A-098** — two independent doubts. (a) *GIN reuse rule* and *LN reuse rule* merged into one rule kind gives 3 → M. (b) *industry* and *product type* read as referenced from elsewhere rather than owned here gives 2 → M. Also noted and **not resolved**: P-10.3 says GIN reuse rules "may vary by industry and product type" while L-9.3 says LN rules "may vary by industry" only — an asymmetry in the keying that the obligations state and do not reconcile.
12. **A-027** — the arguable item is *"display the capacity counter"*, which may belong to a surface (dropping it gives 2, still M). In the other direction, reading "numeric indicators (GINs or LNs)" as two object kinds gives compute-used-GIN, compute-used-LN, compute-available-GIN, compute-available-LN, display = 5 → L.
13. **C-015** — the arguable item is *"identifier"* as a kind this ledger is responsible for rather than one it references; dropping it gives 1 → S.
14. **Class asymmetry given to me, not resolved here** — A-056 (IDoc) is classed `interface` while the five sibling format adapters A-051 to A-055 are classed `behaviour`, and the two classes carry different thresholds. The class comes from `Hotyn-W 1.1`; I record the asymmetry and size each element under the class it was given.
15. **G-12.1 is an open list** — "formats include **at least** Excel, CSV, Numbers, XML, tab-delimited, iDocs, and through the API". Under P-5 the open tail names nothing and was counted nowhere; only the seven named channels have elements.

## 6. Closure violations

Work I judge necessary that the pasted model does not contain. Named, never priced. Items 1, 2 and 5 may be discharged in subtrees outside this batch; they are named on the face of what was pasted.

1. **No element declares the prefix registry itself.** Subtree S-14.2 is named "Prefix registry and X-Customer reference data", yet no sized element holds prefixes as an entity kind. A-027 computes used-versus-available capacity *for a prefix* that nothing in this batch declares, and C-015 ledgers allocations against it. Missing coverage: the prefix record, its owner, and the numeric range it authorises.
2. **No element owns the industry and product-type value sets.** A-074, A-098 and A-117 are all keyed by industry, and A-098 additionally by product type, but no element in the batch declares responsibility for those vocabularies.
3. **No element covers correction and re-submission of rejected import rows.** C-007 reports the validation outcome and C-008 stages and previews, but nothing in S-09.2 acts on a failed validation: correcting the offending rows and resubmitting only those is uncovered.
4. **No element covers change history for configuration.** C-018 administers reuse rules and attribute sets whose values determine dates displayed to users (P-10.3, L-9.3); nothing records who changed a rule or when, so a disputed availability date has no trail.
5. **No element covers export authorisation.** I-8 distinguishes X-Customer-owned data from data published by data owners; nothing in this batch decides who may export which owner's data through A-050, the adapters, or the member API.

## 7. Instrument readings

**Engine: `Hotyn-D 2.0`.** This engine produces no numbers with units of work. Every number below is a count of named things.

- **Elements received:** 30 — 23 sizeable candidates plus 7 aggregates (S-09, S-09.1, S-09.2, S-09.3, S-14, S-14.1, S-14.2) listed for context and never sized.
- **Elements sized:** 22.
- **Size-class distribution:** S = 3 · M = 14 · L = 4 · XL = 1.
- **Statement kinds:** compliance = 0 · behavioural = 0 (no `statement` element in this batch).
- **Unsizeable:** 1 (A-013).
- **Doubts:** 15.
- **Closure violations:** 5.

Position-derived sizes (per-parent, once-bracket, per-environment) are not reported; nor is the D4 requirement-id driver — both are computed outside this engine from the tree and the coverage sets.
