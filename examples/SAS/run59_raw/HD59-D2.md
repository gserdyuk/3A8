<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-15. Not part of the sensor's reply.
run: 59 · reading: HD59-D2 · engine (sensor's own stamp): Hotyn-D 2.0 (title line: # Hotyn-D 2.0: size classes for the N11 and N12 subtrees of HM57-1 (as classified and crossed by Hotyn-W 1.2); §7 line: - **Engine:** Hotyn-D 2.0) ·
subagent: 3a8:work-estimator (plugin) · model (launch record): the harness's `opus` alias (the launch tool offers no
`claude-opus-5` value); the transcript's model attachment and the assistant turn record `claude-opus-5` ·
batch D, repeat 2: subtrees N11, N12 of the run 58 work model (Hotyn-W 1.2 × HM57-1), 24 sized elements ·
prompt prompt_size_D.md, 18 790 chars, md5 be5ce765dfbf45248960baaf7b45c2d5 (generated by make_sizing_prompts58.py; both repeats of
the batch received this file; the transcript's prompt record equals the file byte for byte, final newline included) ·
tool_uses: 0 · duration 315 546 ms · subagent tokens 34 783 ·
orchestrator process CLAUDE_CODE_MAX_OUTPUT_TOKENS=128000.
TRANSIT: one turn, one API message (msg_011Cf5tfFxK2CtBAwXPmL3mS), thinking (content empty in the transcript) then one text block of 16 913 chars; stop end_turn; output tokens 25 967.
No continuation message was injected ("Output token limit hit" occurs 0 times in the transcript). No other
message reached the agent.
PROVENANCE OF THIS TEXT: extracted by script (build_raw59.py) from the harness's per-agent transcript
(`~/.claude/projects/<project>/<session>/subagents/agent-<id>.jsonl`): the single assistant text block after the
prompt, with nothing inserted or removed. The completion notification also carried a reply text; it was not used
as the source. No seam. -->

# Hotyn-D 2.0: size classes for the N11 and N12 subtrees of HM57-1 (as classified and crossed by Hotyn-W 1.2)

## 1. Contamination check

**Clean.**
- **Input 1** (the work model batch) and **Input 2** (the sizing rules) contain none of the following: effort figures, person-day values, rates, prices, budgets, durations, team sizes, prior estimates or rate-table rows. Codes such as A2, D4, G2m and K3 are activity identifiers. Version and catalogue numbers are identifiers too.
- **Ambient context:** the harness added a git status, recent commit subjects, a user memory index and an environment block. I quarantined all of it. I did not act on it, and none of it went into any count below. No files were read and no tools were called.

## 2. Element sizing table

Systematic reading used everywhere: several obligations are shared between elements, and some are wider than any one element. For an element I count only the clause its own declared content names, such as its name tokens or its object. Clauses that other siblings name are counted on those siblings. Where a name token is already covered by an obligation, it is not counted twice (rule P-2).

### Subtree N11 — Import and export

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A051 | Record import pipeline (parse, map, validate, load) | behaviour | 8 actions: (1) parse the import file (name token; no obligation covers it); (2) map imported fields to record attributes (name token; not covered); (3) validate records on import (G-12.4; covers the "validate" name token); (4) create records via import (P-1, L-1; covers the "load" name token); (5) edit existing record attributes via import (P-5, L-5); (6) import full records with hierarchy information (P-11, P-11.2, L-10.2); (7) import hierarchy information for existing records (P-11.2, L-10.2); (8) import data created and managed in an external system (P-19, L-15) | 8 | **XL**. Also a probable coarseness finding about the model (M10) |
| A052 | Record export pipeline (serialise, deliver) | behaviour | 4 actions: (1) export records to a selected format (G-12, G-12.1, D-5, I-8; covers "serialise"); (2) deliver the export (name token; not covered); (3) export hierarchies in a user-friendly format (P-12); (4) print hierarchies (P-12) | 4 | **L** |
| A053 | Excel import and export adapter | interface | 4: (1) import records from Excel (G-12.1, P-19, L-15); (2) export records to Excel (G-12.1, D-5); (3) PC format concern (G-12.2); (4) Mac format concern (G-12.2) | 4 | **M** |
| A054 | CSV import and export adapter | interface | 4: (1) import records from CSV (G-12.1, P-19, L-15); (2) export records to CSV (G-12.1, D-5); (3) PC format concern (G-12.2); (4) Mac format concern (G-12.2) | 4 | **M** |
| A055 | Numbers import and export adapter | interface | 4: (1) import records from Numbers (G-12.1, P-19, L-15); (2) export records to Numbers (G-12.1, D-5); (3) PC format concern (G-12.2); (4) Mac format concern (G-12.2) | 4 | **M** |
| A056 | XML import and export adapter | interface | 4: (1) import records from XML (G-12.1, P-19, L-15); (2) export records to XML (G-12.1, D-5); (3) PC format concern (G-12.2); (4) Mac format concern (G-12.2) | 4 | **M** |
| A057 | Tab-delimited import and export adapter | interface | 4: (1) import records from tab-delimited (G-12.1, P-19, L-15); (2) export records to tab-delimited (G-12.1, D-5); (3) PC format concern (G-12.2); (4) Mac format concern (G-12.2) | 4 | **M** |
| A058 | IDoc import and export adapter | interface | 4: (1) import records from IDoc (G-12.1, P-19, L-15); (2) export records to IDoc (G-12.1, D-5); (3) PC format concern (G-12.2); (4) Mac format concern (G-12.2) | 4 | **M** |
| A060 | Record selection for import and export | behaviour | 2 actions: (1) select one or more records to be imported (G-12.3); (2) select one or more records to be exported (G-12.3, D-5). "Imported or exported" names two outcomes (rule P-3) | 2 | **M** |
| A061 | Import validation result report | surface | 1 task: (1) review the validation results of an import (name token; G-12.4 covers the validation itself, not the task of reviewing it) | 1 | **S** |
| A085 | Import update mode for existing records | behaviour | 2 actions: (1) update attributes of existing records via import (P-5, L-5); (2) update hierarchy information of existing records via import (P-11.2, L-10.2, second branch) | 2 | **M** |

### Subtree N12 — Publish and subscribe

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A010 | Subscription entitlement model (viewable data, export options) | store | 3 entity kinds: (1) client subscription (I-9, G-13.5); (2) viewable-data entitlement (name token; I-9 "which data can be viewed"); (3) export-option entitlement (name token; I-9 "which export options are available") | 3 | **M** |
| A062 | Publication registry (what, to whom, attribute tier) | store | 4 entity kinds: (1) publication permission record, the owner's grant to view, published at any hierarchy level (P-17, L-12, G-13, G-13.1; covers "what"); (2) record group published as a unit (G-13.3); (3) sharing target: individual, user-defined group, controlled group or public (G-13.4; covers "to whom"); (4) attribute tier, basic or full (G-13.2; covers "attribute tier"). I-1 adds no kind | 4 | **L** |
| A063 | Published-data access evaluation | behaviour | 3 actions: (1) evaluate the consumer's subscription for viewing published data (G-13.5); (2) evaluate the publication permission record for the consumer (P-17, L-12); (3) serve published records for viewing in the Data Access module (P-17, L-12). G-13's "multiple share/view functions" counts nothing (rule P-5) | 3 | **M** |
| A064 | Hierarchy-level publication | behaviour | 1 action: (1) publish records at a chosen level in the hierarchy (G-13.1) | 1 | **S** |
| A065 | Basic and full attribute tiers | statement, **behavioural**. Kind phrase: "publish (grant access to view) a basic or a full set of record attributes" (G-13.2) and "Request access to a basic or full record" (D-2). Both describe run-time behaviour | 3 constrained components: (1) publishing (G-13.2 "publish"); (2) viewing (G-13.2 "grant access to view"); (3) access requesting / record subscription (D-2) | 3 | **M** |
| A066 | Publication for a group of records | behaviour | 1 action: (1) publish data for a group of records (G-13.3) | 1 | **S** |
| A067 | Sharing target selection (individual, group, controlled group, public) | behaviour | 4 actions: (1) share records with an individual; (2) share records with a user-defined group; (3) share records with a controlled group managed by X-Customer; (4) share records with the public (all G-13.4; listed outcomes count separately under rule P-3; "selection" is covered) | 4 | **L** |
| A068 | User-defined group management | behaviour | 2 actions: (1) manage user-defined groups (name token; not covered); (2) share records with a user-defined group (G-13.4 clause) | 2 | **M** |
| A069 | Controlled group management by X-Customer | behaviour | 3 actions: (1) manage controlled groups, done by X-Customer (name token; not covered); (2) share records with a controlled group (G-13.4 clause); (3) add a requesting consumer to a controlled group (D-3 "to be added") | 3 | **M** |
| A071 | Access request submission | behaviour | 3 actions: (1) submit a request for access to a basic or full record (G-13.6, D-2; covers "submission"); (2) check the consumer's subscription permits the request (G-13.6 "Based on subscription"); (3) deliver the request to the data owner (D-2.1 "The request is sent to the data owner") | 3 | **M** |
| A072 | Access request approval and rejection by owner | behaviour | 3 actions: (1) approve a view request (G-13.7); (2) reject a view request (G-13.7; rule P-3); (3) make the data available to the requester once approved (D-2.1 "must approve before the data becomes available") | 3 | **M** |
| A137 | Controlled group join request with X-Customer approval | behaviour | 2 actions: (1) request to be added to a controlled group (D-3; covers "join request"); (2) X-Customer approves the join request (name token "with X-Customer approval"; not covered) | 2 | **M** |
| C09 | Publication overview and revocation | surface | 2 tasks: (1) review an overview of own publications, meaning what, to whom and at which tier (name token, read with trigger A062); (2) revoke a publication (name token, read with trigger A062) | 2 | **M** |

## 3. Special counts

Only one activity with a special count appears in this batch: G2m / G3m / G4m, crossed onto A010 and A062. No element here carries A9.

| element | driver | enumeration | count | class |
|---|---|---|---|---|
| A010 | entity kinds loaded from the predecessor applications | None of A010's own coverage (I-9, G-13, G-13.5, D-5) names a predecessor application or any data that already exists there. I cannot give a reason for any kind to come from a predecessor rather than be created at run time. | — | **not assigned**. Special count unsupported (M10); see section 4 |
| A062 | entity kinds loaded from the predecessor applications | 3 kinds, all resting on I-1 in A062's own coverage ("replacing the applications currently available to members" to "create, manage and share data"): (1) **publication permission record**: existing owner grants live in the applications being replaced; if they were re-created at run time, existing consumers would lose access at cutover until every owner published again. (2) **record group**: a migrated group publication (G-13.3) cannot work without the group it points to. (3) **sharing target**: a migrated grant cannot work without its grantee (individual, user-defined group, controlled group). Not counted: **attribute tier**. The basic/full split is defined by this product (G-13.2, A065), and no coverage places it in a predecessor. | 3 | **M** |

## 4. Unsizeable elements

- **No whole element is unsizeable.** All 24 are sized.
- **One special count is unsupported. A010, G2m / G3m / G4m:** the model puts the migration activities on this store, but its declared content and coverage never say that any subscription or entitlement exists in a predecessor application. To make it countable, A010's coverage would need an obligation placing client subscriptions or entitlements in the applications being replaced. This is a model defect (M10).
- **Coarseness finding (M10), not unsizeable:** A051 is XL at 8 actions. It combines creating, updating and importing hierarchy data across the product and location modules. Its coverage also overlaps A085's.

## 5. Doubts

### Enumeration doubts

The class changes only where noted.

1. **A051:** item (7) could merge into (6) as one hierarchy-import action. Item (8) could be read as a source for item (4) rather than a separate action. Dropping both gives 6, which is **L**. Splitting items (4) and (5) into GIN records and LN records gives 10, still XL.
2. **A052:** item (2) "deliver" could be read as covered by G-12.1 "through the API", giving 3, which is **M**. Exporting prefix data (I-8) as a separate object gives 5, still L.
3. **A053–A058:** "PC and Mac formats" could be one named concern, giving 3, still M. Counting the external systems "e.g. QuickBooks, SAP" (P-19, L-15) as a named channel under rule P-1 gives 5, which is **L**. I did not count them because "e.g." makes them examples, not a named channel.
4. **A060:** selecting for import and selecting for export could be one action with two uses, giving 1, which is **S**.
5. **A085:** the first branch of P-11.2 ("full records with hierarchy information") applied to existing records gives 3, still M.
6. **A010:** "client" as an entity kind this store is responsible for gives 4, which is **L**. I did not count it because I-9 names the client only as the holder of the subscription.
7. **A062:** reading the attribute tier as a field of the publication rather than its own kind gives 3, which is **M**. Splitting the sharing target into its four named variants gives 7, which is **XL**.
8. **A063:** item (3), serving records for viewing, could belong to a viewing surface rather than to evaluation, giving 2, still M.
9. **A064:** "choose the hierarchy level" as a separate action gives 2, which is **M**.
10. **A065:** the record-attribute definition (which attributes are basic) as a fourth constrained component gives 4, still M.
11. **A066:** "choose the group of records" as a separate action gives 2, which is **M**.
12. **A067:** one action ("select a target") with four values gives 1, which is **S**. I kept four under rule P-3.
13. **A068:** the G-13.4 sharing clause could belong only to A067, giving 1, which is **S**. Also, "management" names no sub-operations (create, edit, delete, membership), so none are counted.
14. **A069:** D-3 could be read as the consumer's request, which belongs to A137, rather than X-Customer adding the member, giving 2, still M.
15. **A071:** a basic request and a full request as two actions gives 4, which is **L**. Merging (3) into (1) gives 2, still M.
16. **A072:** "review the incoming request" as an action (D-2.1 "sent to the data owner") gives 4, which is **L**. Merging (3) into (1) gives 2, still M.
17. **A137:** "add the requester to the group", the result of approval, as a separate action gives 3, still M. Rejecting a join request is not named, so it is not counted.
18. **A010 special count:** borrowing I-1 from its sibling A062 would give 1 (client subscription), which is **S**, instead of unsupported.
19. **A062 special count:** a strict reading of I-1 says the applications are replaced, not that they currently share data, which gives 0 and an unsupported count. Dropping "record group" gives 2, still M.

### Systematic doubts

20. **Shared-clause reading:** my reading of shared obligations (see the note above the table) decides several counts. The overlapping coverage is: A051 and A085 (P-5, L-5, P-11.2, L-10.2); A067, A068 and A069 (G-13.4); A069 and A137 (D-3); A071 and A072 (G-13.7, D-2.1). Counting every clause of every covered obligation on every element would lengthen A068, A069, A071 and A072.
21. **G-13 "multiple share/view functions":** this is a number without a list. I did not treat it as a rule P-6 defect because G-13.1–G-13.7 name the functions. It adds nothing to A010, A062 or A063.

### Contradictions (named, not resolved)

22. **G-13.5 vs D-2.1:** G-13.5 says "Based on subscription, a data consumer can view published data", so the subscription alone is enough. D-2.1 says the owner "must approve before the data becomes available". G-13.4's public sharing sits uneasily with the approval requirement. I proceeded on each element's own stated reading: A063 on G-13.5, A072 on D-2.1.
23. **"Subscription" has two meanings:** in I-9, G-13.5 and G-13.6 it is a client's subscription (what the client may view and export). In D-2 and G-13 it is a per-record access request ("subscribe to the record"). A010 and A071 are counted on the reading each element's name implies.
24. **G-12.2 vs G-12.1:** G-12.2 requires "PC and Mac formats" for every import/export format, but G-12.1 includes Numbers and iDocs, which have no obvious PC and Mac variants. A055 and A058 are counted as stated.

## 6. Closure violations

Each of these is judged against this batch only. The work may exist in subtrees outside it.

1. **Import and export through the API (G-12.1 "and through the API"):** no element in N11 serves it. The six adapters cover the other six named formats. The missing element would cover reading and writing records through the API.
2. **Enforcing subscription export options at export time (I-9, D-5):** A010 stores export-option entitlements, but no behaviour in the batch applies them when A052 exports. A063 evaluates viewing only.
3. **Holding pending access requests and join requests (G-13.6, G-13.7, D-2.1, D-3):** requests wait between submission (A071, A137) and decision (A072, X-Customer), but no store in the batch holds them or their state.
4. **Holding user-defined and controlled group definitions and their members (G-13.4, D-3):** A068 and A069 manage these groups and A062 refers to them as sharing targets, but no store in the batch holds the groups or their membership.

## 7. Instrument readings

- **Engine:** Hotyn-D 2.0
- **Elements received:** 26 (24 leaves, plus aggregates N11 and N12, which are not sized)
- **Elements sized:** 24 (N11: 11, N12: 13)
- **Size classes:**

| S | M | L | XL |
|---|---|---|---|
| 3 | 17 | 3 | 1 |

  - The XL element is A051, also reported as a coarseness finding (M10).
- **Statement kinds:** compliance 0, behavioural 1 (A065)
- **Special counts assigned:** 1 (A062, M); unsupported: 1 (A010)
- **Unsizeable elements:** 0 (plus 1 unsupported special count, A010)
- **Doubts:** 24 (19 enumeration doubts, of which 13 would change a class; 2 systematic; 3 contradictions)
- **Closure violations:** 4