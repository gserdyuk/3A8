<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-15. Not part of the sensor's reply.
run: 59 · reading: HD59-E1 · engine (sensor's own stamp): Hotyn-D 2.0 (title line: # Hotyn-D 2.0 — size classification of the N13 subtrees in product model HM57-1 (as crossed by Hotyn-W 1.2); §7 line: **Engine: Hotyn-D 2.0**) ·
subagent: 3a8:work-estimator (plugin) · model (launch record): the harness's `opus` alias (the launch tool offers no
`claude-opus-5` value); the transcript's model attachment and the assistant turn record `claude-opus-5` ·
batch E, repeat 1: subtrees N13 of the run 58 work model (Hotyn-W 1.2 × HM57-1), 31 sized elements ·
prompt prompt_size_E.md, 20 993 chars, md5 89a3f5c00330d1ec318c70b58fda2479 (generated by make_sizing_prompts58.py; both repeats of
the batch received this file; the transcript's prompt record equals the file byte for byte, final newline included) ·
tool_uses: 0 · duration 257 366 ms · subagent tokens 31 418 ·
orchestrator process CLAUDE_CODE_MAX_OUTPUT_TOKENS=128000.
TRANSIT: one turn, one API message (msg_011Cf5tpKnk6JDnSQsKsv211), thinking (content empty in the transcript) then one text block of 17 377 chars; stop end_turn; output tokens 21 920.
No continuation message was injected ("Output token limit hit" occurs 0 times in the transcript). No other
message reached the agent.
PROVENANCE OF THIS TEXT: extracted by script (build_raw59.py) from the harness's per-agent transcript
(`~/.claude/projects/<project>/<session>/subagents/agent-<id>.jsonl`): the single assistant text block after the
prompt, with nothing inserted or removed. The completion notification also carried a reply text; it was not used
as the source. No seam. -->

# Hotyn-D 2.0 — size classification of the N13 subtrees in product model HM57-1 (as crossed by Hotyn-W 1.2)

I sized all 31 elements: 4 S, 12 M, 12 L and 3 XL. A081, A099 and A100 are XL, so each is also a model coarseness finding (M10). No element was unsizeable, no statement elements are in this batch, and I read no files.

## 1. Contamination check

**Clean. Classification went ahead.**

- **Pasted input:** it has no effort figure, person-day value, rate, price, budget, duration, team size, prior estimate or rate-table row. The only numbers are:
  - identifier-format lengths inside I-3;
  - catalogue, model and engine version labels;
  - activity codes;
  - the size thresholds.

  None of these is a unit of work.
- **Ambient context, quarantined:** the harness added a git status, recent commit subjects, a user memory index and user-context text. I treated none of it as input, used none of it and did not let it stop the run.

**How I read the coverage (one reading, used for every element):**
- **R1:** Actions, tasks, operations and entity kinds are counted from the element's name plus the full text of every obligation it covers. I don't trim an obligation down to what the element's name suggests.
- **R2:** Matching GIN and LN obligations (P-x and L-y) count as one thing, because each element's name treats records or identifiers generically.
- **R3:** For the broad obligation I-1 ("create, manage and share data"), a verb counts only if no more specific obligation of the same element already covers it (the P-2 coverage test).
- **R4:** "E.g." examples and "etc." add nothing (P-5). Lists of verbs, channels and statuses count one per member (P-3).

## 2. Element sizing table

### N14 — Identifier management

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A003 | Identifier composition rules (prefix plus serial, per-kind length) | behaviour | compose identifier from licensed prefix plus serial part (I-3); enforce per-kind identifier length (I-3); assign check digit (I-3, P-2/L-2); assign identifier automatically (P-2/L-2); assign identifier manually (P-2/L-2) | 5 | **L** |
| A004 | Check digit calculation | behaviour | compose identifier from licensed prefix plus serial part (I-3); enforce per-kind identifier length (I-3); assign check digit (I-3, P-2/L-2; the name token "calculation" is covered here); assign identifier automatically (P-2/L-2); assign identifier manually (P-2/L-2) | 5 | **L** |
| A080 | Identifier registry per prefix | store | GIN (P-2, P-8); LN (L-2); licensed prefix (name token "per prefix", no obligation covers it, so it counts under P-2) | 3 | **M** |
| A081 | Identifier assignment service (auto or manual at any creation step) | behaviour | assign identifier automatically (P-2/L-2); assign identifier manually (P-2/L-2); assign check digit (P-2/L-2); opt for auto-assign or manual at creation (P-2.1/L-2.2); set assignment-mode preference (P-3); auto-assign LN from an industry shared pool (L-2.1); submit record (NFR-16); review record (NFR-16); reject record (NFR-16); approve record (NFR-16); validate identifiers against X-Customer Standards (NFR-16); verify attributes for accuracy (NFR-16); create data (I-1, not otherwise covered); manage data (I-1, not otherwise covered); share data (I-1) | 15 | **XL** |
| A082 | Identifier reservation from auto-assign | behaviour | reserve identifier (P-2.2/L-2.3); hold reserved identifiers out of auto-assign (P-2.2/L-2.3) | 2 | **M** |
| A083 | Assignment mode preference | store | assignment-mode preference (P-3/L-3) | 1 | **S** |
| A087 | Prior-identifier reference on number change | store | prior-identifier reference (link from new number back to old number) (P-5.2/L-5.1) | 1 | **S** |
| A100 | Identifier status management | behaviour | view identifier status (P-10/L-9); view record status (P-10 "record or GIN"); manage record status (P-10); set identifier status Reserved (P-10.1); set identifier status In Use (P-10.1); set identifier status For Reuse (P-10.1); set identifier status Available (P-10.1); update identifier status to reflect record status (P-10.1/L-9.1); display out-of-use date (P-10.3/L-9.3); display reuse-availability date (P-10.3/L-9.3); apply industry- and product-type-specific reuse rules (P-10.3) | 11 | **XL** |
| A102 | Record-to-identifier status synchronisation | behaviour | set identifier status Reserved; set identifier status In Use; set identifier status For Reuse; set identifier status Available (P-10.1, P-3 outcomes); update identifier status to reflect record status (P-10.1/L-9.1; the name token "synchronisation" is covered here) | 5 | **L** |
| A104 | Out-of-use and reuse date display | surface | see date identifier was taken out of use (P-10.3/L-9.3); see date identifier becomes available for reuse (P-10.3/L-9.3) | 2 | **M** |

### N15 — Record lifecycle

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A073 | Single-record creation form driven by attribute sets | surface | create record individually (P-1/L-1); create records via import (P-1/L-1); create record through step-by-step wizard (P-1/L-1); create record by cloning an existing record (P-1/L-1); manage identifier data (I-1, not otherwise covered); share identifier data (I-1) | 6 | **L** |
| A074 | Step-by-step record creation wizard | surface | create record individually; create records via import; create record through step-by-step wizard; create record by cloning an existing record (all P-1/L-1) | 4 | **L** |
| A075 | Record cloning | behaviour | create record individually; import records; create record through wizard; clone existing record (all P-1/L-1) | 4 | **L** |
| A079 | Draft saving of incomplete records | behaviour | save incomplete record as Draft (P-1.1/L-1.1) | 1 | **S** |
| A084 | Record edit form with finalise action | surface | edit record attributes manually (P-5/L-5); edit record attributes via import (P-5/L-5); finalise record (P-4/L-4; the name token "finalise action" is covered here); create data (I-1, not otherwise covered); share data (I-1). I-1 "manage" is covered by the edit tasks | 5 | **L** |
| A099 | Record status management | behaviour | view record status (P-10/L-9); set record status by user (P-10 manage, P-10.2); set record status by system (P-10.2/L-9.2); view identifier status (P-10 "or GIN"); set identifier status (P-10 "manage ... or GIN"); determine whether a hierarchy status change affects other records (P-11.5); notify user of affected hierarchy records (P-11.5); create data (I-1); share data (I-1). I-1 "manage" is covered by P-10 | 9 | **XL** |

### N16 — Record views and actions

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A090 | Record list view with filter and sort | surface | view all records (P-7/L-6); filter records (P-7/L-6); sort records (P-7/L-6); edit a selected record (P-7.1); export a selected record (P-7.1); apply the same action to multiple selected records (P-7.2). "etc." not counted | 6 | **L** |
| A091 | Single-record action menu | surface | edit a selected record; export a selected record (P-7.1/L-6.1). "etc." not counted | 2 | **M** |
| A092 | Multi-record bulk action | behaviour | select multiple records; apply the same action to all selected records (P-7.2/L-6.2) | 2 | **M** |
| A093 | Identifier list view with filter and sort | surface | view all identifiers; filter identifiers; sort identifiers (P-8/L-7) | 3 | **M** |

### N17 — Duplicate handling

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A094 | Duplicate detection engine | behaviour | detect potential duplicate records (name token, no obligation covers the verb); view potential duplicate records (P-9/L-8); remove duplicate (P-9/L-8); edit duplicate (P-9/L-8); alert user of possible duplicates during creation (P-9.1); run duplicate report for completed records (P-9.1) | 6 | **L** |
| A095 | Duplicate review and resolution screen | surface | view potential duplicate records; remove duplicate; edit duplicate (P-9/L-8) | 3 | **M** |
| A097 | Creation-time duplicate alert | surface | respond to possible-duplicate alert during creation; run duplicate report for completed records (P-9.1/L-8.1) | 2 | **M** |
| A098 | Duplicate report for completed records | surface | respond to possible-duplicate alert during creation; run duplicate report for completed records (P-9.1/L-8.1) | 2 | **M** |

### N18 — Hierarchy management

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A106 | Hierarchy tree editor (create, edit, view) | surface | create hierarchy; edit hierarchy; view hierarchy; import hierarchy; arrange hierarchy by drag-and-drop (P-11/L-10); change item level on a record while creating a hierarchy (P-11.4). "Each → case → pallet" is an example and not counted | 6 | **L** |
| A107 | Drag-and-drop hierarchy canvas | surface | create hierarchy; edit hierarchy; view hierarchy; import hierarchy; arrange hierarchy by drag-and-drop (P-11/L-10) | 5 | **L** |
| A108 | Hierarchy import handling | interface | import full records with hierarchy information (P-11.2/L-10.2); import hierarchy information for existing records (P-11.2/L-10.2); create hierarchy (P-11/L-10); edit hierarchy (P-11/L-10); view hierarchy (P-11/L-10) | 5 | **L** |
| A112 | Hierarchy status impact analysis | behaviour | detect status change of a record within a hierarchy; determine whether it affects the status of other records in the hierarchy; notify user (P-11.5/L-10.3) | 3 | **M** |
| A114 | Hierarchy export and print rendering | surface | export hierarchy; print hierarchy (P-12/L-11). "User-friendly format" names no format | 2 | **M** |
| C15 | Hierarchy integrity check (cycles, orphans) | behaviour (derived) | check hierarchy for cycles; check hierarchy for orphans (name tokens; trigger A129, A106) | 2 | **M** |

### N13 — direct leaf

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A121 | Record ownership transfer | behaviour | transfer record ownership to another member (P-18/L-14) | 1 | **S** |

**XL coarseness findings (M10):**
- **A081:** it bundles identifier assignment with the whole NFR-16 approval, validation and verification workflow.
- **A099:** it bundles record status with identifier status and hierarchy impact notification.
- **A100:** it bundles identifier status outcomes with record status and the display of reuse dates and rules.

## 3. Special counts

A9 is not crossed onto any element in this batch. G2m, G3m and G4m are crossed onto A080, A083 and A087.

| id | entity kinds that must be loaded from the systems being replaced | count | band |
|---|---|---|---|
| A080 | **licensed prefix:** each registry is per prefix, so no identifier can be held or auto-assigned until the prefixes members already hold exist; prefixes are licensed outside the system, not created in it at run time. **GIN:** members' GINs are already in use on real products; creating them fresh at run time would let auto-assign re-issue numbers that are taken. **LN:** same reason, for locations. | 3 | **M** |
| A083 | none. A preference can be set by the user at run time, and nothing in the covered obligations says the old systems hold one. | 0 | no band (count below S) |
| A087 | **prior-identifier reference:** a reference is made only when a number changes, so changes that happened in the old systems before cutover can never be captured at run time. | 1 | **S** |

## 4. Unsizeable elements

None. C15 is derived and has no covered obligation, but its name lists two named checks, so it can be counted.

## 5. Doubts

**Reading-wide doubts**
1. **R2, GIN and LN counted as one:** counting GIN and LN as different objects would double every enumeration. A091, A092, A104, A114, A112 and A093 would move up a class or more; the L elements would go to XL.
2. **R3, the I-1 verbs:** without them, A073 stays L (4), A081 stays XL (12), A084 drops to M (3) and A099 drops to L (7 → still XL; see 13).
3. **R1, obligations shared between siblings counted in full:** if each element were limited to what its name covers:
   - A003 would be M (2);
   - A004 would be S (1, calculate check digit);
   - A074, A075, A102 and A107 would be S;
   - A097, A098 and A108 would each lose items: A097 and A098 go to S, A108 to M (2 import operations).

   A003 and A004 have identical coverage sets, so their enumerations are identical.

**Per-element doubts**
4. **A003:** "prefix (6–9 digits)" could be read as a separate length rule, +1 (6, still L).
5. **A004:** "calculate check digit" could be read as separate from "assign check digit", +1 (6, still L).
6. **A073:** "required and non-required attributes that may vary by industry" could be read as a task (fill in the industry attribute set), +1, giving **XL**.
7. **A074 and A075:** the four creation channels could be read as one action or task, "create record", giving **S**.
8. **A080:** the identifier-to-record assignment (P-2) could be read as its own entity kind, +1, giving **L**. For the special count, nothing in A080's own coverage mentions the old systems; that they are being replaced is stated only in I-1, which A080 does not cover.
9. **A081:** "opt for auto-assign or manual" (P-2.1) and "set a preference" (P-3) could be one action, −1 (still XL).
10. **A082:** "hold out from auto-assign" could be read as the purpose of reserving rather than an action, −1, giving **S**.
11. **A083:** separate GIN and LN preferences (P-3 and L-3) could be two entity kinds, +1, giving **M**. The special count of 0 falls below every band; the script will receive no class for the G2m/G3m/G4m items on A083.
12. **A087:** for the special count, nothing says the old systems hold number-change references. If they don't, the count is 0.
13. **A099:** the actor split (set by user / set by system) and the "or GIN" identifier actions are arguable. Without both, the count is 6, giving **L**.
14. **A100:** splitting the actors (system / user) would add 2 (13, still XL). "Currently Reserved, In Use, For Reuse, Available" suggests the list is open-ended. L-9.1 names no LN statuses, and I applied the GIN list under R2.
15. **A090:** "select individual records" could count as a task of its own, +1, giving **XL**.
16. **A092:** "select multiple records" could be read as a screen step rather than a behaviour, −1, giving **S**.
17. **A094:** "detect" could be read as already covered by "potential duplicate records", −1 (5, still L).
18. **A106:** "manually" could count as a creation channel of its own, +1, giving **XL**.
19. **A108:** drag-and-drop could count as a named channel (P-1), +1 (6, still L).
20. **A112:** "detect status change" and "determine effect" could be one action, −1 (2, still M).
21. **C15:** trigger A129 is outside this batch, so any content it adds to C15 can't be seen here.

**Contradictions between obligations (named, not resolved)**
22. **L-2.1 against I-3 and L-2.2:** LNs "automatically assigned from a shared pool" in some industries conflict with I-3 (an LN is formed from the company's own licensed prefix) and with L-2.2 (the licensee can opt for manual). I kept the stated reading: all three are counted.
23. **P-3 against P-2.1:** a standing preference for the assignment mode sits beside a choice made during each creation, and nothing says which one wins.
24. **P-10.1:** the user or the system assigns GIN status, yet GIN status "may update to reflect record status". Nothing says which wins when they disagree.
25. **P-2.1 against L-2.2:** for GINs any "user" chooses the assignment mode; for LNs only the "prefix licensee" does.

## 6. Closure violations

This batch shows only part of the tree; any of these may exist outside N13. None is sized.

1. **Shared LN pool store:** it would hold the industry shared pools that L-2.1 auto-assigns from. A080 is per prefix only.
2. **Store for industry attribute-set definitions:** it would define the industry-varying required and non-required attributes (P-1/L-1) that A073's name says drive the form.
3. **Hierarchy relationship store:** it would hold parent-child links and record item levels (P-11, P-11.4, L-10). N18 has no store.
4. **Record store with status:** it would hold the records and their statuses (P-10.2/L-9.2). No store for records appears in N13.
5. **Reuse-rule configuration:** it would hold the industry- and product-type-specific out-of-use and reuse rules (P-10.3/L-9.3).
6. **Record import interface:** it would cover creating and editing records via import (P-1/L-1, P-5/L-5). The only import element in the batch is A108, which handles hierarchies.
7. **Approval workflow surface:** it would be the screen for submitting, reviewing, rejecting and approving records (NFR-16). Only the behaviour A081 covers that obligation.

## 7. Instrument readings

**Engine: Hotyn-D 2.0**

- Elements received: 37 (31 leaves or derived elements + 6 aggregates: N13–N18)
- Elements sized: 31
- Size-class distribution: S 4 · M 12 · L 12 · XL 3
- Statement kinds: compliance 0 · behavioural 0 (no statement elements in the batch)
- Special counts reported: 3 (A080 M, A083 no band, A087 S)
- Unsizeable count: 0
- Doubts count: 25 (21 enumeration doubts + 4 contradictions)
- Closure violations count: 7
- XL coarseness findings (M10): 3 (A081, A099, A100)