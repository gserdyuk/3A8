<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-15. Not part of the sensor's reply.
run: 59 · reading: HD59-A2 · engine (sensor's own stamp): Hotyn-D 2.0 (title line: # Hotyn-D 2.0: size classes for the N02 subtrees of HM57-1 (as crossed by Hotyn-W 1.2); §7 line: **Engine: Hotyn-D 2.0**) ·
subagent: 3a8:work-estimator (plugin) · model (launch record): the harness's `opus` alias (the launch tool offers no
`claude-opus-5` value); the transcript's model attachment and the assistant turn record `claude-opus-5` ·
batch A, repeat 2: subtrees N02 of the run 58 work model (Hotyn-W 1.2 × HM57-1), 30 sized elements ·
prompt prompt_size_A.md, 18 619 chars, md5 6b68546221905104e660b48b5f26470c (generated by make_sizing_prompts58.py; both repeats of
the batch received this file; the transcript's prompt record equals the file byte for byte, final newline included) ·
tool_uses: 0 · duration 345 312 ms · subagent tokens 36 944 ·
orchestrator process CLAUDE_CODE_MAX_OUTPUT_TOKENS=128000.
TRANSIT: one turn, one API message (msg_011Cf5t14BKiLHatA2HDRLu2), thinking (content empty in the transcript) then one text block of 17 797 chars; stop end_turn; output tokens 28 131.
No continuation message was injected ("Output token limit hit" occurs 0 times in the transcript). No other
message reached the agent.
PROVENANCE OF THIS TEXT: extracted by script (build_raw59.py) from the harness's per-agent transcript
(`~/.claude/projects/<project>/<session>/subagents/agent-<id>.jsonl`): the single assistant text block after the
prompt, with nothing inserted or removed. The completion notification also carried a reply text; it was not used
as the source. No seam. -->

# Hotyn-D 2.0: size classes for the N02 subtrees of HM57-1 (as crossed by Hotyn-W 1.2)

## 1. Contamination check

**Clean. Classification went ahead.**

- **Pasted input:** I checked every number and money-related phrase. The numbers are record volumes and their growth (NFR-2), user counts and the concurrency share (NFR-7), a browser version (NFR-13), a standard version (NFR-21), a growth-horizon year, and requirement or element ids. None of these is effort, a rate, a price, a budget, a project duration, a team size, a prior estimate or a rate-table row. "At no cost to X-Customer" (NFR-5) says who pays; it contains no figure. The year states when growth is expected, not how long the project runs.
- **Ambient harness context was present and has been quarantined:** git status, recent commit subjects, a memory index and environment details. None of it was used.
- I read no files.

## 2. Element sizing table

### Subtree N02: statements directly under N02

| id | name | class · kind | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A005 | Architecture design: enterprise alignment, maintainability, growth scalability | statement · **compliance**, from "aligned with the existing enterprise architecture, maintainable by X-Customer resources" | 1 system constrained: the application architecture (I-4). The name tokens alignment, maintainability and scalability are properties, not components; the existing enterprise architecture is what it is measured against, not something constrained. | 1 | **S** |
| A006 | Future-expansion design provisions (further data types, field-level sharing) | statement · **compliance**, from "while that expansion itself stays out of scope" | 2 components constrained: the data model (must accept "data other than GIN or LN"), the sharing-privilege mechanism (must reach "field level") | 2 | **M** |
| A145 | Data volume scalability design | statement · **behavioural**, from "data latency is currently overnight and must support near-real-time updates" | 2 components constrained: transactional databases, non-transactional databases (the data marts). I read "Data marts (non-transactional)" and "non-transactional databases" as the same component. | 2 | **M** |
| A149 | Technical standards conformance design | statement · **compliance**, from "Adherence to X-Customer technical standards" | 4 components constrained: source code (coding), databases (connection and naming), integrations (integration), technology and product selection (approved technologies and products) | 4 | **M** |
| A150 | Standards deviation impact analysis procedure | statement · **compliance**, from "a deviation requires an impact analysis … by the project core team" | 4 components whose deviation the procedure governs: source code, databases, integrations, technology and product selection. The four analysis dimensions (technical, operational, support, financial) also give 4. | 4 | **M** |
| A151 | Cloud-readiness design provisions | statement · **compliance**, from "The architecture is designed to be cloud-ready" | 1 system constrained: the architecture | 1 | **S** |
| A153 | User volume and concurrency capacity design | statement · **behavioural**, from "[…] of them concurrent" | 2 components constrained: the web-based application (user volume, concurrency, standard frameworks), the client side ("no client-side software installation") | 2 | **M** |

### Subtree N03: Data stores

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A001 | Shared domain data model (companies, users, prefixes, records, identifiers) | store | 5 entity kinds: companies (name token; also named in "company prefixes"), users (name token; I-6 "members"), prefixes (I-1, NFR-2), GIN records (I-1, NFR-2), LN records (I-1, NFR-2). The name token "identifiers" is not counted again (P-2): I-1 already names the identifiers, Global Item Numbers and Location Numbers. | 5 | **L** |
| A012 | X-Customer reference data set (prefix registry, controlled groups) | store | 2 entity kinds: prefix registry entries (name token plus I-1 "prefixes"), controlled groups (name token, not covered by any obligation, so counted). "X-Customer-owned data" and "data published by data owners" (I-11) name no kind (P-5). GIN and LN in I-1 are data members create, not X-Customer reference data. | 2 | **M** |
| A076 | Industry configuration store | store | 3 entity kinds: industries (P-1, L-1 "may vary by industry"), attribute definitions for GIN records (P-1), attribute definitions for LN records (L-1). I read "required and non-required" as a property of each attribute definition per industry, not as a separate kind. | 3 | **M** |
| A089 | Binary asset store for images | store | 1 entity kind: product images (P-6) | 1 | **S** |
| A143 | Non-transactional read store | store | 3 entity kinds: company prefix records, GIN records, LN records (NFR-2) | 3 | **M** |
| A144 | Near-real-time feed to read store | behaviour | 1 action: propagate updates to the read store in near real time (NFR-2 "must support near-real-time updates"). The name token "feed" is the same action (P-2). | 1 | **S** |
| C10 | Industry configuration maintenance screens | surface | 3 user tasks, reading the name with trigger A076: maintain industries, maintain GIN attribute definitions per industry, maintain LN attribute definitions per industry | 3 | **M** |

### Subtree N04: Service and API layer

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A034 | 3scale analytics API client | interface | 5 operations consumed: retrieve usage by hour, by day, by week, by month, by year (G-7.2). "3scale" names the source system, not a protocol or auth concern, so it is not counted. | 5 | **L** |
| A059 | Record import and export API | interface | 5: import GIN records from an external system (P-19), import LN records from an external system (L-15), export records (G-12.1), REST protocol (NFR-4, "REST (preferred) or SOAP" as one protocol concern), API-management compatibility (NFR-4, currently 3scale). The file formats in G-12.1 (Excel, CSV, Numbers, XML, tab-delimited, iDocs) sit alongside "through the API" in the same list, so they are other routes and not operations of this API. | 5 | **L** |
| A140 | Access Data REST API | interface | not sized; see section 4 | — | — |
| A141 | Member API registration in 3scale | interface | 2: register member API access in the API-management solution (name token, already covered by NFR-4 "available to members and compatible with an API management solution", counted once per P-2), REST protocol (NFR-4) | 2 | **M** |
| A148 | Internal REST service layer | behaviour | 2 actions: integrate functionality across the suite, integrate data across the suite (NFR-4, "instead of direct database access or copying"). "Instead of" is a prohibition, not an action. | 2 | **M** |
| C14 | API caller identity mapping from 3scale credentials | behaviour | 2 actions, reading the name with triggers A140, A059, A007: read the caller's 3scale credentials, map the caller to an application identity | 2 | **M** |

### Subtree N05: Web client

| id | name | class · kind | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A002 | Unified application shell with Product, Location, Access Data areas | surface | 5 user tasks: create data (I-1), manage data (I-1), share data (I-1), perform Access Data functions via the web (D-7), use any permitted function regardless of module (I-6). The name tokens for the Product, Location and Access Data areas are covered by I-2's three modules (P-2), and I-2 names architecture, not a user task. NFR-7 names no task. | 5 | **L** |
| A019 | Role-driven UI composition | behaviour | 1 action: present the UI composed by user role (G-2). The name token "composition" is the same action (P-2). | 1 | **S** |
| A152 | Standard web framework foundation (no client install) | statement · **behavioural**, from "[…] of them concurrent" (P-4: run-time wins over the compliance obligations NFR-13 and NFR-21) | 7 systems or components constrained: the web application UI (NFR-7 patterns, frameworks, components; NFR-21 WCAG), the client workstation (NFR-7 no client-side installation), Internet Explorer, Chrome, Firefox, Safari, Edge (NFR-13) | 7 | **L** |
| A163 | Cross-browser compatibility | statement · **compliance**, from "the current and previous versions of Chrome, Firefox, Safari and Edge" | 5 systems constrained: Internet Explorer, Chrome, Firefox, Safari, Edge | 5 | **L** |
| A171 | WCAG 2.0 Level A conformance | statement · **compliance**, from "WCAG 2.0 Level A compliance" | 1 component constrained: the web application UI | 1 | **S** |
| C13 | User-facing error handling | behaviour | 1 action, reading the name with trigger A002: handle errors shown to the user. "User-facing" says where the handling happens; it is not a separate verb. | 1 | **S** |

### Subtree CN01: Background processing

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| C05 | Background job scheduler | behaviour | 1 action, reading the name with triggers A032, A045, A104: schedule background jobs | 1 | **S** |
| C08 | Asynchronous import job queue with progress status | behaviour | 3 actions, reading the name with triggers A051, A145: enqueue import jobs, run queued import jobs asynchronously, report import progress status | 3 | **M** |
| C12 | Search index maintenance from read-store feed | behaviour | 2 actions, reading the name with triggers A133, A144: consume read-store feed changes, update the search index | 2 | **M** |
| C16 | Job run log and retry | behaviour | 2 actions, reading the name with triggers C05, C08: log job runs, retry failed jobs | 2 | **M** |

## 3. Special counts

A9 is not on any element in this batch, so no special count applies for it. G2m/G3m/G4m are on four stores.

| id | activities | enumeration: entity kinds loaded from the predecessor applications, with why each must arrive rather than be created at run time | count | class |
|---|---|---|---|---|
| A001 | G2m, G3m, G4m | **prefixes**: NFR-2 gives current company-prefix record volumes, so they exist today and have already been allocated. **GIN records**: NFR-2 gives current volumes; these are members' existing records, which I-1's replacement must carry over. **LN records**: same reason, from NFR-2 and I-1. **companies**: they own the existing "company prefixes"; a prefix cannot be loaded without its owner. **users**: I-1 replaces "the applications currently available to members", so existing members must be able to work on day one. | 5 | L |
| A012 | G2m, G3m, G4m | **prefix registry entries**: I-1 replaces the current applications that members already use to manage prefix data; prefixes already allocated cannot be recreated at run time without breaking existing numbers. | 1 | S |
| A076 | G2m, G3m, G4m | not countable; see section 4 | — | — |
| A089 | G2m, G3m, G4m | not countable; see section 4 | — | — |

## 4. Unsizeable elements

| id | what could not be counted | what the declaration is missing |
|---|---|---|
| **A140** Access Data REST API | Element class: **unsizeable, model defect (M10)**. No class assigned. | The only operations its coverage names are "all Access Data functions" (D-7). That is a catch-all with no named members (P-5, P-6). The Access Data functions themselves are not in A140's coverage. Two protocol concerns can be counted (REST, API-management compatibility), but sizing on those alone would be a guess that ignores the operations. |
| **A076** Industry configuration store | **Special count only** (G2m/G3m/G4m): unsizeable, M10. The element's own class (M) stands. | P-1 and L-1 describe creating records in the new system. Neither names a predecessor application or data that exists today, so no entity kind can be shown to arrive from a predecessor. Derived surface C10 also shows these kinds can be created at run time. |
| **A089** Binary asset store for images | **Special count only** (G2m/G3m/G4m): unsizeable, M10. The element's own class (S) stands. | P-6 ("Upload product images and add them to records") says nothing about images that exist in a predecessor application. |

## 5. Doubts

**Arguable enumeration items**

1. **A001 "identifiers"**: if GIN/LN identifiers are a kind separate from their records, the count goes 5 → 6 (still L).
2. **A001 "permissions"** (I-6 "based on permissions"): counted as a kind, 5 → 6. Together with doubt 1 the count is 7, which would be **XL**.
3. **A001 special count, users**: members might re-register or come from an identity source outside this store, so 5 → 4 (still L).
4. **A001 special count, companies**: they are named only as qualifiers in "company prefixes", so 5 → 4. Together with doubt 3 the count is 3, which would be M.
5. **A012 GIN and LN** (I-1): if counted as kinds of this store, 2 → 4, which would be **L**.
6. **A012 special count, controlled groups**: if X-Customer-owned groups are read as already existing, 1 → 2, which would be M.
7. **A076 required/non-required designation per industry**: as a separate kind, 3 → 4, which would be **L**. Reading GIN and LN attribute definitions as one kind gives 3 → 2 (still M).
8. **A089 image-to-record attachment** ("add them to records"): as a second kind, 1 → 2, which would be M.
9. **A002 D-7**: "perform Access Data functions" may be part of "share data", so 5 → 4 (still L). Counting the three module areas as navigation tasks gives 5 → 8, which would be **XL**.
10. **A019 "based on user role"**: read as a separate action (resolve the user role), 1 → 2, which would be M.
11. **A034**: read as one operation with five granularity parameters, 5 → 1, which would be **S**. Adding 3scale as an auth concern gives 2, which would be M.
12. **A059 file formats**: counted as payload formats of this API (six formats), 5 → 11, which would be **XL**.
13. **A059 import**: GIN and LN import as one operation, 5 → 4, which would be **M**. Not counting 3scale compatibility as a concern gives the same move.
14. **A141**: if registration in 3scale is per Access Data function, the count depends on the unnamed members of D-7 and cannot be enumerated.
15. **A144 per-kind propagation** (company prefixes, GIN, LN): 1 → 3, which would be M.
16. **A148 "REST APIs available to members"**: as an action (expose to members), 2 → 3 (still M).
17. **A005 per-module constraint**: applying it to each of the three modules (I-2 is not in A005's coverage) gives 1 → 3, which would be M.
18. **A005 kind**: "scalable … given projected member and application growth" could be read as run-time behaviour, which would make it behavioural.
19. **A006 "the final design"**: read as one constrained thing, 2 → 1, which would be S.
20. **A145 near-real-time update path**: counted as a component, 2 → 3 (still M). Also counting the three volume-bearing data sets gives 6, which would be **L**.
21. **A149 standards instead of components**: coding, DB connection, DB naming, integration, technologies, products gives 4 → 6, which would be **L**.
22. **A152 browser versions**: counting "current and previous" versions separately gives 7 → 11, which would be **XL**. Leaving out the client workstation gives 6 (still L).
23. **A163 browser versions**: counting per version gives 5 → 9, which would be **XL**.
24. **A153 client side**: if limited to the capacity aspect in its name, 2 → 1, which would be S.
25. **C05 one schedule per trigger** (A032, A045, A104): 1 → 3, which would be M.
26. **C08 "run queued jobs"**: read as part of the queue, 3 → 2 (still M).
27. **C10**: read as one task ("maintain industry configuration"), 3 → 1, which would be S. Adding the designation task gives 4, which would be L.
28. **C12**: read as one action ("maintain index from feed"), 2 → 1, which would be S.
29. **C13 "present error to user"**: as a separate action, 1 → 2, which would be M.
30. **C14**: read as one action ("map identity from credentials"), 2 → 1, which would be S.

**Tensions in the model (not resolved)**

31. **A001 and A012** both take responsibility for prefixes as an entity kind. The model does not say which store owns them.
32. **A148** is named "Internal", but its coverage (NFR-4) includes "REST APIs available to members". The name and the coverage disagree.
33. **A076 and A089** carry G2m/G3m/G4m even though their declarations name no data from a predecessor (see section 4).

I found no contradiction between the obligation texts themselves. NFR-4 "REST (preferred) or SOAP" is an open choice, and I read it as one protocol concern.

## 6. Closure violations

1. **A145: performance and availability testing (A9) is missing.** A145 is a behavioural statement whose obligation (NFR-2) names measurable targets: projected record volumes and near-real-time data latency. A9 would test those targets. Not sized.
2. **A153: A9 is missing.** Its obligation (NFR-7) names user volume and concurrency targets. A9 would test the concurrency and user-volume capacity. Not sized.
3. **A152: A9 is missing.** Its obligation NFR-7 names the same capacity targets, which is what makes it behavioural. A9 would test those targets. Not sized.

These three were judged from this batch only; A9 might sit elsewhere in HM57-1, outside the N02 subtrees.

## 7. Instrument readings

**Engine: Hotyn-D 2.0**

- Elements received: 35 (5 aggregates, not sized; 30 sizeable elements)
- Elements sized: 29
- Size classes: S 8 · M 15 · L 6 · XL 0
- Statement kinds: compliance 7 · behavioural 3
- Unsizeable: 1 element (A140), plus 2 special counts (A076, A089)
- Special counts given: 2 (A001, A012)
- Doubts: 33 (30 arguable items, 3 model tensions)
- Closure violations: 3
- Ambient harness context: present and quarantined
- Files read: none