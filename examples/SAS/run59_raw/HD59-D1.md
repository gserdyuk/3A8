<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-15. Not part of the sensor's reply.
run: 59 · reading: HD59-D1 · engine (sensor's own stamp): Hotyn-D 2.0 (title line: # Hotyn-D 2.0 sizing: batch N11, N12 of HM57-1 (as crossed by Hotyn-W 1.2); §7 line: - Engine: **Hotyn-D 2.0**) ·
subagent: 3a8:work-estimator (plugin) · model (launch record): the harness's `opus` alias (the launch tool offers no
`claude-opus-5` value); the transcript's model attachment and the assistant turn record `claude-opus-5` ·
batch D, repeat 1: subtrees N11, N12 of the run 58 work model (Hotyn-W 1.2 × HM57-1), 24 sized elements ·
prompt prompt_size_D.md, 18 790 chars, md5 be5ce765dfbf45248960baaf7b45c2d5 (generated by make_sizing_prompts58.py; both repeats of
the batch received this file; the transcript's prompt record equals the file byte for byte, final newline included) ·
tool_uses: 0 · duration 325 901 ms · subagent tokens 35 696 ·
orchestrator process CLAUDE_CODE_MAX_OUTPUT_TOKENS=128000.
TRANSIT: one turn, one API message (msg_011Cf5tbNAkkWTErZg2KXcnP), thinking (content empty in the transcript) then one text block of 17 367 chars; stop end_turn; output tokens 26 891.
No continuation message was injected ("Output token limit hit" occurs 0 times in the transcript). No other
message reached the agent.
PROVENANCE OF THIS TEXT: extracted by script (build_raw59.py) from the harness's per-agent transcript
(`~/.claude/projects/<project>/<session>/subagents/agent-<id>.jsonl`): the single assistant text block after the
prompt, with nothing inserted or removed. The completion notification also carried a reply text; it was not used
as the source. No seam. -->

# Hotyn-D 2.0 sizing: batch N11, N12 of HM57-1 (as crossed by Hotyn-W 1.2)

## 1. Contamination check

**The input is clean.** Neither input contains an effort figure, person-day value, rate, price, budget, duration, team size, prior estimate or rate-table row. The codes A2, A3, A4, A10, D4, G2m, G3m, G4m, K1, K2 and K3 name activities; they are not figures. "Catalogue 1.4", "1.3" and "Hotyn-W 1.2" are version labels.

**Other context was present and set aside.** The harness added a git status, recent commit subjects, a memory index and user context. None of it is input. I did not use it, and it does not stop the run. The memory index mentions a rate table as an idea but gives no rows or figures. I worked only from the pasted Inputs 1 and 2.

No files were read.

## 2. Element sizing table

**One reading applies to every element.** Many obligations are shared by several elements. From a shared obligation I count only the actions that fit the responsibility named in this element's name. Actions the name gives to a different responsibility are left out, and each one left out is listed in Doubts. Examples: a different actor, a different direction (import vs. export), or another path (manually vs. via import). Name tokens follow P-2 and multi-outcome lists follow P-3.

### Subtree N11: Import and export (aggregate, not sized)

| id | name | class (kind) | enumeration | count | size class |
|---|---|---|---|---|---|
| A051 | Record import pipeline (parse, map, validate, load) | behaviour | 1 parse import files in the listed formats (G-12.1; name token "parse" not recounted); 2 map imported fields to record attributes (name token "map"; no covered obligation names it, so counted per P-2); 3 validate records on import (G-12.4; name token "validate" not recounted); 4 load imported records (G-12; name token "load" not recounted); 5 create GIN records via import (P-1); 6 create LN records via import (L-1); 7 edit record attributes via import (P-5, L-5); 8 create a GIN hierarchy via import (P-11); 9 edit a GIN hierarchy via import (P-11); 10 import full records with hierarchy information (P-11.2, L-10.2); 11 import hierarchy information for existing records (P-11.2, L-10.2); 12 import data created and managed in an external system (P-19, L-15) | 12 | **XL** (M10 coarseness finding) |
| A052 | Record export pipeline (serialise, deliver) | behaviour | 1 serialise records into a listed export format (G-12, G-12.1, D-5; name token "serialise"); 2 deliver the export output, including through the API (G-12.1; name token "deliver"); 3 export hierarchies in a user-friendly format (P-12); 4 print hierarchies (P-12); 5 export prefix, GIN and LN data from the Access Data module (I-8) | 5 | **L** |
| A053 | Excel import and export adapter | interface | 1 import records from an Excel file (G-12.1); 2 export records to an Excel file (G-12.1, D-5); 3 PC format (G-12.2); 4 Mac format (G-12.2) | 4 | **M** |
| A054 | CSV import and export adapter | interface | 1 import records from a CSV file (G-12.1); 2 export records to a CSV file (G-12.1, D-5); 3 PC format (G-12.2); 4 Mac format (G-12.2) | 4 | **M** |
| A055 | Numbers import and export adapter | interface | 1 import records from a Numbers file (G-12.1); 2 export records to a Numbers file (G-12.1, D-5); 3 PC format (G-12.2); 4 Mac format (G-12.2) | 4 | **M** |
| A056 | XML import and export adapter | interface | 1 import records from an XML file (G-12.1); 2 export records to an XML file (G-12.1, D-5); 3 PC format (G-12.2); 4 Mac format (G-12.2) | 4 | **M** |
| A057 | Tab-delimited import and export adapter | interface | 1 import records from a tab-delimited file (G-12.1); 2 export records to a tab-delimited file (G-12.1, D-5); 3 PC format (G-12.2); 4 Mac format (G-12.2) | 4 | **M** |
| A058 | IDoc import and export adapter | interface | 1 import records from an IDoc (G-12.1); 2 export records to an IDoc (G-12.1, D-5); 3 PC format (G-12.2); 4 Mac format (G-12.2) | 4 | **M** |
| A060 | Record selection for import and export | behaviour | 1 select one or more records for import (G-12.3); 2 select one or more records for export (G-12.3, D-5) | 2 | **M** |
| A061 | Import validation result report | surface | 1 review the validation result of an import (G-12.4; name "result report") | 1 | **S** |
| A085 | Import update mode for existing records | behaviour | 1 update attributes of existing records via import (P-5, L-5); 2 import hierarchy information for existing records (P-11.2, L-10.2) | 2 | **M** |

### Subtree N12: Publish and subscribe (aggregate, not sized)

| id | name | class (kind) | enumeration | count | size class |
|---|---|---|---|---|---|
| A010 | Subscription entitlement model (viewable data, export options) | store | 1 subscription (I-9 "a client's subscription", G-13.5); 2 viewable-data entitlement (name token; I-9 "which data can be viewed"); 3 export-option entitlement (name token; I-9 "which export options are available") | 3 | **M** |
| A062 | Publication registry (what, to whom, attribute tier) | store | 1 publication, meaning the records published and the hierarchy level they are published at (name "what"; G-13, G-13.1); 2 sharing grant, meaning the target it is shared with: individual, user-defined group, controlled group or public (name "to whom"; G-13.4; P-17 and L-12 "permission records for publishing" read as this same kind); 3 attribute tier, basic or full (name "attribute tier"; G-13.2); 4 record group published together (G-13.3) | 4 | **L** |
| A063 | Published-data access evaluation | behaviour | 1 evaluate the consumer's subscription for viewing published data (G-13.5 "based on subscription"); 2 evaluate the owner's publication permission for the consumer (P-17, L-12 "permission records for publishing"; G-13) | 2 | **M** |
| A064 | Hierarchy-level publication | behaviour | 1 publish records at a chosen level in the hierarchy (G-13.1) | 1 | **S** |
| A065 | Basic and full attribute tiers | statement, **behavioural**. Justifying phrases: "publish (grant access to view) a basic or a full set of record attributes" (G-13.2) and "Request access to a basic or full record (subscribe to the record)" (D-2). P-4 applies: run-time wins over the configuration reading. | 1 publishing by the data owner (G-13.2 "The data owner can publish"); 2 view access granted to consumers (G-13.2 "grant access to view"); 3 access requests and record subscription (D-2 "Request access to a basic or full record (subscribe to the record)"); 4 the record attribute set (G-13.2 "set of record attributes") | 4 | **M** |
| A066 | Publication for a group of records | behaviour | 1 publish data for a group of records (G-13.3) | 1 | **S** |
| A067 | Sharing target selection (individual, group, controlled group, public) | behaviour | 1 share records with an individual; 2 share records with a user-defined group; 3 share records with a controlled group managed by X-Customer; 4 share records with the public (all G-13.4, counted per outcome by P-3; name token "selection" is covered by these four) | 4 | **L** |
| A068 | User-defined group management | behaviour | 1 manage user-defined groups (name token "management"; no covered obligation names managing groups, so counted per P-2); 2 share records with a user-defined group (G-13.4) | 2 | **M** |
| A069 | Controlled group management by X-Customer | behaviour | 1 manage controlled groups as X-Customer (G-13.4 "managed by X-Customer"; name token not recounted); 2 share records with a controlled group (G-13.4); 3 act on requests to be added to a controlled group (D-3) | 3 | **M** |
| A071 | Access request submission | behaviour | 1 request access to a basic record (D-2, G-13.6); 2 request access to a full record (D-2, G-13.6; P-3 outcome split); 3 check the consumer's subscription before the request (G-13.6 "based on subscription"); 4 send the request to the data owner (D-2.1) | 4 | **L** |
| A072 | Access request approval and rejection by owner | behaviour | 1 approve a view request (G-13.7, D-2.1); 2 reject a view request (G-13.7; P-3); 3 make the requested data available once approved (D-2.1 "must approve before the data becomes available") | 3 | **M** |
| A137 | Controlled group join request with X-Customer approval | behaviour | 1 request to be added to a controlled group (D-3; name token "join request" not recounted); 2 approve the join request as X-Customer (name token "X-Customer approval"; no covered obligation names it, so counted per P-2) | 2 | **M** |
| C09 | Publication overview and revocation | surface (derived, trigger A062) | 1 review the owner's publications, meaning what, to whom and at which attribute tier (name token "overview" read with trigger A062); 2 revoke a publication (name token "revocation") | 2 | **M** |

**Coarseness finding (M10): A051 is XL.** It is classed XL and also reported as a probable coarseness finding about the product model. Its 12 counted actions cover creating records, editing attributes, importing hierarchies and importing from an external system, all within one pipeline.

## 3. Special counts

Only activities G2m, G3m and G4m appear on stores in this batch. No element here carries A9.

| element | driver | enumeration, with why each kind must come from the predecessor | count | class |
|---|---|---|---|---|
| A010 | entity kinds loaded from predecessor applications | 1 subscription: I-9 says a client's subscription decides what the client can view and export. Existing clients' subscriptions must be in place at cutover. If they were re-created at run time, every existing client would have no viewing or export rights until then. The two entitlement kinds are not counted: I-9 says they follow from the subscription, so they come with the subscription definition rather than being migrated separately. | 1 | **S** |
| A062 | entity kinds loaded from predecessor applications | 1 publication and 2 sharing grant: I-1 says the solution replaces the applications members use today to "share data". Shares members already hold must survive the replacement; otherwise every owner would have to re-enter them. Attribute tier is not counted: G-13.2 fixes two tiers, basic and full, which the system defines itself. The tier chosen for each publication travels as part of the migrated publication. | 2 | **M** |

## 4. Unsizeable elements

None. Every sized element's declared content and covered obligations support an enumeration.

## 5. Doubts

**Readings that could be one or more items longer or shorter:**

1. **A051:**
   - Items 5 and 6 could be merged, item 4 could be folded into 5 and 6, and items 8 and 9 overlap 10 and 11. The shortest honest reading is about 8, which is still XL.
   - "View a hierarchy ... via import" (P-11) is not counted because viewing via import names no action.
   - The manual, wizard and cloning paths in P-1, P-5 and P-11 are left out by the name.
   - L-10.2 is covered but its parent L-10 is not given, so creating or editing an LN hierarchy via import is not counted. It would add 2 if intended.
   - Items 7 and 11 are also carried by A085 on the same obligations.
2. **A052:**
   - Item 5 could merge into item 1 if "prefix data" is read as a record (4, still L).
   - Item 4, printing, could be read as outside export (4, still L).
   - I-8's "search for, view" are left out by the name. Counting them gives 7, which is XL.
3. **A053 to A058, all six the same:**
   - Importing from an external system (P-19, L-15) is counted as the import operation, not as its own operation. Counting it separately gives 5, which is L.
   - PC and Mac could be read as one cross-platform concern (3, still M).
   - A PC format for Numbers, or separate PC and Mac formats for XML or IDoc, may name nothing real. Dropping them gives 2, still M.
4. **A060:** D-5's export action is left out by the name. Counting it gives 3, still M.
5. **A061:** Correcting records that failed validation is not named, so it is not counted. Counting it gives 2, which is M.
6. **A085:** "Full records with hierarchy information" (P-11.2, L-10.2) is left out because the name says "for existing records". Counting it gives 3, still M.
7. **A010:**
   - "Client" (I-9) could count as a kind stored here (4, L).
   - D-5's "any format" could add an export-format kind.
   - In G-13, "multiple share/view functions" is read as an unnamed catch-all under P-5, not as a stated cardinality under P-6.
   - Special count: none of A010's covered obligations names a predecessor application. The migration reasoning relies on I-1, which only A062 covers. If the predecessor holds entitlements per client, the special count is 3 (M).
8. **A062:**
   - Attribute tier could be a property of a publication rather than an entity kind (3, M). The same applies to record group (3, M).
   - The permission record (P-17, L-12) could be a kind separate from the sharing grant (5, L).
   - User-defined and controlled groups are not counted here; see closure violation 2.
   - The prefix, GIN and LN records named in I-1 belong to the record stores.
   - Special count: record group could also be migrated (3, M).
9. **A063:**
   - "View published data within the Data Access module" (G-13.5, P-17) is left out as outside "evaluation". Counting it gives 3, still M.
   - P-17 could be read as the owner's act of setting permissions (+1). Adding both gives 4, which is L.
10. **A064:** "Any level" could imply publishing down to child records, but no such action is named. It is not counted; counting it gives 2, which is M.
11. **A065:** Items 1 and 2 could be one component, and item 4 could be data rather than a component. Either change gives 3, still M.
12. **A066:** Defining the group of records is implied but not named. Counting it gives 2, which is M.
13. **A067:** This could be read as one "select target" action with four values (1, S). Counting "selection" as well as the four shares gives 5, still L.
14. **A068:**
    - Item 2 is also carried by A067 on G-13.4. Dropping it gives 1, which is S.
    - "Management" names no sub-actions (such as creating a group or adding or removing members), so none are counted.
15. **A069:** Item 3 turns D-3's consumer-side request into the manager's handling of it. Dropping item 3 gives 2, still M. The request itself is also carried by A137.
16. **A071:**
    - Items 1 and 2 could be one request with a tier value (3, M).
    - Item 3 could be a condition rather than an action (3, M).
    - The owner's approve, reject and make-available actions (G-13.7, D-2.1) are left out by the name. Counting them gives 7, which is XL.
17. **A072:**
    - Item 3 could be a consequence of approval rather than its own action (2, M).
    - "The request is sent to the data owner" (D-2.1) could add receiving the request (4, L).
18. **A137:** Rejection of a join request is not named, so it is not counted. Counting it gives 3, still M.
19. **C09:** Revoking could be split by sharing target (revoke per target rather than per publication), but no split is named.

**Contradictions between obligations (named, not resolved):**

20. **G-13.5 vs. D-2.1 and G-13.6.** G-13.5 says a consumer can view published data based on subscription. D-2.1 says the owner must approve before the data becomes available. I proceeded as written: both hold, and A063, A071 and A072 are sized on that basis.
21. **I-9 vs. D-5.** I-9 says a subscription determines which export options are available. D-5 says records can be exported "to any format detailed in the General Requirements". I proceeded on both texts as written; A010 and A052 are sized on that basis.

## 6. Closure violations

I can only see this batch, so each of these may be met by an element in another subtree.

1. **No interface for "through the API".** G-12.1 names the API as an import and export channel, but the batch has only the six file-format adapters (A053 to A058). A052 carries API delivery only as one action; no interface element exposes API import or export.
2. **No store for user-defined and controlled groups and their members.** A068, A069 and A137 manage and join these groups (G-13.4, D-3), and A062 records only who a publication is shared with.
3. **No store for access requests and join requests.** G-13.6, G-13.7 and D-2.1 need a request that waits until the owner approves it. D-3 and A137 need the same for requests to join a controlled group.
4. **No surface for the owner to publish and share.** Publishing at a hierarchy level, choosing a tier, publishing a group and choosing a target (G-13.1 to G-13.4) have behaviour elements but no surface. C09 covers only overview and revocation.
5. **No surface for access requests.** Nothing lets a consumer submit a request (G-13.6, D-2) or lets the owner approve or reject it (G-13.7); A071 and A072 are behaviours only.

## 7. Instrument readings

- Engine: **Hotyn-D 2.0**
- Elements received: 26 (24 to be sized, plus the aggregates N11 and N12, which are not sized)
- Elements sized: 24
- Size-class distribution:

| S | M | L | XL |
|---|---|---|---|
| 3 | 16 | 4 | 1 |

  - S: A061, A064, A066
  - M: A010, A053 to A058, A060, A063, A065, A068, A069, A072, A085, A137, C09
  - L: A052, A062, A067, A071
  - XL: A051 (also an M10 coarseness finding)
- Statement kinds: compliance 0, behavioural 1 (A065)
- Special counts: 2 (A010 S, A062 M)
- Unsizeable: 0
- Doubts: 21 (19 enumeration doubts, 2 contradictions)
- Closure violations: 5