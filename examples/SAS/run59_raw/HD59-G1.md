<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-15. Not part of the sensor's reply.
run: 59 · reading: HD59-G1 · engine (sensor's own stamp): Hotyn-D 2.0 (title line: # Hotyn-D 2.0 sizing: subtrees N21, N22 of HM57-1 (as crossed by Hotyn-W 1.2); §7 line: **Engine: Hotyn-D 2.0**) ·
subagent: 3a8:work-estimator (plugin) · model (launch record): the harness's `opus` alias (the launch tool offers no
`claude-opus-5` value); the transcript's model attachment and the assistant turn record `claude-opus-5` ·
batch G, repeat 1: subtrees N21, N22 of the run 58 work model (Hotyn-W 1.2 × HM57-1), 23 sized elements ·
prompt prompt_size_G.md, 13 712 chars, md5 a616f912180e357a548a7d42e2fe26e5 (generated by make_sizing_prompts58.py; both repeats of
the batch received this file; the transcript's prompt record equals the file byte for byte, final newline included) ·
tool_uses: 0 · duration 291 766 ms · subagent tokens 30 587 ·
orchestrator process CLAUDE_CODE_MAX_OUTPUT_TOKENS=128000.
TRANSIT: one turn, one API message (msg_011Cf5uCqACCy6pKAs9rL7Rx), thinking (content empty in the transcript) then one text block of 13 549 chars; stop end_turn; output tokens 22 878.
No continuation message was injected ("Output token limit hit" occurs 0 times in the transcript). No other
message reached the agent.
PROVENANCE OF THIS TEXT: extracted by script (build_raw59.py) from the harness's per-agent transcript
(`~/.claude/projects/<project>/<session>/subagents/agent-<id>.jsonl`): the single assistant text block after the
prompt, with nothing inserted or removed. The completion notification also carried a reply text; it was not used
as the source. No seam. -->

# Hotyn-D 2.0 sizing: subtrees N21, N22 of HM57-1 (as crossed by Hotyn-W 1.2)

## 1. Contamination check

**Clean. Classification proceeds.**

- **Ambient context:** the harness added context before the task: a git status, recent commit subjects, a memory index and a user-identity note. I set all of it aside and did not use it as input. None of it is a rate-table row or a prior estimate of this batch.
- **Figures in the input:** NFR-8 has a service-level percentage. NFR-14 has response-time thresholds, a percentile and concurrent-user loads. These describe how the product must run. They are not effort, person-days, durations of work, team sizes, rates, prices or budgets. The A9 rule tells me to count them as targets, so they are not contamination. I do not repeat those numbers anywhere below.
- **Result:** no effort figure, rate, price, budget, prior estimate or rate-table row is present.

## 2. Element sizing table

**Reading used for every element (stated once):**
- **Full coverage:** I count every covered obligation in full, including clauses that sibling elements also cover. Dropping clauses would reshape coverage, which I may not do. Where this crosses a class boundary it is logged as a doubt.
- **Record kinds:** "prefix, GIN and LN" are treated as one object, *records*. The verb stays the same across the three kinds (doubt 1).

### Subtree N21: Access Data module

| id | name | class (kind) | enumeration | count | size |
|---|---|---|---|---|---|
| A011 | Public anonymous access profile (limited data, no export) | behaviour | search the limited data set as a non-member (I-10); deny export to non-members (I-10); share records with an individual (G-13.4); share records with a user-defined group (G-13.4); share records with a controlled group managed by X-Customer (G-13.4); share records with the public (G-13.4). Name tokens "limited data" and "no export" are already covered by I-10, so they are not counted again (P-2). | 6 | **L** |
| A070 | Published record viewer (basic or full) | surface | view a published basic record (D-4); view a published full record (D-4); view record hierarchies (D-4); search records (I-8); export records (I-8). The view clauses of G-13.5, P-17/L-12 and I-8 fold into the basic/full view tasks. | 5 | **L** |
| A133 | Search service over read store (prefix, GIN, LN) | behaviour | search records (D-1, I-8); search by field (D-1.1); run advanced search (D-1.2); filter results (D-1.2); view records (I-8); export records (I-8). Name tokens are covered by D-1; "read store" names no action. | 6 | **L** |
| A134 | Search screen with field search | surface | search records (D-1, I-8); search by field (D-1.1); run advanced search (D-1.2); filter results (D-1.2); view records (I-8); export records (I-8). Name token "field search" is covered by D-1.1 (P-2). | 6 | **L** |
| A135 | Advanced search and filter | surface | run advanced search (D-1.2); filter results (D-1.2) | 2 | **M** |
| A138 | Published hierarchy viewer | surface | view a basic record (D-4); view a full record (D-4); view record hierarchies (D-4); search records (I-8); export records (I-8) | 5 | **L** |
| A139 | Record print rendering | surface | print one or more records (D-6) | 1 | **S** |
| A142 | How-to-pay information page | surface | read how to pay for ad hoc data access (D-8) | 1 | **S** |

### Subtree N22: Operational properties

| id | name | class (kind) | enumeration | count | size |
|---|---|---|---|---|---|
| A154 | High-availability deployment design | statement (behavioural: "failover monitoring that notifies administrators of a failure") | failover process; hardware; software; failover monitoring (NFR-8) | 4 | **M** |
| A155 | Failover process and test procedure | statement (behavioural: "failover monitoring that notifies administrators of a failure") | failover process; hardware; software; failover monitoring (NFR-8). Name token "test procedure" is covered by "developed and tested". | 4 | **M** |
| A156 | Hardware and software needs specification | statement (behavioural: "failover monitoring that notifies administrators of a failure") | failover process; hardware; software; failover monitoring (NFR-8). Name token "specification" is covered by "needs identified". | 4 | **M** |
| A157 | Failover monitoring with administrator notification | behaviour | monitor for failover or failure (NFR-8); notify administrators of the failure (NFR-8) | 2 | **M** |
| A158 | Backup procedures | statement (compliance: "comply with X-Customer policies and procedures") | backup procedures; disaster recovery procedures (NFR-9) | 2 | **M** |
| A159 | Disaster recovery procedures | statement (compliance: "comply with X-Customer policies and procedures") | backup procedures; disaster recovery procedures (NFR-9) | 2 | **M** |
| A160 | Security design | statement (compliance: "Appropriate security configuration, processes and procedures") | application solution; security configuration; security processes; security procedures; security design; security components; security frameworks; security libraries; security tools (NFR-10) | 9 | **XL** (M10 coarseness signal) |
| A161 | Security configuration baseline | statement (compliance: "Appropriate security configuration, processes and procedures") | application solution; security configuration; security processes; security procedures; security design; security components; security frameworks; security libraries; security tools (NFR-10) | 9 | **XL** (M10 coarseness signal) |
| A162 | Security processes and procedures | statement (compliance: "Appropriate security configuration, processes and procedures") | application solution; security configuration; security processes; security procedures; security design; security components; security frameworks; security libraries; security tools (NFR-10) | 9 | **XL** (M10 coarseness signal) |
| A164 | Performance design against response-time targets | statement (behavioural: "Response times, 95% of the time under load") | login; navigation; saves; form generation; searches; API responses (NFR-14) | 6 | **L** |
| A166 | System error monitoring hooks | behaviour | detect system errors (NFR-17, name token "monitoring"); send a near-real-time event notification (NFR-17) | 2 | **M** |
| A168 | Business function failure detection hooks | behaviour | detect critical business-function failures (NFR-18, name token "detection"); send a near-real-time event notification (NFR-18) | 2 | **M** |
| A169 | Detailed application logging | behaviour | write detailed application log entries (NFR-19). "for troubleshooting and process verification" names purposes that people carry out using the log, not actions of this element. | 1 | **S** |
| A170 | Audit trail of all activity | store | audit trail entry (NFR-20) | 1 | **S** |
| C11 | Log and audit trail retention and purge | behaviour | retain logs; purge logs; retain audit trail; purge audit trail (derived; trigger A169, A170) | 4 | **L** |

## 3. Special counts

**A164, activity A9 (measurable targets from the content and NFR-14's text):**
- **Enumeration:** login response-time threshold at its stated concurrency; navigation threshold at its stated concurrency; transaction threshold (saves, form generation) at its stated concurrency; search threshold at its stated concurrency; API response threshold at its stated concurrency. The shared percentile is read as a qualifier of all five, not a target of its own.
- **Count and class:** 5, **L**.

**A170, activities G2m / G3m / G4m (entity kinds loaded from predecessor applications):**
- **Result:** nothing can be enumerated. NFR-20 describes an audit trail of activity "taking place at any point in the system". It names no predecessor application and no historical audit data.
- **Why no kind qualifies:** the one entity kind, the audit trail entry, is created at run time by activity in this system. Nothing in the declaration says why any kind must come from a predecessor.
- **Outcome:** no special-count class (see section 4).

## 4. Unsizeable elements

**No element is unsizeable.** All 23 received a class.

One special count has no support:
- **A170, G2m / G3m / G4m:** the declaration names no entity kind loaded from a predecessor application, so the migration driver has nothing to count. This is reported as a model defect (M10): either the declaration is missing the predecessor-sourced kinds, or the migration activities were crossed onto an element whose content does not support them. No class assigned.

## 5. Doubts

1. **Record kinds (A070, A133, A134, A138):** I treated prefix, GIN and LN as one object. Counting one verb per kind would push A133 and A134 to XL, and A070 and A138 to L or XL.
2. **Sibling overlap (A070, A133, A134, A138):**
   - I-8, D-1.2 and D-4 are each counted in full on every element that covers them.
   - If each element counted only the clauses its name serves: A070 becomes 3 (M), A138 becomes 3 (M), A134 becomes 2 (M), and A133 becomes 4 (L, unchanged).
3. **A011, sharing:** G-13.4 is read as four share targets. Reading it as the public target only gives 3 (M). "deny export" is a restriction and could be argued not to be an action; without it the count is 5 (L, unchanged).
4. **A070, subscription and publishing clauses:**
   - Counting "view published data based on subscription" (G-13.5) as its own task gives 6 (L, unchanged).
   - "Permission records for publishing" (P-17/L-12) has no verb, so no task is counted.
   - P-17 and L-12 have identical texts and are counted once.
5. **A133, A134, field search:** "several fields (e.g. name, city)" is an open list of examples and is counted as one action. Counting name and city as separate field searches gives 7 (XL) on both.
6. **A139:** "one or more records" is counted as one task. Reading single and multiple printing as two tasks gives 2 (M).
7. **A154–A156, NFR-8:** the constrained system itself ("Highly available") is implied, not named, so it is not counted. Counting it gives 5 (L). Dropping the failover process as a procedure rather than a component gives 3 (M, unchanged).
8. **A157, NFR-8 verbs:** "developed", "tested" and "identified" are delivery verbs, not actions of this behaviour, so they are excluded. Including them gives 5 (L). Counting run-time failover execution as its own action gives 3 (M, unchanged).
9. **A158, A159, NFR-9:** NFR-9 names no system or component. The two procedures are counted as the constrained artefacts. Reading "systems or components" strictly leaves nothing to count, which would make both elements unsizeable (M10).
10. **A160–A162, NFR-10:** two items are arguable, and removing either one alone drops the count to 8 (L):
    - "processes" and "procedures" may be near-synonyms;
    - "security design" may be a deliverable rather than a constrained thing.
11. **A164:** "transactions (saves, form generation)" is split into its two named members. Keeping it as one item gives 5 (L, unchanged).
12. **A164, A9 targets:** counting each time threshold and each concurrency load separately gives 10 (XL). Counting the percentile as its own target gives 6 (L, unchanged).
13. **A169:** reading "troubleshooting" and "process verification" as two distinct log streams gives 2 (M).
14. **A170:** "all activity" is an unnamed catch-all (P-5), so it contributes no entity kinds beyond the audit trail entry.
15. **C11:** reading retention and purge as two actions over logs and audit trail together gives 2 (M). There is also a tension, not resolved: purging the audit trail sits against NFR-20's "at any point in the system". I read "at any point" as a place in the system, not a length of time.
16. **A166, A168:** detection is carried by "when … occur" and the name token (P-2). If detection is not counted as a named action, both drop to 1 (S).
17. **Contradiction, not resolved:** G-13.4 gives the data owner a sharing action, and it is covered on A011 inside the Access Data module. I-8 says that module "contains no editing functionality". I proceeded on the stated coverage.

## 6. Closure violations

These are gaps within this batch; each may be covered elsewhere in the model. None is sized.

1. **Record export:** I-8 says users can export prefix, GIN and LN data, and I-10 implies members can export. No element in N21 declares export (producing and delivering exported records). The clause exists only through coverage on viewer and search elements.
2. **Subscription entitlement:** G-13.5 says what a consumer can view depends on their subscription. No behaviour decides which records, or which basic or full view, a subscriber may see. A011 covers only the anonymous public profile.
3. **Availability testing:** NFR-8 names a service level and requires the failover process to be "tested". A9 (performance and availability testing) is not crossed onto A154, A155 or A156, which are behavioural statements carrying that target.
4. **Notification dispatch:** NFR-8, NFR-17 and NFR-18 require notifications. No interface element in the batch sends them, and no delivery channel is named.

## 7. Instrument readings

**Engine: Hotyn-D 2.0**

| reading | value |
|---|---|
| Elements received | 25 (23 sized leaves, 2 aggregates: N21, N22) |
| Elements sized | 23 |
| Size-class distribution | S 4 / M 9 / L 7 / XL 3 (A160, A161, A162, each also an M10 coarseness signal) |
| Statement kinds | compliance 5 (A158, A159, A160, A161, A162) / behavioural 4 (A154, A155, A156, A164) |
| Unsizeable elements | 0 (plus 1 unsupported special count: A170, G2m/G3m/G4m) |
| Doubts | 17 |
| Closure violations | 4 |