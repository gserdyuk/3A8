<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-15. Not part of the sensor's reply.
run: 59 · reading: HD59-G2 · engine (sensor's own stamp): Hotyn-D 2.0 (title line: # Hotyn-D 2.0: size classes for HM57-1, subtrees N21 and N22; §7 line: **Engine: Hotyn-D 2.0**) ·
subagent: 3a8:work-estimator (plugin) · model (launch record): the harness's `opus` alias (the launch tool offers no
`claude-opus-5` value); the transcript's model attachment and the assistant turn record `claude-opus-5` ·
batch G, repeat 2: subtrees N21, N22 of the run 58 work model (Hotyn-W 1.2 × HM57-1), 23 sized elements ·
prompt prompt_size_G.md, 13 712 chars, md5 a616f912180e357a548a7d42e2fe26e5 (generated by make_sizing_prompts58.py; both repeats of
the batch received this file; the transcript's prompt record equals the file byte for byte, final newline included) ·
tool_uses: 0 · duration 275 347 ms · subagent tokens 29 626 ·
orchestrator process CLAUDE_CODE_MAX_OUTPUT_TOKENS=128000.
TRANSIT: one turn, one API message (msg_011Cf5uNLhx9xDmy8KoQs1hn), thinking (content empty in the transcript) then one text block of 12 507 chars; stop end_turn; output tokens 21 816.
No continuation message was injected ("Output token limit hit" occurs 0 times in the transcript). No other
message reached the agent.
PROVENANCE OF THIS TEXT: extracted by script (build_raw59.py) from the harness's per-agent transcript
(`~/.claude/projects/<project>/<session>/subagents/agent-<id>.jsonl`): the single assistant text block after the
prompt, with nothing inserted or removed. The completion notification also carried a reply text; it was not used
as the source. No seam. -->

# Hotyn-D 2.0: size classes for HM57-1, subtrees N21 and N22

## 1. Contamination check

**Clean. Classification proceeds.**

- **Ambient content, quarantined.** The harness added content that is not part of the input: an environment block, a git status, recent commit subjects and a user memory index. I did not use any of it, and it does not stop the run.
- **Input scan.** INPUT 1 and INPUT 2 contain no effort figure, person-day value, rate, price, budget, work duration, team size, prior estimate or rate-table row.
- **Figures that look like effort but are not.** NFR-8 and NFR-14 carry service-level, response-time and concurrency figures. These are product requirements, not effort, and I do not repeat them below. D-8 is about how to pay for access but gives no price.
- **Files.** None read. No tool was called.

## 2. Element sizing table

**Reading rules applied the same way to every element:**
- **(a) Coverage counts in full** (P-2: "the test is coverage"). This holds even when a sibling covers the same obligation, and even when the obligation describes the whole module (I-8).
- **(b) One verb over a list of objects of the same kind counts once.** Examples: "prefix, GIN and LN", share recipients, "logs and audit trail". Things of different kinds (record against hierarchy) or outcomes that the name splits ("basic or full") count separately.
- **(c) Behaviour counts only what the system does at run time.** Delivery acts in an obligation (develop, identify) are not counted.
- **(d) Statements count only named systems or components**, including generic kinds such as "hardware". Processes, procedures, designs and policies are not components.

### Subtree N21 — Access Data module

| id | name | class (kind) | enumeration | count | size |
|---|---|---|---|---|---|
| A011 | Public anonymous access profile (limited data, no export) | behaviour | search limited data set as non-member (I-10); refuse export to non-member (I-10); share records with recipient (individual / user-defined group / controlled group / public) (G-13.4). Name tokens "limited data" and "no export" are already covered by I-10 (P-2) | 3 | **M** |
| A070 | Published record viewer (basic or full) | surface | view basic record (D-4); view full record (D-4); view record hierarchies (D-4); search published data (I-8); export published data (I-8). G-13.5 "based on subscription" limits who can view; it is not a separate task. The viewing in P-17 = L-12 is the same viewing already counted | 5 | **L** |
| A133 | Search service over read store (prefix, GIN, LN) | behaviour | search prefix/GIN/LN records by field (D-1, D-1.1; "e.g. name, city" are examples, P-5); run advanced search (D-1.2); filter results (D-1.2); retrieve record for viewing (I-8); export data (I-8) | 5 | **L** |
| A134 | Search screen with field search | surface | search records by field (D-1, D-1.1); advanced search (D-1.2); filter results (D-1.2); view data (I-8); export data (I-8) | 5 | **L** |
| A135 | Advanced search and filter | surface | advanced search (D-1.2); filter (D-1.2) | 2 | **M** |
| A138 | Published hierarchy viewer | surface | view record hierarchies (D-4); view basic record (D-4); view full record (D-4); search data (I-8); export data (I-8) | 5 | **L** |
| A139 | Record print rendering | surface | print records, one or more (D-6) | 1 | **S** |
| A142 | How-to-pay information page | surface | access how-to-pay information for ad hoc data access (D-8) | 1 | **S** |

### Subtree N22 — Operational properties

| id | name | class (kind) | enumeration | count | size |
|---|---|---|---|---|---|
| A154 | High-availability deployment design | statement (**behavioural**: "failover monitoring that notifies administrators of a failure") | hardware; software; failover monitoring (NFR-8) | 3 | **M** |
| A155 | Failover process and test procedure | statement (**behavioural**: "failover monitoring that notifies administrators of a failure") | hardware; software; failover monitoring (NFR-8) | 3 | **M** |
| A156 | Hardware and software needs specification | statement (**behavioural**, by P-4: "failover monitoring that notifies administrators of a failure") | hardware; software; failover monitoring (NFR-8) | 3 | **M** |
| A157 | Failover monitoring with administrator notification | behaviour | monitor for failure; notify administrators of failure; run failover (NFR-8) | 3 | **M** |
| A160 | Security design | statement (**compliance**: "Appropriate security configuration, processes and procedures") | the application solution; components; frameworks; libraries; tools, the last four being "used to secure" it (NFR-10) | 5 | **L** |
| A161 | Security configuration baseline | statement (**compliance**: "Appropriate security configuration, processes and procedures") | the application solution; components; frameworks; libraries; tools (NFR-10) | 5 | **L** |
| A162 | Security processes and procedures | statement (**compliance**: "Appropriate security configuration, processes and procedures") | the application solution; components; frameworks; libraries; tools (NFR-10) | 5 | **L** |
| A164 | Performance design against response-time targets | statement (**behavioural**: "Response times ... under load") | login; navigation; transactions (saves, form generation); searches; API responses (NFR-14) | 5 | **L** |
| A166 | System error monitoring hooks | behaviour | detect system error; send event notification (NFR-17) | 2 | **M** |
| A168 | Business function failure detection hooks | behaviour | detect critical business function failure; send event notification (NFR-18) | 2 | **M** |
| A169 | Detailed application logging | behaviour | write detailed log entries (NFR-19); "for troubleshooting and process verification" is the purpose, not an action | 1 | **S** |
| A170 | Audit trail of all activity | store | audit trail entry, one per activity (NFR-20); "all activity" names no activity kinds (P-5) | 1 | **S** |
| C11 | Log and audit trail retention and purge | behaviour (derived; trigger: A169, A170) | retain log and audit trail entries; purge log and audit trail entries | 2 | **M** |

## 3. Special counts

**A164, activity A9: measurable targets (from the content and NFR-14's text)**
- Enumeration: login response-time target under load; navigation response-time target under load; transaction (saves, form generation) response-time target under load; search response-time target under load; API response-time target under load.
- The percentile applies to all five and is not counted as its own target.
- Count: 5, class **L**.

**A170, activities G2m / G3m / G4m: entity kinds loaded from predecessor applications**
- Enumeration: empty.
- The only entity kind is the audit trail entry. NFR-20 says it records "activity taking place at any point in the system", so the system creates it at run time.
- Nothing in the declaration requires any kind to come from a predecessor application.
- Count: none, **no class**. The model assigns migration work to a store that declares nothing to migrate; see section 4.

## 4. Unsizeable elements

1. **A158 — Backup procedures: unsizeable, model defect (M10).**
   - NFR-9 names only procedures and a policy owner ("comply with X-Customer policies and procedures").
   - It names no system, component or data scope for backup, so there is nothing to count. No class assigned.
   - For information only: its kind would be compliance ("comply with X-Customer policies and procedures").
2. **A159 — Disaster recovery procedures: unsizeable, model defect (M10).**
   - Same obligation, same gap: it does not say which systems or components recovery covers. No class assigned.
   - Kind for information only: compliance, same phrase.
3. **A170, special count for G2m / G3m / G4m: unsizeable, model defect (M10).**
   - Migration activities are assigned to a store that names no entity kind coming from a predecessor.
   - The element keeps its own class, **S**. Only the special count has no class.

## 5. Doubts

1. **A011:** the G-13.4 recipients (individual, user-defined group, controlled group, public) could be four separate share actions. That gives 6, class **L**.
2. **Contradiction, I-10 against G-13.4 (not resolved):** I-10 lets the public see a fixed "limited data set". G-13.4 lets the data owner share individual records "with the public". The two set public scope in different ways. I used both as stated.
3. **Contradiction, I-8 against I-10 (not resolved):** I-8 says users can "search for, view and export". I-10 says non-members "cannot export". I-8 does not say whether "users" includes non-members.
4. **A070:** three readings change the count:
   - I-8 treated as module-wide only (search and export dropped): 3, class **M**.
   - Subscription viewing (G-13.5) counted as its own task: 6, still **L**.
   - "basic or full" merged into one task: 4, still **L**.
5. **A133:**
   - I-8 treated as module-wide only (view and export dropped): 3, class **M**.
   - Each record type (prefix, GIN, LN) counted separately: class **XL**.
6. **A134:**
   - I-8 treated as module-wide only: 3, class **M**.
   - Also dropping D-1.2, which sibling A135 declares by name: 1, class **S**.
7. **A138:** if the basic and full record views from D-4 (declared by name on sibling A070) and the I-8 items are dropped, the count is 1, class **S**.
8. **A139:** "one or more records" could be two tasks, single print and batch print. That gives 2, class **M**.
9. **A154, A155, A156:**
   - These are three statements covering one obligation, and each counts all of it.
   - Counting "failover process" as a component gives 4, still **M**. Counting the system under the service level as named also gives 4, still **M**.
   - By name, A156 reads as compliance. P-4 makes it behavioural.
10. **A157:**
    - Counting delivery acts (develop process, test process, identify hardware and software needs): 6, class **L**.
    - Leaving "run failover" to the procedure (A155): 2, still **M**.
11. **A160, A161, A162:**
    - If components, frameworks, libraries and tools are the means of securing rather than things constrained: 1, class **S**.
    - If "components" is treated as a catch-all (P-5): 4, class **M**.
12. **A164:** splitting transactions into saves and form generation gives 6, still **L**.
13. **A164, A9 special count:**
    - Counting each concurrency level as its own threshold: 10, class **XL**.
    - Counting the percentile as its own target: 6, still **L**.
14. **A168:** "critical business functions" are not named (P-5), so detection is not counted per function. A named list would raise the count.
15. **A169:** logging for troubleshooting and for process verification could be two kinds of log content. That gives 2, class **M**.
16. **C11:**
    - Splitting the actions by object (logs, audit trail): 4, class **L**.
    - No obligation gives a retention rule.
    - Purging the audit trail sits uneasily with NFR-20's "all activity". The phrase is read as "anywhere in the system", not "for all time".
17. **P-17 and L-12:** their texts are identical and are counted once in A070. Two ids cover one obligation.
18. **A158, A159:** they could be sized on an implied "the system", which gives 1, class **S** each. I chose unsizeable because the system is never named (P-6 spirit: no sizing on a guessed reading).

## 6. Closure violations

1. **No availability testing on the availability target.** A154, A155 and A156 are behavioural statements whose obligation (NFR-8) sets an availability service level. None of them has A9 assigned. The missing work would test availability and failover against the NFR-8 service level.
2. **No element declares export by name.** I-8 requires export of prefix, GIN and LN data, and I-10 requires refusing export to non-members. Export appears only through the shared I-8 coverage on A070, A133, A134 and A138 (counted there). The missing element would cover export generation and its permission check.
3. **No subscription check in this batch.** G-13.5 limits viewing "based on subscription". Nothing in N21 or N22 checks a consumer's subscription before viewing. It may exist in a subtree outside this batch.

## 7. Instrument readings

**Engine: Hotyn-D 2.0**

- Elements received: 25 (23 leaves, 2 aggregates, N21 and N22, not sized)
- Elements sized: 21
- Size-class distribution: S 4 / M 9 / L 8 / XL 0
- Statement kinds (sized statements): compliance 3 / behavioural 4
- Unsizeable: 2 elements (A158, A159), plus 1 special count (A170, G2m / G3m / G4m)
- Doubts: 18
- Closure violations: 3