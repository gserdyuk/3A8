<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-15. Not part of the sensor's reply.
run: 59 · reading: HD59-E2 · engine (sensor's own stamp): Hotyn-D 2.0 (title line: # Hotyn-D 2.0: size classification of the N13 subtrees, model HM57-1 (crossed by Hotyn-W 1.2); §7 line: - **Engine:** Hotyn-D 2.0) ·
subagent: 3a8:work-estimator (plugin) · model (launch record): the harness's `opus` alias (the launch tool offers no
`claude-opus-5` value); the transcript's model attachment and the assistant turn record `claude-opus-5` ·
batch E, repeat 2: subtrees N13 of the run 58 work model (Hotyn-W 1.2 × HM57-1), 31 sized elements ·
prompt prompt_size_E.md, 20 993 chars, md5 89a3f5c00330d1ec318c70b58fda2479 (generated by make_sizing_prompts58.py; both repeats of
the batch received this file; the transcript's prompt record equals the file byte for byte, final newline included) ·
tool_uses: 0 · duration 295 200 ms · subagent tokens 33 909 ·
orchestrator process CLAUDE_CODE_MAX_OUTPUT_TOKENS=128000.
TRANSIT: one turn, one API message (msg_011Cf5tvLX4gJABNB4Vvnrnf), thinking (content empty in the transcript) then one text block of 14 740 chars; stop end_turn; output tokens 24 260.
No continuation message was injected ("Output token limit hit" occurs 0 times in the transcript). No other
message reached the agent.
PROVENANCE OF THIS TEXT: extracted by script (build_raw59.py) from the harness's per-agent transcript
(`~/.claude/projects/<project>/<session>/subagents/agent-<id>.jsonl`): the single assistant text block after the
prompt, with nothing inserted or removed. The completion notification also carried a reply text; it was not used
as the source. No seam. -->

# Hotyn-D 2.0: size classification of the N13 subtrees, model HM57-1 (crossed by Hotyn-W 1.2)

## 1. Contamination check

**Clean.** The pasted input has no effort figure, person-day value, rate, price, budget, duration, team size, prior estimate or rate-table row. The only numbers are identifier lengths quoted from the obligations ("12 or 14 digits", "13 digits", "6–9 digits"). They describe identifiers, not work.

The harness added some repository context: a git status, recent commit subjects, a memory index and user context. As instructed, I set all of it aside and did not treat it as contamination. I read no files.

**Counting rules used on every row.**
- **GIN/LN mirror pairs.** Most P-x and L-x obligations say the same thing for the two identifier kinds. Where they name the same action or task on a generic "record" or "identifier", it counts once. Where the texts differ, the difference counts: A003's lengths and L-2.1's shared pool.
- **Stores.** For a store, GIN and LN are the entity kinds themselves, so they count separately (A080).
- **Sibling owners.** Some obligation clauses name an action that a sibling element in this batch owns by name. For example, A004 owns check digits and A108 owns hierarchy import. Those clauses are not counted again on the element that shares the coverage. Each such exclusion that could change a class is listed in §5.

## 2. Element sizing table

### N14 — Identifier management

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A003 | Identifier composition rules (prefix plus serial, per-kind length) | behaviour | compose identifier from licensed prefix plus serial part (I-3); check licensed prefix length 6–9 digits (I-3); enforce GIN length 12 or 14 digits (I-3); enforce LN length 13 digits (I-3) | 4 | **L** |
| A004 | Check digit calculation | behaviour | calculate and assign check digit automatically (P-2/L-2; name token "calculation" covered, P-2) | 1 | **S** |
| A080 | Identifier registry per prefix | store | GIN (P-2, P-8); LN (L-2) | 2 | **M** |
| A081 | Identifier assignment service (auto or manual at any creation step) | behaviour | auto-assign identifier to a record (P-2/L-2); manually assign identifier to a record (P-2/L-2); opt for auto-assign or manual (P-2.1/L-2.2); apply the set assignment preference (P-3); auto-assign LN from an industry shared pool (L-2.1); validate identifier, auto-generated or manual, against X-Customer Standards (NFR-16) | 6 | **L** |
| A082 | Identifier reservation from auto-assign | behaviour | reserve identifier to hold it out from auto-assign (P-2.2/L-2.3) | 1 | **S** |
| A083 | Assignment mode preference | store | assignment mode preference, auto-assign or manual (P-3/L-3) | 1 | **S** |
| A087 | Prior-identifier reference on number change | store | reference from changed identifier back to old number (P-5.2/L-5.1) | 1 | **S** |
| A100 | Identifier status management | behaviour | view identifier status (P-10/L-9); assign status Reserved (P-10.1); assign status In Use (P-10.1); assign status For Reuse (P-10.1); assign status Available (P-10.1); determine reuse-availability date under industry and product-type rules (P-10.3) | 6 | **L** |
| A102 | Record-to-identifier status synchronisation | behaviour | update identifier status to reflect record status (P-10.1/L-9.1) | 1 | **S** |
| A104 | Out-of-use and reuse date display | surface | see date identifier was taken out of use (P-10.3/L-9.3); see date identifier becomes available for reuse (P-10.3/L-9.3) | 2 | **M** |

### N15 — Record lifecycle

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A073 | Single-record creation form driven by attribute sets | surface | create a record individually with its industry-dependent required and non-required attributes (P-1/L-1) | 1 | **S** |
| A074 | Step-by-step record creation wizard | surface | create a record through a step-by-step wizard (P-1/L-1) | 1 | **S** |
| A075 | Record cloning | behaviour | clone an existing record into a new record (P-1/L-1) | 1 | **S** |
| A079 | Draft saving of incomplete records | behaviour | save incomplete record as Draft (P-1.1/L-1.1) | 1 | **S** |
| A084 | Record edit form with finalise action | surface | edit record attributes manually (P-4/P-5/L-4/L-5); finalise record (P-4/L-4; name token covered) | 2 | **M** |
| A099 | Record status management | behaviour | view record status (P-10/L-9); assign status to a record, by system or user (P-10.2/L-9.2) | 2 | **M** |

### N16 — Record views and actions

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A090 | Record list view with filter and sort | surface | view all records in a single view (P-7/L-6); filter records (P-7/L-6); sort records (P-7/L-6); select an individual record (P-7.1); select multiple records (P-7.2) | 5 | **L** |
| A091 | Single-record action menu | surface | select an individual record (P-7.1/L-6.1); take edit action on it (P-7.1/L-6.1); take export action on it (P-7.1/L-6.1); "etc." not counted (P-5) | 3 | **M** |
| A092 | Multi-record bulk action | behaviour | select multiple records (P-7.2/L-6.2); apply the same action to all selected records (P-7.2/L-6.2) | 2 | **M** |
| A093 | Identifier list view with filter and sort | surface | view all identifiers in a single view (P-8/L-7); filter identifiers (P-8/L-7); sort identifiers (P-8/L-7) | 3 | **M** |

### N17 — Duplicate handling

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A094 | Duplicate detection engine | behaviour | detect potential duplicates of a record under creation (P-9.1); detect potential duplicates among completed records (P-9.1); remove a duplicate record (P-9/L-8) | 3 | **M** |
| A095 | Duplicate review and resolution screen | surface | view potential duplicate records (P-9/L-8); remove a duplicate (P-9/L-8); edit a duplicate (P-9/L-8) | 3 | **M** |
| A097 | Creation-time duplicate alert | surface | be alerted of possible duplicate records during creation (P-9.1/L-8.1) | 1 | **S** |
| A098 | Duplicate report for completed records | surface | run duplicate report for completed records (P-9.1/L-8.1) | 1 | **S** |

### N18 — Hierarchy management

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A106 | Hierarchy tree editor (create, edit, view) | surface | create hierarchy manually (P-11/L-10); edit hierarchy (P-11/L-10); view hierarchy (P-11/L-10); change item level on a record while creating a hierarchy (P-11.4) | 4 | **L** |
| A107 | Drag-and-drop hierarchy canvas | surface | create hierarchy via drag-and-drop (P-11/L-10); edit hierarchy via drag-and-drop (P-11/L-10); view hierarchy in visual format (P-11/L-10) | 3 | **M** |
| A108 | Hierarchy import handling | interface | import full records with hierarchy information (P-11.2/L-10.2); import hierarchy information for existing records (P-11.2/L-10.2) | 2 | **M** |
| A112 | Hierarchy status impact analysis | behaviour | determine whether a record's status change affects other records in the hierarchy (P-11.5/L-10.3); notify the user of the effect (P-11.5/L-10.3) | 2 | **M** |
| A114 | Hierarchy export and print rendering | surface | export hierarchy (P-12/L-11); print hierarchy (P-12/L-11) | 2 | **M** |
| C15 | Hierarchy integrity check (cycles, orphans) | behaviour (derived) | detect cycles in hierarchy; detect orphans in hierarchy (name, trigger A129, A106) | 2 | **M** |

### N13 — direct leaf

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A121 | Record ownership transfer | behaviour | transfer record ownership to another member (P-18/L-14) | 1 | **S** |

No element is XL, so no coarseness finding (M10) comes from the thresholds.

## 3. Special counts

These are the G2m/G3m/G4m migration counts on stores. No statement elements are in the batch, so there is no A9 count.

| element | enumeration, with why each kind must come from the predecessor | count | class |
|---|---|---|---|
| A080 | GIN: identifiers already assigned under members' licensed prefixes, with statuses "currently" held (P-10.1), exist before the system. Creating them at run time would issue new numbers instead of the ones already in use. LN: same reason for already-assigned location numbers. | 2 | **M** |
| A083 | None. P-3/L-3 make the preference something a user sets at run time ("Set a preference"). Nothing in the coverage says the predecessor holds preferences that would be lost if not migrated. | 0 | **no class**: 0 is below the S threshold (see §4) |
| A087 | Prior-identifier reference: number changes made in the predecessor applications happened before this system existed. The reference cannot be created at run time because its triggering change is already past. | 1 | **S** |

## 4. Unsizeable elements

**Elements:** none. Every sized element's declaration and coverage supported an enumeration.

**Special count without a class:** A083's G2m/G3m/G4m count is 0, and the pinned table starts at S = 1. The element class (S) stands. Migration work is crossed onto a store for which the content names nothing to migrate. I am reporting this as a finding and have not changed any work item.

## 5. Doubts

1. **Mirror stance (all elements except A003, A080 and C15).** If GIN and LN versions of the same action or task counted separately, these classes would change:
   - S→M: A004, A073, A074, A075, A079, A082, A083, A087, A097, A098, A102, A121
   - M→L: A084, A091, A092, A093, A094, A095, A099, A104, A107, A112, A114
   - L→XL: A081, A090, A100, A106
   - A108 stays M (count 4).
2. **A003 shorter:** "per-kind length" read as one rule, or the prefix-length check read as prefix management's job. Either gives 3 → M. **Longer:** GIN 12 and GIN 14 as two formats gives 5, still L.
3. **A004 longer:** "calculate" and "assign" read as two actions gives 2 → M.
4. **A073 longer:** entering required and entering non-required attributes read as two tasks gives 2 → M.
5. **A081 longer:**
   - Check-digit assignment (P-2/L-2, which A004 owns by name) counted here gives 7 → XL.
   - NFR-16's approval stages submit/reviewed/rejected/approved (4, per P-3) and attribute verification (1) are also in A081's coverage. Counting them gives up to 11 → XL.
   - That coverage looks misplaced on an identifier-assignment element.
   - **Shorter:** "opt for mode" and "apply preference" merged gives 5, still L.
6. **A082 longer:** "hold out from auto-assign" read as its own action gives 2 → M.
7. **A080 special count:** the fact that predecessor applications are being replaced comes from I-1, which A080 does not cover. Counting the licensed prefix as a third kind gives 3, still M.
8. **A083 special count longer:** if the predecessor holds per-member assignment preferences, the count is 1 → S.
9. **A087 special count shorter:** if the predecessor kept no references to old numbers, the count is 0 → no class.
10. **A084 longer:** "edit via import" (P-5/L-5) counted as a task of the form gives 3, still M.
11. **A090 shorter:**
    - Single and multiple selection merged gives 4, still L.
    - Selection left to A091/A092 gives 3 → M.
12. **A092 shorter:** selection left to A090 gives 1 → S. The actions that can be applied in bulk are unnamed in A092's coverage.
13. **A094:**
    - **Shorter:** removal left to A095 gives 2, still M.
    - **Longer:** editing a duplicate counted gives 4 → L.
14. **A098 longer:** reading the report results as a separate task gives 2 → M.
15. **A099 longer:**
    - System and user assignment split gives 3, still M.
    - Adding P-11.5's hierarchy notification (which A112 owns) gives 4 → L.
    - "manage" is an unnamed catch-all and counts nothing (P-5).
16. **A100:**
    - **Longer:** "record out-of-use date" (implied by P-10.3) gives 7 → XL.
    - **Shorter:** viewing status left to a surface gives 5, still L.
    - The status list is "currently" and may change. The LN status set (L-9.1) is unnamed.
17. **A102 longer:** counting by P-3 over the four target statuses gives 4 → L. The obligation does not name which statuses the sync produces.
18. **A104 shorter:** one task, "check out-of-use dates", gives 1 → S.
19. **A106 shorter:** item-level change treated as part of editing gives 3 → M.
20. **A107 shorter:** viewing left to A106 gives 2, still M.
21. **A108 longer:** a generic "import hierarchy" (P-11) counted as a third operation gives 3, still M.
22. **A112 longer:** detecting the status change counted as an action gives 3, still M.
23. **C15:**
    - The trigger A129 is outside this batch, so I counted from the name alone.
    - **Longer:** reporting or rejecting a violation gives 3, still M.
24. **Contradiction, not resolved:** L-2.1 ("Some industries automatically assign LNs from a shared pool") conflicts with L-2.2 ("The prefix licensee can opt for auto-assign or manual") in those industries. I counted both as stated.
25. **Contradiction, not resolved:** P-10.1/L-9.1 let the system or user assign an identifier status, but the same obligations say that status "may update to reflect record status". A user-set status can be overwritten. I counted both as stated.
26. **Uneven coverage between mirror pairs:** P-7.1/P-7.2 are on A090 but L-6.1/L-6.2 are not. P-11.5 is on A099 but L-10.3 is not. P-10.3 is on A100 but L-9.3 is not. P-8 is on A080 but L-7 is not. I counted as given.

## 6. Closure violations

I can only see this batch, so each of these means "absent from the N13 subtrees". None is sized.

1. **Record import.** Nothing handles creating and editing records via import (P-1/L-1 "via import", P-5/L-5 "via import"). A108 imports hierarchies only.
2. **Record approval and attribute verification workflows** (NFR-16: submitted/reviewed/rejected/approved; verification of attributes). No element declares them. They appear only in A081's coverage.
3. **Store for the industry shared LN pool** (L-2.1). A081 assigns from it, but A080 is a registry per prefix.
4. **Store for reuse-availability rules by industry and product type** (P-10.3/L-9.3). A100 applies these rules, but nothing holds them.
5. **Store for hierarchy relationships and item levels** (P-11/L-10, P-11.4). N18 has surfaces, an interface and behaviours but no store.

## 7. Instrument readings

- **Engine:** Hotyn-D 2.0
- **Elements received:** 37 (31 leaves, 6 aggregates N13–N18)
- **Elements sized:** 31
- **Size-class distribution:** S 12 / M 14 / L 5 / XL 0
- **Statement kinds:** compliance 0 / behavioural 0
- **Unsizeable count:** 0 elements (plus 1 special count without a class: A083 migration count = 0)
- **Doubts count:** 26 (24 enumeration doubts, 2 contradictions)
- **Closure violations count:** 5