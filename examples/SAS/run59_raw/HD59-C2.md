<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-15. Not part of the sensor's reply.
run: 59 · reading: HD59-C2 · engine (sensor's own stamp): Hotyn-D 2.0 (title line: # Hotyn-D 2.0: size classification of HM57-1, subtrees N08 and N09 (crossed by Hotyn-W 1.2); §7 line: **Engine: Hotyn-D 2.0**) ·
subagent: 3a8:work-estimator (plugin) · model (launch record): the harness's `opus` alias (the launch tool offers no
`claude-opus-5` value); the transcript's model attachment and the assistant turn record `claude-opus-5` ·
batch C, repeat 2: subtrees N08, N09 of the run 58 work model (Hotyn-W 1.2 × HM57-1), 24 sized elements ·
prompt prompt_size_C.md, 13 082 chars, md5 486e0e9064fad8ef78124f7036276cbf (generated by make_sizing_prompts58.py; both repeats of
the batch received this file; the transcript's prompt record equals the file byte for byte, final newline included) ·
tool_uses: 0 · duration 245 619 ms · subagent tokens 27 592 ·
orchestrator process CLAUDE_CODE_MAX_OUTPUT_TOKENS=128000.
TRANSIT: one turn, one API message (msg_011Cf5tWahRJUyaTf1AXReUz), thinking (content empty in the transcript) then one text block of 13 338 chars; stop end_turn; output tokens 19 792.
No continuation message was injected ("Output token limit hit" occurs 0 times in the transcript). No other
message reached the agent.
PROVENANCE OF THIS TEXT: extracted by script (build_raw59.py) from the harness's per-agent transcript
(`~/.claude/projects/<project>/<session>/subagents/agent-<id>.jsonl`): the single assistant text block after the
prompt, with nothing inserted or removed. The completion notification also carried a reply text; it was not used
as the source. No seam. -->

# Hotyn-D 2.0: size classification of HM57-1, subtrees N08 and N09 (crossed by Hotyn-W 1.2)

All 24 sized elements got a class: 6 S, 11 M, 5 L and 2 XL. No element was unsizeable and there are no statements. The two XL elements, A029 and A037, are also reported as coarseness findings (M10). I read no files.

## 1. Contamination check

**Clean.** The pasted input has no effort figure, person-day value, rate, price, budget, duration, team size, prior estimate or rate-table row. "3scale" in G-7.2 is a product name, not a figure. The ids (N08, A2, G-7.1, catalogue 1.4) are identifiers.

The harness added other context: a git status, recent commit subjects, a memory index, a user email and environment details. As instructed, I set all of it aside and did not use it. It does not count as contamination, and I worked from the pasted input only.

## 2. Element sizing table

**One reading used throughout:** each element is counted from its own covered obligations in full. Tokens in the element name count only when no obligation already covers them (P-2). Where the element name would narrow a shared obligation, the narrower count is listed as a doubt, not used.

### Subtree N08 — Notifications

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A022 | Approval status change notification | behaviour | notify Editors and Approvers of a change in approval status | 1 | **S** |
| A037 | Notification dispatch service | behaviour | dispatch notification by email; dispatch notification by SMS; post notification onscreen; broadcast global notifications; send member-specific notifications; send member-to-member notifications; send member-to-X-Customer notifications; notify user of hierarchy status impact; send near-real-time notifications on system errors | 9 | **XL** |
| A038 | Email channel via client SMTP relay | interface | dispatch notification (operation); email channel over client SMTP relay (protocol concern, P-1; the name token "SMTP relay" is this same concern, not counted again); SMS channel (P-1, named in G-8); onscreen channel (P-1, named in G-8); send application feedback to X-Customer; route feedback to its X-Customer destination (single contact or feedback tracking system are alternatives, so one route) | 6 | **L** |
| A039 | SMS channel via external SMS gateway | interface | dispatch notification (operation); email channel (P-1, named in G-8); SMS channel via external SMS gateway (P-1; the name token is covered by this channel); onscreen channel (P-1, named in G-8) | 4 | **M** |
| A040 | Onscreen notification inbox | surface | view onscreen notifications | 1 | **S** |
| A041 | Global notification broadcast | behaviour | broadcast global notification | 1 | **S** |
| A042 | Record add and update notification subscriptions | behaviour | opt in to record notifications; notify subscriber when a record is added; notify subscriber when a record is updated | 3 | **M** |
| A043 | Directed member and member-to-X-Customer messages incl. record data challenge | behaviour | send member-to-member notification; send member-to-X-Customer notification; challenge record data | 3 | **M** |
| A044 | Communication preferences (method, frequency) | store | user communication preference (holding method and frequency) | 1 | **S** |
| A045 | Notification digest batching by frequency | behaviour | set notification method preference; set notification frequency preference; batch outside-system notifications into digests by frequency (name token, not covered by G-8.5, so counted under P-2) | 3 | **M** |
| A113 | Hierarchy status impact notification | behaviour | determine whether a record's status change affects the status of other records in the hierarchy; notify the user of that impact | 2 | **M** |
| A136 | Access request notification to owner | behaviour | send access request to data owner; record owner approval; make data available after approval | 3 | **M** |
| A167 | Administrator alert notification | behaviour | send near-real-time notification when a system error occurs; send near-real-time notification when a critical business function fails | 2 | **M** |
| C06 | Notification message templates | store (derived, trigger A037) | notification message template | 1 | **S** |
| C07 | SMS contact number capture and verification | behaviour (derived, trigger A039) | capture SMS contact number; verify SMS contact number | 2 | **M** |

### Subtree N09 — Reporting and dashboard

| id | name | class | enumeration (verbatim) | count | size |
|---|---|---|---|---|---|
| A027 | User dashboard start screen | surface | view notifications; view reports; view prefix data ("and other information" counts nothing, P-5) | 3 | **M** |
| A028 | Prefix capacity counter | behaviour | display count of numeric indicators used; display count of numeric indicators available | 2 | **M** |
| A029 | Report engine over read store | behaviour | generate reports; customise adds/changes/deletes reports; schedule adds/changes/deletes reports; run adds/changes/deletes reports; view adds/changes/deletes reports; view usage reports by period; run report data history; run audit reports | 8 | **XL** |
| A030 | Adds, changes, deletes report definitions | behaviour | customise adds/changes/deletes reports; schedule adds/changes/deletes reports; run adds/changes/deletes reports; view adds/changes/deletes reports (name token "definitions" is covered by "customise", P-2) | 4 | **L** |
| A031 | Report customisation | behaviour | customise adds/changes/deletes reports; schedule adds/changes/deletes reports; run adds/changes/deletes reports; view adds/changes/deletes reports (name token "customisation" is covered) | 4 | **L** |
| A032 | Report scheduler | behaviour | customise adds/changes/deletes reports; schedule adds/changes/deletes reports; run adds/changes/deletes reports; view adds/changes/deletes reports (name token "scheduler" is covered) | 4 | **L** |
| A033 | Usage reports by hour, day, week, month, year | surface | view usage report by hour; view usage report by day; view usage report by week; view usage report by month; view usage report by year | 5 | **L** |
| A035 | Report run history | store | report run history entry | 1 | **S** |
| A036 | Audit report definitions | surface | run audit reports; define audit reports (name token "definitions", covered by no obligation, so counted under P-2) | 2 | **M** |

## 3. Special counts

G2m / G3m / G4m are crossed onto three stores: A035, A044 and C06. No element in the batch carries A9.

**Test used for all three:** a kind counts when it must already exist at cutover, rather than being created at run time through use, and I state the reason.

| element | enumeration | count | class |
|---|---|---|---|
| A035 Report run history | none. The report run history entry is created each time a report runs (G-7.3), and nothing in the obligation asks for history from before cutover. | 0 | **none**: the pinned table starts at 1. Not rounded up. |
| A044 Communication preferences | none. G-8.5 says "A user can set communication preferences", so the user creates the preference at run time. No source in a predecessor application is named. | 0 | **none**: the pinned table starts at 1. Not rounded up. |
| C06 Notification message templates | notification message template: A037 cannot send a single notification until a template exists, so this kind must be present at cutover. | 1 | **S** |

## 4. Unsizeable elements

**None.** Every element's content and covered obligations name what is counted. No stated number lacks its members (P-6).

**XL coarseness findings (M10).** These elements are sized XL and also reported as probable coarseness in the product model:
- **A029, Report engine over read store:** it covers G-7 through G-7.4, which are also split out across A030, A031, A032, A033, A035 and A036.
- **A037, Notification dispatch service:** it covers G-8 through G-8.3, P-11.5 and NFR-17, which are also split out across A038 to A043, A113 and A167.

## 5. Doubts

**Counts that could honestly be one item longer or shorter**
1. **A022:** splitting by recipient role (Editors, Approvers) gives 2, which is M.
2. **A027:** "dashboard or start screen" could be read as a single task, "view start screen", giving 1, which is S.
3. **A028:** splitting "GINs or LNs" into two indicator types gives 4 (L). Reading it as one "display prefix capacity" gives 1 (S).
4. **A029:** "generate reports" (G-7) and "run reports" (G-7.1) may be one action. That gives 7, still XL.
5. **A030:** if "define report definitions" is not covered by "customise", the count is 5, still L. If the name narrows the element to definitions only, the count is 1 (S), or 3 (M) when split by adds, changes and deletes. Splitting all four verbs by those three subjects gives 12, which is XL.
6. **A031:** if the name narrows the element to "customise" only, the count is 1, which is S.
7. **A032:** if the name narrows the element to "schedule" only, the count is 1, which is S.
8. **A033:** the five periods could be one task with a period selector, giving 1, which is S.
9. **A035:** G-7.3 says "Run report data history", which could name a second kind (historical report data), giving 2 (M). The name ("Report run history") and the obligation can be read differently: a history of report runs, or running reports over historical data.
10. **A036:** "definitions" could mean a catalogue of predefined reports rather than a user task, giving 1, which is S.
11. **A037:** counting the three channels as one action gives 7, still XL.
12. **A038:** if the name narrows the element to email only (dropping the SMS and onscreen channels), the count is 4 (M). Counting both feedback routes gives 7, still L.
13. **A039:** if the name narrows the element to SMS only, the count is 2, still M.
14. **A040:** receiving notifications on three channels could be three tasks, giving 3 (M). Actions like marking read or dismissing are implied by "inbox" but not named, so not counted.
15. **A041:** the example "scheduled server downtime" could be a second action, giving 2, which is M.
16. **A042:** if the "e.g." text is only an example and not scope, the count is 1, which is S.
17. **A043:** "challenge record data" could be folded into the member-to-X-Customer message, giving 2, still M.
18. **A044:** method and frequency could be two entity kinds, giving 2, which is M.
19. **A045:** "set preferences" could be one action, giving 2, still M.
20. **A113:** the impact check is implied by "if it affects" rather than named as a verb. Without it the count is 1, which is S.
21. **A136:** if the name narrows the element to the notification only, the count is 1 (S). D-2.1 names only approval; refusing a request is not named.
22. **A167:** counting detection of errors and failures as separate actions gives 4, which is L.
23. **C06:** templates could be separate kinds per channel (email, SMS, onscreen), giving 3, which is M.
24. **C07:** "verify" could be read as two actions (send code, confirm code), giving 3, still M.

**Special counts**
25. **A035:** if G-7.3 is read as requiring history from before cutover, the count is 1 (S). As stated it is 0. Either way, migration activities are crossed onto a store with nothing to migrate, which is a question about Hotyn-W's crossing.
26. **A044:** 0, with the same question about the crossing.
27. **C06:** the input does not say whether templates come from a predecessor or are written new. If written new, the kind still has to be pre-loaded but is not migrated, and the count would be 0.

**Contradictions and open choices, named and not resolved**
28. **G-9** says feedback is routed to "a single X-Customer contact *or* a feedback tracking system". The choice is not made; I counted one route.
29. **G-8** says "email, SMS and/or onscreen". Whether all three channels are required is not decided; I read it as all three, as A038 to A040 exist.
30. **P-11.5 and L-10.3** have identical text under two ids. I counted it once on A113. P-11.5 is also counted on A037.
31. **Overlapping coverage:** the same obligations are counted on A029 and on A030 to A036, and on A037 and on A038 to A043, A113 and A167. Scope is counted more than once across the batch. Fixing this means reshaping elements, which is not my job.

## 6. Closure violations

I only see this batch, so either item may exist in another subtree.

1. **No source for usage data behind A033.** G-7.2 notes "current reporting utilises 3scale", but no interface in the batch collects usage events or metrics to report by hour, day, week, month and year. It would cover taking in usage data from the metering source.
2. **No handling of a record data challenge.** A043 sends a challenge (G-8.3), but no element in the batch receives it, rules on it, or reports the outcome back. It would cover handling the challenge after it is sent.

## 7. Instrument readings

**Engine: Hotyn-D 2.0**

| reading | value |
|---|---|
| elements received | 26 (24 sized, 2 aggregates: N08, N09) |
| elements sized | 24 |
| size-class distribution | S 6 / M 11 / L 5 / XL 2 |
| statement kinds | compliance 0 / behavioural 0 |
| special counts | 3 computed: 1 classed S (C06), 2 at zero with no class (A035, A044) |
| XL coarseness findings (M10) | 2 (A029, A037) |
| unsizeable | 0 |
| doubts | 31 |
| closure violations | 2 |