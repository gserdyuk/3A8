<!-- ORCHESTRATOR HEADER, added at transcription 2026-09-15. Not part of the sensor's reply.
run: 59 · reading: HD59-A1 · engine (sensor's own stamp): Hotyn-D 2.0 (title line: # Hotyn-D 2.0: size classification, subtrees under N02 of `HM57-1` (crossed by `Hotyn-W 1.2`); §7 line: - **Engine:** Hotyn-D 2.0) ·
subagent: 3a8:work-estimator (plugin) · model (launch record): the harness's `opus` alias (the launch tool offers no
`claude-opus-5` value); the transcript's model attachment and the assistant turn record `claude-opus-5` ·
batch A, repeat 1: subtrees N02 of the run 58 work model (Hotyn-W 1.2 × HM57-1), 30 sized elements ·
prompt prompt_size_A.md, 18 619 chars, md5 6b68546221905104e660b48b5f26470c (generated by make_sizing_prompts58.py; both repeats of
the batch received this file; the transcript's prompt record equals the file byte for byte, final newline included) ·
tool_uses: 0 · duration 332 797 ms · subagent tokens 36 121 ·
orchestrator process CLAUDE_CODE_MAX_OUTPUT_TOKENS=128000.
TRANSIT: one turn, one API message (msg_011Cf5swAp4mSBeavvWxJerB), thinking (content empty in the transcript) then one text block of 17 329 chars; stop end_turn; output tokens 27 181.
No continuation message was injected ("Output token limit hit" occurs 0 times in the transcript). No other
message reached the agent.
PROVENANCE OF THIS TEXT: extracted by script (build_raw59.py) from the harness's per-agent transcript
(`~/.claude/projects/<project>/<session>/subagents/agent-<id>.jsonl`): the single assistant text block after the
prompt, with nothing inserted or removed. The completion notification also carried a reply text; it was not used
as the source. No seam. -->

# Hotyn-D 2.0: size classification, subtrees under N02 of `HM57-1` (crossed by `Hotyn-W 1.2`)

## 1. Contamination check

**Clean. Classification went ahead.**

- **Forbidden content:** the pasted input has no effort figure, person-day value, rate, price, budget, duration, team size, prior estimate or rate-table row.
- **Numbers in the obligation texts:** NFR-2 and NFR-7 give record counts, user counts, a concurrency share and a target year. These describe the scope, not effort. I did not repeat them below.
- **The word "cost":** NFR-5 says "at no cost to X-Customer". That is a policy with no figure attached, so it is not contamination.
- **Version labels:** "WCAG 2.0" and "Internet Explorer 9" appear only as names, and element or obligation ids appear only as ids. None of them are counts.
- **Repository context:** the harness added a git status, recent commit subjects, a memory index and user context. I set all of it aside, did not treat it as input, and did not act on it.
- **Files:** none were read.

## 2. Element sizing table

### N03 — Data stores

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A001 | Shared domain data model (companies, users, prefixes, records, identifiers) | store | 5 entity kinds: companies (name token; also "company prefixes" in NFR-2), users / members (name token; "members" in I-1, I-6), company prefixes (I-1, NFR-2), GIN records (I-1, NFR-2), LN records (I-1, NFR-2). Not counted: "records" and "identifiers", because the GIN/LN kinds already cover them (P-2). | 5 | **L** |
| A012 | X-Customer reference data set (prefix registry, controlled groups) | store | 2 entity kinds: prefix registry entries (name; prefixes in I-1), controlled groups (name token; no obligation covers it, so it counts under P-2). Not counted: "X-Customer-owned data" and "data published by data owners", which are unnamed catch-alls (P-5). | 2 | **M** |
| A076 | Industry configuration store | store | 3 entity kinds: industries (P-1, L-1 "vary by industry"), attribute definitions (P-1, L-1 "attributes"), per-industry attribute requirement status, required / non-required (P-1, L-1) | 3 | **M** |
| A089 | Binary asset store for images | store | 1 entity kind: product images (P-6; "binary asset" is covered by it under P-2) | 1 | **S** |
| A143 | Non-transactional read store | store | 3 entity kinds: company prefix records, GIN records, LN records (NFR-2, named for "transactional and non-transactional databases") | 3 | **M** |
| A144 | Near-real-time feed to read store | behaviour | 1 action: push updates to the data marts at near-real-time latency (NFR-2 "must support near-real-time updates"; the name token "feed" is covered by it) | 1 | **S** |
| C10 | Industry configuration maintenance screens | surface | 3 user tasks (name read with trigger A076): maintain industries, maintain attribute definitions, set each attribute's required / non-required status per industry | 3 | **M** |

### N04 — Service and API layer

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| A034 | 3scale analytics API client | interface | 2: retrieve usage reports by period (G-7.2 "view usage reports by hour, day, week, month and year"), 3scale analytics API as the named protocol concern (G-7.2 "current reporting utilises 3scale") | 2 | **M** |
| A059 | Record import and export API | interface | 5: import GIN records from an external system (P-19), import LN records from an external system (L-15), export records (G-12.1; name token), REST/SOAP web-service protocol (NFR-4; "through the API" in G-12.1 is the same concern), API-management compatibility with 3scale (NFR-4) | 5 | **L** |
| A140 | Access Data REST API | interface | not counted, see section 4 | — | — |
| A141 | Member API registration in 3scale | interface | 2: register member API access in 3scale (name token; no covered obligation names this action, so it counts under P-2), 3scale API-management concern (NFR-4) | 2 | **M** |
| A148 | Internal REST service layer | behaviour | 2 actions: integrate functionality across the suite through web services, integrate data across the suite through web services (NFR-4). Not counted: "instead of direct database access or copying", which is a prohibition, not an action. | 2 | **M** |
| C14 | API caller identity mapping from 3scale credentials | behaviour | 1 action: map 3scale credentials to caller identity (name read with triggers A140, A059, A007) | 1 | **S** |

### N05 — Web client

| id | name | class (kind) | enumeration | count | size |
|---|---|---|---|---|---|
| A002 | Unified application shell with Product, Location, Access Data areas | surface | 6 user tasks: open the Product (GIN) area (I-2), open the Location (LN) area (I-2), open the Access Data area (I-2, D-7), create data (I-1), manage data (I-1), share data (I-1). Not counted: "all Access Data functions" (P-5), permission-based visibility (I-6, not a task), no client install (NFR-7, not a task). | 6 | **L** |
| A019 | Role-driven UI composition | behaviour | 1 action: present a role-driven UI based on user role (G-2; "composition" is covered under P-2) | 1 | **S** |
| A152 | Standard web framework foundation (no client install) | statement (**behavioural**: "of them concurrent", NFR-7; run-time wins under P-4) | 8 systems/components: web client UI built on standard patterns, frameworks and components (NFR-7; WCAG in NFR-21 constrains the same UI), user client machine with no install (NFR-7), Internet Explorer, Chrome, Firefox, Safari, Edge (NFR-13), application capacity for concurrent users (NFR-7) | 8 | **L** |
| A163 | Cross-browser compatibility | statement (**compliance**: "Accessible on Internet Explorer 9 and above, and on the current and previous versions of Chrome, Firefox, Safari and Edge") | 5 systems: Internet Explorer, Chrome, Firefox, Safari, Edge | 5 | **L** |
| A171 | WCAG 2.0 Level A conformance | statement (**compliance**: "WCAG 2.0 Level A compliance, within reasonable accommodation") | 1 component: the web user interface, which is the scope the named standard sets | 1 | **S** |
| C13 | User-facing error handling | behaviour | 1 action: handle errors for the user (name read with trigger A002) | 1 | **S** |

### CN01 — Background processing

| id | name | class | enumeration | count | size |
|---|---|---|---|---|---|
| C05 | Background job scheduler | behaviour | 1 action: schedule background jobs (name; triggers A032, A045, A104 are outside the batch) | 1 | **S** |
| C08 | Asynchronous import job queue with progress status | behaviour | 3 actions: enqueue import jobs, process import jobs asynchronously, report job progress status (name read with triggers A051, A145) | 3 | **M** |
| C12 | Search index maintenance from read-store feed | behaviour | 2 actions: consume read-store feed changes, update the search index (name read with triggers A133, A144) | 2 | **M** |
| C16 | Job run log and retry | behaviour | 2 actions: log job runs, retry failed jobs (name read with triggers C05, C08) | 2 | **M** |

### N02 — own statement leaves

| id | name | class (kind) | enumeration | count | size |
|---|---|---|---|---|---|
| A005 | Architecture design: enterprise alignment, maintainability, growth scalability | statement (**behavioural**: "scalable and sustainable given the projected member and application growth") | 1 system: the application architecture (I-4). Not counted: the existing enterprise architecture, which is the alignment reference and is not itself constrained. | 1 | **S** |
| A006 | Future-expansion design provisions (further data types, field-level sharing) | statement (**compliance**: "accommodates future expansion … while that expansion itself stays out of scope") | 2 components: data model record types ("data other than GIN or LN"), sharing-privilege model ("sharing privileges down to field level") | 2 | **M** |
| A145 | Data volume scalability design | statement (**behavioural**: "must support near-real-time updates") | 3 components: transactional databases, non-transactional databases / data marts, data update path (latency "currently overnight", NFR-2) | 3 | **M** |
| A149 | Technical standards conformance design | statement (**compliance**: "Adherence to X-Customer technical standards") | 4 components: source code (coding), database (connection and naming), integrations (integration), technology and product stack (approved technologies and products) | 4 | **M** |
| A150 | Standards deviation impact analysis procedure | statement (**compliance**: "a deviation requires an impact analysis … by the project core team") | 4 components where a deviation may occur: source code, database (connection and naming), integrations, technology and product stack (NFR-5) | 4 | **M** |
| A151 | Cloud-readiness design provisions | statement (**compliance**: "designed to be cloud-ready") | 1 system: the architecture (NFR-6) | 1 | **S** |
| A153 | User volume and concurrency capacity design | statement (**behavioural**: "of them concurrent") | 3 components: web client on standard patterns, frameworks and components; user client machine with no install; application capacity for concurrent users (NFR-7) | 3 | **M** |

## 3. Special counts

Only G2m / G3m / G4m apply, on stores A001, A012, A076 and A089. No element in the batch carries A9. A143 has no G activity.

| id | kinds loaded from predecessor applications, with why each must arrive rather than be created at run time | count | class |
|---|---|---|---|
| A001 | **Companies:** they own the existing prefixes, so migrated prefixes have no owner without them. **Users / members:** members of the applications being replaced (I-1) must keep their access and ownership. **Company prefixes:** NFR-2 gives a current stock that grows from today. **GIN records:** current stock named in NFR-2, already created by members. **LN records:** current stock named in NFR-2, already created by members. | 5 | **L** |
| A012 | **Prefix registry entries:** these prefixes are already issued to members of the replaced applications (I-1); recreating them at run time would mean issuing them again. **Controlled groups:** reference data X-Customer already owns and serves (I-11). | 2 | **M** |
| A076 | **Industries**, **attribute definitions** and **per-industry required / non-required status:** the GIN and LN records loaded into A001 were captured under industry-specific attribute rules (P-1, L-1). They can only be reconciled against those same rules, taken from the predecessor rather than re-authored. | 3 | **M** |
| A089 | None. The coverage (P-6, "upload product images") describes images being created at run time and names no predecessor source. | 0 | **no class**: below the S threshold, see doubt 8 |

## 4. Unsizeable elements

- **A140 — Access Data REST API** is *unsizeable: model defect (M10)*.
  - **What is missing:** the operations it exposes. The only thing that names them is D-7, "Perform **all Access Data functions** via the web interface or the API", and under P-5 that catch-all counts nothing.
  - **Why no class:** its declaration names only protocol concerns (REST, 3scale compatibility, from NFR-4). Sizing on those alone would mean assuming the API has no operations.
  - **What would fix it:** the Access Data functions it exposes need to be named in its own coverage.

## 5. Doubts

Each doubt names the item that could go either way. Where a threshold is crossed, the line says so.

1. **A001:** reading "identifiers" (GIN/LN numbers) as a kind separate from the records adds one (still L). Counting sharing or permission grants (I-1 "share", I-6 "permissions") adds one more. **Both together give 7, which is XL.**
2. **A001 special count:** users / members could be re-provisioned instead of migrated. That removes one (4, still L).
3. **A012:** counting the GIN and LN records named in I-1 adds two (**4, L**). Also, both A001 and A012 declare prefixes, so which store owns them is unclear.
4. **A012 special count:** controlled groups could be authored new instead of migrated. That removes one (**1, S**).
5. **A076:** splitting attribute definitions into GIN and LN sets adds one (**4, L**). Merging attribute definitions with requirement status removes one (still M).
6. **A076 special count:** its coverage never names a predecessor. If the configuration is authored new through C10, the count is **0 and there is no class**.
7. **A089:** counting the image-to-record link ("add them to records") adds one (**2, M**).
8. **A089 special count:** G2m, G3m and G4m are crossed onto this store, but its coverage has nothing for them to count. Either the crossing has no driver, or the coverage leaves out predecessor images. I am reporting it, not removing it.
9. **A143:** NFR-2 names its three kinds jointly for "transactional and non-transactional databases". If that naming does not carry over to the data marts, the element would be unsizeable.
10. **A144:** counting change capture from the transactional store as its own action adds one (**2, M**).
11. **C10:** under P-3, "required / non-required" could be two tasks, adding one (**4, L**). Reading it as a single "maintain configuration" task gives 1 (**S**).
12. **A034:** counting the five named report periods as separate operations gives 6 (**L**).
13. **A059:**
    - Merging GIN and LN import removes one (**4, M**).
    - Treating the six file formats in G-12.1 (Excel, CSV, Numbers, XML, tab-delimited, iDocs) as protocol concerns gives 11 (**XL**).
    - Counting REST and SOAP as two protocols adds one (6, still L).
14. **A141:** counting REST as its own protocol concern adds one (3, still M).
15. **A148:** counting "REST APIs available to members" as an action adds one (still M). Merging functionality and data integration removes one (**1, S**).
16. **C14:** counting credential validation separately from mapping adds one (**2, M**).
17. **A002:** crossing I-1's create / manage / share with prefixes, GIN and LN gives 9 (**XL**). Leaving I-1's tasks to module surfaces outside the batch gives 3 (**M**). Merging the three area-opening tasks into one gives 4 (still L).
18. **A019:** counting "resolve user role" as its own action adds one (**2, M**).
19. **A152:** counting current and previous browser versions as separate systems gives 12 (**XL**). Leaving concurrency capacity to A153 removes one (still L).
20. **A163:** splitting current and previous versions of Chrome, Firefox, Safari and Edge gives 9 (**XL**).
21. **A171:** its only component is implied by the standard's own scope. A strict reading gives 0, which would make it unsizeable.
22. **C13:** counting "present error to user" separately from "handle error" adds one (**2, M**).
23. **C05:** the triggers A032, A045 and A104 are outside the batch, so the count rests on the name alone. Counting "run scheduled jobs" adds one (**2, M**).
24. **C12:** merging consume and update into one "maintain index" action gives 1 (**S**).
25. **C16:** counting log and retry separately for each triggering job kind (C05, C08) gives 4 (**L**).
26. **C08:** folding job processing into the queue removes one (2, still M).
27. **A005:** counting the enterprise architecture as constrained adds one (**2, M**). The kind could also be read as compliance if scalability is taken as design-only.
28. **A006:** reading the constrained thing as "the final design" alone gives 1 (**S**).
29. **A145:** counting company prefixes, GIN and LN as constrained data sets gives 6 (**L**).
30. **A149 and A150:**
    - **Splits:** separating database connection from database naming, or technologies from products, adds one each (**5, L**).
    - **Shared coverage:** A150's coverage is identical to A149's, so their enumerations duplicate each other. Counting A150's four impact dimensions instead also gives 4.
31. **A152 and A153:** both enumerate NFR-7 in full, so the same constrained components are counted on two elements.
32. **Contradiction (not resolved):** NFR-7 requires "industry-standard web design patterns, frameworks and components", while NFR-13 requires access on "Internet Explorer 9 and above". Current standard frameworks may not support that browser. I sized A152 and A163 on the text as written.

## 6. Closure violations

These are pieces of work I think are needed but the model does not contain. They are named here, not sized.

1. **Initial population of the read store (A143).** A144 only feeds changes, and A143 carries no load activity. This work would fill the data marts (NFR-2) with the existing company prefix, GIN and LN records before the first change arrives. A133, outside the batch, might already hold it.
2. **Initial build of the search index.** C12 only maintains the index from the feed. This work would make the existing records searchable at go-live. A133, outside the batch, might already hold it.

## 7. Instrument readings

- **Engine:** Hotyn-D 2.0
- **Elements received:** 35 (5 aggregates: N02, N03, N04, N05, CN01; 30 eligible for sizing)
- **Elements sized:** 29
- **Size-class distribution:** S 9 · M 15 · L 5 · XL 0
- **Statement kinds:** compliance 6 (A006, A149, A150, A151, A163, A171) · behavioural 4 (A005, A145, A152, A153)
- **Special counts:** 4 (A001 L, A012 M, A076 M, A089 no class)
- **Unsizeable:** 1 (A140)
- **Doubts:** 32 (31 enumeration doubts, 1 contradiction)
- **Closure violations:** 2