# Session record — 2026-08-21…22 — the course correction, the rate card, and the first deliverable

**Written to be read without the conversation, as the handoff into the FaxRxTx validation session
(этап 3).** Facts separated from readings; the author's decisions listed as such. Repository state:
everything in the working tree, nothing committed (the author's standing choice — work stays local).

## 1. What this session did, in one paragraph

Diagnosed the project's circling (review: the variance program had hit its floor because rules
pinned form while the magnitude stayed sampled), and executed the exit in full: an exit criterion
was pinned; the estimator was split into a classifier (`Hotyn-D 2.0`) and a pinned rate table; three
classification batches measured (×1.016 → ×1.000), the whole model assembled (1342–1353 pd, spread
×1.008); the second sensor returned after 18 idle days (reference-class pair), gap-blind Step C
rates were produced, and the first Hotyn-era Steps B–D ran — ending in the first deliverable in the
METHODOLOGY format, plus a priced options table for the four step-change risks. Six new registered
runs (23–28), all raw output transcribed.

## 2. Instrument state

| instrument | start | end | change |
|---|---|---|---|
| `Hotyn-D` | 1.0 (judgement pricing) | **2.0** (classifier: counts named things, emits **no** effort figures; prices live outside) | 1.0 archived at `docs/archive/work-estimator-1.0.md` |
| rate table | did not exist | **v0.1 + addendum A1** (`docs/rate_table.md`), written gap-blind by new role `Hotyn-K 1.0` (× Fable 5, stamped) | uncalibrated external norms; unit adjudicated (below) |
| technology catalogue | 1.1 | **1.4** | 1.2: size classes & drivers (W10, S/M/L/**XL**, statement kinds); 1.3: precedents P-1…P-3; 1.4: P-4…P-6 + A9 needs ≥1 stated target |
| assumptions (BMS) | v2 | **v3** | A7: person-day = **assigned working day** (adjudicated) |
| exit criterion | none | **v1.0** (`docs/exit_criterion.md`) | ≥4 outcome cases; P50 within ×1.3 on ≥3; P10–P90 covers on ≥3; no fitting on the tested case |
| `Lytin-R / K / G` | unchanged | unchanged | first Hotyn-era runs; `calibration-rates` still fails to register — working copy `rates-step-c` (same content) registers fine |

## 3. Pinned inputs as they stand

`requirements_product.md` N=68 (md5 `0c2dea478b993e4451a66f9468633f1e`) · `requirements_work.md` N=5
· `assumptions.md` **v3** · `open_questions.md` (10, not askable) · catalogue **1.4** ·
`technology_declaration.md` · rate table **v0.1+A1** · product model HM19-OA1 (78 elements) · work
models HW21-A1/B/C1 (repeat-1 crossings pinned; known repeat-2 deltas carried as declared
sensitivities).

## 4. Facts

- **Run 23** (batch B, n=2, Opus, stale 1.0 definition + task-pinned 2.0 rules): 19/22 identical;
  ΣE ×1.016 (was ×1.288 under judgement pricing); both refused N17/N18 identically.
- **Run 24** (A and C, n=2 each, real 2.0 definition, catalogue 1.3): **C 22/22, ×1.0000** — first
  perfect pair in the project; A 16/21, ×1.042 (scope-matched ×1.070); zero divergence on
  adjudicated boundaries; all five A-divergences on then-unadjudicated ones (→ P-4…P-6).
- **Run 25** (assembly): **1342.13 / 1352.92 pd** (N86 branch, ×1.008); root C3 ≈ 181 (the
  runs-21/22 "nothing prices the subtrees meeting" gap, closed); once-layer 63.7 (7.0% of leaf);
  8 named holes; A0 closed on all 73 ids.
- **Run 26** (reference class, n=2 — first repeatability reading of this sensor): P10/P50/P80/P90 =
  650/1050/1600/2050 and 420/800/1250/1650; P50 ×1.31, narrowing to ×1.24 at P90; July's n=1
  (950/1800) inside the band.
- **Run 27** (Steps B–D): centre gap ×1.28–1.69 **with the bottom-up high** (sign inverted vs
  history); chain → **centre 1898.81/1912.70, corridor 1611–2504** (non-percentile band);
  **explained share 0%**; calibrated centre ≥ class P90 → **no reserve headroom**; false convergence
  refused in three frames.
- **Run 28** (step-event options, gap-blind): E-1 direct integrations 12.8/23.6/51.6 pd·unit +
  45/124/149 once (N=20 → 301/596/1181) · E-2 own SMS 60/178/239 (gateway netted) · E-3 disruption
  response 112/212/288 (additive) · E-4 process engine 117/192/252 (H2 netted) · X-13 (E-1∧E-3)
  12/22/37; no probabilities, six binding usage constraints.
- **Protocol facts:** agent definitions load with a lag — the run-23 pair executed the stale 1.0
  definition (probe-before-batch is now standing practice, and the probe held for runs 24+);
  **the harness injects `gitStatus` with commit subjects into sensor contexts** — a Lytin-era
  gap-shaped ratio reached two gap-blind runs; both quarantined it and recommended stripping git
  context for sensors; run 28's sensor refused full validity until the orchestrator supplied the
  base's engine stamp the prompt had omitted.

## 5. Rule defects found by the runs

Prompt header said 21 sizeable elements, truth 22 (both run-23 sensors caught it, sized all 22) ·
pricing script dropped N38/N42 (caught by the 197-item crosscheck before results were read) · rate
table labels E7 "model bracket" while the driver is environment count (values stand; v0.2 note) ·
A9 had no zero-target case (crossing generated items where obligations state no threshold; 1.4
moves it to the crossing filter) · run 28's prompt omitted the base stamp (sensor refused silently
proceeding; stamp supplied in transcription).

## 6. Readings — mine, each with what would overturn it

- **R1. The law of the record: everything priced from a table agrees (≤×1.016, often ×1.000);
  everything sampled from a model diverges (×1.288–×2).** *Overturned by:* a tabled batch pair
  diverging materially, or a sampled pair agreeing at table level.
- **R2. Case law binds; the freedom relocates to un-adjudicated boundaries, and each relocation now
  costs one precedent line, not a method version.** Batch C after 1.3 = ×1.0000; batch A diverged
  only where 1.3 was silent; 1.4 closed those. *Overturned by:* divergence recurring on an
  adjudicated boundary.
- **R3. After the author's adjudications (units = assigned days; class conditional-on-narrowings),
  the ×1.28–1.69 gap is real and sits on the bottom-up side: the table's level (sign unknown) and
  the root-assembly convention (~181 pd, 33–62% of the gap).** *Overturned by:* FaxRxTx replay
  landing near its fact — which would vindicate the table and point at the class instead.
- **R4. Options-not-reserves is the honest tail form when the centre sits at the class P90**: bimodal
  events priced as conditional deltas with netting, probabilities left to the client. *Overturned
  by:* a client/contract context where an EV-reserve is contractually required — then a sourced
  probability act is needed on top.

## 7. Method decisions (the author's, 2026-08-21…22)

Exit criterion thresholds (×1.3; 3 of 4; ≥4 cases) · rate-card design approved; table v0.1 approved
· size drivers approved with **XL added** (priced as XL *and* reported as probable M10) · statement
kinds; U4 and D3 bracket-sized · N17/N18 reclassified statement-behavioural (not retroactive) ·
precedents P-1…P-6 adjudicated · **person-day = assigned working day** (A7 v3; both ×0.8 forks
dissolved; the face-value gap is units-consistent) · **class = stated, weight ≈ 1 conditional on
the pinned narrowings** (grounds: structural membership, findings §11.4 applied; neighbour mass
lives in the step events) · **U5 both halves**: bid conditional + priced options table · P2
(Opus↔Sonnet classifier pair) deferred — no decision hangs on it · Sonnet remains diagnostic-only.

## 8. Debts, cheapest first — and the FaxRxTx handoff

1. **Этап 3 — FaxRxTx через цепочку против FACT.md.** The concrete sequence for the next session:
   (a) pin a FaxRxTx obligation list (the same act done for BMS on 2026-08-20 — extraction + split
   from `examples/FaxRxTx/SYSTEM.md` + assumptions; ids, md5); (b) write the FaxRxTx technology
   declaration from catalogue 1.4 (2007-era stack — the **vintage question** the rates run flagged:
   the table's norms are modern; the era-transfer is part of what the comparison measures — record,
   do not pre-adjust); (c) `Hotyn-M 1.1` (n=2) → crossing → `Hotyn-D 2.0` classes (n=2) → table →
   assembly; (d) **raw comparison against `FACT.md` first** (the fact stays sealed from every
   sensor; the orchestrator opens it only after the assembly is written down); (e) then the one
   permitted calibration at n=1: a single global level factor; (f) score against
   `docs/exit_criterion.md` expectations for n=1 (a gross-error gate, not proof). Probe the
   definitions before the first batch; strip/paste inputs, never rely on ambient context.
2. **U2–U4 verifications** (DPA work presence · responsive baseline of the table · migration
   absence) — one reading pass each; a "no" becomes a named hole, never a G1 absorption.
3. **Protocol fix:** strip `gitStatus`/commit subjects from sensor launches (two independent
   catches). Mechanism to investigate: launching sensors from a cwd outside the repo, or a harness
   setting.
4. **OA2 structure axis** (×1.65 nodes): cross + classify + assemble the second product model → the
   structure component of the corridor in pd, currently named-not-quantified.
5. **U1** — a gap-blind round on cross-counterparty risk correlation.
6. Commit the tree when the author chooses; `articles/` (the cheat cards) included.

## 9. Open cross-session questions

Does ×1.016 hold on a second project (FaxRxTx answers) · is the table's level right (only outcomes
answer; the diagnosis put the burden there) · does the case-law rate of new boundaries fall per
batch (three batches → 3, then 3, then 0 new — is it converging?) · is the RC pair's ×1.31 P50
spread typical of the sensor or of this RFP · does the era-transfer of a modern rate table to a
2007 project hold at all.

## 10. Reproduction

```bash
python examples/BMS/run23_raw/price_work_model.py   # batch B, both size sets
python examples/BMS/run24_raw/price_run24.py        # batches A and C
python examples/BMS/run25_raw/assemble.py           # the whole-model assembly, both N86 variants
```

Run 26–28 are prose-only (quantiles, rates, options — no arithmetic beyond the diagnosis chain,
which is recomputable by hand from `run27_raw/diagnosis.md` §5). The deliverable:
`examples/BMS/estimate_BMS_2026-08-22.md`. The course-correction analysis that started all of it:
`docs/review_2026-08-21_running_in_circles.md` + `docs/proposal_rate_card.md`.
